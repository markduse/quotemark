import React, { useState, useMemo, useEffect, useRef, useDeferredValue } from "react";
import { useAuth } from "./AuthContext";
import { supabase } from "./supabase";
import { track, faceBand } from "./analytics";
import FEX_RATES from "./data/fex_rates.json";
import RATE_FACTORS from "./data/rate_factors.json";
import RESTRICTIONS from "./data/restrictions.json";
import TERM_RATES from "./data/term_rates.json";
import IUL_RATES  from "./data/iul_rates.json";
import IUL_STATE_AVAIL from "./data/iul_state_availability.json";

// ── US STATES ──
const US_STATES = ['AL','AK','AZ','AR','CA','CO','CT','DE','FL','GA','HI','ID','IL','IN','IA','KS','KY','LA','ME','MD','MA','MI','MN','MS','MO','MT','NE','NV','NH','NJ','NM','NY','NC','ND','OH','OK','OR','PA','RI','SC','SD','TN','TX','UT','VT','VA','WA','WV','WI','WY','DC'];
const STATE_NAMES = {AL:'Alabama',AK:'Alaska',AZ:'Arizona',AR:'Arkansas',CA:'California',CO:'Colorado',CT:'Connecticut',DE:'Delaware',FL:'Florida',GA:'Georgia',HI:'Hawaii',ID:'Idaho',IL:'Illinois',IN:'Indiana',IA:'Iowa',KS:'Kansas',KY:'Kentucky',LA:'Louisiana',ME:'Maine',MD:'Maryland',MA:'Massachusetts',MI:'Michigan',MN:'Minnesota',MS:'Mississippi',MO:'Missouri',MT:'Montana',NE:'Nebraska',NV:'Nevada',NH:'New Hampshire',NJ:'New Jersey',NM:'New Mexico',NY:'New York',NC:'North Carolina',ND:'North Dakota',OH:'Ohio',OK:'Oklahoma',OR:'Oregon',PA:'Pennsylvania',RI:'Rhode Island',SC:'South Carolina',SD:'South Dakota',TN:'Tennessee',TX:'Texas',UT:'Utah',VT:'Vermont',VA:'Virginia',WA:'Washington',WV:'West Virginia',WI:'Wisconsin',WY:'Wyoming',DC:'D.C.'};

// ── CARRIER RULES ──
const STATE_RULES = {
  rn:   { excludeStates: ['LA','NY','WA'] },
  acc:  { excludeStates: [] },
  ahl:  { excludeStates: [] },
  cont: { excludeStates: [] },
  ra:   { excludeStates: [] },
  ls:   { excludeStates: [] },
  amam: { excludeStates: [] },
  moo:  { excludeStates: [] },
  cbg:  { excludeStates: [] },
  ta:   { excludeStates: [] },
  lb:   { excludeStates: [] },
  pf:   { excludeStates: [] },
  amr:  { excludeStates: [] },
  for:  { excludeStates: [] },
  afl:  { excludeStates: [] },
  laf:  { excludeStates: [] },
  uhl:  { excludeStates: [] },
  fid:  { excludeStates: [] },
  elco: { excludeStates: [] },
  balt_sg: { excludeStates: [] },
  sl_pp: { excludeStates: [] },
  sl:    { excludeStates: [] },
  ail:   { excludeStates: ['MA','MN','NH','NJ','NC','WA'] },
};
// Age-dependent face caps from carrier product tables
const AGE_FACE_BANDS = {
  amam:[{aMin:0,aMax:50,max:50000},{aMin:51,aMax:75,max:35000},{aMin:76,aMax:85,max:25000}],
  cont:[{aMin:45,aMax:55,max:50000},{aMin:56,aMax:65,max:40000},{aMin:66,aMax:75,max:35000},{aMin:76,aMax:89,max:25000}],
  acc:[{aMin:45,aMax:55,max:50000},{aMin:56,aMax:65,max:40000},{aMin:66,aMax:75,max:35000},{aMin:76,aMax:89,max:25000}],
  ahl:[{aMin:50,aMax:75,max:40000},{aMin:76,aMax:85,max:25000}],
  for:[{aMin:50,aMax:80,max:35000},{aMin:81,aMax:85,max:15000}],
  lb:[{aMin:18,aMax:80,max:40000}],
  ls:[{aMin:50,aMax:85,max:30000}],
  moo:[{aMin:45,aMax:85,max:50000}],
  rn:[{aMin:50,aMax:85,max:40000}],
  ta:[{aMin:0,aMax:55,max:50000},{aMin:56,aMax:75,max:40000},{aMin:76,aMax:85,max:25000}],
  uhl:[{aMin:20,aMax:60,max:100000},{aMin:61,aMax:80,max:50000}],
  cbg:[{aMin:50,aMax:80,max:25000}],
  fid:[{aMin:50,aMax:85,max:40000}],
};
function getFaceCap(carrierId, age) {
  const bands = AGE_FACE_BANDS[carrierId];
  if (!bands) return 100000;
  for (const b of bands) { if (age >= b.aMin && age <= b.aMax) return b.max; }
  return bands[bands.length-1].max;
}
// Legacy flat caps for carriers not in age bands
const FACE_CAPS = {
  ra:50000, pf:50000, amr:30000, bl_sg:25000,
  afl:50000, laf:50000, bl:50000, elco:25000, balt_sg:25000,
  sl_pp:25000, sl:30000, ail:30000,
};
// Carriers disabled in code — incomplete data, not ready for agents
const FORCE_DISABLED = new Set(['sl_pp','amam_gs','cbg','amr','ahl_gs','rna_gi','sl','ail','balt_sg']);

// AGE_MAX entries can be a number (applies to all tiers) OR an object
// {B,C,D,E} for tier-specific caps. Aetna/AHL/CVS go up to 89 for
// Preferred/Standard but Modified caps at 75.
const AGE_MAX = {
  acc:  {B:89, C:89, D:75},
  ahl:  {B:89, C:89, D:75},
  cont: 89, // Cont only offers Preferred (B)
  ahl_gs:85, rn:85,
  ra:79,  ls:80,  amam:85,   amam_gs:85, moo:85, cbg:80,
  ta:85,  ta_exp:85, lb:80,  pf:79,  amr:85, for:85,
  afl:79, laf:79, uhl:80, fid:85,
  bl_sg:69, elco:80, sl_pp:85, sl:85, ail:80, rna_gi:85,
};
function getAgeMax(carrierId, tier) {
  const v = AGE_MAX[carrierId];
  if (v == null) return Infinity;
  if (typeof v === 'number') return v;
  return v[tier] ?? Infinity;
}

// ── Better Life — Better Final Expense ────────────────────────────
// Formula: Math.round((rate * face/1000 + 78) * 0.085 * 100) / 100
// ri: 0=male_NT, 1=male_T, 2=female_NT, 3=female_T
// Issue ages 40–85 | Level cap $50k | Graded cap $50k
const BL_SI={  // Simplified Issue (Level)
  40:[27.28,36.19,20.98,27.41],41:[28.22,37.76,21.75,28.56],42:[29.15,39.34,22.51,29.71],
  43:[30.09,40.91,23.28,30.86],44:[31.02,42.49,24.04,32.01],45:[31.96,44.06,24.81,33.16],
  46:[33.25,46.16,25.76,34.61],47:[34.54,48.26,26.71,36.06],48:[35.84,50.35,27.65,37.51],
  49:[37.13,52.45,28.60,38.96],50:[38.42,54.55,29.55,40.41],51:[40.06,57.19,30.79,42.23],
  52:[41.70,59.83,32.04,44.05],53:[43.34,62.47,33.28,45.88],54:[44.98,65.11,34.53,47.70],
  55:[46.62,67.75,35.77,49.52],56:[48.61,70.96,37.38,51.81],57:[50.60,74.17,38.99,54.11],
  58:[52.58,77.38,40.59,56.40],59:[54.57,80.59,42.20,58.70],60:[56.56,83.80,43.81,60.99],
  61:[59.30,88.02,46.09,64.27],62:[62.04,92.23,48.38,67.55],63:[64.77,96.45,50.66,70.83],
  64:[67.51,100.66,52.95,74.11],65:[70.25,104.88,55.23,77.39],66:[74.59,111.08,58.78,82.32],
  67:[78.93,117.27,62.33,87.25],68:[83.26,123.47,65.87,92.19],69:[87.60,129.66,69.42,97.12],
  70:[91.94,135.86,72.97,102.05],71:[98.92,144.86,78.39,109.03],72:[105.90,153.86,83.81,116.00],
  73:[112.87,162.85,89.24,122.98],74:[119.85,171.85,94.66,129.95],75:[126.83,180.85,100.08,136.93],
  76:[137.69,192.44,108.67,146.10],77:[148.54,204.03,117.27,155.27],78:[159.40,215.62,125.86,164.44],
  79:[170.25,227.21,134.46,173.61],80:[181.11,238.80,143.05,182.78],81:[197.83,261.74,153.80,195.63],
  82:[214.55,284.68,164.55,208.48],83:[231.27,307.63,175.30,221.33],84:[247.99,330.57,186.05,234.18],
  85:[264.71,353.51,196.80,247.03]
};
const BL_GRD={  // Graded Benefit
  40:[57.87,66.40,44.39,50.92],41:[60.15,69.02,45.89,52.64],42:[62.56,71.79,47.48,54.47],
  43:[65.07,74.66,49.12,56.35],44:[67.67,77.65,50.84,58.32],45:[70.40,80.78,52.62,60.36],
  46:[71.55,83.04,53.60,62.08],47:[72.70,85.30,54.58,63.79],48:[73.84,87.56,55.55,65.51],
  49:[74.99,89.82,56.53,67.22],50:[76.14,92.08,57.51,68.94],51:[78.85,96.16,59.52,71.77],
  52:[81.55,100.24,61.53,74.60],53:[84.26,104.31,63.53,77.43],54:[86.96,108.39,65.54,80.26],
  55:[89.67,112.47,67.55,83.09],56:[92.70,117.63,70.08,86.65],57:[95.73,122.78,72.61,90.21],
  58:[98.75,127.94,75.13,93.76],59:[101.78,133.09,77.66,97.32],60:[104.81,138.25,80.19,100.88],
  61:[108.94,144.60,83.74,105.90],62:[113.07,150.95,87.30,110.92],63:[117.19,157.29,90.85,115.93],
  64:[121.32,163.64,94.41,120.95],65:[125.45,169.99,97.96,125.97],66:[132.50,179.56,104.18,134.00],
  67:[139.56,189.13,110.40,142.04],68:[146.61,198.69,116.61,150.07],69:[153.67,208.26,122.83,158.11],
  70:[160.72,217.83,129.05,166.14],71:[174.44,234.33,139.47,178.79],72:[188.17,250.82,149.89,191.45],
  73:[201.89,267.32,160.32,204.10],74:[215.62,283.81,170.74,216.76],75:[229.34,300.31,181.16,229.41],
  76:[246.83,311.71,202.06,249.34],77:[264.32,323.12,222.95,269.27],78:[281.80,334.52,243.85,289.19],
  79:[299.29,345.93,264.74,309.12],80:[316.78,357.33,285.64,329.05],81:[350.04,400.84,302.61,350.95],
  82:[383.29,444.35,319.58,372.85],83:[416.55,487.87,336.56,394.75],84:[449.80,531.38,353.53,416.65],
  85:[483.06,574.89,370.50,438.55]
};
function blQuote(tbl,age,male,smoker,face){
  const row=tbl[age]; if(!row) return null;
  const ri=(male?0:2)+(smoker?1:0);
  return Math.round((row[ri]*face/1000+78)*0.085*100)/100;
}
// ── RATE TABLES ──
const ACC = {
  pf:[[35.30,50.40,27.20,43.30],[37.50,52.20,28.80,44.10],[39.70,54.00,30.40,44.90],[41.90,55.80,32.00,45.70],[44.10,57.60,33.60,46.50],[46.40,59.50,35.30,47.40],[48.00,61.90,36.70,48.80],[49.60,64.30,38.10,50.20],[51.20,66.70,39.50,51.60],[52.80,69.10,40.90,53.00],[54.40,71.60,42.30,54.40],[56.00,75.40,43.90,56.80],[57.60,79.20,45.50,59.20],[59.20,83.00,47.10,61.60],[60.80,86.80,48.70,64.00],[62.50,90.70,50.40,66.50],[65.90,97.20,52.80,70.50],[69.30,103.70,55.20,74.50],[72.70,110.20,57.60,78.50],[76.10,116.70,60.00,82.50],[79.60,123.00,62.50,86.70],[86.10,133.50,66.50,92.50],[92.60,144.00,70.50,98.30],[99.10,154.50,74.50,104.10],[105.60,165.00,78.50,109.90],[111.90,175.40,82.70,115.90],[120.80,191.70,88.50,128.00],[129.70,208.00,94.30,140.10],[138.60,224.30,100.10,152.20],[147.50,240.60,105.90,164.30],[156.30,257.10,111.90,176.40],[169.00,281.30,120.00,189.30],[181.70,305.50,128.10,202.20],[194.40,329.70,136.20,215.10],[207.10,353.90,144.30,228.00],[219.80,378.00,152.20,240.90]],
  st:[[42.30,58.50,38.30,54.40],[44.10,61.70,39.90,56.00],[45.90,64.90,41.50,57.60],[47.70,68.10,43.10,59.20],[49.50,71.30,44.70,60.80],[51.40,74.60,46.40,62.50],[54.40,79.40,48.00,65.10],[57.40,84.20,49.60,67.70],[60.40,89.00,51.20,70.30],[63.40,93.80,52.80,72.90],[66.50,98.80,54.40,75.60],[70.50,107.90,56.80,80.20],[74.50,117.00,59.20,84.80],[78.50,126.10,61.60,89.40],[82.50,135.20,64.00,94.00],[86.70,144.20,66.50,98.80],[92.30,156.30,69.90,103.80],[97.90,168.40,73.30,108.80],[103.50,180.50,76.70,113.80],[109.10,192.60,80.10,118.80],[114.90,204.60,83.70,124.00],[124.00,224.80,88.50,131.10],[133.10,245.00,93.30,138.20],[142.20,265.20,98.10,145.30],[151.30,285.40,102.90,152.40],[160.30,305.50,107.90,159.30],[173.80,322.20,117.60,176.40],[187.30,338.90,127.30,193.50],[200.80,355.60,137.00,210.60],[214.30,372.30,146.70,227.70],[227.80,389.10,156.30,245.00],[252.80,424.40,174.50,267.70],[277.80,459.70,192.70,290.40],[302.80,495.00,210.90,313.10],[327.80,530.30,229.10,335.80],[352.80,565.80,247.40,358.60]],
  md:[[52.50,65.40,40.50,61.30],[54.60,68.50,42.80,63.80],[56.70,71.60,45.10,66.30],[58.80,74.70,47.40,68.80],[60.90,77.80,49.70,71.30],[62.80,81.00,51.90,73.70],[67.40,87.60,54.40,77.00],[72.00,94.20,56.90,80.30],[76.60,100.80,59.40,83.60],[81.20,107.40,61.90,86.90],[85.70,114.20,64.40,90.30],[91.20,120.80,66.30,95.90],[96.70,127.40,68.20,101.50],[102.20,134.00,70.10,107.10],[107.70,140.60,72.00,112.70],[113.10,147.40,73.70,118.40],[117.70,157.60,78.50,125.90],[122.30,167.80,83.30,133.40],[126.90,178.00,88.10,140.90],[131.50,188.20,92.90,148.40],[135.90,198.30,97.60,155.80],[150.30,213.00,110.30,165.10],[164.70,227.70,123.00,174.40],[179.10,242.40,135.70,183.70],[193.50,257.10,148.40,193.00],[207.70,272.00,160.90,202.50]]
};
const AHL = {
  pf:[[20.63,29.46,14.68,23.37],[21.71,30.24,15.27,23.52],[22.79,31.03,15.87,23.66],[23.88,31.82,16.46,23.81],[24.96,32.60,17.06,23.95],[26.04,33.39,17.65,24.10],[26.34,34.25,18.35,25.25],[26.64,35.11,19.05,26.40],[26.94,35.98,19.75,27.55],[27.24,36.84,20.45,28.70],[27.54,37.70,21.15,29.85],[28.28,39.96,22.09,31.36],[29.02,42.21,23.02,32.87],[29.77,44.47,23.96,34.38],[30.51,46.72,24.89,35.89],[31.25,48.98,25.83,37.40],[33.27,52.40,26.97,39.41],[35.29,55.83,28.12,41.42],[37.31,59.25,29.26,43.43],[39.33,62.68,30.41,45.44],[41.35,66.10,31.55,47.45],[44.27,70.60,33.52,49.56],[47.19,75.10,35.49,51.68],[50.11,79.60,37.46,53.79],[53.03,84.10,39.43,55.91],[55.95,88.60,41.40,58.02],[60.39,96.59,45.14,65.36],[64.83,104.58,48.88,72.71],[69.27,112.57,52.62,80.05],[73.71,120.56,56.36,87.40],[78.15,128.55,60.10,94.74],[85.12,141.71,64.96,102.50],[92.09,154.86,69.81,110.26],[99.06,168.02,74.67,118.03],[106.03,181.17,79.52,125.79],[113.00,194.33,84.38,133.55]],
  st:[[22.97,31.76,19.15,29.47],[24.12,33.75,19.72,29.50],[25.28,35.74,20.29,29.54],[26.43,37.73,20.86,29.57],[27.59,39.72,21.43,29.60],[28.74,41.71,22.00,29.64],[29.64,43.25,23.04,31.71],[30.54,44.79,24.08,33.78],[31.45,46.32,25.12,35.85],[32.35,47.86,26.16,37.93],[33.25,49.40,27.20,40.00],[35.27,53.94,28.61,42.62],[37.29,58.48,30.02,45.24],[39.31,63.02,31.42,47.86],[41.33,67.56,32.83,50.48],[43.35,72.10,34.24,53.10],[46.17,78.14,35.82,56.13],[48.99,84.18,37.40,59.16],[51.81,90.22,38.99,62.19],[54.63,96.26,40.57,65.22],[57.45,102.30,42.15,68.25],[61.99,112.39,44.73,70.85],[66.53,122.48,47.31,73.45],[71.07,132.57,49.88,76.06],[75.61,142.66,52.46,78.66],[80.15,152.75,55.04,81.26],[86.90,161.11,60.17,90.30],[93.65,169.47,65.30,99.35],[100.40,177.83,70.43,108.39],[107.15,186.19,75.56,117.44],[113.90,194.55,80.69,126.48],[126.40,212.22,89.29,137.04],[138.90,229.89,97.89,147.61],[151.40,247.56,106.50,158.17],[163.90,265.23,115.10,168.74],[176.40,282.90,123.70,179.30]],
  md:[[26.75,33.33,21.05,34.85],[28.13,35.34,22.03,35.25],[29.51,37.36,23.01,35.65],[30.89,39.38,23.99,36.05],[32.27,41.39,24.97,36.45],[33.66,43.41,25.95,36.85],[35.49,46.15,27.20,38.94],[37.33,48.89,28.45,41.03],[39.17,51.62,29.70,43.12],[41.01,54.36,30.95,45.21],[42.85,57.10,32.20,47.30],[45.59,60.42,33.96,51.01],[48.33,63.74,35.71,54.71],[51.07,67.06,37.47,58.42],[53.81,70.38,39.22,62.13],[56.55,73.70,40.98,65.84],[58.83,78.79,42.85,69.67],[61.11,83.88,44.71,73.50],[63.39,88.97,46.58,77.33],[65.67,94.06,48.45,81.17],[67.95,99.15,50.32,85.00],[75.13,106.52,56.34,88.25],[82.31,113.89,62.37,91.50],[89.49,121.26,68.40,94.75],[96.67,128.63,74.42,98.00],[103.85,136.00,80.45,101.25]]
};
const CONT = {
  pf:[[36.50,52.00,28.00,44.50],[38.30,53.20,29.30,44.90],[40.10,54.40,30.60,45.30],[41.90,55.60,31.90,45.70],[43.70,56.80,33.20,46.10],[45.60,58.00,34.70,46.60],[47.70,61.20,36.50,48.50],[49.80,64.40,38.30,50.40],[51.90,67.60,40.10,52.30],[54.00,70.80,41.90,54.20],[56.20,74.00,43.50,56.00],[57.60,77.40,45.00,58.20],[59.00,80.80,46.50,60.40],[60.40,84.20,48.00,62.60],[61.80,87.60,49.50,64.80],[63.00,91.00,51.00,67.00],[66.40,97.60,53.40,71.00],[69.80,104.20,55.80,75.00],[73.20,110.80,58.20,79.00],[76.60,117.40,60.60,83.00],[80.00,124.00,63.00,87.00],[86.60,134.60,67.00,93.00],[93.20,145.20,71.00,99.00],[99.80,155.80,75.00,105.00],[106.40,166.40,79.00,111.00],[113.00,177.00,83.00,117.00],[121.80,193.60,89.00,129.20],[130.60,210.20,95.00,141.40],[139.40,226.80,101.00,153.60],[148.20,243.40,107.00,165.80],[157.00,260.00,113.00,178.00],[169.80,284.40,121.20,191.60],[182.60,308.80,129.40,205.20],[195.40,333.20,137.60,218.80],[208.20,357.60,145.80,232.40],[221.00,382.00,154.00,246.00]]
};
const RN = {
  siwl_st:[[40.20,57.62,34.25,53.55],[41.69,60.37,35.13,54.52],[43.18,63.12,36.01,55.49],[44.66,65.87,36.90,56.47],[46.15,68.62,37.78,57.44],[47.64,71.37,38.66,58.41],[48.86,73.34,38.75,60.71],[50.08,75.31,38.85,63.01],[51.31,77.28,38.94,65.31],[52.53,79.25,39.04,67.61],[53.75,81.22,39.13,69.91],[55.77,85.76,41.17,73.38],[57.80,90.30,43.21,76.85],[59.82,94.83,45.25,80.31],[61.85,99.37,47.29,83.78],[63.87,103.91,49.33,87.25],[68.28,110.18,52.39,91.79],[72.69,116.45,55.45,96.32],[77.09,122.71,58.50,100.86],[81.50,128.98,61.56,105.39],[85.91,135.25,64.62,109.93],[92.60,147.36,69.53,116.44],[99.29,159.48,74.44,122.96],[105.97,171.59,79.34,129.47],[112.66,183.71,84.25,135.99],[119.35,195.82,89.16,142.50],[129.76,208.60,98.24,150.60],[140.17,221.38,107.33,158.70],[150.58,234.17,116.41,166.79],[160.99,246.95,125.50,174.89],[171.40,259.73,134.58,182.99],[187.04,280.34,144.45,197.32],[202.67,300.95,154.31,211.65],[218.31,321.55,164.18,225.99],[233.94,342.16,174.04,240.32],[249.58,362.77,183.91,254.65]],
  gdb:[[65.00,82.75,47.00,59.70],[65.60,84.66,47.64,61.75],[66.20,86.57,48.28,63.79],[66.80,88.48,48.92,65.84],[67.40,90.39,49.56,67.88],[68.00,92.30,50.20,69.93],[68.70,94.12,50.90,72.31],[69.40,95.94,51.60,74.70],[70.10,97.76,52.30,77.08],[70.80,99.58,53.00,79.47],[71.50,101.40,53.70,81.85],[75.83,107.97,56.74,86.39],[80.16,114.54,59.78,90.94],[84.49,121.10,62.82,95.48],[88.82,127.67,65.86,100.03],[93.15,134.24,68.90,104.57],[100.26,144.91,73.64,111.77],[107.37,155.57,78.38,118.97],[114.48,166.24,83.12,126.17],[121.59,176.90,87.86,133.37],[128.70,187.57,92.60,140.57],[143.56,200.56,102.26,151.86],[158.42,213.55,111.92,163.14],[173.28,226.53,121.58,174.43],[188.14,239.52,131.24,185.71],[203.00,252.51,140.90,197.00],[220.00,267.59,152.86,208.54],[237.00,282.67,164.82,220.08],[254.00,297.76,176.78,231.63],[271.00,312.84,188.74,243.17],[288.00,327.92,200.70,254.71],[310.20,351.25,219.02,268.96],[332.39,374.57,237.33,283.21],[354.59,397.90,255.65,297.45],[376.78,421.22,273.96,311.70],[398.98,444.55,292.28,325.95]],
  gi:[[93.60,71.40],[97.52,74.36],[101.44,77.32],[105.36,80.28],[109.28,83.24],[113.20,86.20],[118.56,91.20],[123.92,96.20],[129.28,101.20],[134.64,106.20],[140.00,111.20],[147.68,116.96],[155.36,122.72],[163.04,128.48],[170.72,134.24],[178.40,140.00],[192.72,150.00],[207.04,160.00],[221.36,170.00],[235.68,180.00],[250.00,190.00],[271.00,206.00],[292.00,222.00],[313.00,238.00],[334.00,254.00],[355.00,270.00],[367.00,294.00],[379.00,318.00],[391.00,342.00],[403.00,366.00],[415.00,390.00]]
};

// ── NEW CARRIER RATE TABLES (CSV-sourced) ──
// Formula: modal_factor × (rate_per_1k × face/1000 + policy_fee)
// Each row: [MaleNS, MaleSM, FemaleNS, FemaleSM], each entry [rate_per_1k, policy_fee] or null
const n = null;

// Royal Arcanum (Whole Life) | Whole Life Level | modal=0.08961
const RA_M=0.08961;
const RA={45:[[1.833,0.99],n,n,n],46:[[1.891,1.0],n,n,n],47:[[1.953,1.0],n,n,n],48:[[2.017,1.0],n,n,n],49:[[2.086,1.0],n,n,n],50:[[2.16,1.0],n,[1.955,1.0],n],51:[[2.238,0.99],n,[2.022,0.99],n],52:[[2.32,0.99],n,[2.094,0.99],n],53:[[2.41,1.0],n,[2.169,1.0],n],54:[[2.524,1.01],n,[2.248,1.0],n],55:[[2.645,1.0],n,[2.333,1.0],n],56:[[2.773,1.0],n,[2.422,1.0],n],57:[[2.908,1.0],n,[2.517,0.99],n],58:[[3.052,1.0],n,[2.617,0.99],n],59:[[3.206,1.01],n,[2.724,0.99],n],60:[n,n,[2.837,1.01],n],61:[n,n,[2.96,1.0],n],62:[n,n,[3.09,1.01],n],63:[n,n,[3.238,1.0],n],64:[n,n,[3.399,0.99],n],65:[n,[5.831,1.0],n,n],66:[n,[6.065,1.0],n,n],67:[n,[6.406,1.0],n,n],68:[n,[6.765,0.99],n,n],69:[n,[7.144,0.99],n,n],70:[n,[7.504,0.99],n,n],71:[n,[7.883,1.0],n,n],72:[n,[8.29,1.0],n,n],73:[n,[8.731,1.0],n,n],74:[n,[9.211,1.01],n,n],75:[n,[9.723,1.01],n,n],76:[n,[10.302,0.99],n,n],77:[n,[11.121,0.99],n,n],78:[n,[11.788,1.0],n,n],79:[n,[12.508,0.99],n,n]};

// Lifeshield | Survivor Level | modal=0.084984
const LS_M=0.084984;
const LS={45:[[1.95,2.12],n,n,n],46:[[2.032,2.12],n,n,n],47:[[2.117,2.12],n,n,n],48:[[2.205,2.12],n,n,n],49:[[2.298,2.12],n,n,n],50:[[2.394,2.12],n,[2.128,2.12],n],51:[[2.492,2.13],n,[2.204,2.13],n],52:[[2.594,2.13],n,[2.282,2.13],n],53:[[2.7,2.12],n,[2.362,2.13],n],54:[[2.81,2.13],n,[2.449,2.12],n],55:[[2.927,2.13],n,[2.542,2.13],n],56:[[3.046,2.12],n,[2.644,2.12],n],57:[[3.163,2.12],n,[2.751,2.12],n],58:[[3.289,2.12],n,[2.865,2.12],n],59:[[3.432,2.13],n,[2.985,2.13],n],60:[n,n,[3.114,2.12],n],61:[n,n,[3.246,2.13],n],62:[n,n,[3.382,2.13],n],63:[n,n,[3.528,2.12],n],64:[n,n,[3.687,2.13],n],65:[n,[6.945,2.13],n,n],66:[n,[7.41,2.13],n,n],67:[n,[7.908,2.13],n,n],68:[n,[8.445,2.12],n,n],69:[n,[9.022,2.12],n,n],70:[n,[9.644,2.13],n,n],71:[n,[10.288,2.12],n,n],72:[n,[10.951,2.13],n,n],73:[n,[11.669,2.12],n,n],74:[n,[12.473,2.12],n,n],75:[n,[13.399,2.13],n,n],76:[n,[14.447,2.13],n,n],77:[n,[15.594,2.13],n,n],78:[n,[16.84,2.13],n,n],79:[n,[18.185,2.12],n,n]};

// ── American Amicable / Occidental Life — Senior Choice ──────────────
// Formula: Math.round((rate * face/1000 + 30) * 0.088 * 100) / 100
// ri: 0=male_ns, 1=male_sm, 2=female_ns, 3=female_sm
// Immediate: B/C caps $50k | Graded: D caps $25k
const AMAM_IMM={
  50:[32.96,43.12,27.30,32.55],51:[34.90,45.03,29.36,33.62],52:[36.67,47.09,30.58,35.34],
  53:[39.14,49.42,32.21,37.29],54:[40.94,51.61,33.74,38.73],55:[42.49,53.82,35.28,40.94],
  56:[44.18,56.05,36.42,42.23],57:[45.32,58.29,37.70,44.20],58:[47.64,61.08,38.77,45.91],
  59:[49.50,63.35,40.17,47.70],60:[50.47,65.82,40.48,49.01],61:[53.38,70.04,42.85,51.46],
  62:[56.09,73.13,44.50,54.08],63:[58.71,76.01,46.44,56.85],64:[61.80,79.64,48.50,59.78],
  65:[64.89,83.43,50.47,62.57],66:[69.24,88.51,53.59,65.88],67:[73.78,93.22,56.34,69.33],
  68:[78.70,98.88,59.45,72.10],69:[83.12,104.55,62.52,77.12],70:[86.53,108.72,65.61,79.02],
  71:[92.03,115.15,69.53,83.20],72:[97.83,121.93,73.65,87.61],73:[104.40,129.60,78.84,92.61],
  74:[111.76,137.51,83.69,97.75],75:[119.74,147.55,89.87,104.29],76:[128.75,157.59,95.83,112.49],
  77:[138.02,168.10,101.29,120.00],78:[150.28,180.87,108.15,127.85],79:[161.92,191.58,116.60,139.06],
  80:[174.07,203.53,126.18,150.62],81:[187.87,216.30,135.75,164.14],82:[202.91,229.56,146.26,179.51],
  83:[217.02,246.08,158.11,195.69],84:[232.78,266.64,170.98,214.76],85:[248.49,289.69,185.66,236.13]
};
const AMAM_GRD={
  50:[40.10,60.54,31.60,39.42],51:[42.35,63.59,33.24,41.70],52:[44.61,66.64,34.88,43.99],
  53:[47.16,70.09,36.73,46.58],54:[49.72,73.54,38.58,49.16],55:[52.27,76.99,40.43,51.76],
  56:[54.51,80.07,42.11,54.62],57:[56.86,83.32,43.88,57.63],58:[59.33,86.73,45.73,60.79],
  59:[61.91,90.30,47.68,64.11],60:[63.91,93.06,49.18,66.67],61:[67.32,97.77,51.75,71.04],
  62:[71.08,102.96,54.58,75.86],63:[74.96,108.31,57.49,80.83],64:[79.08,113.99,60.58,86.11],
  65:[83.43,120.00,63.86,91.67],66:[89.84,127.56,68.27,97.27],67:[96.82,135.81,73.08,103.39],
  68:[104.25,144.57,78.19,109.89],69:[112.25,154.02,83.70,115.36],70:[116.03,158.49,86.30,120.21],
  71:[123.89,167.77,91.71,127.72],72:[133.90,178.25,97.82,134.86],73:[144.20,190.28,104.83,143.78],
  74:[155.02,204.35,113.30,152.18],75:[166.09,217.59,120.77,164.03],76:[179.53,237.11,129.78,174.29],
  77:[196.73,255.76,140.60,180.79],78:[215.27,274.12,154.50,193.50],79:[234.33,295.71,167.38,207.22],
  80:[254.20,313.12,182.31,224.54],81:[269.86,316.15,197.76,238.85],82:[283.87,320.54,213.21,258.06],
  83:[296.64,325.48,227.63,278.28],84:[307.97,336.06,241.02,301.39],85:[312.35,359.73,248.49,328.83]
};

// ── SCRAPED RATE TABLES: Direct monthly premiums at $10k face ──
// Formula: scaleRate(ageRateMap, age, face) interpolates between ages and scales by face
// Ages in data: 50, 55, 60, 65, 70, 75, 80 (5-year intervals)
// Interpolation: linear between known ages, linear scaling by face/10000

function scaleRate(ageRateMap, age, face) {
  const knownAges = Object.keys(ageRateMap).map(Number).sort((a,b)=>a-b);
  if (age < knownAges[0] || age > knownAges[knownAges.length-1]) return null;
  // Find surrounding ages
  let lo = knownAges[0], hi = knownAges[knownAges.length-1];
  for (let i = 0; i < knownAges.length - 1; i++) {
    if (age >= knownAges[i] && age <= knownAges[i+1]) {
      lo = knownAges[i]; hi = knownAges[i+1]; break;
    }
  }
  const rateLo = ageRateMap[lo], rateHi = ageRateMap[hi];
  if (rateLo == null || rateHi == null) return null;
  const t = (hi === lo) ? 0 : (age - lo) / (hi - lo);
  const rate10k = rateLo + t * (rateHi - rateLo);
  return Math.round(rate10k * face / 10000 * 100) / 100;
}


// ── Senior Life Whole Life — direct monthly premiums (policy fee included) ──
// Ages 50-85 | No tobacco distinction
// B=Super Preferred [10k-30k] | C=Standard [2k-20k] | D=Modified [2k-15k] | E=GI [2k-10k]
const SL_WL_M={
  B:{ // breakpoints: 10k,15k,20k,25k,30k
    50:[28.81,41.68,54.55,67.43,80.30],51:[30.06,43.57,57.07,70.57,84.07],52:[29.50,42.73,55.95,69.17,82.39],
    53:[30.88,44.79,58.70,72.61,86.52],54:[32.31,46.93,61.56,76.18,90.81],55:[33.82,49.20,64.58,79.96,95.34],
    56:[35.55,51.79,68.03,84.28,100.52],57:[37.32,54.46,71.59,88.72,105.85],58:[39.29,57.40,75.51,93.63,111.74],
    59:[41.36,60.51,79.66,98.81,117.96],60:[43.58,63.84,84.10,104.36,124.62],61:[46.13,67.66,89.20,110.73,132.27],
    62:[52.19,76.76,101.32,125.89,150.45],63:[51.80,76.17,100.54,124.91,149.28],64:[54.94,80.89,106.83,132.77,158.71],
    65:[58.15,85.69,113.24,140.78,168.33],66:[62.91,92.83,122.76,152.68,182.61],67:[66.97,98.93,130.88,162.84,194.79],
    68:[71.03,105.02,139.01,173.00,206.98],69:[75.11,111.13,147.15,183.18,219.20],70:[78.20,115.77,153.34,190.91,228.48],
    71:[85.26,126.37,167.47,208.57,249.67],72:[89.48,132.69,175.90,219.11,262.32],73:[93.63,138.91,184.20,229.48,274.76],
    74:[97.78,145.13,192.49,239.85,287.21],75:[101.90,151.32,200.74,250.16,299.57],76:[112.26,166.86,221.46,276.06,330.66],
    77:[122.62,182.40,242.18,301.96,361.74],78:[131.19,195.25,259.32,323.38,387.45],79:[139.02,207.00,274.98,342.95,410.93],
    80:[147.25,219.34,291.43,363.52,435.62],81:[167.11,249.14,331.16,413.19,495.21],82:[190.83,284.72,378.61,472.49,566.38],
    83:[221.11,330.14,439.16,548.19,657.21],84:[255.67,381.98,508.28,634.59,760.89],85:[297.19,444.26,591.33,738.40,885.46]},
  C:{ // breakpoints: 2k,5k,10k,15k,20k
    50:[12.81,27.43,51.80,76.17,100.54],51:[13.26,28.57,54.08,79.59,105.09],52:[13.72,29.71,56.36,83.02,109.67],
    53:[14.18,30.85,58.64,86.43,114.22],54:[14.63,31.99,60.93,89.86,118.80],55:[15.09,33.13,63.21,93.28,123.35],
    56:[15.59,34.38,65.70,97.01,128.33],57:[16.09,35.62,68.19,100.75,133.31],58:[16.96,37.80,72.54,107.28,142.02],
    59:[17.37,38.84,74.61,110.39,146.17],60:[18.41,41.43,79.80,118.17,156.54],61:[19.44,44.02,84.98,125.94,166.91],
    62:[20.69,47.13,91.21,135.28,179.35],63:[21.93,50.24,97.43,144.61,191.79],64:[23.18,53.35,103.65,153.94,204.24],
    65:[24.42,56.47,109.87,163.28,216.68],66:[25.67,59.58,116.09,172.61,229.13],67:[27.12,63.21,123.35,183.50,243.64],
    68:[28.57,66.84,130.61,194.39,258.16],69:[30.02,70.47,137.87,205.28,272.68],70:[31.68,74.61,146.17,217.72,289.27],
    71:[33.55,79.28,155.50,231.72,307.94],72:[35.62,84.46,165.87,247.27,328.68],73:[37.70,89.65,176.24,262.83,349.42],
    74:[39.98,95.35,187.65,279.94,372.23],75:[42.47,101.58,200.09,298.61,397.12],76:[45.16,108.32,213.57,318.83,424.08],
    77:[48.07,115.57,228.09,340.60,453.12],78:[51.38,123.87,244.68,365.49,486.30],79:[55.12,133.20,263.35,393.49,523.63],
    80:[59.27,143.57,284.09,424.60,565.11],81:[60.69,147.13,291.19,435.26,579.33],82:[62.35,151.28,299.50,447.72,595.94],
    83:[64.01,155.44,307.81,460.19,612.56],84:[65.67,159.59,316.12,472.66,629.19],85:[67.34,163.75,324.44,485.12,645.81]},
  D:{ // breakpoints: 2k,5k,10k,15k
    50:[20.87,47.59,92.12,136.65],51:[20.98,47.85,92.64,137.43],52:[21.08,48.10,93.14,138.18],
    53:[21.64,49.51,95.96,142.40],54:[22.12,50.70,98.35,145.99],55:[22.59,51.89,100.73,149.56],
    56:[23.07,53.09,103.11,153.14],57:[23.55,54.28,105.49,156.71],58:[24.02,55.47,107.87,160.28],
    59:[24.50,56.66,110.26,163.86],60:[24.98,57.85,112.64,167.43],61:[25.45,59.04,115.02,171.00],
    62:[25.93,60.24,117.41,174.59],63:[26.41,61.43,119.79,178.16],64:[26.88,62.62,122.17,181.73],
    65:[27.36,63.81,124.56,185.31],66:[27.86,65.07,127.08,189.10],67:[29.47,69.09,135.12,201.14],
    68:[31.08,73.10,143.14,213.18],69:[32.68,77.12,151.17,225.23],70:[34.29,81.13,159.20,237.26],
    71:[35.88,85.12,167.18,249.24],72:[37.48,89.11,175.17,261.22],73:[39.08,93.11,183.17,273.22],
    74:[40.68,97.11,191.16,285.20],75:[42.47,101.58,200.09,298.61],76:[45.16,108.32,213.57,318.83],
    77:[48.07,115.57,228.09,340.60],78:[51.38,123.87,244.68,365.49],79:[55.12,133.20,263.35,393.49],
    80:[59.27,143.57,284.09,424.60],81:[60.69,147.13,291.19,435.26],82:[62.35,151.28,299.50,447.72],
    83:[64.01,155.44,307.81,460.19],84:[65.67,159.59,316.12,472.66],85:[67.34,163.75,324.44,485.12]},
  E:{ // breakpoints: 2k,5k,10k
    50:[22.65,52.05,101.03],51:[22.77,52.33,101.59],52:[22.88,52.60,102.14],
    53:[23.50,54.16,105.26],54:[24.02,55.47,107.87],55:[24.55,56.78,110.49],
    56:[25.07,58.09,113.12],57:[25.60,59.40,115.74],58:[26.12,60.71,118.35],
    59:[26.64,62.02,120.98],60:[27.17,63.33,123.60],61:[27.69,64.64,126.23],
    62:[28.22,65.95,128.84],63:[28.74,67.26,131.46],64:[29.27,68.57,134.09],
    65:[29.79,69.88,136.71],66:[30.35,71.27,139.49],67:[32.11,75.69,148.33],
    68:[33.88,80.10,157.15],69:[35.65,84.52,165.99],70:[37.41,88.93,174.80],
    71:[39.17,93.33,183.59],72:[40.92,97.72,192.38],73:[42.68,102.12,201.18],
    74:[44.44,106.51,209.97],75:[46.20,110.90,218.75],76:[48.17,115.83,228.60],
    77:[50.14,120.75,238.44],78:[52.11,125.67,248.29],79:[55.12,133.20,263.35],
    80:[59.27,143.57,284.09],81:[60.69,147.13,291.19],82:[62.35,151.28,299.50],
    83:[64.01,155.44,307.81],84:[65.67,159.59,316.12],85:[67.34,163.75,324.44]}
};
const SL_WL_F={
  B:{ // breakpoints: 10k,15k,20k,25k,30k
    50:[23.75,34.09,44.44,54.78,65.13],51:[24.84,35.73,46.61,57.50,68.39],52:[24.38,35.04,45.70,56.36,67.01],
    53:[25.40,36.57,47.74,58.91,70.07],54:[26.42,38.10,49.78,61.46,73.13],55:[27.68,39.98,52.29,64.60,76.91],
    56:[28.86,41.76,54.66,67.55,80.45],57:[30.22,43.80,57.38,70.95,84.53],58:[31.48,45.70,59.91,74.12,88.33],
    59:[33.17,48.22,63.27,78.33,93.38],60:[34.54,50.27,66.01,81.75,97.49],61:[36.77,53.63,70.48,87.34,104.19],
    62:[38.43,56.11,73.80,91.48,109.17],63:[39.98,58.43,76.89,95.35,113.81],64:[41.54,60.78,80.02,99.26,118.50],
    65:[43.10,63.11,83.13,103.15,123.17],66:[45.83,67.22,88.60,109.99,131.38],67:[48.55,71.30,94.04,116.79,139.54],
    68:[51.26,75.37,99.47,123.57,147.67],69:[53.99,79.46,104.92,130.39,155.86],70:[56.72,83.55,110.38,137.21,164.04],
    71:[59.44,87.63,115.82,144.01,172.20],72:[62.16,91.71,121.26,150.81,180.36],73:[64.87,95.78,126.68,157.59,188.50],
    74:[67.61,99.88,132.16,164.43,196.71],75:[70.33,103.96,137.60,171.23,204.87],76:[78.16,115.71,153.26,190.80,228.35],
    77:[86.00,127.47,168.95,210.42,251.89],78:[93.84,139.23,184.62,230.01,275.40],79:[101.66,150.96,200.26,249.56,298.86],
    80:[109.51,162.74,215.97,269.20,322.42],81:[136.62,203.40,270.18,336.96,403.74],82:[161.43,240.62,319.80,398.99,478.18],
    83:[192.80,287.67,382.53,477.40,572.27],84:[228.45,341.15,453.85,566.55,679.24],85:[271.06,405.05,539.05,673.05,807.05]},
  C:{ // breakpoints: 2k,5k,10k,15k,20k
    50:[10.94,22.76,42.47,62.17,81.87],51:[11.23,23.49,43.92,64.35,84.78],52:[11.52,24.22,45.37,66.53,87.69],
    53:[11.81,24.94,46.82,68.70,90.58],54:[12.12,25.72,48.37,71.03,93.69],55:[12.50,26.65,50.24,73.84,97.43],
    56:[12.91,27.69,52.32,76.95,101.58],57:[13.32,28.72,54.38,80.04,105.71],58:[13.74,29.76,56.46,83.16,109.85],
    59:[14.15,30.79,58.52,86.25,113.99],60:[14.57,31.83,60.60,89.36,118.13],61:[15.33,33.74,64.42,95.10,125.78],
    62:[16.10,35.65,68.25,100.84,133.43],63:[16.86,37.57,72.08,106.59,141.10],64:[17.79,39.87,76.69,113.50,150.31],
    65:[18.82,42.47,81.87,121.28,160.68],66:[20.07,45.58,88.09,130.61,173.13],67:[21.52,49.21,95.35,141.50,187.65],
    68:[22.97,52.84,102.61,152.39,202.16],69:[24.63,56.98,110.91,164.83,218.76],70:[26.50,61.65,120.24,178.83,237.42],
    71:[28.36,66.32,129.57,192.83,256.09],72:[30.23,70.98,138.91,206.83,274.75],73:[32.10,75.65,148.24,220.83,293.42],
    74:[34.17,80.84,158.61,236.39,314.16],75:[36.45,86.54,170.02,253.50,336.97],76:[38.94,92.76,182.46,272.16,361.86],
    77:[41.64,99.50,195.94,292.38,388.82],78:[44.54,106.76,210.46,314.16,417.86],79:[47.65,114.54,226.02,337.49,448.97],
    80:[51.18,123.35,243.64,363.94,484.23],81:[55.12,133.20,263.35,393.49,523.63],82:[57.02,137.96,272.86,407.76,542.66],
    83:[58.92,142.72,282.38,422.04,561.70],84:[60.82,147.47,291.88,436.29,580.70],85:[62.73,152.23,301.39,450.56,599.73]},
  D:{ // breakpoints: 2k,5k,10k,15k
    50:[19.71,44.68,86.29,127.91],51:[19.73,44.73,86.39,128.06],52:[19.75,44.78,86.50,128.21],
    53:[19.77,44.83,86.60,128.37],54:[19.79,44.88,86.71,128.53],55:[19.81,44.94,86.81,128.69],
    56:[19.83,44.99,86.92,128.85],57:[19.85,45.04,87.02,129.00],58:[19.87,45.10,87.13,129.17],
    59:[19.90,45.15,87.24,129.34],60:[19.92,45.20,87.35,129.49],61:[20.18,45.87,88.68,131.49],
    62:[20.66,47.06,91.05,135.05],63:[20.99,47.88,92.69,137.51],64:[21.38,48.86,94.66,140.47],
    65:[21.77,49.84,96.61,143.39],66:[22.46,51.56,100.05,148.55],67:[23.69,54.63,106.21,157.78],
    68:[25.06,58.07,113.08,168.08],69:[26.44,61.51,119.96,178.41],70:[27.82,64.95,126.84,188.73],
    71:[29.63,69.47,135.89,202.30],72:[31.44,74.01,144.95,215.90],73:[33.25,78.53,154.00,229.47],
    74:[35.06,83.06,163.06,243.05],75:[36.87,87.58,172.11,256.63],76:[38.94,92.76,182.46,272.16],
    77:[41.64,99.50,195.94,292.38],78:[44.54,106.76,210.46,314.16],79:[47.65,114.54,226.02,337.49],
    80:[51.18,123.35,243.64,363.94],81:[55.12,133.20,263.35,393.49],82:[57.02,137.96,272.86,407.76],
    83:[58.92,142.72,282.38,422.04],84:[60.82,147.47,291.88,436.29],85:[62.73,152.23,301.39,450.56]},
  E:{ // breakpoints: 2k,5k,10k
    50:[21.37,48.84,94.61],51:[21.39,48.89,94.72],52:[21.41,48.95,94.83],
    53:[21.44,49.01,94.95],54:[21.46,49.07,95.08],55:[21.49,49.13,95.19],
    56:[21.51,49.19,95.31],57:[21.53,49.24,95.42],58:[21.56,49.30,95.54],
    59:[21.58,49.36,95.66],60:[21.60,49.41,95.77],61:[21.90,50.15,97.24],
    62:[22.42,51.45,99.85],63:[22.78,52.36,101.65],64:[23.22,53.45,103.84],
    65:[23.64,54.51,105.97],66:[24.40,56.41,109.76],67:[25.75,59.79,116.53],
    68:[27.26,63.57,124.08],69:[28.78,67.35,131.65],70:[30.29,71.14,139.22],
    71:[32.28,76.12,149.18],72:[34.28,81.10,159.14],73:[36.27,86.08,169.09],
    74:[38.26,91.06,179.06],75:[40.25,96.04,189.01],76:[42.31,101.18,199.30],
    77:[44.37,106.33,209.60],78:[46.43,111.48,219.90],79:[48.49,116.62,230.19],
    80:[51.18,123.35,243.64],81:[55.12,133.20,263.35],82:[57.02,137.96,272.86],
    83:[58.92,142.72,282.38],84:[60.82,147.47,291.88],85:[62.73,152.23,301.39]}
};

function slWlQuote(age,male,smoker,tier,face){
  if(age<50||age>85)return null;
  const tbl=male?SL_WL_M:SL_WL_F;
  const td=tbl[tier];if(!td)return null;
  const r=td[age];if(!r)return null;
  let bps,minF,maxF;
  if(tier==='B'){bps=[10000,15000,20000,25000,30000];minF=10000;maxF=30000;}
  else if(tier==='C'){bps=[2000,5000,10000,15000,20000];minF=2000;maxF=20000;}
  else if(tier==='D'){bps=[2000,5000,10000,15000];minF=2000;maxF=15000;}
  else if(tier==='E'){bps=[2000,5000,10000];minF=2000;maxF=10000;}
  else return null;
  const f=Math.max(minF,Math.min(maxF,face));
  if(f<=bps[0])return Math.round(r[0]*100)/100;
  if(f>=bps[bps.length-1])return Math.round(r[r.length-1]*100)/100;
  for(let i=0;i<bps.length-1;i++){
    if(f>=bps[i]&&f<=bps[i+1]){
      const t=(f-bps[i])/(bps[i+1]-bps[i]);
      return Math.round((r[i]+t*(r[i+1]-r[i]))*100)/100;
    }
  }
  return null;
}

// ── American Income Life ──────────────────────────────────────────
// Formula: Math.ceil(rate_per_1000 * face/1000 / 12 * 100) / 100  (round HIGH)
// WL Regular: ages 50-59 | Level benefit | B=NTU, C=TU | max face $30,000
// Senior Graded WL: ages 60-80 | Graded (25/50/75/100% yrs 1-4+) | D | TU max age 75
// Row format: [M-NTU, M-TU, F-NTU, F-TU]
const AIL_WL={
  50:[46.84,62.19,37.94,50.60], 51:[49.47,65.70,40.33,53.09],
  52:[52.24,69.38,42.80,55.68], 53:[55.19,73.27,45.37,58.44],
  54:[58.31,77.26,48.05,61.32], 55:[61.51,81.39,50.78,64.32],
  56:[64.83,85.62,53.60,67.50], 57:[68.31,89.96,56.49,70.79],
  58:[72.04,94.53,59.45,74.33], 59:[75.99,99.38,62.46,78.04]
};
const AIL_SRGWL={
  60:[81.97,106.82,67.01,83.75],  61:[86.55,112.37,70.29,88.01],
  62:[91.41,118.25,73.78,92.50],  63:[96.57,124.47,77.62,97.25],
  64:[102.04,131.02,81.92,102.27],65:[108.06,137.97,86.79,107.91],
  66:[114.64,146.70,92.64,114.30],67:[121.69,156.00,99.14,121.17],
  68:[129.23,165.94,106.27,128.46],69:[137.34,176.63,114.06,136.29],
  70:[146.06,188.12,122.49,144.71],71:[156.85,200.44,131.46,153.76],
  72:[168.48,213.60,141.12,163.42],73:[180.92,227.57,151.49,173.67],
  74:[194.26,242.33,162.59,184.50],75:[208.43,257.89,174.47,195.91],
  76:[223.46,null,187.29,null],   77:[239.55,null,200.89,null],
  78:[256.79,null,215.31,null],   79:[275.41,null,230.52,null],
  80:[294.92,null,246.55,null]
};

function ailQuote(age,male,smoker,tier,face){
  const ri=(male?0:2)+(smoker?1:0);
  const f=Math.min(face,30000);
  // Ages 50-59: WL Regular level benefit — show on Tier B and C (tobacco handled by smoker toggle)
  if(tier==='B'||tier==='C'){
    if(age<50||age>59)return null;
    const row=AIL_WL[age];if(!row||row[ri]==null)return null;
    return Math.ceil(row[ri]*f/1000/12*100)/100;
  }
  // Ages 60-80: Senior Graded WL — Tier D only (TU not available 76+)
  if(tier==='D'){
    if(age<60||age>80)return null;
    const row=AIL_SRGWL[age];if(!row)return null;
    const r=row[ri];if(r==null)return null;
    return Math.ceil(r*f/1000/12*100)/100;
  }
  return null;
}



// Liberty Bankers | SIMPL Preferred | modal=0.092494
const LBP_M=0.092494;
const LBP={45:[[2.409,4.62],n,n,n],46:[[2.508,4.62],n,n,n],47:[[2.606,4.62],n,n,n],48:[[2.695,4.63],n,n,n],49:[[2.777,4.62],n,n,n],50:[[2.857,4.63],n,[2.171,4.63],n],51:[[2.976,4.62],n,[2.272,4.62],n],52:[[3.094,4.63],n,[2.373,4.62],n],53:[[3.202,4.63],n,[2.464,4.63],n],54:[[3.3,4.62],n,[2.547,4.62],n],55:[[3.397,4.62],n,[2.629,4.62],n],56:[[3.573,4.63],n,[2.748,4.63],n],57:[[3.749,4.64],n,[2.868,4.63],n],58:[[3.91,4.63],n,[2.977,4.62],n],59:[[4.055,4.63],n,[3.075,4.62],n],60:[n,n,[3.173,4.62],n],61:[n,n,[3.361,4.63],n],62:[n,n,[3.551,4.63],n],63:[n,n,[3.723,4.63],n],64:[n,n,[3.878,4.62],n],65:[n,[8.583,4.63],n,n],66:[n,[9.175,4.63],n,n],67:[n,[9.767,4.63],n,n],68:[n,[10.305,4.63],n,n],69:[n,[10.789,4.63],n,n],70:[n,[11.274,4.62],n,n],71:[n,[12.24,4.62],n,n],72:[n,[13.205,4.63],n,n],73:[n,[14.083,4.63],n,n],74:[n,[14.874,4.63],n,n],75:[n,[15.663,4.64],n,n],76:[n,[16.795,4.63],n,n],77:[n,[17.927,4.63],n,n],78:[n,[18.956,4.63],n,n],79:[n,[19.881,4.64],n,n]};

// Liberty Bankers | SIMPL Standard | modal=0.092499
const LBS_M=0.092499;
const LBS={45:[[2.892,4.62],n,n,n],46:[[3.026,4.62],n,n,n],47:[[3.159,4.62],n,n,n],48:[[3.281,4.63],n,n,n],49:[[3.39,4.63],n,n,n],50:[[3.499,4.63],n,[2.808,4.63],n],51:[[3.734,4.63],n,[2.943,4.63],n],52:[[3.969,4.63],n,[3.078,4.63],n],53:[[4.183,4.62],n,[3.202,4.63],n],54:[[4.374,4.63],n,[3.312,4.63],n],55:[[4.567,4.62],n,[3.423,4.63],n],56:[[4.824,4.62],n,[3.565,4.62],n],57:[[5.08,4.63],n,[3.707,4.62],n],58:[[5.314,4.63],n,[3.836,4.63],n],59:[[5.524,4.63],n,[3.952,4.62],n],60:[n,n,[4.067,4.63],n],61:[n,n,[4.356,4.62],n],62:[n,n,[4.644,4.63],n],63:[n,n,[4.906,4.63],n],64:[n,n,[5.142,4.63],n],65:[n,[11.042,4.62],n,n],66:[n,[11.938,4.63],n,n],67:[n,[12.834,4.63],n,n],68:[n,[13.649,4.63],n,n],69:[n,[14.382,4.62],n,n],70:[n,[15.115,4.63],n,n],71:[n,[16.313,4.63],n,n],72:[n,[17.512,4.63],n,n],73:[n,[18.601,4.62],n,n],74:[n,[19.581,4.63],n,n],75:[n,[20.562,4.62],n,n],76:[n,[22.227,4.62],n,n],77:[n,[23.892,4.62],n,n],78:[n,[25.405,4.63],n,n],79:[n,[26.767,4.62],n,n]};

// Polish Falcons | Level | modal=0.0833
const PF_M=0.0833;
const PF2={50:[[2.638,2.5],n,[2.273,2.5],n],51:[[2.739,2.51],n,[2.348,2.49],n],52:[[2.847,2.49],n,[2.424,2.5],n],53:[[2.957,2.49],n,[2.503,2.49],n],54:[[3.071,2.49],n,[2.583,2.49],n],55:[[3.188,2.51],n,[2.667,2.5],n],56:[[3.328,2.51],n,[2.784,2.5],n],57:[[3.476,2.49],n,[2.907,2.49],n],58:[[3.627,2.51],n,[3.034,2.5],n],59:[[3.787,2.5],n,[3.168,2.5],n],60:[n,n,[3.307,2.5],n],61:[n,n,[3.456,2.49],n],62:[n,n,[3.61,2.5],n],63:[n,n,[3.774,2.49],n],64:[n,n,[3.942,2.51],n],65:[n,[8.79,2.51],n,n],66:[n,[9.313,2.5],n,n],67:[n,[9.866,2.5],n,n],68:[n,[10.453,2.49],n,n],69:[n,[11.074,2.51],n,n],70:[n,[11.732,2.5],n,n],71:[n,[12.723,2.5],n,n],72:[n,[13.798,2.5],n,n],73:[n,[14.963,2.5],n,n],74:[n,[16.227,2.5],n,n],75:[n,[17.597,2.5],n,n],76:[n,[18.939,2.5],n,n],77:[n,[20.384,2.49],n,n],78:[n,[21.938,2.5],n,n],79:[n,[23.612,2.49],n,n]};

// Americo | Eagle Select Plan 1 (Preferred) | modal=0.094988
const AMRP_M=0.094988;
const AMRP={45:[[2.331,3.79],n,n,n],46:[[2.392,3.8],n,n,n],47:[[2.453,3.8],n,n,n],48:[[2.531,3.8],n,n,n],49:[[2.608,3.81],n,n,n],50:[[2.686,3.81],n,[2.197,3.79],n],51:[[2.757,3.8],n,[2.284,3.8],n],52:[[2.827,3.8],n,[2.371,3.79],n],53:[[2.955,3.81],n,[2.439,3.81],n],54:[[3.083,3.81],n,[2.509,3.8],n],55:[[3.214,3.8],n,[2.58,3.79],n],56:[[3.357,3.79],n,[2.7,3.8],n],57:[[3.498,3.8],n,[2.819,3.81],n],58:[[3.643,3.79],n,[2.912,3.8],n],59:[[3.786,3.81],n,[3.006,3.8],n],60:[n,n,[3.103,3.81],n],61:[n,n,[3.263,3.79],n],62:[n,n,[3.42,3.8],n],63:[n,n,[3.54,3.81],n],64:[n,n,[3.662,3.79],n],65:[n,[9.284,3.79],n,n],66:[n,[9.827,3.8],n,n],67:[n,[10.376,3.8],n,n],68:[n,[11.127,3.79],n,n],69:[n,[11.877,3.8],n,n],70:[n,[12.63,3.79],n,n],71:[n,[13.378,3.8],n,n],72:[n,[14.135,3.8],n,n],73:[n,[15.499,3.79],n,n],74:[n,[16.861,3.81],n,n],75:[n,[18.226,3.8],n,n],76:[n,[19.243,3.79],n,n],77:[n,[20.267,3.79],n,n],78:[n,[21.867,3.8],n,n],79:[n,[23.468,3.8],n,n]};

// Americo | Eagle Select Plan 2 (Standard) | modal=0.095006
const AMRS_M=0.095006;
const AMRS={45:[[3.292,3.8],n,n,n],46:[[3.354,3.81],n,n,n],47:[[3.42,3.8],n,n,n],48:[[3.493,3.8],n,n,n],49:[[3.569,3.8],n,n,n],50:[[3.65,3.8],n,[3.181,3.81],n],51:[[3.733,3.81],n,[3.249,3.8],n],52:[[3.82,3.8],n,[3.321,3.79],n],53:[[3.937,3.8],n,[3.358,3.79],n],54:[[4.062,3.8],n,[3.397,3.79],n],55:[[4.197,3.8],n,[3.438,3.8],n],56:[[4.338,3.81],n,[3.482,3.8],n],57:[[4.486,3.8],n,[3.525,3.81],n],58:[[4.692,3.8],n,[3.659,3.79],n],59:[[4.907,3.8],n,[3.812,3.79],n],60:[n,n,[3.989,3.8],n],61:[n,n,[4.197,3.8],n],62:[n,n,[4.421,3.79],n],63:[n,n,[4.604,3.81],n],64:[n,n,[4.8,3.79],n],65:[n,[14.084,3.81],n,n],66:[n,[14.765,3.8],n,n],67:[n,[15.46,3.79],n,n],68:[n,[16.55,3.8],n,n],69:[n,[18.732,3.8],n,n],70:[n,[22.005,3.8],n,n],71:[n,[26.369,3.8],n,n],72:[n,[31.825,3.8],n,n],73:[n,[33.58,3.81],n,n],74:[n,[35.337,3.8],n,n],75:[n,[37.093,3.8],n,n]};


// Foresters PlanRight — rebuilt from Nov 2019 rate book
// Formula: (rate/1000 * face + 36) * 0.0875 | cert fee already baked into entry[1]=3.15
const FOR_P={50:[[2.8849,3.15],[4.2298,3.15],[2.2899,3.15],[3.4702,3.15]],51:[[2.982,3.15],[4.4231,3.15],[2.3922,3.15],[3.6041,3.15]],52:[[3.0791,3.15],[4.6165,3.15],[2.4946,3.15],[3.7389,3.15]],53:[[3.1771,3.15],[4.809,3.15],[2.5961,3.15],[3.8736,3.15]],54:[[3.2742,3.15],[5.0024,3.15],[2.6985,3.15],[4.0075,3.15]],55:[[3.3714,3.15],[5.1949,3.15],[2.8009,3.15],[4.1422,3.15]],56:[[3.5376,3.15],[5.4119,3.15],[2.8857,3.15],[4.3006,3.15]],57:[[3.7048,3.15],[5.628,3.15],[2.9706,3.15],[4.4581,3.15]],58:[[3.871,3.15],[5.845,3.15],[3.0555,3.15],[4.6156,3.15]],59:[[4.0373,3.15],[6.062,3.15],[3.1412,3.15],[4.774,3.15]],60:[[4.2035,3.15],[6.2781,3.15],[3.2261,3.15],[4.9315,3.15]],61:[[4.4144,3.15],[6.657,3.15],[3.374,3.15],[5.1782,3.15]],62:[[4.6261,3.15],[7.0367,3.15],[3.5219,3.15],[5.425,3.15]],63:[[4.837,3.15],[7.4156,3.15],[3.6689,3.15],[5.6717,3.15]],64:[[5.0479,3.15],[7.7945,3.15],[3.7275,3.15],[5.9185,3.15]],65:[[5.166,3.15],[8.1742,3.15],[3.7852,3.15],[6.1652,3.15]],66:[[5.4827,3.15],[8.6669,3.15],[4.004,3.15],[6.384,3.15]],67:[[5.8004,3.15],[9.1595,3.15],[4.2219,3.15],[6.6036,3.15]],68:[[6.1171,3.15],[9.6512,3.15],[4.4406,3.15],[6.8224,3.15]],69:[[6.4348,3.15],[10.1439,3.15],[4.6585,3.15],[7.0411,3.15]],70:[[6.7515,3.15],[10.6365,3.15],[4.8773,3.15],[7.2599,3.15]],71:[[7.5714,3.15],[11.6672,3.15],[5.46,3.15],[7.8418,3.15]],72:[[8.1664,3.15],[12.698,3.15],[5.8931,3.15],[8.4228,3.15]],73:[[8.7622,3.15],[13.7279,3.15],[6.3262,3.15],[9.0046,3.15]],74:[[9.3581,3.15],[14.7586,3.15],[6.7594,3.15],[9.5865,3.15]],75:[[9.954,3.15],[15.7885,3.15],[7.1925,3.15],[10.1675,3.15]],76:[[10.8351,3.15],[16.9216,3.15],[7.9126,3.15],[11.046,3.15]],77:[[11.7163,3.15],[18.0539,3.15],[8.6327,3.15],[11.9236,3.15]],78:[[12.5974,3.15],[19.1861,3.15],[9.3529,3.15],[12.8021,3.15]],79:[[13.4776,3.15],[20.3184,3.15],[10.073,3.15],[13.6798,3.15]],80:[[14.3587,3.15],[21.4506,3.15],[10.7931,3.15],[14.5574,3.15]],81:[[15.4394,3.15],[22.911,3.15],[11.8904,3.15],[15.7876,3.15]],82:[[16.5191,3.15],[24.3723,3.15],[12.9867,3.15],[17.0179,3.15]],83:[[17.5997,3.15],[25.8335,3.15],[14.0831,3.15],[18.2481,3.15]],84:[[18.6795,3.15],[27.2947,3.15],[15.1804,3.15],[19.4784,3.15]],85:[[19.7601,3.15],[28.7551,3.15],[16.2767,3.15],[20.7086,3.15]]};
const FOR_S={50:[[4.1895,3.15],[5.7505,3.15],[3.3915,3.15],[4.4004,3.15]],51:[[4.466,3.15],[6.1617,3.15],[3.5114,3.15],[4.6042,3.15]],52:[[4.7451,3.15],[6.5774,3.15],[3.6321,3.15],[4.8125,3.15]],53:[[5.0286,3.15],[6.9974,3.15],[3.7537,3.15],[5.0207,3.15]],54:[[5.313,3.15],[7.4226,3.15],[3.8771,3.15],[5.2316,3.15]],55:[[5.6017,3.15],[7.8514,3.15],[4.0014,3.15],[5.4434,3.15]],56:[[5.8957,3.15],[8.2757,3.15],[4.144,3.15],[5.6262,3.15]],57:[[6.1906,3.15],[8.701,3.15],[4.2875,3.15],[5.8091,3.15]],58:[[6.4846,3.15],[9.1254,3.15],[4.4292,3.15],[5.992,3.15]],59:[[6.7795,3.15],[9.5497,3.15],[4.5727,3.15],[6.1749,3.15]],60:[[7.0726,3.15],[9.9741,3.15],[4.7154,3.15],[6.3569,3.15]],61:[[7.4882,3.15],[10.5271,3.15],[5.0242,3.15],[6.7821,3.15]],62:[[7.9109,3.15],[11.088,3.15],[5.3384,3.15],[7.2144,3.15]],63:[[8.26,3.15],[11.5491,3.15],[5.6044,3.15],[7.5801,3.15]],64:[[8.61,3.15],[12.0094,3.15],[5.8695,3.15],[7.9467,3.15]],65:[[8.96,3.15],[12.4696,3.15],[6.1355,3.15],[8.3125,3.15]],66:[[9.3467,3.15],[12.9596,3.15],[6.5144,3.15],[8.8331,3.15]],67:[[9.7335,3.15],[13.4496,3.15],[6.8915,3.15],[9.3546,3.15]],68:[[10.1194,3.15],[13.9405,3.15],[7.2704,3.15],[9.8744,3.15]],69:[[10.5061,3.15],[14.4296,3.15],[7.6475,3.15],[10.395,3.15]],70:[[11.3286,3.15],[15.6669,3.15],[8.0264,3.15],[10.9156,3.15]],71:[[12.3427,3.15],[16.8551,3.15],[8.7517,3.15],[11.7714,3.15]],72:[[13.356,3.15],[18.0434,3.15],[9.8577,3.15],[12.628,3.15]],73:[[14.3692,3.15],[19.2325,3.15],[10.612,3.15],[13.4846,3.15]],74:[[15.3825,3.15],[20.4199,3.15],[11.3689,3.15],[14.3395,3.15]],75:[[16.3966,3.15],[21.609,3.15],[12.1231,3.15],[15.1961,3.15]],76:[[17.7677,3.15],[23.4456,3.15],[13.2764,3.15],[16.3949,3.15]],77:[[19.1389,3.15],[25.2822,3.15],[14.4287,3.15],[17.5936,3.15]],78:[[19.5344,3.15],[25.8283,3.15],[14.8391,3.15],[17.8972,3.15]],79:[[20.8399,3.15],[27.5774,3.15],[15.9381,3.15],[19.0391,3.15]],80:[[22.1454,3.15],[29.3265,3.15],[17.0354,3.15],[20.1801,3.15]],81:[[24.0572,3.15],[31.3014,3.15],[18.8335,3.15],[22.1987,3.15]],82:[[25.97,3.15],[33.278,3.15],[20.6316,3.15],[24.2156,3.15]],83:[[27.881,3.15],[35.2537,3.15],[22.4297,3.15],[26.2342,3.15]],84:[[29.7929,3.15],[37.2295,3.15],[24.2279,3.15],[28.252,3.15]],85:[[31.7047,3.15],[39.2052,3.15],[26.026,3.15],[30.2706,3.15]]};
const FOR_B={50:[[4.347,3.15],[5.775,3.15],[3.402,3.15],[5.25,3.15]],51:[[4.6113,3.15],[6.195,3.15],[3.5341,3.15],[5.565,3.15]],52:[[4.8764,3.15],[6.615,3.15],[3.6662,3.15],[5.88,3.15]],53:[[5.1406,3.15],[7.035,3.15],[3.7992,3.15],[6.195,3.15]],54:[[5.4057,3.15],[7.455,3.15],[3.9314,3.15],[6.51,3.15]],55:[[5.67,3.15],[7.875,3.15],[4.0635,3.15],[6.825,3.15]],56:[[5.9535,3.15],[8.316,3.15],[4.2332,3.15],[6.93,3.15]],57:[[6.237,3.15],[8.757,3.15],[4.4039,3.15],[7.035,3.15]],58:[[6.5205,3.15],[9.198,3.15],[4.5736,3.15],[7.14,3.15]],59:[[6.804,3.15],[9.639,3.15],[4.7442,3.15],[7.245,3.15]],60:[[7.0875,3.15],[10.08,3.15],[4.914,3.15],[7.35,3.15]],61:[[7.5031,3.15],[10.605,3.15],[5.1782,3.15],[7.56,3.15]],62:[[7.9187,3.15],[11.13,3.15],[5.4434,3.15],[7.77,3.15]],63:[[8.3353,3.15],[11.655,3.15],[5.7076,3.15],[7.98,3.15]],64:[[8.7509,3.15],[12.18,3.15],[5.9728,3.15],[8.19,3.15]],65:[[9.1665,3.15],[12.705,3.15],[6.237,3.15],[8.4,3.15]],66:[[9.639,3.15],[13.482,3.15],[6.6526,3.15],[8.967,3.15]],67:[[10.1115,3.15],[14.259,3.15],[7.0682,3.15],[9.534,3.15]],68:[[10.584,3.15],[15.036,3.15],[7.4848,3.15],[10.101,3.15]],69:[[11.0565,3.15],[15.813,3.15],[7.9004,3.15],[10.668,3.15]],70:[[11.529,3.15],[16.59,3.15],[8.316,3.15],[11.235,3.15]],71:[[12.5877,3.15],[17.808,3.15],[9.2234,3.15],[12.075,3.15]],72:[[13.6456,3.15],[19.026,3.15],[10.1307,3.15],[12.915,3.15]],73:[[14.7044,3.15],[20.244,3.15],[11.0373,3.15],[13.755,3.15]],74:[[15.7622,3.15],[21.462,3.15],[11.9446,3.15],[14.595,3.15]],75:[[16.821,3.15],[22.68,3.15],[12.852,3.15],[15.435,3.15]],76:[[18.5789,3.15],[24.759,3.15],[14.0612,3.15],[16.8,3.15]],77:[[20.3367,3.15],[26.838,3.15],[15.2714,3.15],[18.165,3.15]],78:[[22.0938,3.15],[28.917,3.15],[16.4806,3.15],[19.53,3.15]],79:[[23.8516,3.15],[30.996,3.15],[17.6907,3.15],[20.895,3.15]],80:[[25.6095,3.15],[33.075,3.15],[18.9,3.15],[22.26,3.15]]};


// Aflac | Preferred | modal=0.087504
const AFLP_M=0.087504;
const AFLP={45:[[2.535,4.2],n,n,n],46:[[2.613,4.21],n,n,n],47:[[2.694,4.19],n,n,n],48:[[2.772,4.2],n,n,n],49:[[2.851,4.21],n,n,n],50:[[2.931,4.19],n,[2.277,4.21],n],51:[[3.026,4.2],n,[2.327,4.21],n],52:[[3.121,4.2],n,[2.376,4.21],n],53:[[3.215,4.21],n,[2.427,4.19],n],54:[[3.311,4.2],n,[2.476,4.19],n],55:[[3.407,4.19],n,[2.526,4.19],n],56:[[3.544,4.2],n,[2.628,4.21],n],57:[[3.682,4.2],n,[2.732,4.21],n],58:[[3.821,4.19],n,[2.837,4.2],n],59:[[3.957,4.21],n,[2.941,4.2],n],60:[n,n,[3.045,4.2],n],61:[n,n,[3.232,4.19],n],62:[n,n,[3.418,4.2],n],63:[n,n,[3.605,4.2],n],64:[n,n,[3.792,4.19],n],65:[n,[8.325,4.2],n,n],66:[n,[8.756,4.19],n,n],67:[n,[9.185,4.2],n,n],68:[n,[9.616,4.19],n,n],69:[n,[10.045,4.2],n,n],70:[n,[10.475,4.21],n,n],71:[n,[11.515,4.2],n,n],72:[n,[12.556,4.19],n,n],73:[n,[13.595,4.2],n,n],74:[n,[14.636,4.19],n,n],75:[n,[15.675,4.2],n,n],76:[n,[16.715,4.2],n,n],77:[n,[17.754,4.21],n,n],78:[n,[18.795,4.2],n,n],79:[n,[19.836,4.19],n,n]};

// Aflac | Standard | modal=0.0875
const AFLS_M=0.0875;
const AFLS={45:[[3.535,4.2],n,n,n],46:[[3.633,4.2],n,n,n],47:[[3.731,4.2],n,n,n],48:[[3.828,4.2],n,n,n],49:[[3.926,4.2],n,n,n],50:[[4.024,4.2],n,[3.176,4.19],n],51:[[4.135,4.19],n,[3.226,4.19],n],52:[[4.244,4.21],n,[3.275,4.2],n],53:[[4.354,4.2],n,[3.325,4.2],n],54:[[4.465,4.19],n,[3.376,4.2],n],55:[[4.574,4.21],n,[3.425,4.21],n],56:[[4.759,4.2],n,[3.536,4.2],n],57:[[4.943,4.2],n,[3.646,4.19],n],58:[[5.127,4.21],n,[3.755,4.21],n],59:[[5.312,4.2],n,[3.866,4.2],n],60:[n,n,[3.976,4.2],n],61:[n,n,[4.244,4.2],n],62:[n,n,[4.511,4.21],n],63:[n,n,[4.779,4.19],n],64:[n,n,[5.046,4.2],n],65:[n,[12.224,4.21],n,n],66:[n,[12.895,4.2],n,n],67:[n,[13.565,4.2],n,n],68:[n,[14.234,4.21],n,n],69:[n,[14.905,4.2],n,n],70:[n,[15.575,4.2],n,n],71:[n,[17.225,4.19],n,n],72:[n,[18.874,4.21],n,n],73:[n,[20.525,4.2],n,n],74:[n,[22.175,4.2],n,n],75:[n,[23.826,4.19],n,n],76:[n,[25.475,4.2],n,n],77:[n,[27.125,4.2],n,n],78:[n,[28.776,4.19],n,n],79:[n,[30.425,4.21],n,n]};

// Lafayette Life | Level | modal=0.084995
const LAF_M=0.084995;
const LAF={45:[[2.311,2.55],n,n,n],46:[[2.391,2.55],n,n,n],47:[[2.475,2.54],n,n,n],48:[[2.56,2.54],n,n,n],49:[[2.648,2.56],n,n,n],50:[[2.741,2.54],n,[2.489,2.56],n],51:[[2.835,2.55],n,[2.558,2.56],n],52:[[2.934,2.54],n,[2.63,2.55],n],53:[[3.034,2.56],n,[2.702,2.55],n],54:[[3.14,2.55],n,[2.777,2.55],n],55:[[3.248,2.56],n,[2.855,2.54],n],56:[[3.401,2.55],n,[2.97,2.55],n],57:[[3.56,2.56],n,[3.09,2.55],n],58:[[3.728,2.55],n,[3.214,2.56],n],59:[[3.903,2.55],n,[3.344,2.55],n],60:[n,n,[3.479,2.55],n],61:[n,n,[3.62,2.55],n],62:[n,n,[3.767,2.54],n],63:[n,n,[3.918,2.56],n],64:[n,n,[4.076,2.56],n],65:[n,[8.479,2.56],n,n],66:[n,[8.921,2.56],n,n],67:[n,[9.388,2.54],n,n],68:[n,[9.877,2.55],n,n],69:[n,[10.392,2.55],n,n],70:[n,[10.935,2.54],n,n],71:[n,[11.505,2.55],n,n],72:[n,[12.104,2.55],n,n],73:[n,[12.735,2.56],n,n],74:[n,[13.401,2.54],n,n],75:[n,[14.099,2.55],n,n],76:[n,[15.042,2.56],n,n],77:[n,[16.049,2.55],n,n],78:[n,[17.124,2.54],n,n],79:[n,[18.269,2.55],n,n]};

// UHL | Premier (Preferred) | modal=0.086997
const UHLP_M=0.086997;
const UHLP={45:[[2.395,4.35],n,n,n],46:[[2.504,4.35],n,n,n],47:[[2.618,4.36],n,n,n],48:[[2.708,4.34],n,n,n],49:[[2.8,4.36],n,n,n],50:[[2.92,4.36],n,[2.421,4.35],n],51:[[3.03,4.34],n,[2.494,4.34],n],52:[[3.144,4.34],n,[2.571,4.35],n],53:[[3.339,4.35],n,[2.683,4.35],n],54:[[3.523,4.36],n,[2.803,4.34],n],55:[[3.723,4.36],n,[2.9,4.36],n],56:[[3.937,4.35],n,[3.033,4.36],n],57:[[4.097,4.35],n,[3.174,4.36],n],58:[[4.236,4.35],n,[3.298,4.34],n],59:[[4.382,4.34],n,[3.462,4.35],n],60:[n,n,[3.583,4.36],n],61:[n,n,[3.701,4.35],n],62:[n,n,[3.887,4.35],n],63:[n,n,[4.007,4.35],n],64:[n,n,[4.135,4.35],n],65:[n,[10.359,4.35],n,n],66:[n,[11.109,4.35],n,n],67:[n,[11.958,4.34],n,n],68:[n,[12.705,4.36],n,n],69:[n,[13.544,4.35],n,n],70:[n,[14.464,4.35],n,n],71:[n,[15.628,4.35],n,n],72:[n,[16.881,4.36],n,n],73:[n,[18.463,4.35],n,n],74:[n,[19.888,4.34],n,n],75:[n,[21.737,4.35],n,n],76:[n,[23.056,4.35],n,n],77:[n,[24.509,4.35],n,n],78:[n,[25.881,4.36],n,n],79:[n,[27.387,4.35],n,n]};

// UHL | Deluxe (Standard) | modal=0.087011
const UHLD_M=0.087011;
const UHLD={45:[[3.275,4.36],n,n,n],46:[[3.401,4.36],n,n,n],47:[[3.489,4.36],n,n,n],48:[[3.574,4.35],n,n,n],49:[[3.706,4.35],n,n,n],50:[[3.796,4.36],n,[3.386,4.35],n],51:[[3.947,4.34],n,[3.455,4.35],n],52:[[4.052,4.36],n,[3.528,4.36],n],53:[[4.191,4.35],n,[3.613,4.34],n],54:[[4.395,4.34],n,[3.759,4.34],n],55:[[4.562,4.34],n,[3.854,4.35],n],56:[[4.8,4.36],n,[4.02,4.34],n],57:[[4.997,4.34],n,[4.129,4.35],n],58:[[5.166,4.35],n,[4.252,4.36],n],59:[[5.343,4.36],n,[4.453,4.36],n],60:[n,n,[4.594,4.36],n],61:[n,n,[4.745,4.35],n],62:[n,n,[4.984,4.34],n],63:[n,n,[5.138,4.34],n],64:[n,n,[5.302,4.35],n],65:[n,[11.443,4.35],n,n],66:[n,[12.272,4.35],n,n],67:[n,[13.209,4.34],n,n],68:[n,[14.035,4.35],n,n],69:[n,[14.963,4.34],n,n],70:[n,[15.976,4.36],n,n],71:[n,[17.264,4.34],n,n],72:[n,[18.648,4.36],n,n],73:[n,[20.396,4.34],n,n],74:[n,[21.969,4.34],n,n],75:[n,[24.012,4.35],n,n],76:[n,[25.47,4.34],n,n],77:[n,[27.073,4.36],n,n],78:[n,[28.591,4.35],n,n],79:[n,[30.254,4.34],n,n]};

// UHL | Guaranteed Issue | modal=0.087002
const UHLG_M=0.087002;
const UHLG={50:[[8.688,4.35],n,n,n],51:[[9.252,4.36],n,n,n],52:[[9.431,4.35],n,n,n],53:[[9.565,4.35],n,n,n],54:[[9.712,4.36],n,n,n],55:[[9.877,4.34],n,n,n],56:[[10.057,4.34],n,n,n],57:[[10.254,4.36],n,n,n],58:[[10.495,4.35],n,n,n],59:[[10.753,4.35],n,n,n],60:[[11.031,4.35],n,n,n],61:[[11.329,4.34],n,n,n],62:[[11.65,4.34],n,[10.072,4.35],n],63:[[11.921,4.35],n,[10.225,4.34],n],64:[[12.222,4.35],n,[10.399,4.35],n],65:[n,n,[10.597,4.36],n],66:[n,n,[10.826,4.34],[13.396,4.34]],67:[n,n,[11.082,4.35],[13.762,4.36]],68:[n,n,[11.356,4.34],[14.15,4.36]],69:[n,n,[11.672,4.35],[14.587,4.34]],70:[n,n,[12.483,4.35],[15.809,4.36]],71:[n,n,[12.876,4.35],[16.364,4.35]],72:[n,n,[13.317,4.35],[16.976,4.34]],73:[n,n,[13.865,4.34],[17.728,4.35]],74:[n,n,[14.464,4.35],[18.546,4.35]],75:[n,n,[15.134,4.36],[19.43,4.36]]};


// CSV rate_per_1k values are already monthly premiums (modal already baked in by source)
// Formula: rate_per_1k × face/1000 + policy_fee
// Fallback order: exact ri → same gender/other tobacco → other gender → any available
function csvLookup(tbl, _modal, age, male, smoker, face) {
  const row = tbl[age];
  if (!row) return null;
  const ri = (male ? 0 : 2) + (smoker ? 1 : 0);
  const fallbacks = [ri, ri ^ 1, ri ^ 2, ri ^ 3];
  for (const tryRi of fallbacks) {
    const entry = row[tryRi];
    if (entry) return Math.round((entry[0] * face / 1000 + entry[1]) * 100) / 100;
  }
  return null;
}

const CONDITIONS = [
  // CLEAN HEALTH
  {id:'none',          label:'No significant conditions',                              tier:'B', cat:'Clean Health',     meds:''},

  // HEART & BLOOD
  {id:'htn',           label:'Hypertension (controlled)',                              tier:'B', cat:'Heart & Blood',    meds:'lisinopril amlodipine losartan metoprolol atenolol hydrochlorothiazide hctz ramipril valsartan blood pressure bp'},
  {id:'hcl',           label:'High cholesterol (controlled)',                          tier:'B', cat:'Heart & Blood',    meds:'statin atorvastatin lipitor simvastatin zocor rosuvastatin crestor pravastatin cholesterol lipid'},
  {id:'afib_ctrl',     label:'Atrial fibrillation / irregular heartbeat',              tier:'B', cat:'Heart & Blood',    meds:'eliquis apixaban xarelto rivaroxaban warfarin coumadin afib irregular heartbeat arrhythmia'},
  {id:'aneurysm_old',  label:'Aneurysm - treated 2+ years ago',                       tier:'B', cat:'Heart & Blood',    meds:'aneurysm aortic'},
  {id:'pad_old',       label:'PAD / PVD - peripheral artery disease',                 tier:'B', cat:'Heart & Blood',    meds:'cilostazol pletal peripheral artery pad pvd claudication circulation'},
  {id:'angina_2plus',  label:'Angina (chest pain) - treated 2+ years ago',            tier:'C', cat:'Heart & Blood',    meds:'nitroglycerin nitro angina chest pain'},
  {id:'angio_2plus',   label:'Angioplasty - 2+ years ago',                            tier:'C', cat:'Heart & Blood',    meds:'angioplasty balloon stent cardiac'},
  {id:'mi_4plus',      label:'Heart attack - 4+ years ago (stable)',                  tier:'C', cat:'Heart & Blood',    meds:'clopidogrel plavix aspirin heart attack mi cardiac'},
  {id:'stent_2plus',   label:'Stent / bypass surgery - 2+ years ago',                 tier:'C', cat:'Heart & Blood',    meds:'stent bypass cardiac surgery atorvastatin lipitor'},
  {id:'stroke_4plus',  label:'Stroke / TIA - 4+ years ago (stable)',                  tier:'C', cat:'Heart & Blood',    meds:'warfarin apixaban eliquis stroke tia mini-stroke'},
  {id:'cardiomyo_old', label:'Cardiomyopathy - 2+ years ago',                         tier:'C', cat:'Heart & Blood',    meds:'cardiomyopathy heart muscle'},
  {id:'cad',           label:'Coronary artery disease (CAD)',                          tier:'C', cat:'Heart & Blood',    meds:'coronary artery disease cad ischemic'},
  {id:'pacemaker_old', label:'Pacemaker / defibrillator - implanted 2+ years ago',    tier:'C', cat:'Heart & Blood',    meds:'pacemaker defibrillator icd implant'},
  {id:'mi_2to4',       label:'Heart attack - 2 to 4 years ago',                       tier:'D', cat:'Heart & Blood',    meds:'recent heart attack mi stent'},
  {id:'stroke_2to4',   label:'Stroke / TIA - 2 to 4 years ago',                      tier:'D', cat:'Heart & Blood',    meds:'recent stroke tia'},
  {id:'angina_1yr',    label:'Angina (chest pain) - within last year',                tier:'D', cat:'Heart & Blood',    meds:'nitroglycerin nitro angina recent'},
  {id:'stent_1to2',    label:'Stent / heart surgery - 1 to 2 years ago',              tier:'D', cat:'Heart & Blood',    meds:'stent bypass recent surgery cardiac'},
  {id:'pacemaker_new', label:'Pacemaker / defibrillator - implanted within 2 years',  tier:'D', cat:'Heart & Blood',    meds:'pacemaker defibrillator icd implant recent'},
  {id:'chf',           label:'Congestive heart failure (CHF)',                         tier:'D', cat:'Heart & Blood',    meds:'furosemide lasix spironolactone digoxin carvedilol sacubitril entresto heart failure chf edema ejection fraction'},
  {id:'mi_1yr',        label:'Heart attack - last 12 months',                         tier:'E', cat:'Knockout',         meds:'recent heart attack mi cardiac'},
  {id:'stroke_1yr',    label:'Stroke - last 12 months',                               tier:'E', cat:'Knockout',         meds:'recent stroke cva'},

  // DIABETES
  {id:'diabetes_oral', label:'Diabetes - oral medications only (type 2)',              tier:'B', cat:'Diabetes',         meds:'metformin glucophage glipizide glimepiride jardiance empagliflozin farxiga dapagliflozin januvia sitagliptin trulicity dulaglutide ozempic oral diabetes sugar a1c t2d'},
  {id:'diabetes_ins',  label:'Diabetes - insulin dependent',                           tier:'C', cat:'Diabetes',         meds:'insulin lantus glargine novolog humalog basaglar toujeo insulin-dependent t1d'},
  {id:'diabetes_comp', label:'Diabetes with complications - neuropathy, retinopathy, nephropathy', tier:'D', cat:'Diabetes', meds:'neuropathy retinopathy nephropathy diabetic complications peripheral'},
  {id:'diabetes_coma', label:'Diabetic coma / insulin shock / amputation due to diabetes', tier:'E', cat:'Knockout',    meds:'diabetic coma insulin shock amputation limb loss'},

  // RESPIRATORY
  {id:'asthma_b',      label:'Asthma - managed, no steroids',                         tier:'B', cat:'Respiratory',      meds:'albuterol inhaler asthma wheeze bronchospasm ventolin'},
  {id:'asthma_mod',    label:'Asthma - moderate, requires steroids',                   tier:'C', cat:'Respiratory',      meds:'prednisone steroid inhaler albuterol asthma'},
  {id:'sleep_apnea',   label:'Sleep apnea - CPAP, no oxygen required',                tier:'B', cat:'Respiratory',      meds:'cpap bipap snoring apnea sleep'},
  {id:'copd_no_o2',    label:'COPD / emphysema - no home oxygen',                     tier:'C', cat:'Respiratory',      meds:'tiotropium spiriva advair fluticasone salmeterol symbicort budesonide copd emphysema chronic bronchitis inhaler'},
  {id:'bronchitis_chr',label:'Chronic bronchitis',                                     tier:'C', cat:'Respiratory',      meds:'chronic bronchitis inhaler corticosteroid bronchodilator'},
  {id:'copd_o2',       label:'COPD requiring home oxygen',                             tier:'E', cat:'Knockout',         meds:'home oxygen o2 concentrator tank copd severe emphysema'},
  {id:'pulm_fib',      label:'Pulmonary fibrosis',                                    tier:'E', cat:'Knockout',         meds:'pulmonary fibrosis interstitial lung pirfenidone ofev nintedanib'},

  // CANCER
  {id:'basal_cell',    label:'Basal cell / squamous cell skin cancer',                 tier:'B', cat:'Cancer',           meds:'basal cell squamous cell skin cancer bcc scc mohs'},
  {id:'cancer_5plus',  label:'Cancer - in remission 5+ years',                        tier:'B', cat:'Cancer',           meds:'remission survivor cancer oncology'},
  {id:'cancer_4plus',  label:'Cancer - in remission 4+ years',                        tier:'C', cat:'Cancer',           meds:'cancer remission survivor 4 years oncology'},
  {id:'cancer_2to4',   label:'Cancer - in remission 2 to 4 years',                   tier:'D', cat:'Cancer',           meds:'cancer remission 2 years chemo radiation'},
  {id:'melanoma_2to4', label:'Melanoma - in remission 2 to 4 years',                 tier:'D', cat:'Cancer',           meds:'melanoma skin cancer malignant'},
  {id:'cancer_lt2',    label:'Cancer - active or treated under 2 years ago',          tier:'E', cat:'Knockout',         meds:'active chemo radiation tumor chemotherapy cancer current'},
  {id:'chemo_active',  label:'Currently receiving chemotherapy or radiation',          tier:'E', cat:'Knockout',         meds:'chemotherapy radiation oncology active treatment current'},

  // NEUROLOGICAL
  {id:'seizures_rare', label:'Seizures / epilepsy - fewer than 6 per year',           tier:'B', cat:'Neurological',     meds:'levetiracetam keppra lamotrigine lamictal phenytoin dilantin epilepsy seizure'},
  {id:'neuropathy',    label:'Neuropathy - not diabetes related',                      tier:'B', cat:'Neurological',     meds:'gabapentin neurontin pregabalin lyrica neuropathy nerve pain'},
  {id:'ms',            label:'Multiple sclerosis (MS) - ambulatory',                  tier:'C', cat:'Neurological',     meds:'tecfidera copaxone glatiramer ocrelizumab ms multiple sclerosis'},
  {id:'parkinsons',    label:'Parkinson disease - stable',                             tier:'C', cat:'Neurological',     meds:'carbidopa levodopa sinemet ropinirole pramipexole parkinsons'},
  {id:'seizures_freq', label:'Seizures - 6 or more within 2 years',                  tier:'D', cat:'Neurological',     meds:'frequent seizures epilepsy uncontrolled'},
  {id:'alzheimers',    label:'Alzheimer / dementia / memory loss / cognitive decline', tier:'E', cat:'Knockout',         meds:'donepezil aricept memantine namenda alzheimers dementia memory cognitive'},
  {id:'als',           label:'ALS - Lou Gehrig disease',                              tier:'E', cat:'Knockout',         meds:'riluzole als amyotrophic lateral sclerosis lou gehrig motor neuron'},

  // MENTAL HEALTH
  {id:'anxiety',       label:'Anxiety / depression - stable, age 19+',                tier:'B', cat:'Mental Health',    meds:'sertraline zoloft escitalopram lexapro fluoxetine prozac paroxetine paxil citalopram buspizone ssri antidepressant anxiety depression'},
  {id:'ptsd',          label:'PTSD - age 19+',                                        tier:'B', cat:'Mental Health',    meds:'prazosin ptsd post traumatic stress trauma'},
  {id:'bipolar',       label:'Bipolar disorder - managed',                             tier:'B', cat:'Mental Health',    meds:'lithium lamotrigine valproate quetiapine seroquel bipolar mood stabilizer'},
  {id:'schizophrenia', label:'Schizophrenia - stable, age 18+',                       tier:'C', cat:'Mental Health',    meds:'risperidone risperdal olanzapine zyprexa aripiprazole abilify clozapine schizophrenia psychosis'},
  {id:'alcohol_2plus', label:'Alcohol / drug abuse - treatment 2+ years ago',         tier:'C', cat:'Mental Health',    meds:'suboxone naltrexone vivitrol methadone alcohol rehab substance abuse sober'},
  {id:'alcohol_2yr',   label:'Alcohol / drug abuse - treatment within 2 years',       tier:'D', cat:'Mental Health',    meds:'recent alcohol rehab drug treatment substance abuse suboxone naltrexone'},

  // KIDNEY
  {id:'kidney_disease',label:'Chronic kidney disease - CKD, not on dialysis',         tier:'D', cat:'Kidney',           meds:'ckd chronic kidney creatinine egfr renal benazepril kidney disease failure'},
  {id:'dialysis',      label:'Currently on dialysis',                                  tier:'E', cat:'Knockout',         meds:'dialysis hemodialysis esrd renal failure kidney'},

  // LIVER
  {id:'hep_c_cured',   label:'Hepatitis C - cured or resolved 2+ years ago',          tier:'C', cat:'Liver',            meds:'hepatitis c hep c cured sovaldi harvoni epclusa ledipasvir'},
  {id:'hep_b',         label:'Hepatitis B - stable',                                  tier:'C', cat:'Liver',            meds:'hepatitis b hbsag entecavir baraclude tenofovir viread'},
  {id:'hep_c_active',  label:'Hepatitis C - current or within 2 years',               tier:'D', cat:'Liver',            meds:'hepatitis c active current treatment hcv'},
  {id:'cirrhosis',     label:'Cirrhosis of the liver',                                 tier:'D', cat:'Liver',            meds:'cirrhosis liver lactulose rifaximin hepatic failure'},

  // OTHER CONDITIONS
  {id:'thyroid',       label:'Thyroid condition - controlled',                         tier:'B', cat:'Other',            meds:'levothyroxine synthroid armour thyroid hypothyroid hyperthyroid'},
  {id:'arthritis',     label:'Arthritis - managed',                                   tier:'B', cat:'Other',            meds:'methotrexate hydroxychloroquine plaquenil naproxen meloxicam celebrex osteoarthritis joint pain arthritis'},
  {id:'rheum_arth',    label:'Rheumatoid arthritis',                                   tier:'B', cat:'Other',            meds:'methotrexate hydroxychloroquine humira adalimumab etanercept enbrel rheumatoid arthritis ra'},
  {id:'gerd',          label:'GERD / acid reflux - controlled',                        tier:'B', cat:'Other',            meds:'omeprazole prilosec pantoprazole protonix esomeprazole nexium famotidine pepcid heartburn reflux gerd'},
  {id:'obesity',       label:'Obesity - BMI under 40',                                 tier:'B', cat:'Other',            meds:'wegovy ozempic semaglutide bmi overweight obese weight'},
  {id:'crohn',         label:'Crohn disease / ulcerative colitis - managed',           tier:'B', cat:'Other',            meds:'mesalamine humira adalimumab infliximab remicade crohns colitis ibd inflammatory bowel'},
  {id:'lupus',         label:'Lupus / systemic lupus erythematosus - SLE',            tier:'C', cat:'Other',            meds:'hydroxychloroquine plaquenil belimumab lupus sle autoimmune'},
  {id:'chronic_pain',  label:'Chronic pain - 6 or more narcotic prescriptions per year',tier:'C',cat:'Other',           meds:'oxycodone hydrocodone oxycontin vicodin percocet morphine fentanyl tramadol narcotic opioid pain chronic'},
  {id:'sarcoidosis',   label:'Sarcoidosis',                                            tier:'C', cat:'Other',            meds:'prednisone sarcoidosis sarcoid'},
  {id:'pancreatitis',  label:'Pancreatitis - chronic',                                 tier:'C', cat:'Other',            meds:'pancreatic enzymes creon pancreatitis pancreas'},
  {id:'hep_c_curr',    label:'Hepatitis C - chronic or currently active',              tier:'C', cat:'Other',            meds:'hepatitis hcv hep c ribavirin interferon'},
  {id:'restless_leg',  label:'Restless leg syndrome - check meds for Parkinson overlap',tier:'B',cat:'Other',           meds:'ropinirole pramipexole requip mirapex restless leg rls'},
  {id:'dui_2plus',     label:'DUI - 2+ years ago',                                    tier:'C', cat:'Other',            meds:'dui dwi drunk driving'},
  {id:'dui_2yr',       label:'DUI - within last 2 years',                              tier:'D', cat:'Other',            meds:'recent dui dwi drunk driving'},
  {id:'sickle_cell',   label:'Sickle cell anemia',                                     tier:'D', cat:'Other',            meds:'hydroxyurea droxia sickle cell hemoglobin voxelotor oxbryta'},

  // KNOCKOUT / GI ONLY
  {id:'home_o2_24hr',  label:'Home oxygen required 24 hours per day',                 tier:'E', cat:'Knockout',         meds:'24 hour oxygen continuous concentrator tank home o2'},
  {id:'nursing_home',  label:'Confined to nursing home or assisted living',            tier:'E', cat:'Knockout',         meds:'nursing home skilled nursing snf long term care assisted living facility'},
  {id:'hospice',       label:'Currently in hospice care',                              tier:'E', cat:'Knockout',         meds:'hospice palliative terminal end of life'},
  {id:'transplant_rec',label:'Organ transplant - any organ',                           tier:'E', cat:'Knockout',         meds:'tacrolimus prograf mycophenolate cellcept cyclosporine transplant organ'},
  {id:'wheelchair',    label:'Confined to wheelchair due to illness or disease',       tier:'E', cat:'Knockout',         meds:'wheelchair electric scooter mobility disabled confined'},
  {id:'aids_hiv',      label:'AIDS / HIV / ARC',                                      tier:'E', cat:'Knockout',         meds:'hiv aids atripla truvada biktarvy dolutegravir antiretroviral'},
  {id:'terminal',      label:'Terminal illness - life expectancy under 2 years',       tier:'E', cat:'Knockout',         meds:'terminal illness prognosis palliative hospice'},
  {id:'felony',        label:'Felony - currently incarcerated, on probation/parole, or awaiting trial', tier:'E', cat:'Knockout', meds:'felony probation parole incarceration jail prison awaiting trial conviction outstanding fines restitution'},
]
// `dot` stays a hex literal (it feeds string-concat alpha suffixes like dot+'CC'
// and non-text dot backgrounds); `txt` is the theme-aware CSS var for TEXT so
// tier-colored labels stay ≥4.5:1 in light mode too.
const TIER_INFO = {
  B:{label:'Level — Preferred',  short:'Preferred',    dot:'#A78BFA', txt:'var(--qm-tier-purple)', pill:'rgba(167,139,250,0.12)', bd:'rgba(167,139,250,0.3)'},
  C:{label:'Level — Standard',   short:'Standard',     dot:'#177452', txt:'var(--qm-tier-green)',  pill:'rgba(23,116,82,0.12)',  bd:'rgba(23,116,82,0.3)'},
  D:{label:'Modified Benefit',   short:'Modified',     dot:'#EAB308', txt:'var(--qm-tier-amber)',  pill:'rgba(234,179,8,0.12)',  bd:'rgba(234,179,8,0.3)'},
  E:{label:'Guaranteed Issue',   short:'GI Only',      dot:'#b42318', txt:'var(--qm-tier-red)',    pill:'rgba(180,35,24,0.12)',  bd:'rgba(180,35,24,0.3)'},
};

const GSB = [
  {key:'gold',   label:'Gold',   medal:'🥇', color:'#4a45d1', default:15000},
  {key:'silver', label:'Silver', medal:'🥈', color:'#94A3B8', default:10000},
  {key:'bronze', label:'Bronze', medal:'🥉', color:'#B45309', default:5000},
];



// ═══════════════════════════════════════════════════════════
// ── TERM LIFE CARRIERS — Driven by src/data/term_rates.json (ITK scrape) ──
// ═══════════════════════════════════════════════════════════
// Data shape: TERM_RATES[product][term][tier][class][age][face] = monthly_premium
//   product  — e.g. "Mutual of Omaha (Term Life Express)"
//   term     — '10' | '15' | '20' | '25' | '30' | '35' | '40'
//   tier     — carrier-specific tier name (e.g. 'Preferred Plus', 'Approved')
//   class    — 'MNS' | 'MS' | 'FNS' | 'FS'
//   age      — anchor age as string (25, 30, 35, 40, 45, 50, 55, 60, 65, 70, 75)
//   face     — anchor face as string (50000, 100000, 250000, 500000, 1000000)

// ── HEALTH CLASS ↔ CARRIER TIER MAPPING ──
// Agents pick a generic class (Pref+/Pref/Std+/Std). Each carrier names their
// tiers differently — Foresters has 'Approved', Protective has 'Ultimate Preferred',
// etc. We map the generic to whatever the carrier actually offers, walking DOWN
// the ladder if the carrier doesn't have the agent's chosen class.
// Each generic class lists the carrier-specific tier names that BELONG in that
// class. Order = preference within the class (first match wins). When a carrier
// doesn't offer a tier in the chosen class, resolveTier() walks DOWN to the
// next class (pp → p → sp → s) and the worst case falls back to the carrier's
// first tier (handles single-tier products like HMS, Trendsetter, Continuation).
const HEALTH_CLASS_TIERS = {
  pp: ['Preferred Plus','Ultimate Preferred','Elite'],
  p:  ['Preferred','Super Preferred'],
  sp: ['Standard Plus','Select','Level'],
  s:  ['Standard','Approved'],
};
const HEALTH_CLASS_ORDER = ['pp','p','sp','s'];
const HEALTH_CLASS_LABEL = { pp:'Preferred Plus', p:'Preferred', sp:'Standard Plus', s:'Standard' };
const HEALTH_CLASS_SHORT = { pp:'Pref+', p:'Pref', sp:'Std+', s:'Std' };

// Resolve a generic health class → the carrier's actual tier name.
// Walks the chosen class's tier list first; if no carrier match, walks DOWN
// the ladder (pp→p→sp→s). Final fallback: the carrier's first tier (catches
// single-tier products like Americo HMS, Trans Trendsetter, Continuation,
// Payment Protector, Simple Term, Foresters Strong Foundation).
function resolveTier(productData, generic) {
  if (!productData) return null;
  const tiers = Object.keys(productData);
  if (!tiers.length) return null;
  const startIdx = Math.max(0, HEALTH_CLASS_ORDER.indexOf(generic || 'p'));
  for (let i = startIdx; i < HEALTH_CLASS_ORDER.length; i++) {
    for (const t of HEALTH_CLASS_TIERS[HEALTH_CLASS_ORDER[i]] || []) {
      if (tiers.includes(t)) return t;
    }
  }
  // No match in any class — return first carrier tier (single-tier products)
  return tiers[0];
}

// Legacy auto-pick: used only when no health class is provided.
const TERM_TIER_PRIORITY = [
  'Preferred Plus','Ultimate Preferred','Super Preferred','Elite','Preferred',
  'Select','Standard Plus','Standard Extra','Standard','Approved','Level',
  'Trendsetter Super 2021','Trendsetter LB 2017',
  'Continuation 10','Continuation 25','HMS','Payment Protector','Payment Protector Continuation',
  'Simple Term','Simple Term Deluxe',
];

function pickBestTier(productData) {
  if (!productData) return null;
  const tiers = Object.keys(productData);
  for (const t of TERM_TIER_PRIORITY) if (tiers.includes(t)) return t;
  return tiers[0] || null;
}

// Bilinear interpolation across (age, face) anchors.
// Anchors at age: 25, 30, 35, 40, 45, 50, 55, 60, 65, 70, 75
// Anchors at face: 50000, 100000, 250000, 500000, 1000000
function termPrem(product, termLen, age, classKey, face, tierOverride=null) {
  const productData = TERM_RATES?.[product]?.[String(termLen)];
  if (!productData) return null;
  const tier = tierOverride || pickBestTier(productData);
  if (!tier || !productData[tier]) return null;
  const clsTable = productData[tier][classKey];
  if (!clsTable) return null;

  const ages = Object.keys(clsTable).map(Number).sort((a,b)=>a-b);
  if (!ages.length) return null;
  if (age < ages[0] || age > ages[ages.length-1]) return null;

  // Find bracketing ages
  let aLo = ages[0], aHi = ages[ages.length-1];
  if (clsTable[String(age)]) { aLo = aHi = age; }
  else for (let i = 0; i < ages.length-1; i++) {
    if (age >= ages[i] && age <= ages[i+1]) { aLo = ages[i]; aHi = ages[i+1]; break; }
  }

  function rateAt(a) {
    const faceTbl = clsTable[String(a)];
    if (!faceTbl) return null;
    const faces = Object.keys(faceTbl).map(Number).sort((a,b)=>a-b);
    if (!faces.length) return null;
    if (face < faces[0] || face > faces[faces.length-1]) return null;
    if (faceTbl[String(face)] != null) return faceTbl[String(face)];
    let fLo = faces[0], fHi = faces[faces.length-1];
    for (let i = 0; i < faces.length-1; i++) {
      if (face >= faces[i] && face <= faces[i+1]) { fLo = faces[i]; fHi = faces[i+1]; break; }
    }
    const pLo = faceTbl[String(fLo)], pHi = faceTbl[String(fHi)];
    if (pLo == null || pHi == null) return null;
    if (fLo === fHi) return pLo;
    return pLo + (face - fLo) / (fHi - fLo) * (pHi - pLo);
  }

  const rLo = rateAt(aLo);
  const rHi = rateAt(aHi);
  if (rLo == null || rHi == null) return null;
  const rate = aLo === aHi ? rLo : rLo + (age - aLo) / (aHi - aLo) * (rHi - rLo);
  return Math.round(rate * 100) / 100;
}

// ═══════════════════════════════════════════════════════════
// ── IUL LOOKUP — bilinear interp on age × premium anchors ──
// ═══════════════════════════════════════════════════════════
// Data shape: IUL_RATES[product][class][age][premium] = face_amount
// Mirrors ITK's IUL quoter: input premium → output face amount per carrier.

// Documented product minimum face per carrier (NOT the lowest scraped anchor —
// the scrape only captured down to a coverage floor, e.g. ~$67k at Americo 70,
// but Americo actually issues to $50k). Used to extrapolate sub-anchor faces.
const IUL_MIN_FACE = {
  'Americo (Instant Decision IUL)': 50000,
  'Mutual of Omaha (Indexed Universal Life Express)': 25000,
  'American Amicable (Intelligent Choice IUL)': 25000,
};

// Carrier issue rules enforced by the QUOTE engine — independent of the sparse
// scrape bounds. Without this the engine would quote whatever the scraped data
// happens to reach: MOO data runs to $536k at 18 but MOO caps at $300k (and
// bands DOWN with age), so the tool would over-promise coverage the carrier
// won't issue. faceBands are checked low→high age; first band whose aMax ≥ age
// wins. Sources: MOO Express Life Reference Guide; Americo Series 336 IUL guide;
// AmAm Intelligent Choice quoter. Verify against current carrier guides.
const IUL_LIMITS = {
  'Mutual of Omaha (Indexed Universal Life Express)': {
    // Per MOO Simplified Issue UW Guide (618352_0426): IUL Express (IULE) shares
    // the Term Life Express band table — 18-50: $25k-$550k, 51-60: $25k-$450k,
    // 61-75: $25k-$350k. (Prior 300/250/150 was from an outdated guide.)
    minAge: 18, maxAge: 75, minFace: 25000,
    faceBands: [{ aMax: 50, max: 550000 }, { aMax: 60, max: 450000 }, { aMax: 75, max: 350000 }],
  },
  'Americo (Instant Decision IUL)': {
    // Product issues to age 80, but ITK (our data source) only quotes Americo
    // IUL through age 70 — confirmed: age 71+ returns no Americo. So we cap at
    // 70 (honest to what we can quote). Face $450k flat, all ages (scraped).
    minAge: 18, maxAge: 70, minFace: 50000,
    faceBands: [{ aMax: 70, max: 450000 }],
  },
  'American Amicable (Intelligent Choice IUL)': {
    // Verified against the carrier quoter: min $25k ($20k rejected), max $450k
    // flat at every age 18-75 (no age banding; $600k+ rejected). Grid scraped
    // to $450k.
    minAge: 18, maxAge: 75, minFace: 25000,
    faceBands: [{ aMax: 75, max: 450000 }],
  },
};
const iulAgeOK   = (product, age) => { const L = IUL_LIMITS[product]; return !L || (age >= L.minAge && age <= L.maxAge); };
const iulMaxFace = (product, age) => {
  const L = IUL_LIMITS[product]; if (!L) return Infinity;
  for (const b of L.faceBands) if (age <= b.aMax) return b.max;
  return L.faceBands[L.faceBands.length - 1].max;
};

// Per-product state availability for IUL carriers (from the carrier's own
// quoter). Loaded from data so a re-scrape updates it without code edits. A
// product with an entry shows ONLY in its listed states; no entry ⇒ everywhere.
// American-Amicable Intelligent Choice IUL is NOT sold in Michigan (confirmed
// against the carrier quoter), so it's gated out when the agent picks MI.
const iulStates = (product) => {
  const list = IUL_STATE_AVAIL[product];
  return Array.isArray(list) && list.length ? new Set(list) : null;
};

function iulFaceFor(product, classKey, age, premium) {
  const tbl = IUL_RATES?.[product]?.[classKey];
  if (!tbl) return null;
  const ages = Object.keys(tbl).map(Number).sort((a,b)=>a-b);
  if (!ages.length || age < ages[0] || age > ages[ages.length-1]) return null;
  let aLo = ages[0], aHi = ages[ages.length-1];
  if (tbl[String(age)]) { aLo = aHi = age; }
  else for (let i=0;i<ages.length-1;i++) if (age>=ages[i]&&age<=ages[i+1]) { aLo=ages[i]; aHi=ages[i+1]; break; }

  // Premium lookup at a single age, with snap-to-nearest-anchor when premium
  // is outside the row's range. Critical for sparse female-tier data where
  // some ages only have a single premium anchor.
  function faceAt(a) {
    const row = tbl[String(a)];
    if (!row) return null;
    const prems = Object.keys(row).map(Number).sort((a,b)=>a-b);
    if (!prems.length) return null;
    if (row[String(premium)] != null) return row[String(premium)];
    // Snap-to-nearest when premium is outside the row's anchors
    if (premium < prems[0])                      return row[String(prems[0])];
    if (premium > prems[prems.length-1])         return row[String(prems[prems.length-1])];
    // Interpolate between bracketing anchors
    let pLo = prems[0], pHi = prems[prems.length-1];
    for (let i=0;i<prems.length-1;i++) if (premium>=prems[i]&&premium<=prems[i+1]) { pLo=prems[i]; pHi=prems[i+1]; break; }
    const fLo = row[String(pLo)], fHi = row[String(pHi)];
    if (fLo == null || fHi == null) return null;
    if (pLo === pHi) return fLo;
    return fLo + (premium - pLo) / (pHi - pLo) * (fHi - fLo);
  }
  const fL = faceAt(aLo), fH = faceAt(aHi);
  // Fallback when one bracketing age can't serve this premium — use the other
  if (fL == null && fH == null) return null;
  if (fL == null) return Math.round(fH);
  if (fH == null) return Math.round(fL);
  return Math.round(aLo === aHi ? fL : fL + (age - aLo) / (aHi - aLo) * (fH - fL));
}

// Solve for premium given a target face amount — inverse of iulFaceFor.
//
// CRITICAL: the premium anchors must come from the carrier's ACTUAL row data,
// not a hardcoded list. iulFaceFor snaps premiums below a row's minimum up to
// that minimum's face — so if we naively probed [50,100,150,…] at an age whose
// real minimum is $150, the $50 and $100 probes return the $150 face, and a
// target below that face would resolve to a $50/mo premium the carrier will
// never write. We build the anchor set from the row keys instead, so the floor
// is the carrier's true minimum premium.
function iulPremiumForFace(product, classKey, age, targetFace) {
  const tbl = IUL_RATES?.[product]?.[classKey];
  if (!tbl) return null;
  const ages = Object.keys(tbl).map(Number).sort((a, b) => a - b);
  if (!ages.length || age < ages[0] || age > ages[ages.length - 1]) return null;
  // Bracketing age rows (same logic as iulFaceFor)
  let aLo = ages[0], aHi = ages[ages.length - 1];
  if (tbl[String(age)]) { aLo = aHi = age; }
  else for (let i = 0; i < ages.length - 1; i++) if (age >= ages[i] && age <= ages[i+1]) { aLo = ages[i]; aHi = ages[i+1]; break; }
  // Real premium anchors = those present in the row(s). For an interpolated
  // age, require the premium in BOTH bracketing rows so the face is genuine
  // (not snapped from a missing anchor).
  const premsHi = new Set(Object.keys(tbl[String(aHi)] || {}).map(Number));
  const realPrems = Object.keys(tbl[String(aLo)] || {}).map(Number)
    .filter(p => aLo === aHi || premsHi.has(p))
    .sort((a, b) => a - b);
  if (!realPrems.length) return null;
  // Genuine (premium, face) points for this profile
  const pts = realPrems
    .map(p => ({ prem: p, face: iulFaceFor(product, classKey, age, p) }))
    .filter(pt => pt.face != null);
  if (!pts.length) return null;
  const minPt = pts[0], maxPt = pts[pts.length - 1];
  // At/below our lowest scraped anchor. NOTE: that lowest anchor reflects scrape
  // COVERAGE, not the carrier's true minimum face — e.g. Americo age 70 was only
  // captured down to ~$67k, but Americo actually issues to $50k. IUL premium is
  // ~linear in face, so extrapolate down the curve to the carrier's documented
  // minimum instead of falsely claiming "won't issue $65k".
  if (targetFace <= minPt.face) {
    if (targetFace === minPt.face) return { premium: minPt.prem };
    const trueMin = IUL_MIN_FACE[product] || 25000;
    if (pts.length >= 2) {
      const p0 = pts[0], p1 = pts[1];
      const slope = (p1.prem - p0.prem) / (p1.face - p0.face);
      const at = (f) => Math.max(0, Math.round((p0.prem + (f - p0.face) * slope) * 100) / 100);
      if (targetFace >= trueMin) return { premium: at(targetFace) };
      return { premium: at(trueMin), floored: true, minFace: trueMin };
    }
    return { premium: minPt.prem, floored: targetFace < minPt.face, minFace: minPt.face };
  }
  // Above the carrier's max achievable face → cap (UI shows "maxes at $X").
  // Exactly AT the max is issuable — only strictly above gets flagged.
  if (targetFace >= maxPt.face) {
    if (targetFace === maxPt.face) return { premium: maxPt.prem };
    return { premium: maxPt.prem, capped: true, maxFace: maxPt.face };
  }
  // Interpolate the premium within the real anchor pair that spans the target.
  for (let i = 0; i < pts.length - 1; i++) {
    const lo = pts[i], hi = pts[i+1];
    if (targetFace >= lo.face && targetFace <= hi.face) {
      if (lo.face === hi.face) return { premium: lo.prem };
      return { premium: Math.round((lo.prem + (targetFace - lo.face) / (hi.face - lo.face) * (hi.prem - lo.prem)) * 100) / 100 };
    }
  }
  return null;
}

// Auto-generate IUL quote carriers from iul_rates.json keys.
const IUL_QUOTE_CARRIERS = Object.keys(IUL_RATES).map(product => {
  const m = product.match(/^(.*?)\s*\((.+)\)\s*$/);
  const name = m ? m[1].trim() : product;
  const sub  = m ? m[2].trim() : 'IUL';
  return {
    id: 'iul_quote_' + product.toLowerCase().replace(/[^a-z0-9]+/g,'_').replace(/^_|_$/g,''),
    product,
    name, sub,
    availableStates: iulStates(product),   // null ⇒ available everywhere
    brand: name.startsWith('Mutual of Omaha') ? '#1D4ED8'
         : name.startsWith('Americo')         ? '#1E40AF'
         : '#4a45d1',
    fn: (age, male, smoker, premium) => {
      const cls = (male?'M':'F') + (smoker?'S':'NS');
      return iulFaceFor(product, cls, age, premium);
    },
    // Inverse: input target face, output required premium
    fnPremium: (age, male, smoker, targetFace) => {
      const cls = (male?'M':'F') + (smoker?'S':'NS');
      return iulPremiumForFace(product, cls, age, targetFace);
    },
  };
});

// Slug helper — product name → carrier id
function termSlug(productName) {
  return 'term_' + productName.toLowerCase()
    .replace(/[()]/g, '').replace(/[^a-z0-9]+/g, '_').replace(/^_+|_+$/g, '');
}

// ── UW BADGE LOGIC ──
// Mark caught it: top tiers (Preferred Plus / Preferred) of multi-tier products
// typically require labs even on carriers that have instant paths for Standard.
// Trans Trendsetter Super 2021 Pref+ requires recent blood work, for example.
// So the badge has to be aware of which tier the agent landed on.
//
// Products fall into 3 categories:
//   1. ALWAYS_INSTANT — designed as no-exam (single tier or all-instant)
//   2. ALWAYS_EXAM    — traditional UW required regardless of tier
//   3. TIER_DEPENDENT — instant for Std/Std+, exam for Pref+/Pref
//
// Verified against carrier UW guides + product brochures (May 2026).

// Products that are TRULY instant regardless of tier:
//   - Single-tier "Approved" products (no preferred class to underwrite)
//   - Carriers whose entire product line is designed as no-exam
const TERM_ALWAYS_INSTANT = new Set([
  // AmAm — Term Made Simple per Mark + single-tier "Approved" products
  'American Amicable (Term Made Simple)',
  'American Amicable (Home Protector)',          // POS decision, single-tier
  // Americo — entire "Instant Decision Term Series"
  'Americo (Continuation 10)',
  'Americo (Continuation 25)',
  'Americo (HMS Term 100)',
  'Americo (HMS Term 125)',
  'Americo (Payment Protector)',
  'Americo (Payment Protector Continuation)',
  // Foresters Strong Foundation — single-tier non-med w/ POS decision
  'Foresters (Strong Foundation)',
  // InstaBrain — designed as fully-online no-exam product line
  'InstaBrain (IB Term)',
  'InstaBrain (PureTerm)',
  // MOO Express series — simplified-issue by design
  'Mutual of Omaha (Term Life Express)',
  // Royal Neighbors Jet — instant POS, no para-med
  'Royal Neighbors (Jet Term)',
]);

// Products that ALWAYS require para-medical exam + blood/urine regardless of tier:
const TERM_ALWAYS_EXAM = new Set([
  'Foresters (Your Term Medical)',
  'John Hancock (Simple Term with Vitality 2023)',
  'Kansas City Life',
  'National Life Group (LSW Level Term)',
  'Protective (Classic Choice Term)',
  'Transamerica (Trendsetter LB 2017)',
]);

// Tier-dependent products — instant for Std/Std+, EXAM for Pref+/Pref.
// Most carriers require recent labs to certify the top rate classes.
// Trans Trendsetter Super 2021 confirmed by Mark (Pref+ needs recent bloods).
const TERM_TIER_DEPENDENT = new Set([
  'Transamerica (Trendsetter Super 2021)',
  'SBLI (EasyTrak)',
]);

const TOP_TIERS_REQUIRING_LABS = new Set([
  'Preferred Plus', 'Ultimate Preferred', 'Elite', 'Super Preferred', 'Preferred'
]);

// Compute the UW badge for a given product + the tier it resolved to.
// Returns 'instant' | 'medical' | 'simplified' (default = no badge rendered).
function getUwType(productName, tierUsed) {
  if (TERM_ALWAYS_INSTANT.has(productName)) return 'instant';
  if (TERM_ALWAYS_EXAM.has(productName))    return 'medical';
  if (TERM_TIER_DEPENDENT.has(productName)) {
    return TOP_TIERS_REQUIRING_LABS.has(tierUsed) ? 'medical' : 'instant';
  }
  // Default (no badge): simplified-issue (MIB/Rx/phone). Includes:
  //   AmAm Easy Term, Home Certainty, Pioneer Security, Safecare, Survivor Protector
  //   Foresters Your Term (non-med, being discontinued)
  //   UHL Simple Term
  return 'simplified';
}

// Brand colors per term carrier company — used by initials-fallback logo
// and the left-border accent on result rows.
const TERM_BRAND = {
  'Mutual of Omaha': '#1D4ED8',
  'American Amicable': '#3B82F6',
  'Americo': '#1E40AF',
  'Foresters': '#6B21A8',
  'InstaBrain': '#0EA5E9',           // Fidelity Life's InstaBrain product line
  'John Hancock': '#000000',
  'Kansas City Life': '#15803D',
  'National Life Group': '#0F766E',
  'Protective': '#DC2626',
  'Royal Neighbors': '#16A34A',
  'SBLI': '#1E3A8A',
  'Transamerica': '#b42318',
  'UHL': '#B91C1C',
};

// Auto-generate one carrier entry per product in term_rates.json
const TERM_CARRIERS = Object.keys(TERM_RATES).map(product => {
  const terms = Object.keys(TERM_RATES[product]).sort((a,b)=>+a-+b);
  // Split "Company (Product)" → display name + subtitle
  const m = product.match(/^(.*?)\s*\((.+)\)\s*$/);
  const name = m ? m[1].trim() : product;
  const sub  = m ? m[2].trim() : 'Term Life';
  // Compute issue-age min/max — the widest range across any tier/class combo.
  // Drives the "compatible carriers" banner so agents know who can write the client.
  let minAge = Infinity, maxAge = 0;
  for (const tl of terms) {
    for (const tier of Object.keys(TERM_RATES[product][tl] || {})) {
      for (const cls of Object.keys(TERM_RATES[product][tl][tier] || {})) {
        const ages = Object.keys(TERM_RATES[product][tl][tier][cls] || {}).map(Number);
        if (ages.length) { minAge = Math.min(minAge, ...ages); maxAge = Math.max(maxAge, ...ages); }
      }
    }
  }
  return {
    id: termSlug(product),
    product,                   // full key for term_rates.json lookup
    name,
    sub,
    abbr: name.split(/\s+/).map(w => w[0]).join('').toUpperCase().slice(0,3),
    brand: TERM_BRAND[name] || '#4a45d1',
    // uwType is now computed at render time via getUwType(product, tierUsed)
    // since some products are tier-dependent (Trans Super, SBLI)
    enabled: true,
    termOnly: true,
    supportedTerms: terms,
    minAge: isFinite(minAge) ? minAge : null,
    maxAge: maxAge || null,
    fn: (age, male, smoker, healthClass, face, termLen) => {
      const tl = String(termLen || '10');
      if (!terms.includes(tl)) return null;
      const cls = (male ? 'M' : 'F') + (smoker ? 'S' : 'NS');
      const productData = TERM_RATES[product]?.[tl];
      const tier = resolveTier(productData, healthClass);
      return termPrem(product, tl, age, cls, face, tier);
    }
  };
});

// ═══════════════════════════════════════════════════════════
// ── IUL CARRIERS — Scope B: static spec sheet comparison ──
// ═══════════════════════════════════════════════════════════
// Only carriers in Mark's portfolio with TRUE indexed UL products.
// Verified May 2026: AmAm + Foresters do NOT offer IUL (only fixed UL).
// AmAm Products at a Glance lists 3 UL products (Easy UL, UL Performer,
// SecureLife Plus) — all flexible-premium adjustable life, none indexed.
// Foresters ezbiz lists 1 UL product (SMART UL) — traditional UL.
const IUL_CARRIERS = [
  {
    id: 'iul_moo_express',
    type: 'IUL',
    name: 'Mutual of Omaha',
    product: 'IUL Express',
    abbr: 'MOO',
    brand: '#003B71',
    issueAges: { min: 18, max: 75 },
    faceRange: {
      min: 25000, max: 550000,
      ageBands: [
        { ages: '18–50', min: 25000, max: 550000 },
        { ages: '51–60', min: 25000, max: 450000 },
        { ages: '61–75', min: 25000, max: 350000 },
      ],
    },
    cap: 8.5, par: 100, floor: 0,
    indexStrategy: 'S&P 500 · Annual Point-to-Point',
    underwriting: 'Simplified Issue · no medical exam',
    dbOptions: ['A: Level'],
    riders: ['Accelerated Death Benefit (Terminal)', 'Chronic Illness', "Children's Term", 'Disability Income'],
    bestFor: 'Simplified-issue clients age 50–75 wanting tax-advantaged accumulation without exam hassle.',
    knockouts: ['Diabetes diagnosed <45', 'Pacemaker', 'Hodgkin\'s', 'Severe asthma', 'Muscular Dystrophy', 'Sickle Cell'],
    source: 'MOO Express Life Reference Guide (27857_0823)',
    verified: '2026-05',
  },
  {
    id: 'iul_americo_instant',
    type: 'IUL',
    name: 'Americo',
    product: 'Instant Decision IUL (Series 336)',
    abbr: 'AME',
    brand: '#E31837',
    issueAges: { min: 18, max: 80 },
    faceRange: { min: 50000, max: 450000, ageBands: null },
    cap: 9.5, par: 100, floor: 0,
    indexStrategy: 'S&P 500 · Annual Point-to-Point (cap OR participation rate option)',
    underwriting: 'Simplified Issue · eApp + instant decision · non-med up to $450k',
    dbOptions: ['A: Level', 'B: Increasing'],
    riders: ['Critical Illness (included)', 'Chronic Illness (included)', 'Terminal Illness (included)'],
    bestFor: 'Fast simplified-issue IUL. Living benefits included at no extra cost. Choose between capped or participation-rate crediting.',
    knockouts: [],
    source: 'americoiul.com SpecFlyerIUL + IUL Agent Guide (Series 336). Current cap at Tools.Americoagent.com',
    verified: '2026-05',
    capNote: 'Cap as of Apr 2025 — verify current rate via Americo Agent Portal',
  },
];
const fmtFace = n => '$' + (n >= 1000000 ? (n/1000000)+'M' : (n/1000)+'k');

// Each carrier gets a single neutral initials label — no per-carrier glow colors

// Each carrier gets a single neutral initials label — no per-carrier glow colors

// ── TERM LIFE CARRIERS ──
// American Amicable Term 10yr rates (per $1000 monthly)
const AMERICAN_AMICABLETERM10_MN = {"18":16.426000000000002,"19":16.568,"20":16.710000000000004,"21":16.852000000000004,"22":16.994,"23":17.136000000000003,"24":17.278000000000002,"25":17.42,"30":18.13,"35":21.3,"40":25.2,"45":32.67,"50":45,"55":63.63,"60":88.74,"65":136.17,"70":213.21,"75":400.5};
const AMERICAN_AMICABLETERM10_MT = {"18":23.839999999999996,"19":24.419999999999995,"20":24.999999999999996,"21":25.58,"22":26.159999999999997,"23":26.74,"24":27.32,"25":27.9,"30":30.8,"35":38.1,"40":49.14,"45":69.75,"50":69.75,"55":150.93,"60":209,"65":364.77,"70":585.99,"75":861.3};
const AMERICAN_AMICABLETERM10_FN = {"18":13.698,"19":13.914000000000001,"20":14.130000000000003,"21":14.346000000000004,"22":14.562000000000001,"23":14.778000000000002,"24":14.994,"25":15.21,"30":16.29,"35":18.18,"40":21.24,"45":27.9,"50":27.9,"55":44.64,"60":55.98,"65":78.12,"70":112.41,"75":243};
const AMERICAN_AMICABLETERM10_FT = {"18":10.463999999999999,"19":11.782,"20":13.100000000000001,"21":14.418,"22":15.736000000000002,"23":17.054000000000002,"24":18.372,"25":19.69,"30":26.28,"35":32.76,"40":43.62,"45":58.66,"50":75.86,"55":105,"60":138.27,"65":203.32,"70":203.32,"75":203.32};

function american_amicableTerm10(age, male, smoker, face) {
  const tbl = male ? (smoker ? AMERICAN_AMICABLETERM10_MT : AMERICAN_AMICABLETERM10_MN) 
                  : (smoker ? AMERICAN_AMICABLETERM10_FT : AMERICAN_AMICABLETERM10_FN);
  const knownAges = Object.keys(tbl).map(Number).sort((a,b)=>a-b);
  if (age < knownAges[0] || age > knownAges[knownAges.length-1]) return null;
  
  // Find rate at this age (with interpolation)
  let rate;
  if (tbl[age]) {
    rate = tbl[age];
  } else {
    // Interpolate between surrounding ages
    let lo = knownAges[0], hi = knownAges[knownAges.length-1];
    for (let i = 0; i < knownAges.length - 1; i++) {
      if (age > knownAges[i] && age < knownAges[i+1]) {
        lo = knownAges[i]; hi = knownAges[i+1]; break;
      }
    }
    const rateLo = tbl[lo], rateHi = tbl[hi];
    if (rateLo == null || rateHi == null) return null;
    const t = (age - lo) / (hi - lo);
    rate = rateLo + t * (rateHi - rateLo);
  }
  
  // Scale by face amount (rates are per $100k, scale to actual face)
  return Math.round(rate * face / 100000 * 100) / 100;
}

// Instabrain Term 10yr rates (per $1000 monthly)
const INSTABRAINTERM10_MN = {"18":11.75,"19":11.75,"20":11.75,"21":11.750000000000002,"22":11.75,"23":11.75,"24":11.75,"25":11.75,"30":11.75,"35":11.75,"40":13.83,"45":16.79,"50":22.71,"55":29.93,"60":52.46};
const INSTABRAINTERM10_MT = {"18":16.873999999999995,"19":17.221999999999994,"20":17.569999999999997,"21":17.917999999999996,"22":18.266,"23":18.613999999999997,"24":18.961999999999996,"25":19.31,"30":21.05,"35":24.36,"40":32.8,"45":45.85,"50":45.85,"55":98.22,"60":186.01};
const INSTABRAINTERM10_FN = {"18":9.838,"19":9.873999999999999,"20":9.91,"21":9.946,"22":9.982000000000001,"23":10.018,"24":10.053999999999998,"25":10.09,"30":10.27,"35":10.27,"40":12.09,"45":14.7,"50":14.7,"55":26.62,"60":45.68};
const INSTABRAINTERM10_FT = {"18":11.335999999999995,"19":11.718,"20":12.1,"21":12.482,"22":12.863999999999999,"23":13.245999999999999,"24":13.627999999999998,"25":14.01,"30":15.92,"35":19.23,"40":25.23,"45":35.93,"50":53.51,"55":86.04,"60":168.08};

function instabrainTerm10(age, male, smoker, face) {
  const tbl = male ? (smoker ? INSTABRAINTERM10_MT : INSTABRAINTERM10_MN) 
                  : (smoker ? INSTABRAINTERM10_FT : INSTABRAINTERM10_FN);
  const knownAges = Object.keys(tbl).map(Number).sort((a,b)=>a-b);
  if (age < knownAges[0] || age > knownAges[knownAges.length-1]) return null;
  
  // Find rate at this age (with interpolation)
  let rate;
  if (tbl[age]) {
    rate = tbl[age];
  } else {
    // Interpolate between surrounding ages
    let lo = knownAges[0], hi = knownAges[knownAges.length-1];
    for (let i = 0; i < knownAges.length - 1; i++) {
      if (age > knownAges[i] && age < knownAges[i+1]) {
        lo = knownAges[i]; hi = knownAges[i+1]; break;
      }
    }
    const rateLo = tbl[lo], rateHi = tbl[hi];
    if (rateLo == null || rateHi == null) return null;
    const t = (age - lo) / (hi - lo);
    rate = rateLo + t * (rateHi - rateLo);
  }
  
  // Scale by face amount (rates are per $100k, scale to actual face)
  return Math.round(rate * face / 100000 * 100) / 100;
}

// John Hancock Term 10yr rates (per $1000 monthly)
const JOHN_HANCOCKTERM10_MN = {"18":22.64,"19":22.64,"20":22.64,"21":22.64,"22":22.64,"23":22.64,"24":22.64,"25":22.64,"30":22.64,"35":24.32,"40":29.5,"45":36.95,"50":54.16,"55":78.71,"60":115.42};
const JOHN_HANCOCKTERM10_MT = {"18":34.18,"19":34.18000000000001,"20":34.18,"21":34.18,"22":34.18,"23":34.18,"24":34.18,"25":34.18,"30":34.18,"35":39.37,"40":53.8,"45":84.89,"50":84.89,"55":190.36,"60":284.44};
const JOHN_HANCOCKTERM10_FN = {"18":17.809999999999995,"19":17.810000000000002,"20":17.81,"21":17.810000000000002,"22":17.81,"23":17.81,"24":17.81,"25":17.81,"30":17.81,"35":20.15,"40":22.78,"45":29.82,"50":29.82,"55":57.82,"60":83.47};
const JOHN_HANCOCKTERM10_FT = {"18":25.163999999999994,"19":25.491999999999997,"20":25.82,"21":26.148000000000003,"22":26.476000000000003,"23":26.803999999999995,"24":27.131999999999998,"25":27.46,"30":29.1,"35":31.44,"40":39.22,"45":61.58,"50":94.2,"55":127.14,"60":187.43};

function john_hancockTerm10(age, male, smoker, face) {
  const tbl = male ? (smoker ? JOHN_HANCOCKTERM10_MT : JOHN_HANCOCKTERM10_MN) 
                  : (smoker ? JOHN_HANCOCKTERM10_FT : JOHN_HANCOCKTERM10_FN);
  const knownAges = Object.keys(tbl).map(Number).sort((a,b)=>a-b);
  if (age < knownAges[0] || age > knownAges[knownAges.length-1]) return null;
  
  // Find rate at this age (with interpolation)
  let rate;
  if (tbl[age]) {
    rate = tbl[age];
  } else {
    // Interpolate between surrounding ages
    let lo = knownAges[0], hi = knownAges[knownAges.length-1];
    for (let i = 0; i < knownAges.length - 1; i++) {
      if (age > knownAges[i] && age < knownAges[i+1]) {
        lo = knownAges[i]; hi = knownAges[i+1]; break;
      }
    }
    const rateLo = tbl[lo], rateHi = tbl[hi];
    if (rateLo == null || rateHi == null) return null;
    const t = (age - lo) / (hi - lo);
    rate = rateLo + t * (rateHi - rateLo);
  }
  
  // Scale by face amount (rates are per $100k, scale to actual face)
  return Math.round(rate * face / 100000 * 100) / 100;
}

// Mutual of Omaha Term 10yr rates (per $1000 monthly)
const MUTUAL_OF_OMAHATERM10_MN = {"18":14.562000000000001,"19":14.935999999999996,"20":15.309999999999999,"21":15.683999999999997,"22":16.058,"23":16.432000000000002,"24":16.806,"25":17.18,"30":19.05,"35":22.61,"40":28.48,"45":39.43,"50":58.3,"55":89.89,"60":124.96,"65":205.5,"70":349.59,"75":592.83};
const MUTUAL_OF_OMAHATERM10_MT = {"18":25.003999999999998,"19":25.412,"20":25.82,"21":26.228,"22":26.636,"23":27.044,"24":27.451999999999998,"25":27.86,"30":29.9,"35":38,"40":57.23,"45":81.97,"50":81.97,"55":167.77,"60":235.49,"65":438.06,"70":733.09};
const MUTUAL_OF_OMAHATERM10_FN = {"18":13.416,"19":13.648,"20":13.879999999999999,"21":14.111999999999998,"22":14.344,"23":14.575999999999999,"24":14.808,"25":15.04,"30":16.2,"35":19.14,"40":22.25,"45":31.86,"50":31.86,"55":59.99,"60":88.2,"65":144.54,"70":290.59,"75":529.46};
const MUTUAL_OF_OMAHATERM10_FT = {"18":21.722,"19":22.026,"20":22.330000000000002,"21":22.634000000000004,"22":22.938000000000002,"23":23.242000000000004,"24":23.546000000000003,"25":23.85,"30":25.37,"35":32.13,"40":48.77,"45":75.38,"50":103.95,"55":152.72,"60":209.95,"65":359.38,"70":359.38,"75":359.38};

function mutual_of_omahaTerm10(age, male, smoker, face) {
  const tbl = male ? (smoker ? MUTUAL_OF_OMAHATERM10_MT : MUTUAL_OF_OMAHATERM10_MN) 
                  : (smoker ? MUTUAL_OF_OMAHATERM10_FT : MUTUAL_OF_OMAHATERM10_FN);
  const knownAges = Object.keys(tbl).map(Number).sort((a,b)=>a-b);
  if (age < knownAges[0] || age > knownAges[knownAges.length-1]) return null;
  
  // Find rate at this age (with interpolation)
  let rate;
  if (tbl[age]) {
    rate = tbl[age];
  } else {
    // Interpolate between surrounding ages
    let lo = knownAges[0], hi = knownAges[knownAges.length-1];
    for (let i = 0; i < knownAges.length - 1; i++) {
      if (age > knownAges[i] && age < knownAges[i+1]) {
        lo = knownAges[i]; hi = knownAges[i+1]; break;
      }
    }
    const rateLo = tbl[lo], rateHi = tbl[hi];
    if (rateLo == null || rateHi == null) return null;
    const t = (age - lo) / (hi - lo);
    rate = rateLo + t * (rateHi - rateLo);
  }
  
  // Scale by face amount (rates are per $100k, scale to actual face)
  return Math.round(rate * face / 100000 * 100) / 100;
}

// SBLI Term 10yr rates (per $1000 monthly)
const SBLITERM10_MN = {"18":7.8100000000000005,"19":7.8100000000000005,"20":7.81,"21":7.81,"22":7.81,"23":7.8100000000000005,"24":7.8100000000000005,"25":7.81,"30":7.81,"35":7.98,"40":8.49,"45":11.03,"50":14.23,"55":20.79,"60":30.08,"65":53.07,"70":86.47,"75":151.26};
const SBLITERM10_MT = {"18":24.82,"19":24.82,"20":24.82,"21":24.82,"22":24.82,"23":24.82,"24":24.82,"25":24.82,"30":24.82,"35":26.32,"40":36.77,"45":53.49,"50":53.49,"55":124.74,"60":188.86,"65":307.14,"70":417.95,"75":907.72};
const SBLITERM10_FN = {"18":7.559999999999999,"19":7.5600000000000005,"20":7.56,"21":7.559999999999999,"22":7.56,"23":7.5600000000000005,"24":7.56,"25":7.56,"30":7.56,"35":7.64,"40":8.07,"45":10.01,"50":10.01,"55":18.13,"60":24.3,"65":37.86,"70":59.28,"75":110.07};
const SBLITERM10_FT = {"18":21.086,"19":21.158,"20":21.23,"21":21.302000000000003,"22":21.374000000000002,"23":21.446,"24":21.518,"25":21.59,"30":21.95,"35":26.32,"40":36.77,"45":53.49,"50":84.08,"55":124.74,"60":188.86,"65":307.14,"70":307.14,"75":307.14};

function sbliTerm10(age, male, smoker, face) {
  const tbl = male ? (smoker ? SBLITERM10_MT : SBLITERM10_MN) 
                  : (smoker ? SBLITERM10_FT : SBLITERM10_FN);
  const knownAges = Object.keys(tbl).map(Number).sort((a,b)=>a-b);
  if (age < knownAges[0] || age > knownAges[knownAges.length-1]) return null;
  
  // Find rate at this age (with interpolation)
  let rate;
  if (tbl[age]) {
    rate = tbl[age];
  } else {
    // Interpolate between surrounding ages
    let lo = knownAges[0], hi = knownAges[knownAges.length-1];
    for (let i = 0; i < knownAges.length - 1; i++) {
      if (age > knownAges[i] && age < knownAges[i+1]) {
        lo = knownAges[i]; hi = knownAges[i+1]; break;
      }
    }
    const rateLo = tbl[lo], rateHi = tbl[hi];
    if (rateLo == null || rateHi == null) return null;
    const t = (age - lo) / (hi - lo);
    rate = rateLo + t * (rateHi - rateLo);
  }
  
  // Scale by face amount (rates are per $100k, scale to actual face)
  return Math.round(rate * face / 100000 * 100) / 100;
}

// Royal Neighbors Term 10yr rates (per $1000 monthly)
const ROYAL_NEIGHBORSTERM10_MN = {"18":11.067999999999998,"19":11.083999999999998,"20":11.1,"21":11.115999999999998,"22":11.132000000000001,"23":11.148,"24":11.164,"25":11.18,"30":11.26,"35":11.35,"40":12.76,"45":14.17,"50":21.38,"55":28.42,"60":49.02,"65":69.78,"70":137.9,"75":207.68};
const ROYAL_NEIGHBORSTERM10_MT = {"18":20.147999999999996,"19":20.323999999999995,"20":20.499999999999996,"21":20.676,"22":20.851999999999997,"23":21.028,"24":21.204,"25":21.38,"30":22.26,"35":23.23,"40":34.06,"45":44.97,"50":44.97,"55":114.49,"60":187.44,"65":268.66,"70":485.58,"75":708.05};
const ROYAL_NEIGHBORSTERM10_FN = {"18":9.684000000000003,"19":9.772000000000002,"20":9.860000000000001,"21":9.948000000000002,"22":10.036,"23":10.124,"24":10.212000000000002,"25":10.3,"30":10.74,"35":11.26,"40":12.67,"45":14.08,"50":14.08,"55":24.46,"60":39.86,"65":55.26,"70":110.62,"75":165.97};
const ROYAL_NEIGHBORSTERM10_FT = {"18":14.659999999999997,"19":15.029999999999998,"20":15.399999999999999,"21":15.77,"22":16.14,"23":16.509999999999998,"24":16.88,"25":17.25,"30":19.1,"35":20.86,"40":28.6,"45":36.34,"50":63.1,"55":89.85,"60":155.23,"65":219.82,"70":219.82,"75":219.82};

function royal_neighborsTerm10(age, male, smoker, face) {
  const tbl = male ? (smoker ? ROYAL_NEIGHBORSTERM10_MT : ROYAL_NEIGHBORSTERM10_MN) 
                  : (smoker ? ROYAL_NEIGHBORSTERM10_FT : ROYAL_NEIGHBORSTERM10_FN);
  const knownAges = Object.keys(tbl).map(Number).sort((a,b)=>a-b);
  if (age < knownAges[0] || age > knownAges[knownAges.length-1]) return null;
  
  // Find rate at this age (with interpolation)
  let rate;
  if (tbl[age]) {
    rate = tbl[age];
  } else {
    // Interpolate between surrounding ages
    let lo = knownAges[0], hi = knownAges[knownAges.length-1];
    for (let i = 0; i < knownAges.length - 1; i++) {
      if (age > knownAges[i] && age < knownAges[i+1]) {
        lo = knownAges[i]; hi = knownAges[i+1]; break;
      }
    }
    const rateLo = tbl[lo], rateHi = tbl[hi];
    if (rateLo == null || rateHi == null) return null;
    const t = (age - lo) / (hi - lo);
    rate = rateLo + t * (rateHi - rateLo);
  }
  
  // Scale by face amount (rates are per $100k, scale to actual face)
  return Math.round(rate * face / 100000 * 100) / 100;
}

// Transamerica Term 10yr rates (per $1000 monthly)
const TRANSAMERICATERM10_MN = {"18":7.949999999999999,"19":7.98,"20":8.01,"21":8.04,"22":8.07,"23":8.1,"24":8.13,"25":8.16,"30":8.31,"35":8.31,"40":9.61,"45":11.46,"50":15.31,"55":20.83,"60":34.04,"65":43.66,"70":81.9,"75":138.34};
const TRANSAMERICATERM10_MT = {"18":18.73,"19":18.73,"20":18.73,"21":18.729999999999997,"22":18.730000000000004,"23":18.73,"24":18.73,"25":18.73,"30":18.73,"35":18.73,"40":29.58,"45":35.79,"50":35.79,"55":75.43,"60":122.57,"65":185.64,"70":274.98,"75":476.09};
const TRANSAMERICATERM10_FN = {"18":6.969999999999999,"19":6.970000000000001,"20":6.97,"21":6.969999999999999,"22":6.970000000000001,"23":6.97,"24":6.969999999999999,"25":6.97,"30":6.97,"35":7.57,"40":7.99,"45":9.95,"50":9.95,"55":17.41,"60":25.46,"65":34.39,"70":56.26,"75":97.56};
const TRANSAMERICATERM10_FT = {"18":14.831999999999997,"19":14.996000000000002,"20":15.16,"21":15.324000000000002,"22":15.488,"23":15.652000000000001,"24":15.815999999999999,"25":15.98,"30":16.8,"35":16.8,"40":23.63,"45":29.92,"50":40.46,"55":51.89,"60":90.7,"65":110.43,"70":110.43,"75":110.43};

function transamericaTerm10(age, male, smoker, face) {
  const tbl = male ? (smoker ? TRANSAMERICATERM10_MT : TRANSAMERICATERM10_MN) 
                  : (smoker ? TRANSAMERICATERM10_FT : TRANSAMERICATERM10_FN);
  const knownAges = Object.keys(tbl).map(Number).sort((a,b)=>a-b);
  if (age < knownAges[0] || age > knownAges[knownAges.length-1]) return null;
  
  // Find rate at this age (with interpolation)
  let rate;
  if (tbl[age]) {
    rate = tbl[age];
  } else {
    // Interpolate between surrounding ages
    let lo = knownAges[0], hi = knownAges[knownAges.length-1];
    for (let i = 0; i < knownAges.length - 1; i++) {
      if (age > knownAges[i] && age < knownAges[i+1]) {
        lo = knownAges[i]; hi = knownAges[i+1]; break;
      }
    }
    const rateLo = tbl[lo], rateHi = tbl[hi];
    if (rateLo == null || rateHi == null) return null;
    const t = (age - lo) / (hi - lo);
    rate = rateLo + t * (rateHi - rateLo);
  }
  
  // Scale by face amount (rates are per $100k, scale to actual face)
  return Math.round(rate * face / 100000 * 100) / 100;
}

// ── DATA-DRIVEN RATE LOOKUP ENGINE ──
// Formula: Monthly = RateFactor × (faceAmount / 1000)
// Source: InsuranceToolkits API — Illinois, EFT, verified 2025
// ── Simple rate lookup: nearest band at or below requested face ──
// No interpolation. No upward capping. Returns {prem, face} or null.
function fexLookup(company, planName, age, male, smoker, face, isGI=false) {
  const key = `${company}||${planName}`;
  const combo = isGI ? 'MNS' : (male ? (smoker ? 'MS' : 'MNS') : (smoker ? 'FS' : 'FNS'));
  const bands = FEX_RATES?.[key]?.[combo]?.[String(age)];
  if (!bands || Object.keys(bands).length === 0) return null;
  const r = RESTRICTIONS?.[company];
  if (r && face < r.minFace) return null;
  const sorted = Object.keys(bands).map(Number).sort((a,b)=>a-b);
  // Largest available band <= requested face (never over-promise)
  // If face < all bands (e.g. juvenile), use minimum band
  const valid = sorted.filter(b => b <= face);
  const effFace = valid.length ? valid[valid.length-1] : sorted[0];
  // Between anchors: interpolate exactly. FE premiums are affine in face
  // (annual rate × units + policy fee), so the straight line between two
  // scraped anchors IS the carrier's own rate math — this quotes the same
  // faces ITK returns between our scrape points instead of snapping down.
  if (valid.length && face > effFace) {
    const next = sorted[valid.length]; // first anchor above the requested face
    if (next != null) {
      const p0 = bands[String(effFace)], p1 = bands[String(next)];
      if (p0 != null && p1 != null && p1 > p0) {
        const prem = p0 + (face - effFace) * (p1 - p0) / (next - effFace);
        return { prem: Math.round(prem * 100) / 100, face };
      }
    }
  }
  const prem = bands[String(effFace)];
  if (prem == null) return null;
  return { prem: Math.round(prem * 100) / 100, face: effFace };
}

// fexPrem: thin wrapper — carrier fn() calls use this, returns just the prem number
// buildResult detects the {prem,face} object from fexLookup and unwraps it
function fexPrem(company, planName, age, male, smoker, face, isGI=false) {
  const r = fexLookup(company, planName, age, male, smoker, face, isGI);
  return r || null; // return the full {prem,face} object — buildResult handles it
}

// ── FORMULA-BASED RATE CALC ──
// For carriers with ratebook factors: monthly = (annualRatePer1k × units + policyFee) × modalFactor
function factorCalc(carrier, tier, age, male, smoker, face) {
  const cfg = RATE_FACTORS?.[carrier];
  if (!cfg) return null;
  // Face validation — defer to AGE_FACE_BANDS (set in buildResult)
  const combo = male ? (smoker ? 'MS' : 'MNS') : (smoker ? 'FS' : 'FNS');
  const rate = cfg.tiers?.[tier]?.[combo]?.[String(age)];
  if (rate == null) return null;
  // Face rounding per carrier, validated against LIVE ITK (July 8, 2026):
  //  - continuous: LB, Foresters, Trans Imm/Exp issue EXACT faces (ITK quotes
  //    $8,300 as $8,300) — units may be fractional.
  //  - nearest: AHL rounds to the NEAREST $500 (ITK: 8,300 → 8,500).
  //  - floor (default): Accendo etc. round DOWN to the unit (ITK: 8,300 → 8,000).
  const mode = cfg.faceRounding || 'floor';
  const effFace = mode === 'continuous' ? face
                : mode === 'nearest'    ? Math.round(face / cfg.unitSize) * cfg.unitSize
                :                         Math.floor(face / cfg.unitSize) * cfg.unitSize;
  if (effFace < cfg.unitSize) return null;
  const units = effFace / cfg.unitSize;
  const fee = (cfg.policyFeeThreshold && effFace < cfg.policyFeeThreshold) ? (cfg.policyFeeLow||cfg.policyFee) : cfg.policyFee;
  const monthly = Math.round((rate * units + fee) * cfg.modalFactor * 100) / 100;
  return { prem: monthly, face: effFace, roundedTo: mode === 'continuous' ? null : cfg.unitSize };
}

// State check: company-only restriction keys
function fexStateOK(company, state) {
  if (!state) return true;
  const r = RESTRICTIONS?.[company];
  if (!r) return true;
  if (r.blockedStates && r.blockedStates.includes(state)) return false;
  if (r.availableStates && r.availableStates.length > 0) return r.availableStates.includes(state);
  return true;
}

const CARRIERS = [
  {id:'acc',  name:'Accendo / CVS',  sub:'Protection Series FE', abbr:'AC', enabled:true,
   product:{B:'Preferred',C:'Standard',D:'Modified',E:null},
   stateCheck:(s)=>(fexStateOK('CVS (Aetna Accendo)',s)),
   fn:(age,male,smoker,tier,face)=>{
     if(tier==='B') return factorCalc('accendo','preferred',age,male,smoker,face);
     if(tier==='C') return factorCalc('accendo','standard',age,male,smoker,face);
     if(tier==='D') return factorCalc('accendo','modified',age,male,smoker,face);
     return null;
   }},
  {id:'ahl',  name:'American Home Life', sub:'Patriot Series FE', abbr:'AH', enabled:true,
   product:{B:'Preferred',C:'Standard',D:'Modified',E:null},
   stateCheck:(s)=>(fexStateOK('American Home Life (Patriot Series)',s)),
   fn:(age,male,smoker,tier,face)=>{
     if(tier==='B') return factorCalc('ahl','preferred',age,male,smoker,face);
     if(tier==='C') return factorCalc('ahl','standard',age,male,smoker,face);
     if(tier==='D') return factorCalc('ahl','modified',age,male,smoker,face);
     return null;
   }},
  {id:'cont', name:'Aetna / Continental', sub:'Protection Series FE', abbr:'CL', enabled:true,
   product:{B:'Preferred',C:null,D:null,E:null},
   stateCheck:(s)=>(fexStateOK('Aetna (Protection Series)',s)),
   fn:(age,male,smoker,tier,face)=>{
     if(tier==='B') return factorCalc('aetna_cont','preferred',age,male,smoker,face);
     return null;
   }},
  {id:'rn',   name:'Royal Neighbors',    sub:'Ensured Legacy FE', abbr:'RN', enabled:true,
   product:{B:'Standard',C:'Standard',D:'Graded',E:'Guaranteed Issue'},
   stateCheck:(s)=>(fexStateOK('Royal Neighbors (Ensured Legacy)',s)),
   fn:(age,male,smoker,tier,face)=>{
     if(tier==='B'||tier==='C') return factorCalc('royal_neighbors','standard',age,male,smoker,face);
     if(tier==='D') return factorCalc('royal_neighbors','graded',age,male,smoker,face);
     if(tier==='E') return factorCalc('royal_neighbors','gi',age,male,smoker,face);
     return null;
   }},
  // ── NEW CARRIERS ──
  {id:'moo',  name:'Mutual of Omaha',    sub:'Living Promise', abbr:'MO', enabled:true,
   product:{B:'Level',C:'Level',D:'Graded',E:null},
   stateCheck:(s)=>(fexStateOK('Mutual of Omaha (Living Promise)',s)),
   fn:(age,male,smoker,tier,face)=>{
     // Juvenile (1-17): MOO Children's Whole Life — guaranteed issue, no medical questions
     if(age<18){
       const r=fexLookup('Mutual of Omaha (Children\'s Whole Life)','Children\'s Whole Life Approved',age,male,false,face,true);
       return r ? {...r,sub:"Children's Whole Life",productName:'Approved'} : null;
     }
     if(tier==='B'||tier==='C') return fexPrem('Mutual of Omaha (Living Promise)','Living Promise Level',age,male,smoker,face);
     if(tier==='D') return fexPrem('Mutual of Omaha (Living Promise)','Living Promise Graded',age,male,smoker,face);
     return null;
   }},
  {id:'ta',   name:'Transamerica',       sub:'Immediate Solution', abbr:'TA', enabled:true,
   product:{B:'Preferred',C:'Standard',D:'Graded',E:null},
   stateCheck:(s)=>(fexStateOK('Transamerica (Solutions)',s)),
   fn:(age,male,smoker,tier,face)=>{
     if(tier==='B') return factorCalc('transamerica_imm','preferred',age,male,smoker,face);
     if(tier==='C') return factorCalc('transamerica_imm','standard',age,male,smoker,face);
     if(tier==='D') return fexPrem('Transamerica (Solutions)','Easy Solution Graded',age,male,smoker,face);
     return null;
   }},
  {id:'laf',  name:'Lafayette Life',     sub:'Whole Life', abbr:'LF', enabled:false,
   product:{B:'Level',C:'Level',D:null,E:null},
   fn:(age,male,smoker,tier,face)=>{if(tier!=='B'&&tier!=='C')return null;return csvLookup(LAF,LAF_M,age,male,smoker,face);}},
  {id:'for',  name:'Foresters',          sub:'PlanRight Whole Life', abbr:'FR', enabled:true,
   product:{B:'Preferred',C:'Standard',D:'Basic',E:null},
   stateCheck:(s)=>(fexStateOK('Foresters (PlanRight)',s)),
   fn:(age,male,smoker,tier,face)=>{
     if(tier==='B') return factorCalc('foresters','preferred',age,male,smoker,face);
     if(tier==='C') return factorCalc('foresters','standard',age,male,smoker,face);
     if(tier==='D') return factorCalc('foresters','basic',age,male,smoker,face);
     return null;
   }},
  {id:'afl',  name:'Aflac',              sub:'Final Expense WL', abbr:'AF', enabled:false,
   product:{B:'Preferred',C:'Standard',D:null,E:null},
   fn:(age,male,smoker,tier,face)=>{
     if(tier==='B') return fexPrem('Aflac (Final Expense)','Preferred',age,male,smoker,face);
     if(tier==='C') return fexPrem('Aflac (Final Expense)','Standard',age,male,smoker,face);
     return null;
   }},
  {id:'amr',  name:'Americo',            sub:'Eagle Select', abbr:'AM', enabled:false,
   product:{B:'Plan 1',C:'Plan 2',D:'Plan 3',E:null},
   stateCheck:(s)=>(fexStateOK('Americo',s)),
   fn:(age,male,smoker,tier,face)=>{
     if(tier==='B') return fexPrem('Americo','Eagle Select Plan 1',age,male,smoker,face);
     if(tier==='C') return fexPrem('Americo','Eagle Select Plan 2',age,male,smoker,face);
     if(tier==='D') return fexPrem('Americo','Eagle Select Plan 3',age,male,smoker,face);
     return null;
   }},
  {id:'uhl',  name:'United Home Life',   sub:'Premier / Deluxe / GI', abbr:'UH', enabled:true,
   product:{B:'Premier',C:'Deluxe',D:'Graded (EIWL)',E:'Guaranteed Issue'},
   stateCheck:(s)=>(fexStateOK('UHL',s)),
   fn:(age,male,smoker,tier,face)=>{
     if(tier==='B') return fexPrem('UHL','Premier',age,male,smoker,face);
     if(tier==='C') return fexPrem('UHL','Deluxe',age,male,smoker,face);
     if(tier==='D') return fexPrem('UHL','EIWL',age,male,smoker,face);
     if(tier==='E') return fexPrem('UHL','Guaranteed Issue',age,male,smoker,face,true);
     return null;
   }},
  {id:'fid',  name:'Fidelity Life',      sub:'RAPIDecision FE / GI', abbr:'FD', enabled:true,
   product:{B:'Level',C:'Level',D:null,E:'Guaranteed Issue'},
   stateCheck:(s)=>(fexStateOK('Fidelity (RAPIDecision Final Expense)',s)),
   fn:(age,male,smoker,tier,face)=>{
     if(tier==='B'||tier==='C') return factorCalc('fidelity','level',age,male,smoker,face);
     if(tier==='E') return factorCalc('fidelity','guaranteed',age,male,smoker,face);
     return null;
   }},
  {id:'cbg',  name:'Corebridge Financial', sub:'SIWL / GIWL', abbr:'CB', enabled:false,
   product:{B:'SimpliNow Legacy Max',C:'SimpliNow Legacy Max',D:null,E:'Guaranteed Issue'},
   stateCheck:(s)=>(fexStateOK('AIG (SIWL)',s)),
   fn:(age,male,smoker,tier,face)=>{
     if(tier==='B'||tier==='C') return fexPrem('AIG (SIWL)','SimpliNow Legacy Max',age,male,smoker,face);
     if(tier==='E') return factorCalc('corebridge_gi','gi',age,male,smoker,face);
     return null;
   }},
  {id:'lb',   name:'Liberty Bankers',    sub:'SIMPL Whole Life', abbr:'LB', enabled:true,
   product:{B:'SIMPL Preferred',C:'SIMPL Standard',D:'SIMPL Modified',E:null},
   stateCheck:(s)=>(fexStateOK('Liberty Bankers',s)),
   fn:(age,male,smoker,tier,face)=>{
     if(tier==='B') return factorCalc('liberty_bankers','preferred',age,male,smoker,face);
     if(tier==='C') return factorCalc('liberty_bankers','standard',age,male,smoker,face);
     if(tier==='D') return fexPrem('Liberty Bankers','SIMPL Modified',age,male,smoker,face);
     return null;
   }},
  {id:'amam', name:'American Amicable',  sub:'Senior Choice', abbr:'AA', enabled:true,
   product:{B:'Immediate',C:'Immediate',D:'Graded',E:null},
   stateCheck:(s)=>(fexStateOK('American Amicable (Senior Choice)',s)),
   fn:(age,male,smoker,tier,face)=>{
     if(tier==='B'||tier==='C') return factorCalc('amam_sc','immediate',age,male,smoker,face);
     if(tier==='D') return fexPrem('American Amicable (Senior Choice)','Senior Choice Graded',age,male,smoker,face);
     return null;
   }},
  {id:'ra',   name:'Royal Arcanum',      sub:'Whole Life / SIWL', abbr:'RA', enabled:false,
   product:{B:'Level',C:'Level',D:null,E:'Simplified Issue'},
   fn:(age,male,smoker,tier,face)=>{
     if(tier==='B'||tier==='C') return fexPrem('Royal Arcanum (Whole Life)','Whole Life Level',age,male,smoker,face);
     if(tier==='E') return fexPrem('Royal Arcanum (SIWL)','Simplified Issue Level',age,male,smoker,face);
     return null;
   }},
  {id:'bl_sg', name:'Baltimore Life',    sub:'iProvide FE', abbr:'BL', enabled:true,
   product:{B:'Preferred',C:'Standard',D:'Modified',E:null},
   stateCheck:(s)=>(fexStateOK('Baltimore Life (iProvide 45-69)',s)),
   fn:(age,male,smoker,tier,face)=>{
     // iProvide 45-69 for ages 50-69, iProvide 70+ for ages 70-80
     const co = age<=69 ? 'Baltimore Life (iProvide 45-69)' : 'Baltimore Life (iProvide 70+)';
     const pref = age<=69 ? 'iProvide (45-69) Preferred' : 'iProvide (70+) Preferred';
     const std  = age<=69 ? 'iProvide (45-69) Standard'  : 'iProvide (70+) Standard';
     const mod  = age<=69 ? 'iProvide (45-69) Modified'  : null;
     if(tier==='B') return fexPrem(co, pref, age, male, smoker, face);
     if(tier==='C') return fexPrem(co, std,  age, male, smoker, face);
     if(tier==='D') return mod ? fexPrem(co, mod, age, male, smoker, face) : null;
     return null;
   }},
  {id:'ls',   name:'Lifeshield',         sub:'Survivor Level / Graded', abbr:'LS', enabled:true,
   product:{B:'Level',C:'Level',D:'Graded',E:null},
   stateCheck:(s)=>(fexStateOK('Lifeshield',s)),
   fn:(age,male,smoker,tier,face)=>{
     if(tier==='B'||tier==='C') return factorCalc('lifeshield','level',age,male,smoker,face);
     if(tier==='D') return fexPrem('Lifeshield','Survivor Graded',age,male,smoker,face);
     return null;
   }},
  {id:'pf',   name:'Polish Falcons',     sub:'Whole Life Level', abbr:'PF', enabled:false,
   product:{B:'Level',C:'Level',D:null,E:null},
   fn:(age,male,smoker,tier,face)=>{if(tier!=='B'&&tier!=='C')return null;return fexPrem('Polish Falcons (Whole Life)','Whole Life Level',age,male,smoker,face);}},
  {id:'occ_gs', name:'Occidental Life',  sub:'Golden Solution', abbr:'OC', enabled:false,
   product:{B:'Immediate',C:'Immediate',D:null,E:null},
   fn:(age,male,smoker,tier,face)=>{if(tier!=='B'&&tier!=='C')return null;return fexPrem('Occidental Life (Golden Solution)','Golden Solution Immediate',age,male,smoker,face);}},
  {id:'comb', name:'Combined Insurance', sub:'Final Expense Life', abbr:'CO', enabled:false,
   product:{B:'Preferred',C:'Standard',D:'Substandard',E:null},
   fn:(age,male,smoker,tier,face)=>{
     if(tier==='B') return fexPrem('Combined (Final Expense Life)','Preferred',age,male,smoker,face);
     if(tier==='C') return fexPrem('Combined (Final Expense Life)','Standard',age,male,smoker,face);
     if(tier==='D') return fexPrem('Combined (Final Expense Life)','Substandard',age,male,smoker,face);
     return null;
   }},
  {id:'eth',  name:'Ethos',              sub:'Advantage WL + Estate', abbr:'ET', enabled:false,
   product:{B:'Advantage',C:'Advantage',D:null,E:null},
   fn:(age,male,smoker,tier,face)=>{if(tier!=='B'&&tier!=='C')return null;return fexPrem('Ethos (Advantage Whole Life + Estate Plan)','Advantage Whole Life + Estate Plan',age,male,smoker,face);}},
  {id:'bl',   name:'Better Life',          sub:'Better Final Expense', abbr:'BL', enabled:false,
   product:{B:'Level',C:'Level',D:'Graded',E:null},
   fn:(age,male,smoker,tier,face)=>{
     if(tier==='B'||tier==='C') return blQuote(BL_SI,age,male,smoker,Math.min(face,50000));
     if(tier==='D') return blQuote(BL_GRD,age,male,smoker,Math.min(face,25000));
     return null;
   }},
  {id:'elco', name:'Elco Mutual',          sub:'Silver Eagle', abbr:'EM', enabled:false, // no rate data — ITK returned no Elco rows in the carrier scrape; enable after a successful scrape
   product:{B:'Premier',C:'Standard',D:'Graded',E:'Guaranteed Issue'},
   stateCheck:(s)=>(fexStateOK('Elco (Silver Eagle)',s)),
   fn:(age,male,smoker,tier,face)=>{
     if(tier==='B') return fexPrem('Elco (Silver Eagle)','Silver Eagle Premier',age,male,smoker,face);
     if(tier==='C') return fexPrem('Elco (Silver Eagle)','Silver Eagle Standard',age,male,smoker,face);
     if(tier==='D') return fexPrem('Elco (Silver Eagle)','Silver Eagle Graded',age,male,smoker,face);
     if(tier==='E') return fexPrem('Elco (Silver Eagle)','Silver Eagle Guaranteed Issue',age,male,smoker,face,true);
     return null;
   }},
  {id:'balt_sg', name:'Baltimore Life',    sub:'Silver Guard', abbr:'BT', enabled:false,
   product:{B:'Special',C:'Standard',D:'Silver Guard Special',E:null},
   fn:(age,male,smoker,tier,face)=>{
     if(tier==='B') return fexPrem('Baltimore Life (Silver Guard)','Silver Guard Special',age,male,smoker,face);
     if(tier==='C') return fexPrem('Baltimore Life (Silver Guard)','Silver Guard Standard',age,male,smoker,face);
     return null;
   }},
  {id:'sl_pp', name:'Senior Life',         sub:'Platinum Protection', abbr:'SL', enabled:false,
   product:{B:'Level',C:null,D:null,E:null},
   stateCheck:(s)=>(fexStateOK('Senior Life (Platinum Protection)',s)),
   fn:(age,male,smoker,tier,face)=>{
     if(tier!=='B') return null;
     return factorCalc('senior_life','level',age,male,smoker,face);
   }},
  {id:'sl',    name:'Senior Life',          sub:'Whole Life (EI)', abbr:'SL', enabled:false,
   product:{B:null,C:'Level — Standard',D:'Modified',E:'Guaranteed Issue'},
   fn:(age,male,smoker,tier,face)=>slWlQuote(age,male,smoker,tier,face)},
  {id:'ail',   name:'American Income Life', sub:'Whole Life / Graded', abbr:'AI', enabled:false,
   product:{B:'Level — Non-Tobacco',C:'Level — Tobacco',D:'Senior Graded (3-yr)',E:null},
   fn:(age,male,smoker,tier,face)=>ailQuote(age,male,smoker,tier,face)},
  // ── NEW CARRIERS FROM RATE SHEET ──
  {id:'ta_exp', name:'Transamerica',  sub:'Express Solution', abbr:'TX', enabled:true,
   product:{B:'Express Select',C:'Express Select',D:'Express Graded',E:null},
   stateCheck:(s)=>(fexStateOK('Transamerica (Express)',s)),
   fn:(age,male,smoker,tier,face)=>{
     if(tier==='B'||tier==='C') return factorCalc('transamerica_exp','select',age,male,smoker,face);
     if(tier==='D') return fexPrem('Transamerica (Express)','Express Graded',age,male,smoker,face);
     return null;
   }},
  {id:'ahl_gs', name:'American Home Life', sub:'GuideStar 45+', abbr:'AG', enabled:false,
   product:{B:'Level',C:'Level',D:'Graded',E:null},
   stateCheck:(s)=>(fexStateOK('American Home Life (GuideStar 45+)',s)),
   fn:(age,male,smoker,tier,face)=>{
     if(tier==='B'||tier==='C') return fexPrem('American Home Life (GuideStar 45+)','GuideStar Level',age,male,smoker,face);
     if(tier==='D') return fexPrem('American Home Life (GuideStar 45+)','GuideStar Graded',age,male,smoker,face);
     return null;
   }},
  {id:'amam_gs', name:'American Amicable', sub:'Golden Solution', abbr:'AG', enabled:false,
   product:{B:'Immediate',C:'Immediate',D:'Graded',E:null},
   stateCheck:(s)=>(fexStateOK('American Amicable (Golden Solution)',s)),
   fn:(age,male,smoker,tier,face)=>{
     if(tier==='B'||tier==='C') return factorCalc('amam_gs','immediate',age,male,smoker,face);
     if(tier==='D') return fexPrem('American Amicable (Golden Solution)','Golden Solution Graded',age,male,smoker,face);
     return null;
   }},
  {id:'rna_gi', name:'Royal Neighbors',   sub:'Guaranteed Issue', abbr:'RG', enabled:false,
   product:{B:null,C:null,D:null,E:'Guaranteed Issue'},
   stateCheck:(s)=>(fexStateOK('Royal Neighbors (Ensured Legacy)',s)),
   fn:(age,male,smoker,tier,face)=>{
     if(tier==='E') return fexPrem('Royal Neighbors (Ensured Legacy)','Ensured Legacy Guaranteed Issue',age,male,smoker,face,true);
     return null;
   }},
  // ── TERM LIFE CARRIERS (injected below) ──
  ...TERM_CARRIERS,
];


// ── COMP RATES (% of premium, from agency grid) ──
// Used purely for badge display — no numbers shown to agents
// B=Preferred C=Level/Standard D=Modified E=GI
const COMP_RATES = {
  acc:  { B:115, C:115, D:89,  E:null },
  ahl:  { B:115, C:115, D:115, E:null },
  cont: { B:115, C:115, D:null,E:null },
  rn:   { B:110, C:110, D:null,E:23   },
  ta:   { B:110, C:110, D:null,E:null },
  fid:  { B:null,C:null,D:null,E:55   },
  cbg:  { B:null,C:null,D:null,E:55   },
  sl:   { B:110, C:110, D:100, E:80  },
  ail:  { B:110, C:110, D:110, E:null },
};

// ── CARRIER META: logo domain (Clearbit) + eApp link ──
const CARRIER_META = {
  acc:  { img:'/logos/acc.png',   eapp:'https://www.aetnaseniorproducts.com/ssi/index.html', brand:'#DC2626' }, // CVS/Accendo
  ahl:  { img:'/logos/ahl.png',   eapp:'https://ahlpatriotseries.com/',                       brand:'#1D4ED8' }, // AHL
  cont: { img:'/logos/cont.png', eapp:'https://www.aetnaseniorproducts.com/ssi/index.html', brand:'#7C3AED' }, // Continental/Aetna
  rn:   { img:'/logos/rn.png',    eapp:'https://agent.royalneighbors.org/login',              brand:'#16A34A' }, // Royal Neighbors
  ta:    { img:'/logos/ta.png',       eapp:'https://www.transamerica.com/financial-professionals/', brand:'#b42318' }, // Transamerica Immediate Solution
  ta_exp:{ img:'/logos/ta.png',       eapp:'https://www.transamerica.com/financial-professionals/', brand:'#b42318' }, // Transamerica Express Solution
  fid:  { img:'/logos/fid.png',   eapp:'https://portal.instabrain.io/App/?redirected=true',   brand:'#1E40AF' }, // Fidelity Life
  cbg:  { img:'/logos/cbg.avif',  eapp:'https://www.connext.corebridgefinancial.com/life/connext-portal/public/login', brand:'#9333EA' }, // Corebridge
  uhl:  { img:'/logos/uhl.png',   eapp:'https://agentportal.unitedhomelife.com/home',          brand:'#B91C1C' }, // UHL
  moo:  { img:'/logos/moo.jpg',   eapp:'https://www.mutualofomaha.com/agent-login',            brand:'#1D4ED8' }, // Mutual of Omaha
  laf:  { img:'/logos/laf.png',       eapp:'https://www.lafayettelife.com/agent-resources',        brand:'#8B5CF6' }, // Lafayette
  for:  { img:'/logos/for.png',   eapp:'https://www.forestersfinancial.com/us/agent-portal',   brand:'#6B21A8' }, // Foresters
  amr:  { img:'/logos/amr.svg',       eapp:'https://www.americo.com/agent-access',                brand:'#1E40AF' }, // Americo
  amam: { img:'/logos/amam.png', eapp:'https://www.insuranceapplication.com',                brand:'#3B82F6' }, // American Amicable
  // Added May 2026 — branded SVG wordmarks for term carriers without official PNGs
  jhc:        { img:'/logos/jhc.svg',        eapp:'https://www.johnhancock.com/financial-professionals.html', brand:'#000000' }, // John Hancock
  kcl:        { img:'/logos/kcl.svg',        eapp:'https://www.kclife.com/agents',                            brand:'#15803D' }, // Kansas City Life
  nlg:        { img:'/logos/nlg.svg',        eapp:'https://www.nationallife.com/agents',                      brand:'#0F766E' }, // National Life Group
  protective: { img:'/logos/protective.svg', eapp:'https://www.protective.com/financial-professionals',       brand:'#003E7E' }, // Protective Life
  sbli:       { img:'/logos/sbli.svg',       eapp:'https://www.sbli.com/agents',                              brand:'#1E3A8A' }, // SBLI
  bl_sg:{ img:'/logos/balt.png',                     eapp:'https://www.baltlife.com',                       brand:'#1D4ED8' }, // Baltimore Life Silver Guard
  ts:   { img:'/logos/ts.png',        eapp:'https://www.trustage.com/agents',                brand:'#FDE68A' }, // TruStage
  ls:   { img:'/logos/ls.jpg',    eapp:'https://www.lifeshield.com/agent',               brand:'#1D4ED8' }, // LifeShield
  lb:   { img:'/logos/lb.png',    eapp:'https://www.libertybankers.com/agent-portal',    brand:'#1D4ED8' }, // Liberty Bankers
  bl:   { img:'',                     eapp:'https://www.betterlifeins.com/agents',            brand:'#EAB308' }, // Better Life
  elco: { img:'',                     eapp:'https://www.elcomutual.com/agent-portal',         brand:'#059669' }, // Elco Mutual
  balt_sg: { img:'',                  eapp:'https://www.baltlife.com/agent-portal',           brand:'#0369A1' }, // Baltimore Life
  sl_pp: { img:'/logos/sl.png',   eapp:'https://www.seniorlife.com/agent-portal',         brand:'#7C3AED' }, // Senior Life PP
  sl:    { img:'/logos/sl.png',   eapp:'https://www.seniorlife.com/agent-portal',         brand:'#7C3AED' }, // Senior Life WL
  ail:   { img:'/logos/ail.png',                    eapp:'https://www.ailife.com/agent',                    brand:'#1D4ED8' }, // American Income Life
  // ── TERM LIFE CARRIERS ──
  american_amicable: { img:'',        eapp:'https://www.insuranceapplication.com',              brand:'#CBD5E1' }, // American Amicable — Light Gray
  instabrain: { img:'',              eapp:'https://portal.instabrain.io/App/?redirected=true',  brand:'#38BDF8' }, // Instabrain — Light Blue
  john_hancock: { img:'',            eapp:'https://www.johnhancock.com/agent-portal',           brand:'#1E40AF' }, // John Hancock — Blue
  mutual_of_omaha: { img:'',         eapp:'https://www.mutualofomaha.com/agent-login',          brand:'#2563EB' }, // Mutual of Omaha — Royal Blue
  // sbli already defined above with /logos/sbli.svg — don't redefine
  royal_neighbors: { img:'',         eapp:'https://agent.royalneighbors.org/login',             brand:'#16A34A' }, // Royal Neighbors — Green
  transamerica: { img:'',            eapp:'https://www.transamerica.com/financial-professionals/', brand:'#b42318' }, // Transamerica — Red
};

function getCompBadge(carrierId, tier) {
  const rates = COMP_RATES[carrierId];
  if(!rates) return null;
  const pct = rates[tier];
  if(pct == null) return null;
  if(pct >= 110) return 'high';
  if(pct < 90)   return 'low';
  return 'mid';
}

function getAutoTier(sel){const active=sel.filter(id=>id!=='none');if(!active.length)return 'B';const order=['E','D','C','B'],tiers=active.map(id=>CONDITIONS.find(c=>c.id===id)?.tier||'B');for(const t of order)if(tiers.includes(t))return t;return 'B';}

// ── TERM UNDERWRITING ENGINE ──
// Maps each FE condition to its impact on a TERM quote. Term UW is stricter
// than FE — many "Standard FE" risks are decline-on-term. 'decline' means
// the client likely won't qualify; we still show them at 'Std' for now but
// surface a warning. Calibrated against ITK underwriting playbooks.
const COND_TERM_IMPACT = {
  // Clean
  none: 'pp',
  // Heart & Blood — mild
  htn: 'p', hcl: 'p',
  // Heart & Blood — moderate (resolved/controlled)
  afib_ctrl: 'sp', aneurysm_old: 'sp', pad_old: 'sp',
  // Heart & Blood — significant but stable
  angina_2plus: 's', angio_2plus: 's', mi_4plus: 's', stent_2plus: 's',
  stroke_4plus: 's', cardiomyo_old: 's', cad: 's', pacemaker_old: 's',
  // Heart & Blood — recent or active
  mi_2to4: 'decline', stroke_2to4: 'decline', angina_1yr: 'decline',
  stent_1to2: 'decline', pacemaker_new: 'decline', chf: 'decline',
  mi_1yr: 'decline', stroke_1yr: 'decline',
  // Diabetes
  diabetes_oral: 'sp', diabetes_ins: 's',
  diabetes_comp: 'decline', diabetes_coma: 'decline',
  // Respiratory
  asthma_b: 'p', sleep_apnea: 'p',
  asthma_mod: 'sp', copd_no_o2: 's', bronchitis_chr: 's',
  copd_o2: 'decline', pulm_fib: 'decline',
  // Cancer
  basal_cell: 'p', cancer_5plus: 'sp', cancer_4plus: 's',
  cancer_2to4: 'decline', melanoma_2to4: 'decline',
  cancer_lt2: 'decline', chemo_active: 'decline',
  // Neurological
  seizures_rare: 'sp', neuropathy: 'p',
  ms: 's', parkinsons: 's',
  seizures_freq: 'decline', alzheimers: 'decline', als: 'decline',
  // Mental Health
  anxiety: 'p', ptsd: 'p', bipolar: 'sp',
  schizophrenia: 's', alcohol_2plus: 'sp',
  alcohol_2yr: 'decline',
  // Kidney
  kidney_disease: 'decline', dialysis: 'decline',
  // Liver
  hep_c_cured: 'sp', hep_b: 'sp',
  hep_c_active: 's', cirrhosis: 'decline', hep_c_curr: 's',
  // Other
  thyroid: 'p', arthritis: 'p', rheum_arth: 'p', gerd: 'p',
  obesity: 'sp', crohn: 'sp', restless_leg: 'p',
  lupus: 's', chronic_pain: 's', sarcoidosis: 's', pancreatitis: 's',
  dui_2plus: 's', dui_2yr: 'decline', sickle_cell: 'decline',
  // Knockouts (term always declines)
  home_o2_24hr: 'decline', nursing_home: 'decline', hospice: 'decline',
  transplant_rec: 'decline', wheelchair: 'decline', aids_hiv: 'decline',
  terminal: 'decline', felony: 'decline',
};

const TERM_CLASS_RANK = { pp:0, p:1, sp:2, s:3, decline:99 };

// Compute BMI from height (inches) and weight (lbs). Returns null if invalid.
function calcBMI(heightIn, weightLb) {
  const h = +heightIn, w = +weightLb;
  if (!h || !w || h < 36 || h > 90 || w < 60 || w > 700) return null;
  return (w / (h * h)) * 703;
}

// BMI → term class (rough industry standard build chart).
function bmiToTermClass(bmi) {
  if (bmi == null) return null;
  if (bmi >= 40) return 'decline';
  if (bmi >= 36) return 's';
  if (bmi >= 32) return 'sp';
  if (bmi >= 30) return 'p';
  if (bmi >= 19) return 'pp';
  return 'sp'; // underweight is a flag too
}


// Recommend a term health class from conditions + BMI + age + family hx.
// Returns { recommended: 'pp'|'p'|'sp'|'s'|'decline', reasons: [string] }
function recommendTermClass(selectedConds, bmi, age, opts = {}) {
  let worst = 'pp';
  const reasons = [];
  const active = (selectedConds || []).filter(id => id !== 'none');
  for (const cid of active) {
    const impact = COND_TERM_IMPACT[cid] || 'p';
    if (TERM_CLASS_RANK[impact] > TERM_CLASS_RANK[worst]) {
      worst = impact;
      const cond = CONDITIONS.find(c => c.id === cid);
      reasons.push(`${cond?.label || cid}`);
    }
  }
  if (bmi != null) {
    const bc = bmiToTermClass(bmi);
    if (bc && TERM_CLASS_RANK[bc] > TERM_CLASS_RANK[worst]) {
      worst = bc;
      reasons.push(`BMI ${bmi.toFixed(1)}`);
    }
  }
  // Family hx of premature CVD/cancer (parent before 60) — bump down one class
  if (opts.familyHx && worst !== 'decline') {
    const bumped = worst === 'pp' ? 'p' : worst === 'p' ? 'sp' : worst === 'sp' ? 's' : 's';
    if (TERM_CLASS_RANK[bumped] > TERM_CLASS_RANK[worst]) {
      worst = bumped;
      reasons.push('Family history (pre-60)');
    }
  }
  // Smoker — verified against ITK raw data:
  //   age <60 smokers: most quote at Pref or Std+, none at Pref+
  //   age 60-69 smokers: most quote at Std+ or Std
  //   age 70+ smokers: 11 products quote at Std, only 3 at higher tiers
  // So smoker bumps one class always, with age floors for older smokers.
  if (opts.smoker && worst !== 'decline') {
    const bumped = worst === 'pp' ? 'p' : worst === 'p' ? 'sp' : worst === 'sp' ? 's' : 's';
    if (TERM_CLASS_RANK[bumped] > TERM_CLASS_RANK[worst]) {
      worst = bumped;
      reasons.push('Smoker');
    }
    if (age != null) {
      if (age >= 60 && TERM_CLASS_RANK[worst] < TERM_CLASS_RANK['sp']) {
        worst = 'sp';
        reasons.push('Smoker 60+');
      }
      if (age >= 70 && TERM_CLASS_RANK[worst] < TERM_CLASS_RANK['s']) {
        worst = 's';
        reasons.push('Smoker 70+');
      }
    }
  }
  // Non-smoker age penalty — Preferred Plus rarely issued past 65, Pref past 75
  if (age != null && !opts.smoker) {
    if (age >= 65 && worst === 'pp') { worst = 'p'; reasons.push('Age 65+'); }
    if (age >= 75 && worst === 'p')  { worst = 'sp'; reasons.push('Age 75+'); }
  }
  return { recommended: worst, reasons };
}
function solveForFace(budget,age,male,smoker,tier,fn){
  // Exact inversion of the carrier's face→premium curve for premium mode.
  // Premium is affine in face (annual rate × units + policy fee) and
  // fexLookup interpolates between scraped anchors, so a $1-granularity
  // binary search lands on the same face ITK's budget mode computes.
  //
  // Returns the carrier's EFFECTIVE face, not the raw search midpoint —
  // factorCalc rounds to unit size ($500/$1k) and fexLookup caps at the top
  // anchor, and quoting the effective face downstream means buildResult
  // re-derives the identical quote (no phantom "capped" flag / gray cards).
  const quote=(f)=>{
    const r=fn(age,male,smoker,tier,f);
    const p=(r && typeof r==='object') ? r.prem : r;
    return p==null ? null : { p, eff:(r && typeof r==='object' && r.face!=null) ? r.face : f };
  };
  // Probe ladder: fn() returns null below the carrier's minimum face, which
  // would strand a naive binary search — walk up to the first quotable face.
  let lo=null, bestEff=null;
  for(const f of [1000,2000,3000,5000,7500,10000,15000,25000,50000]){
    const q=quote(f);
    if(!q) continue;                       // below min face / data gap — probe higher
    if(q.p>budget+0.001) return null;      // cheapest quotable face already over budget
    lo=f; bestEff=q.eff;
    break;
  }
  if(bestEff==null) return null;           // nothing quotable at any probe
  let L=lo+1, H=100000;
  while(L<=H){
    const mid=(L+H)>>1;
    const q=quote(mid);
    if(q && q.p<=budget+0.001){ bestEff=q.eff; L=mid+1; } else H=mid-1;
  }
  return bestEff;
}

// ── CASH VALUE CORRIDOR ──
// Calibrated against real whole-life illustrations (Americo, Royal Neighbors,
// LifeShield). Age-tiered breakpoint tables of [year, lowPct, highPct] —
// returns a percentage of total premiums paid, not a future-value calc.
// This is the tried-and-true math that was in production through 10e83e5.
function calculateCVCorridor(monthlyPremium, policyYears, issueAge) {
  const annualPremium = monthlyPremium * 12;
  const totalPaid     = Math.round(annualPremium * policyYears);

  // Age-tiered breakpoints [year, lowPct, highPct]
  // Under 60: slower start, builds higher long term (lower mortality load early)
  const UNDER60 = [
    [1,0,0],[2,0,0],[3,0.08,0.12],[5,0.18,0.25],
    [10,0.28,0.38],[15,0.35,0.44],[20,0.38,0.50],[30,0.42,0.54]
  ];
  // 60-70: faster initial build, slightly lower ceiling
  const AGE60_70 = [
    [1,0,0],[2,0.05,0.07],[3,0.10,0.22],[5,0.20,0.35],
    [10,0.28,0.46],[15,0.32,0.50],[20,0.34,0.52],[30,0.36,0.53]
  ];
  // 71+: heavy mortality cost, lower ceiling but still meaningful
  const AGE71PLUS = [
    [1,0,0],[2,0,0],[3,0.08,0.20],[5,0.20,0.34],
    [10,0.30,0.44],[15,0.32,0.46],[20,0.32,0.45],[30,0.34,0.46]
  ];

  const table = issueAge < 60 ? UNDER60 : issueAge <= 70 ? AGE60_70 : AGE71PLUS;

  // Linear interpolation between breakpoints
  function interp(yr) {
    if (yr <= table[0][0]) return [table[0][1], table[0][2]];
    for (let i = 1; i < table.length; i++) {
      if (yr <= table[i][0]) {
        const [y0,l0,h0] = table[i-1];
        const [y1,l1,h1] = table[i];
        const t = (yr - y0) / (y1 - y0);
        return [l0 + t*(l1-l0), h0 + t*(h1-h0)];
      }
    }
    return [table[table.length-1][1], table[table.length-1][2]];
  }

  const [lowPct, highPct] = interp(policyYears);
  const low  = Math.round(totalPaid * lowPct);
  const high = Math.round(totalPaid * highPct);
  return { low, high, totalPaid, annualPremium };
}

// ── COVERAGE ESTIMATOR (FEX/WL reverse-lookup) ──
// Given current age + policy years + monthly premium + gender, estimates
// the face amount of an existing WL/FE policy.
//
// Method:
//   1. Look up today's most aggressive (Preferred-tier) face at this
//      (issue_age, gender NS, premium) — call this "book"
//   2. Lowball by a gender-specific discount band because:
//      a. Real policies typically aren't Preferred-tier (most are Std/Mod)
//      b. Policies issued 5-15 years ago were priced less competitively
//         than today's rate book — face per dollar has improved
//   3. Apply [low_factor, high_factor]:
//        Male:   [0.50, 0.75]  → "down 25–50%"
//        Female: [0.60, 0.85]  → "down 15–40%"
//
// Hardcoded to Non-Smoker class — where ~80% of FE buyers land.
// ROP and GI policies land lower than this range (caveat in the UI).
//
// Returns: { low, high, issueAge } or { error }
function estimateCoverage({ currentAge, policyYears, monthlyPremium, gender }) {
  if (!currentAge || !policyYears || !monthlyPremium) return { error: 'missing inputs' };
  const issueAge = currentAge - policyYears;
  if (issueAge < 1) return { error: 'policy years exceed current age' };

  // Always use the MALE rate as the baseline. Female pricing in the raw rate
  // table gets ~2x the face for the same premium (longer life expectancy),
  // which produced unrealistically wide spreads in practice. Instead we
  // compute the male estimate and then bump by 15–25% for females — keeps the
  // numbers proportional and the spread tight.
  // Anchor products in preference order: PlanRight Preferred is the calibrated
  // FE anchor but only issues 50–85; Transamerica Immediate Solution Preferred
  // issues 1–85 and covers the younger issue ages (e.g. 55-year-old with a
  // policy 7+ years in force → issue age < 50).
  const ANCHORS = [
    'Foresters (PlanRight)||PlanRight Preferred',
    'Transamerica (Solutions)||Immediate Solution Preferred',
    'Liberty Bankers||SIMPL Preferred',
  ];
  let ageMap = null;
  for (const key of ANCHORS) {
    ageMap = FEX_RATES?.[key]?.['MNS']?.[String(issueAge)];
    if (ageMap) break;
  }
  if (!ageMap) return { error: 'insufficient products matched' };
  const candidates = Object.entries(ageMap).map(([face, prem]) => ({
    face: Number(face), prem: Number(prem),
  }));
  if (!candidates.length) return { error: 'insufficient products matched' };
  candidates.sort((a, b) =>
    Math.abs(a.prem - monthlyPremium) - Math.abs(b.prem - monthlyPremium)
  );
  const book = candidates[0].face;

  // Male lowball band: 50–75% of book ("down 25–50%")
  const round = n => Math.round(n / 250) * 250;
  let low  = round(book * 0.50);
  let high = round(book * 0.75);

  // Female adjustment: 15–25% more coverage than male at each end
  if (gender === 'female') {
    low  = round(low  * 1.15);
    high = round(high * 1.25);
  }

  // Always keep a visible range
  if (high <= low) high = low + 250;
  return { issueAge, low, high };
}

// ── CASH VALUE PROJECTION COMPONENT ──
// Tried-and-true desktop card. Single corridor for the agent's chosen
// policyYears (no slider — the form's "Years In-Force" input drives it).
const CashValueProjection = ({ monthlyPremium, policyYears, issueAge, C, isDark }) => {
  const fmt = n => '$' + Math.round(n).toLocaleString();
  const d = calculateCVCorridor(monthlyPremium, policyYears, issueAge || 65);
  const card = {background:'#fff',border:'1px solid #eae9e6',borderRadius:12,boxShadow:'0 1px 2px rgba(25,24,23,0.04)'};
  const eyebrow = {fontSize:11,fontWeight:600,letterSpacing:'0.08em',color:'#a09c94',textTransform:'uppercase'};

  return (
    <div style={{display:'flex',flexDirection:'column',gap:16,maxWidth:680,margin:'0 auto',width:'100%'}}>

      {/* Title row (design 3b) */}
      <div style={{display:'flex',alignItems:'center',gap:12,flexWrap:'wrap'}}>
        <div>
          <div style={{fontSize:20,fontWeight:700,color:'#191817',letterSpacing:'-0.01em'}}>Cash value estimator</div>
          <div style={{fontSize:13,color:'#78746e',marginTop:2}}>
            Whole life · ${monthlyPremium.toLocaleString()}/mo · {policyYears} yr{policyYears===1?'':'s'} in-force · Issued age {issueAge||65}
          </div>
        </div>
        <span style={{marginLeft:'auto',background:'#f0efed',borderRadius:99,padding:'3px 10px',fontSize:11.5,color:'#78746e',fontWeight:600,whiteSpace:'nowrap'}}>
          {(issueAge||65) < 60 ? 'Under-60' : (issueAge||65) <= 70 ? 'Age 60–70' : 'Age 71+'} tier
        </span>
      </div>

      {/* Stat cards */}
      <div style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:10}}>
        <div style={{...card,padding:'16px 18px'}}>
          <div style={{...eyebrow,marginBottom:8}}>Annual Premium</div>
          <div style={{fontSize:22,fontWeight:700,color:'#191817',letterSpacing:'-0.01em'}}>{fmt(d.annualPremium)}</div>
        </div>
        <div style={{...card,padding:'16px 18px'}}>
          <div style={{...eyebrow,marginBottom:8}}>Total Premiums Paid</div>
          <div style={{fontSize:22,fontWeight:700,color:'#191817',letterSpacing:'-0.01em'}}>{fmt(d.totalPaid)}</div>
        </div>
      </div>

      {/* Surrender-value card */}
      <div style={{...card,padding:22}}>
        <div style={{...eyebrow,color:'#4a45d1',marginBottom:16}}>
          Estimated Surrender Value · After {policyYears} Year{policyYears===1?'':'s'}
        </div>
        <div style={{display:'flex',alignItems:'baseline',gap:10,flexWrap:'wrap',marginBottom:16}}>
          <span style={{fontSize:32,fontWeight:700,color:'#191817',letterSpacing:'-0.02em'}}>{fmt(d.low)}</span>
          <span style={{fontSize:20,color:'#a09c94',fontWeight:400}}>–</span>
          <span style={{fontSize:32,fontWeight:700,color:'#4a45d1',letterSpacing:'-0.02em'}}>{fmt(d.high)}</span>
        </div>

        {/* Range bar — track #f0efed, gradient fill spanning 18%–92% */}
        <div style={{marginBottom:16}}>
          <div style={{height:8,borderRadius:99,background:'#f0efed',overflow:'hidden',position:'relative'}}>
            <div style={{position:'absolute',left:'18%',top:0,height:'100%',width:'74%',
              background:'linear-gradient(90deg,#c9c6f2,#4a45d1)',borderRadius:99}}/>
          </div>
          <div style={{display:'flex',justifyContent:'space-between',fontSize:11,color:'#a09c94',marginTop:6}}>
            <span>Conservative {fmt(d.low)}</span><span style={{color:'#4740c8',fontWeight:600}}>Target {fmt(d.high)}</span>
          </div>
        </div>

        <div style={{fontSize:11.5,color:'#78746e',lineHeight:1.6,background:'#fbfbfa',border:'1px solid #f0efed',borderRadius:8,padding:'10px 14px'}}>
          <strong style={{color:'#191817',fontWeight:600}}>Agent reference only.</strong> Estimates calibrated to real whole life carrier illustrations. Actual cash value depends on carrier, policy form, dividends, and loan history — pull the in-force illustration before discussing numbers.
        </div>
      </div>

      {/* Early-year warning */}
      {policyYears <= 2 && (
        <div style={{display:'flex',alignItems:'flex-start',gap:10,background:'#fdecec',border:'1px solid #f5c6c2',borderRadius:10,padding:'12px 16px'}}>
          <div>
            <div style={{fontSize:12.5,fontWeight:600,color:'#b42318',marginBottom:3}}>Early policy period</div>
            <div style={{fontSize:12,color:'#78746e',lineHeight:1.6}}>Most whole life policies accumulate little or no cash value during the first 2 years. Front-loaded carrier charges and mortality costs absorb the majority of early premiums. Significant accumulation typically begins in year 3+.</div>
          </div>
        </div>
      )}
    </div>
  );
};

// ── COVERAGE ESTIMATE COMPONENT ──
// Sits below the Cash Value card on the CV tab. Anchors face estimate to
// two reference products (Foresters PlanRight Standard high, Aetna
// Accendo Modified low). Gender comes from the shared Client Info intake.
const CoverageEstimate = ({ monthlyPremium, policyYears, currentAge, gender, C, isDark }) => {
  const fmt = n => '$' + Math.round(n).toLocaleString();
  const r = estimateCoverage({ currentAge, policyYears, monthlyPremium, gender });
  const card = {background:'#fff',border:'1px solid #eae9e6',borderRadius:12,boxShadow:'0 1px 2px rgba(25,24,23,0.04)'};

  return (
    <div style={{display:'flex',flexDirection:'column',gap:14,maxWidth:680,margin:'0 auto',width:'100%',marginTop:16}}>
      {r.error ? (
        <div style={{background:'#fbfbfa',border:'1px solid #eae9e6',borderRadius:10,padding:16,fontSize:13,color:'#78746e',textAlign:'center'}}>
          {r.error === 'insufficient products matched'
            ? 'No reference data at this issue age. Estimator covers issue ages 50–85 (WL/FE buyer range).'
            : r.error === 'policy years exceed current age'
            ? 'Policy years cannot exceed current age.'
            : 'Enter current age, monthly premium, and years in-force.'}
        </div>
      ) : (
        <div style={{...card,padding:22}}>
          <div style={{fontSize:11,fontWeight:600,letterSpacing:'0.08em',color:'#4a45d1',textTransform:'uppercase',marginBottom:14}}>
            Estimated Death Benefit · Issued at age {r.issueAge}
          </div>
          <div style={{display:'flex',alignItems:'baseline',gap:10,flexWrap:'wrap',marginBottom:14}}>
            <span style={{fontSize:32,fontWeight:700,color:'#191817',letterSpacing:'-0.02em'}}>{fmt(r.low)}</span>
            <span style={{fontSize:20,color:'#a09c94'}}>–</span>
            <span style={{fontSize:32,fontWeight:700,color:'#4a45d1',letterSpacing:'-0.02em'}}>{fmt(r.high)}</span>
          </div>
          <div style={{fontSize:12,color:'#78746e',paddingTop:12,borderTop:'1px solid #f0efed',lineHeight:1.6,fontStyle:'italic'}}>
            (If the policy is ROP or Guaranteed Issue, the actual coverage is likely much less than this range.)
          </div>
        </div>
      )}
      <div style={{fontSize:11.5,color:'#78746e',lineHeight:1.7,background:'#fbfbfa',border:'1px solid #f0efed',borderRadius:8,padding:'10px 14px'}}>
        <strong style={{color:'#191817',fontWeight:600}}>Educational estimate.</strong> For conversational guidance only — verify actual coverage with policy documents.
      </div>
    </div>
  );
};

function calcAge(mm,dd,yyyy){if(!mm||!dd||!yyyy||yyyy.length<4)return null;const b=new Date(+yyyy,+mm-1,+dd),t=new Date();let a=t.getFullYear()-b.getFullYear();if(t.getMonth()-b.getMonth()<0||(t.getMonth()===b.getMonth()&&t.getDate()<b.getDate()))a--;return isNaN(a)?null:a;}

const fmt$ = n => (n!=null&&typeof n==='number') ? `$${n.toFixed(2)}` : '—';
const fmtF = n => (n!=null&&typeof n==='number') ? `$${n.toLocaleString()}` : '—';

// ── DESIGN TOKENS ──
// Light "fintech" palette — single theme, per design_handoff_quotemarko_redesign.
// Legacy token names kept so every existing C.* reference maps onto the new
// system: the old gold accent slot now carries the design's indigo.
const C_TOKENS = {
  bg0:'#f8f8f7', bg1:'#f8f8f7', bg2:'#ffffff', bg3:'#ffffff', bg4:'#f0efed',
  bd:'#eae9e6', bd2:'#dedcd7',
  t0:'#191817', t1:'#191817', t2:'#78746e', t3:'#a09c94', t4:'#a09c94',
  blue:'#4a45d1', blueBg:'#eef0fe', blueBd:'#c9c6f2',
  gold:'#4a45d1', goldBg:'#eef0fe', goldBd:'#dbd9f4', goldText:'#4740c8',
  green:'#177452',
  cardBg:'#ffffff', cardShadow:'0 1px 2px rgba(25,24,23,0.04)',
  ctaDisabled:'#dedcd7',
  selActive:'#191817', selActiveTxt:'#ffffff', selInactive:'#ffffff', selInactiveTxt:'#78746e',
  pageBg:'#f8f8f7',
  // New-design extras (hover tints, insets, placeholders)
  ink:'#191817', hairline:'#f0efed', inset:'#fbfbfa', placeholder:'#b5b1a8',
  rowHover:'#f7f7ff', accent:'#4a45d1', accentHover:'#403bbd', accentActive:'#3b37ad',
  accentTint:'#eef0fe', accentTintText:'#4740c8', accentBorder:'#dbd9f4',
};


// Hover tooltip badge for product tier on quote cards
// "Standard" and "Level" are the SAME underwriting tier (full immediate
// benefit, day-1 coverage) — carriers just name it differently. They share
// identical green styling so the list reads as one tier; the badge word
// follows the carrier's own product naming so it matches the plan line.
// Text colors come from the --qm-tier-* CSS vars (set in the theme effect) so
// badges stay readable in BOTH themes — the raw 300-weight hexes measured
// 1.7–2.5:1 on light-mode white cards.
// Tier pills per the redesign handoff: soft tinted, borderless, radius 99px.
const LEVEL_TIER = { bg:'#e8f6ef', bd:'transparent', color:'#177452' };
const BADGE_STYLES = {
  preferred: { bg:'#eef0fe', bd:'transparent', color:'#4740c8', label:'Preferred', tip:'Full immediate benefit from day 1. Client passed simplified underwriting — best available rate class.' },
  level:     { ...LEVEL_TIER, label:'Level',    tip:'Full immediate benefit from day 1. Same tier as "Standard" — this carrier calls it Level.' },
  standard:  { ...LEVEL_TIER, label:'Standard', tip:'Full immediate benefit from day 1. Same tier as "Level" — this carrier calls it Standard.' },
  modified:  { bg:'#fdf3e0', bd:'transparent', color:'#96660f', label:'Modified',  tip:'Graded death benefit — 2 to 3 year waiting period before full coverage applies.' },
  gi:        { bg:'#fdecec', bd:'transparent', color:'#b42318', label:'GI',        tip:'Guaranteed Issue — no health questions. Graded benefit period applies. Approved regardless of health.' },
};
function badgeType(tier, productName) {
  if(tier==='E') return 'gi';
  if(tier==='D') return 'modified';
  const name=(productName||'').toLowerCase();
  if(name.includes('preferred')) return 'preferred';
  if(name.includes('standard'))  return 'standard';
  return 'level';
}
// ═══ CLIENT SHEET ═══
// Digital replacement for the loose paper sheet agents scribble on during a
// call. Plain fields → Save → branded one-page PDF named
// firstname_lastname_carrier_month_year_clientsheet.pdf (carrier = the
// Carrier field, else APPROVED, else In Underwriting, else "pending").
// No encryption by design.
const SHEET_FIELDS = [
  {k:'fullName',       label:'Full Name',           sec:'Client'},
  {k:'address',        label:'Address'},
  {k:'dob',            label:'Date of Birth',       ph:'mm/dd/yyyy'},
  {k:'phone',          label:'Phone Number',        im:'tel'},
  {k:'email',          label:'Email Address',       im:'email'},
  {k:'heightWeight',   label:'Height / Weight',     ph:'5\'10" / 190 lb'},
  {k:'dl',             label:"Driver's License #"},
  {k:'ssn',            label:'Social Security #'},
  {k:'bene1',          label:'Primary Bene',        sec:'Beneficiaries'},
  {k:'bene1Dob',       label:'Primary Bene DOB',    ph:'mm/dd/yyyy'},
  {k:'bene2',          label:'Contingent Bene'},
  {k:'bene2Dob',       label:'Contingent Bene DOB', ph:'mm/dd/yyyy'},
  {k:'bankName',       label:'Bank Name',           sec:'Banking'},
  {k:'routing',        label:'Routing Number',      im:'numeric'},
  {k:'account',        label:'Account Number',      im:'numeric'},
  {k:'carrier',        label:'Carrier',             sec:'Policy Status'},
  {k:'ap',             label:'AP',                  im:'decimal', ph:'annual premium'},
  {k:'effectiveDate',  label:'Effective Date',      ph:'mm/dd/yyyy'},
  {k:'declinedThrough',label:'Declined Through'},
  {k:'inUnderwriting', label:'In Underwriting'},
  {k:'approved',       label:'APPROVED 🙂'},
];
// Second client (husband & wife calls) — revealed by the spouse checkbox.
// Shared things (address, phone, bank, benes) stay on the main fields.
const SPOUSE_FIELDS = [
  {k:'sp_fullName',     label:'Full Name'},
  {k:'sp_dob',          label:'Date of Birth', ph:'mm/dd/yyyy'},
  {k:'sp_heightWeight', label:'Height / Weight'},
  {k:'sp_dl',           label:"Driver's License #"},
  {k:'sp_ssn',          label:'Social Security #'},
];
const SHEET_EMPTY = {...Object.fromEntries([...SHEET_FIELDS,...SPOUSE_FIELDS].map(f=>[f.k,''])), notes:'', spouse:false};

function sheetFileName(sheet){
  const slug = v => (v||'').toLowerCase().replace(/[^a-z0-9]+/g,'_').replace(/^_+|_+$/g,'');
  const parts = (sheet.fullName||'').trim().split(/\s+/);
  const first = slug(parts[0]) || 'client';
  const last  = parts.length>1 ? slug(parts[parts.length-1]) : '';
  const carrier = slug(sheet.carrier || sheet.approved || sheet.inUnderwriting || '') || 'pending';
  const d = new Date();
  const mm = String(d.getMonth()+1).padStart(2,'0');
  return [first,last,carrier,mm,d.getFullYear(),'clientsheet'].filter(Boolean).join('_')+'.pdf';
}

async function buildClientSheetPdf(sheet, agentEmail, agentName){
  const { jsPDF } = await import('jspdf'); // lazy — keeps the main bundle lean
  const doc = new jsPDF({unit:'pt',format:'letter'});
  const W = 612, M = 48, NAVY=[25,24,23], GOLD=[74,69,209], GRAY=[120,116,110], INK=[25,24,23], LINE=[222,220,215];
  // Header band — branded to the signed-in AGENT, not QuoteMark. Last word of
  // the name renders gold (same two-tone treatment as the old wordmark).
  const who = (agentName||'').trim() || (agentEmail||'').split('@')[0] || 'Client Sheet';
  doc.setFillColor(...NAVY); doc.rect(0,0,W,64,'F');
  doc.setFillColor(...GOLD); doc.rect(0,64,W,3,'F');
  doc.setFont('helvetica','bold'); doc.setFontSize(20);
  const words = who.split(/\s+/);
  const head  = words.length>1 ? words.slice(0,-1).join(' ')+' ' : words[0];
  const tail  = words.length>1 ? words[words.length-1] : '';
  doc.setTextColor(255,255,255); doc.text(head, M, 40);
  if(tail){ doc.setTextColor(...GOLD); doc.text(tail, M + doc.getTextWidth(head), 40); }
  doc.setFontSize(9);
  doc.setTextColor(...GOLD); doc.text('CLIENT SHEET', W-M, 32, {align:'right'});
  doc.setTextColor(203,213,225); doc.setFont('helvetica','normal');
  doc.text(new Date().toLocaleDateString('en-US',{month:'long',day:'numeric',year:'numeric'}), W-M, 46, {align:'right'});
  // Field rows
  let y = 92;
  const pageGuard = () => { if (y > 726){ doc.addPage(); y = 60; } };
  const row = (label, value) => {
    pageGuard();
    doc.setFont('helvetica','bold'); doc.setFontSize(7.5); doc.setTextColor(...GRAY);
    doc.text(label.toUpperCase(), M, y);
    doc.setFont('helvetica','normal'); doc.setFontSize(10.5); doc.setTextColor(...INK);
    doc.text(String(value||'').slice(0,90) || ' ', M+150, y);
    doc.setDrawColor(...LINE); doc.setLineWidth(0.6);
    doc.line(M+145, y+4, W-M, y+4);
    y += 23;
  };
  const section = (name) => {
    y += 8; pageGuard();
    doc.setFont('helvetica','bold'); doc.setFontSize(8.5); doc.setTextColor(...GOLD);
    doc.text(name.toUpperCase(), M, y);
    doc.setDrawColor(...GOLD); doc.setLineWidth(0.8);
    doc.line(M + doc.getTextWidth(name.toUpperCase()) + 8, y-2.5, W-M, y-2.5);
    y += 16;
  };
  for (const f of SHEET_FIELDS){
    if (f.k==='bene1' && sheet.spouse){
      section('Client 2 (Spouse)');
      for (const sf of SPOUSE_FIELDS) row(sf.label, sheet[sf.k]);
    }
    if (f.sec) section(f.sec);
    row(f.label.replace(' 🙂',''), sheet[f.k]);
  }
  // Notes block
  section('Notes');
  doc.setFont('helvetica','normal'); doc.setFontSize(10); doc.setTextColor(...INK);
  const lines = doc.splitTextToSize(sheet.notes || '—', W - M*2);
  for (const ln of lines){
    if (y > 742){ doc.addPage(); y = 60; }
    doc.text(ln, M, y); y += 15;
  }
  // Footer
  doc.setFontSize(7); doc.setTextColor(...GRAY);
  doc.text(`Prepared by ${who}${agentEmail ? ' · '+agentEmail : ''} · ${new Date().toLocaleString()}`, M, 768);
  return doc;
}

// Product-tab glyphs — inline SVGs instead of emoji (emoji render differently
// per OS and read informal next to the navy/gold brand). currentColor follows
// the tab's active/inactive text color automatically.
const TabIcon = ({name, size=13}) => {
  const P = {stroke:'currentColor', strokeWidth:1.8, strokeLinecap:'round', strokeLinejoin:'round', fill:'none'};
  return (
    <svg width={size} height={size} viewBox="0 0 24 24" aria-hidden="true" style={{flexShrink:0,marginRight:5,verticalAlign:'-2px'}}>
      {name==='fex' && <path {...P} d="M3 21h18M4 9.5h16M6.5 9.5v8.5m3.5-8.5v8.5m4-8.5v8.5m3.5-8.5v8.5M3.5 9.5 12 3l8.5 6.5"/>}
      {name==='term' && <><circle {...P} cx="12" cy="13.5" r="7.5"/><path {...P} d="M12 9.5v4l2.8 1.8M9.5 2.5h5"/></>}
      {name==='iul' && <path {...P} d="M3.5 3.5v17h17M7 15l3.8-4.8 2.9 2.9L18.5 7"/>}
      {name==='sheet' && <><rect {...P} x="5" y="3" width="14" height="18" rx="2"/><path {...P} d="M9 8.5h6M9 12.5h6M9 16.5h3.5"/></>}
      {name==='cv' && <><circle {...P} cx="12" cy="12" r="8.5"/><path {...P} d="M12 7.2v9.6M14.8 9.3c0-1.2-1.25-2.1-2.8-2.1s-2.8.9-2.8 2.1c0 1.3 1.1 1.8 2.8 2.2 1.7.4 2.8 1 2.8 2.2 0 1.2-1.25 2.1-2.8 2.1s-2.8-.9-2.8-2.1"/></>}
    </svg>
  );
};

const TierBadge = ({tier, productName}) => {
  const [hov,setHov] = React.useState(false);
  const type = badgeType(tier, productName);
  const s = BADGE_STYLES[type];
  return (
    <span style={{position:'relative',display:'inline-flex'}}
      onMouseEnter={()=>setHov(true)} onMouseLeave={()=>setHov(false)}>
      <span style={{display:'inline-flex',alignItems:'center',background:s.bg,color:s.color,borderRadius:99,padding:'3px 10px',fontSize:11.5,fontWeight:600,cursor:'default',whiteSpace:'nowrap'}}>
        {s.label}
      </span>
      {hov&&(
        <span style={{position:'absolute',bottom:'calc(100% + 6px)',left:0,background:'#fff',border:'1px solid #eae9e6',color:'#78746e',fontSize:11,borderRadius:8,padding:'8px 11px',width:210,lineHeight:1.5,zIndex:9999,pointerEvents:'none',boxShadow:'0 4px 16px rgba(25,24,23,0.12)'}}>
          {s.tip}
        </span>
      )}
    </span>
  );
};


// Comp badge — 💰 only for high comp (≥110%); low comp shows nothing
const CompBadge = ({carrierId, tier}) => {
  // ALL hooks must be declared before any early return (React rules)
  const [open,setOpen]   = React.useState(false);
  const [openUp,setOpenUp] = React.useState(true);
  const ref = React.useRef(null);
  const level = getCompBadge(carrierId, tier);
  React.useEffect(()=>{
    if(!open) return;
    const close = ()=>setOpen(false);
    setTimeout(()=>document.addEventListener('click',close),0);
    return ()=>document.removeEventListener('click',close);
  },[open]);
  // Early return AFTER all hooks
  if(!level || level!=='high') return null;
  const emoji = '💰';
  const color = 'var(--qm-tier-green)'; // theme-aware — readable in light mode too
  const bg    = 'rgba(23,116,82,0.12)';
  const bd    = 'rgba(23,116,82,0.35)';
  const tip   = 'Strong commission contract. One of the better-compensated products in this tier.';
  const handleOpen = () => {
    if(ref.current){
      const rect = ref.current.getBoundingClientRect();
      setOpenUp(rect.top > 160);
    }
    setOpen(o=>!o);
  };
  return (
    <span ref={ref} style={{position:'relative',display:'inline-flex'}}
      onMouseEnter={()=>setOpen(true)} onMouseLeave={()=>setOpen(false)}
      onClick={e=>{e.stopPropagation();handleOpen();}}>
      <span style={{display:'inline-flex',alignItems:'center',gap:4,background:bg,border:`1px solid ${bd}`,color,borderRadius:5,padding:'3px 8px',fontSize:9,fontWeight:700,letterSpacing:0.6,cursor:'pointer',whiteSpace:'nowrap'}}>
        {emoji} COMP
      </span>
      {open&&(
        <span style={{
          position:'absolute',
          [openUp?'bottom':'top']:openUp?'calc(100% + 8px)':'calc(100% + 8px)',
          right:0,
          background:'#0F2035',border:'1px solid #2A4F78',color:'#CBD5E1',
          fontSize:11,borderRadius:8,padding:'9px 12px',
          width:210,lineHeight:1.6,zIndex:9999,pointerEvents:'none',
          boxShadow:'0 8px 24px rgba(0,0,0,0.8)',
          whiteSpace:'normal'
        }}>
          {tip}
        </span>
      )}
    </span>
  );
};

// Carrier logo — white badge container so logos render in their natural colors
// Map a term product's display name to an existing FE logo key.
// Term carrier IDs are auto-generated (e.g. 'term_mutual_of_omaha_term_life_express')
// and not in CARRIER_META, so this falls back via the company prefix.
function termLogoKey(name) {
  if (!name) return null;
  const n = name.toLowerCase();
  if (n.startsWith('mutual of omaha')) return 'moo';
  if (n.startsWith('american amicable')) return 'amam';
  if (n.startsWith('americo')) return 'amr';
  if (n.startsWith('foresters')) return 'for';
  if (n.startsWith('instabrain')) return 'fid';        // InstaBrain is Fidelity Life's brand
  if (n.startsWith('royal neighbors')) return 'rn';
  if (n.startsWith('transamerica')) return 'ta';
  if (n.startsWith('uhl')) return 'uhl';
  if (n.startsWith('john hancock')) return 'jhc';
  if (n.startsWith('kansas city')) return 'kcl';
  if (n.startsWith('national life')) return 'nlg';
  if (n.startsWith('protective')) return 'protective';
  if (n.startsWith('sbli')) return 'sbli';
  return null;
}

const CarrierLogo = ({carrierId, name, small=false}) => {
  // Redesign: square white tile (42px desktop / 38px mobile-small), real logo at
  // ~32px contain-fit, 2-letter monogram fallback (600 12px #a09c94).
  const meta = CARRIER_META[carrierId] || CARRIER_META[termLogoKey(name)];
  const [err,setErr] = React.useState(false);
  const size = small ? 38 : 42;
  const monogram = (name || '?')
    .replace(/\s*\(.+?\)\s*/g, '')
    .split(/\s+/)
    .map(w => w[0])
    .filter(Boolean)
    .slice(0, 2)
    .join('')
    .toUpperCase();
  return (
    <div style={{width:size,height:size,borderRadius:9,background:'#fff',border:'1px solid #f0efed',display:'flex',alignItems:'center',justifyContent:'center',overflow:'hidden',flexShrink:0,boxSizing:'border-box'}}>
      {meta?.img && !err
        ? <img src={meta.img} alt={name} onError={()=>setErr(true)} style={{width:size-10,height:size-10,objectFit:'contain'}}/>
        : <span style={{fontSize:12,fontWeight:600,color:'#a09c94',letterSpacing:0.3}}>{monogram}</span>}
    </div>
  );
};

// Underwriting type badge for term result rows.
//   instant  → green pill, ⚡ INSTANT  (eApp instant decision, no exam)
//   medical  → amber pill, 🩺 EXAM    (paramedical + blood/urine required)
//   simplified → nothing rendered (the default — no extra UI noise)
// Issue-type badges per the redesign: rounded rect r5, 600 10.5px, ls .05em.
const UWBadge = ({uwType, productName, tier, small=false}) => {
  if (uwType === 'instant') {
    const tip = productName && TERM_TIER_DEPENDENT.has(productName)
      ? `Instant POS decision at ${tier}. (Pref+/Pref tiers on this product require recent labs and would not be instant.)`
      : 'Instant decision via eApp — no exam, no blood work';
    return (
      <span title={tip} style={{
        display:'inline-flex',alignItems:'center',
        background:'#e8f6ef',color:'#177452',borderRadius:5,padding:small?'2px 6px':'3px 8px',
        fontSize:small?9.5:10.5,fontWeight:600,letterSpacing:'0.05em',whiteSpace:'nowrap',
        fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif",lineHeight:1.2,
      }}>INSTANT</span>
    );
  }
  if (uwType === 'medical') {
    const tip = productName && TERM_TIER_DEPENDENT.has(productName)
      ? `${tier} on this product requires recent labs / paramedical exam. Drop to Std/Std+ for instant POS.`
      : 'Requires paramedical exam + blood work';
    return (
      <span title={tip} style={{
        display:'inline-flex',alignItems:'center',
        background:'#fdf3e0',color:'#96660f',borderRadius:5,padding:small?'2px 6px':'3px 8px',
        fontSize:small?9.5:10.5,fontWeight:600,letterSpacing:'0.05em',whiteSpace:'nowrap',
        fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif",lineHeight:1.2,
      }}>{small?'MED EXAM':'REQUIRES MED EXAM'}</span>
    );
  }
  return null;
};

// eApp button per the redesign: rest = white bg / indigo text / #dbd9f4 border;
// hover fills solid indigo (handled by .qm-eapp CSS). compact = row cell;
// full = full-width card footer.
const EAppBtn = ({carrierId, compact=false, lightMode=false}) => {
  const meta = CARRIER_META[carrierId];
  if(!meta?.eapp) return null;
  const base = {
    padding:'7px 14px',borderRadius:8,fontSize:13,fontWeight:600,
    color:'#4a45d1',border:'1px solid #dbd9f4',background:'#fff',
    boxShadow:'0 1px 2px rgba(25,24,23,.06)',textDecoration:'none',
    whiteSpace:'nowrap',cursor:'pointer',boxSizing:'border-box',
    fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif",
  };
  if(compact) {
    return (
      <a className="qm-eapp" href={meta.eapp} target="_blank" rel="noopener noreferrer"
        onClick={()=>track('e-App Click',{carrier:carrierId,variant:'compact'})}
        style={{...base,display:'inline-flex',alignItems:'center',gap:4,flexShrink:0}}>
        e-App →
      </a>
    );
  }
  return (
    <a className="qm-eapp" href={meta.eapp} target="_blank" rel="noopener noreferrer"
      onClick={()=>track('e-App Click',{carrier:carrierId,variant:'full'})}
      style={{...base,display:'flex',alignItems:'center',justifyContent:'center',gap:4,width:'100%',marginTop:'auto'}}>
      e-App →
    </a>
  );
};

// Small reusable chip/pill
const TierPill = ({tier}) => {
  const t = TIER_INFO[tier];
  return (
    <span style={{display:'inline-flex',alignItems:'center',gap:5,background:t.pill,border:`1px solid ${t.bd}`,color:t.txt,borderRadius:20,padding:'3px 10px',fontSize:11,fontWeight:600,letterSpacing:0.3,whiteSpace:'nowrap'}}>
      <span style={{width:6,height:6,borderRadius:'50%',background:t.dot,flexShrink:0}}/>
      {t.short}
    </span>
  );
};

export default function QuoteMark() {
  const { session, signOut } = useAuth();
  const [portalLoading, setPortalLoading] = useState(false);
  async function openCustomerPortal() {
    if (portalLoading) return;
    track('Manage Subscription');
    setPortalLoading(true);
    try {
      const { data: { session: s } } = await supabase.auth.getSession();
      if (!s) throw new Error('Not signed in');
      const res = await fetch('/.netlify/functions/customer-portal', {
        method: 'POST',
        headers: { 'Authorization': `Bearer ${s.access_token}`, 'Content-Type': 'application/json' },
      });
      const data = await res.json();
      if (data.url) window.location.href = data.url;
      else throw new Error(data.error || 'Could not open billing portal');
    } catch (e) {
      alert(e.message);
      setPortalLoading(false);
    }
  }
  useEffect(()=>{
    // Fonts now load from index.html <head> (branded on first frame). Just clear margins.
    document.documentElement.style.cssText='margin:0;padding:0;overflow-x:hidden';
    document.body.style.cssText='margin:0;padding:0;overflow-x:hidden';
    // Inject hover/active lift CSS for toggle buttons
    const s=document.createElement('style');
    s.id='qm-btn-styles';
    s.textContent=`
      /* Redesign interaction system — everything transitions all .12s ease */
      .qm-btn { transition: all .12s ease !important; }
      .qm-btn:hover:not(:disabled) { background: #f6f5f3; }
      /* Nav product tabs (text pills) */
      .qm-navtab { transition: all .12s ease !important; }
      .qm-navtab:hover { background: #f6f5f3 !important; color: #191817 !important; }
      /* Segmented-control inactive segment */
      .qm-seg { transition: all .12s ease !important; }
      .qm-seg:hover { background: #f6f5f3 !important; }
      /* Quote row — highlight + shift */
      .qm-row { transition: all .12s ease !important; }
      .qm-row:hover { background: #f7f7ff !important; border-color: #c9c6f2 !important; box-shadow: 0 2px 8px rgba(74,69,209,.08) !important; transform: translateX(3px); }
      /* e-App button — rest white/indigo, hover solid indigo */
      .qm-eapp { transition: all .12s ease !important; }
      .qm-eapp:hover { background: #4a45d1 !important; color: #fff !important; border-color: #4a45d1 !important; box-shadow: 0 2px 6px rgba(74,69,209,.35) !important; }
      .qm-eapp:active { background: #3b37ad !important; box-shadow: 0 1px 2px rgba(74,69,209,.3) !important; }
      /* Primary CTA — indigo */
      .qm-cta { transition: all .12s ease !important; }
      .qm-cta:hover:not(:disabled) { background: #403bbd !important; }
      .qm-cta:active:not(:disabled) { background: #3b37ad !important; }
      /* Bordered secondary (Contact) */
      .qm-secondary { transition: all .12s ease !important; }
      .qm-secondary:hover { border-color: #191817 !important; }
      /* Profile pill */
      .qm-pill { transition: all .12s ease !important; }
      .qm-pill:hover { border-color: #dedcd7 !important; background: #fbfbfa !important; }
      @keyframes spin { to { transform: rotate(360deg); } }
      /* Keyboard focus — one indigo ring everywhere (mouse users never see it) */
      *:focus-visible { outline: 2px solid #4a45d1 !important; outline-offset: 2px !important; border-radius: 4px; }
      @keyframes qmRise { from { opacity: 0; transform: translateY(6px); } to { opacity: 1; transform: translateY(0); } }
      .qm-rise { animation: qmRise 0.16s ease-out backwards; }
    `;
    if(!document.getElementById('qm-btn-styles')) document.head.appendChild(s);
  },[]);

  const [age,setAge]           = useState('');
  const [dob,setDob]           = useState({mm:'',dd:'',yyyy:''});
  const [usState,setUsState]   = useState('');
  // ── Session persistence: restore last quote on reload ──
  const _lq = (() => { try { return JSON.parse(sessionStorage.getItem('qm_last_quote')||'{}'); } catch(e) { return {}; } })();
  const [mode,setMode]         = useState(_lq.mode||'face');
  const [faceAmt,setFaceAmt]   = useState(_lq.faceAmt||10000);
  const [gender,setGender]     = useState(_lq.gender||'male');
  const [smoker,setSmoker]     = useState(_lq.smoker||false);
  const [budget,setBudget]     = useState(60);
  const [gsbOn,setGsbOn]       = useState(false);
  const [gsbFace,setGsbFace]   = useState({gold:15000,silver:10000,bronze:5000});
  const [gsbRaw,setGsbRaw]     = useState({gold:'15000',silver:'10000',bronze:'5000'});
  const handleGsbChange = (key,val) => {
    setGsbRaw(p=>({...p,[key]:val}));
    const n = parseInt(val.replace(/[^0-9]/g,''));
    if(!isNaN(n) && n>=1000) setGsbFace(p=>({...p,[key]:n}));
  };
  const handleGsbBlur = (key) => {
    const n = parseInt(gsbRaw[key]);
    const clamped = isNaN(n)||n<1000 ? 1000 : Math.min(n,100000);
    setGsbFace(p=>({...p,[key]:clamped}));
    setGsbRaw(p=>({...p,[key]:String(clamped)}));
  };
  const [selected,setSelected] = useState(['none']);
  const [search,setSearch]     = useState('');
  const [hasQuoted,setHasQuoted]   = useState(false);
  const [iulSpecsOpen,setIulSpecsOpen] = useState(false); // IUL "Carrier Specs" collapsed by default
  const [termCompatOpen,setTermCompatOpen] = useState(false); // Term "Compatible carriers" banner expanded list
  // ── CTA click feedback — pulse animation + haptic on mobile.
  // Wrap your onClick handler in fireCta(e, () => { ...real handler }).
  const fireCta = (e, fn) => {
    const el = e.currentTarget;
    if (el) {
      el.classList.remove('qm-cta-pulse');
      void el.offsetWidth; // restart CSS animation
      el.classList.add('qm-cta-pulse');
      setTimeout(()=>el && el.classList.remove('qm-cta-pulse'), 460);
    }
    if (typeof navigator !== 'undefined' && navigator.vibrate) navigator.vibrate(12);
    fn && fn();
  };
  const [tierOvr,setTierOvr]   = useState(null);
  const [uwOpen,setUwOpen]     = useState(false);
  const [carrierPanel,setCarrierPanel] = useState(false);
  const [carrierPanelTab,setCarrierPanelTab] = useState('fex'); // 'fex'|'term'|'iul'
  const [carriers,setCarriers] = useState(CARRIERS.map(c=>({...c})));
  const [reqForm,setReqForm]   = useState({name:'',state:'',notes:''});
  const mobileResultsRef = React.useRef(null);
  const [hovCard, setHovCard] = useState(null);
  const dobDdRef  = React.useRef(null);
  const dobYyyyRef = React.useRef(null);
  const dobDdRefD  = React.useRef(null);
  const dobYyyyRefD = React.useRef(null);
  const termDobDdRef   = React.useRef(null);
  const termDobYyyyRef = React.useRef(null);
  const termDobDdRefM  = React.useRef(null);
  const termDobYyyyRefM= React.useRef(null);
  const [reqSent,setReqSent]   = useState(false);

  // ── TERM MODE STATE ──
  const [quoteMode,setQuoteMode] = useState('fe'); // 'fe' | 'term' | 'iul' | 'cv'
  const [cvMonthly,setCvMonthly]     = useState('');
  const [cvPolicyYrs,setCvPolicyYrs] = useState('');

  // ── CLIENT SHEET STATE ──
  // Persisted to localStorage so a mid-call refresh doesn't lose the sheet.
  const [sheet,setSheet] = useState(()=>{ try{ return {...SHEET_EMPTY, ...JSON.parse(localStorage.getItem('qm_client_sheet')||'{}')}; }catch(e){ return {...SHEET_EMPTY}; } });
  // Sensitive fields (SSN, account) render masked by default; per-field Show toggles.
  const [showSensitive,setShowSensitive] = useState({});
  useEffect(()=>{ try{ localStorage.setItem('qm_client_sheet', JSON.stringify(sheet)); }catch(e){} },[sheet]);
  const [sheetSaving,setSheetSaving] = useState(false);
  const [sheetMsg,setSheetMsg] = useState(null); // {t:'ok'|'err', m}
  const saveClientSheet = async () => {
    if(!sheet.fullName.trim()){ setSheetMsg({t:'err',m:'Full Name is required — it names the file.'}); return; }
    setSheetSaving(true); setSheetMsg(null);
    try{
      const doc = await buildClientSheetPdf(sheet, session?.user?.email, profileName);
      const fname = sheetFileName(sheet);
      let saved = false;
      // Chrome/Edge: save dialog pre-pointed at Desktop with the filename filled in
      if (window.showSaveFilePicker) {
        try {
          const h = await window.showSaveFilePicker({suggestedName:fname, startIn:'desktop', types:[{description:'PDF document', accept:{'application/pdf':['.pdf']}}]});
          const w = await h.createWritable(); await w.write(doc.output('blob')); await w.close();
          saved = true;
        } catch(e){
          if(e && e.name==='AbortError'){ setSheetSaving(false); return; } // user cancelled — not an error
        }
      }
      if(!saved) doc.save(fname); // Safari/mobile: normal download
      track('Client Sheet Saved',{});
      setSheetMsg({t:'ok',m:`Saved ${fname}`});
    }catch(e){ console.error('[QuoteMark] client sheet PDF failed:',e); setSheetMsg({t:'err',m:'PDF failed to generate — try again.'}); }
    setSheetSaving(false);
  };
  const [termLength,setTermLength] = useState('10'); // '10' | '15' | '20' | '30'
  const [termFace,setTermFace] = useState(100000); // $25k-$1M, 5k step
  const [termMode,setTermMode] = useState('face'); // 'face' | 'budget'
  const [issueFilter,setIssueFilter] = useState('ALL'); // 'ALL' | 'INSTANT' | 'EXAM' — client-side filter of loaded term quotes
  const [termBudget,setTermBudget] = useState(100); // monthly budget in $
  const [termHealth,setTermHealth] = useState('p'); // 'pp'=Preferred Plus, 'p'=Preferred, 'sp'=Standard Plus, 's'=Standard
  const [termHealthManual,setTermHealthManual] = useState(false); // true once user clicks a pill — disables auto-recommend
  const [termAge,setTermAge] = useState(''); // term allows 18-75
  const [termDob,setTermDob] = useState({mm:'',dd:'',yyyy:''});
  // Term-specific underwriting inputs
  const [termHtFt,setTermHtFt]   = useState(''); // e.g. '5'
  const [termHtIn,setTermHtIn]   = useState(''); // e.g. '10'
  const [termWtLb,setTermWtLb]   = useState(''); // weight in lbs
  const [termFamHx,setTermFamHx] = useState(false); // family hx of cancer/CVD before 60

  // IUL quoter inputs — supports both directions:
  //   'face'    = input premium → output face per carrier (default)
  //   'premium' = input face → output required premium per carrier
  const [iulMode,setIulMode]       = useState('premium'); // 'premium' = enter Face amount (default, matches FE); 'face' = enter Monthly budget
  const [iulPremium,setIulPremium] = useState(200);    // $/month input
  const [iulFace,setIulFace]       = useState(150000); // target face input

  // ── PROFILE STATE ──
  const [mobileTab2, setMobileTab2] = useState(null); // null | 'profile'
  const [showProfileModal, setShowProfileModal] = useState(false);
  const [profileName, setProfileName] = useState('');
  const [profileSaving, setProfileSaving] = useState(false);
  const [profileSaved, setProfileSaved] = useState(false);
  const [pwForm, setPwForm] = useState({cur:'',next:'',confirm:''});
  const [pwMsg, setPwMsg] = useState(null);
  const [pwChanging, setPwChanging] = useState(false);
  const [carriersSaved, setCarriersSaved] = useState(false);
  // Single light theme (redesign): isDark pinned false so legacy ternaries
  // resolve to their light branch until each screen's rebuild removes them.
  const isDark = false;
  const C = C_TOKENS;
  useEffect(()=>{
    document.documentElement.style.background = C.pageBg;
    document.body.style.background = C.pageBg;
    document.body.style.fontFamily = "'Instrument Sans','Helvetica Neue',Arial,sans-serif";
    // Handoff: tabular numerals on every numeric value — inherit from body.
    document.body.style.fontVariantNumeric = 'tabular-nums';
    const rs = document.documentElement.style;
    rs.setProperty('--qm-tier-purple', '#4740c8');
    rs.setProperty('--qm-tier-green',  '#177452');
    rs.setProperty('--qm-tier-amber',  '#96660f');
    rs.setProperty('--qm-tier-red',    '#b42318');
  },[]);

  // ── CONTACT MODAL STATE ──
  const [showContact, setShowContact] = useState(false);
  const [contactType, setContactType] = useState('carrier');
  const [contactForm, setContactForm] = useState({name:'',email:'',company:'',message:''});
  const [contactSending, setContactSending] = useState(false);
  const [contactSent, setContactSent] = useState(false);
  const [contactErr, setContactErr] = useState(null);


  const sendContact = async () => {
    if(!contactForm.name||!contactForm.email||!contactForm.message) return;
    setContactSending(true); setContactErr(null);
    try {
      const res = await fetch('/.netlify/functions/contact',{
        method:'POST',
        headers:{'Content-Type':'application/json'},
        body:JSON.stringify({type:contactType,...contactForm})
      });
      if(res.ok){setContactSent(true);}
      else setContactErr('Failed to send — please try again.');
    } catch{ setContactErr('Network error — please try again.'); }
    setContactSending(false);
  };

  // Load profile data + carrier prefs from Supabase on mount
  useEffect(()=>{
    if(!session) return;
    supabase.from('profiles').select('display_name,carrier_prefs').eq('id',session.user.id).single()
      .then(({data})=>{
        if(!data) return;
        if(data.display_name) setProfileName(data.display_name);
        if(data.carrier_prefs && Array.isArray(data.carrier_prefs)){
          const savedIds = data.carrier_prefs;
          // Use module-scope FORCE_DISABLED — Supabase prefs cannot re-enable these
          setCarriers(prev=>prev.map(c=>({
            ...c,
            enabled: FORCE_DISABLED.has(c.id) ? false :
              (c.termOnly ? !savedIds.includes('__disabled_'+c.id) : savedIds.includes(c.id))
          })));
        }
      });
  },[session]);

  const saveCarrierPrefs = async () => {
    if(!session) return;
    setCarriersSaved(false);
    const enabledIds = carriers.filter(c=>c.enabled).map(c=>c.id);
    await supabase.from('profiles').update({carrier_prefs:enabledIds}).eq('id',session.user.id);
    setCarriersSaved(true);
    setTimeout(()=>setCarriersSaved(false),2500);
  };

  const saveProfile = async () => {
    if(!session) return;
    setProfileSaving(true);
    await supabase.from('profiles').update({display_name:profileName.trim()||null}).eq('id',session.user.id);
    setProfileSaving(false);
    setProfileSaved(true);
    setTimeout(()=>setProfileSaved(false),2500);
  };



  const changePassword = async () => {
    if(pwForm.next !== pwForm.confirm){setPwMsg({err:true,msg:'Passwords do not match'});return;}
    if(pwForm.next.length < 8){setPwMsg({err:true,msg:'Password must be at least 8 characters'});return;}
    setPwChanging(true);
    const {error} = await supabase.auth.updateUser({password:pwForm.next});
    setPwChanging(false);
    if(error) setPwMsg({err:true,msg:error.message});
    else{setPwMsg({err:false,msg:'Password updated!'});setPwForm({cur:'',next:'',confirm:''});}
    setTimeout(()=>setPwMsg(null),3500);
  };

  // ── MOBILE DETECTION ──
  const [isMobile, setIsMobile] = useState(() => window.innerWidth < 768 || window.screen.width < 768);
  const [mobileTab, setMobileTab] = useState('quote'); // 'quote' | 'results'
  useEffect(() => {
    const handle = () => setIsMobile(window.innerWidth < 768);
    window.addEventListener('resize', handle);
    return () => window.removeEventListener('resize', handle);
  }, []);

  useEffect(()=>{ const a=calcAge(dob.mm,dob.dd,dob.yyyy);if(a!==null&&a>=1&&a<=89)setAge(String(a)); },[dob]);

  const autoTier = useMemo(()=>getAutoTier(selected),[selected]);
  const uwTier   = tierOvr||autoTier;

  // ── TERM UW RECOMMENDATION ──
  // Recompute the recommended term class whenever inputs change. If the user
  // hasn't manually clicked a pill, auto-sync termHealth to the recommendation.
  const termBMI = useMemo(()=>{
    const inches = (parseInt(termHtFt)||0)*12 + (parseInt(termHtIn)||0);
    return calcBMI(inches, +termWtLb);
  },[termHtFt,termHtIn,termWtLb]);
  const termRec = useMemo(()=>{
    const a = parseInt(age, 10);
    return recommendTermClass(selected, termBMI, isNaN(a)?null:a, {familyHx: termFamHx, smoker});
  },[selected,termBMI,age,termFamHx,smoker]);
  useEffect(()=>{
    if (termHealthManual) return;
    if (termRec.recommended === 'decline') return; // keep user's class; show warning instead
    setTermHealth(termRec.recommended);
  },[termRec.recommended,termHealthManual]);
  const ageNum   = parseInt(age);
  const ageOK    = age && ageNum>=1 && ageNum<=89;
  // CV results render inline as the agent types (no Get Quotes step) — scroll
  // them into view the moment the inputs become valid so they aren't missed
  // below the fold on mobile.
  const cvResultRef = useRef(null);
  const cvValid = ageOK && Number(cvMonthly) > 0 && Number(cvPolicyYrs) > 0;
  useEffect(()=>{
    if(quoteMode==='cv' && cvValid && cvResultRef.current){
      cvResultRef.current.scrollIntoView({behavior:'smooth', block:'nearest'});
    }
  },[cvValid, quoteMode]);
  // ── Age-bracket helpers — drive intake UX (FE / WL spans 1-89 now) ──
  const isJuvenile = ageOK && ageNum < 18;        // 1-17 — no medical, juvenile WL
  const isYoungAdult = ageOK && ageNum >= 18 && ageNum < 50; // 18-49 — adult WL
  const isSenior = ageOK && ageNum >= 50;         // 50-89 — traditional FE
  // Auto-snap face slider to age-appropriate default when bracket changes
  useEffect(()=>{
    if (!ageOK) return;
    if (isJuvenile && faceAmt > 50000) setFaceAmt(25000);   // Juvenile typical: $25k
    else if (isYoungAdult && faceAmt < 5000) setFaceAmt(20000); // Young adult: $20k
    // Seniors keep whatever they had (existing FE default $10k)
  // eslint-disable-next-line react-hooks/exhaustive-deps
  },[isJuvenile, isYoungAdult]);
  // Force smoker=false for juveniles (tobacco UI hidden, kids don't smoke)
  useEffect(()=>{ if (isJuvenile && smoker) setSmoker(false); },[isJuvenile]); // eslint-disable-line

  const toggleOvr = (t) => {
    setTierOvr(p => {
      const next = p === t ? null : t;
      if (next !== null) track('Tier Override', { to: next });
      return next;
    });
  };
  // Persist last quote params to sessionStorage whenever they change
  useEffect(() => {
    try { sessionStorage.setItem('qm_last_quote', JSON.stringify({mode,faceAmt,gender,smoker})); } catch(e){}
  }, [mode, faceAmt, gender, smoker]);
  const toggleCond = (id) => {
    ; setTierOvr(null);
    if(id==='none'){setSelected(['none']);return;}
    setSelected(p=>{const w=p.filter(c=>c!=='none');if(w.includes(id)){const n=w.filter(c=>c!==id);return n.length?n:['none'];}return [...w,id];});
  };
  const toggleCarrier = (id) => setCarriers(p => {
    const target = p.find(c => c.id === id);
    if (!target) return p;
    const newEnabled = !target.enabled;
    return p.map(c => {
      if (c.id === id) return {...c, enabled: newEnabled};
      // Also sync any term carrier linked to this FEX carrier
      if (c.linkedId === id) return {...c, enabled: newEnabled};
      return c;
    });
  });

  const q = search.toLowerCase();
  const filteredConds = React.useMemo(()=>{
    if(!q) return CONDITIONS;
    const results = [];
    const seen = new Set();
    // 1. Label starts with query (highest priority)
    CONDITIONS.forEach(c=>{ if(c.label.toLowerCase().replace('⚠ ','').startsWith(q)){results.push({...c,_score:0});seen.add(c.id);} });
    // 2. Any word in label starts with query
    CONDITIONS.forEach(c=>{ if(seen.has(c.id))return; if(c.label.toLowerCase().split(/\s+/).some(w=>w.startsWith(q))){results.push({...c,_score:1});seen.add(c.id);} });
    // 3. Label contains query anywhere
    CONDITIONS.forEach(c=>{ if(seen.has(c.id))return; if(c.label.toLowerCase().includes(q)){results.push({...c,_score:2});seen.add(c.id);} });
    // 4. Meds field contains query
    CONDITIONS.forEach(c=>{ if(seen.has(c.id))return; if(c.meds&&c.meds.toLowerCase().includes(q)){results.push({...c,_score:3});seen.add(c.id);} });
    // 5. Category contains query
    CONDITIONS.forEach(c=>{ if(seen.has(c.id))return; if(c.cat.toLowerCase().includes(q)){results.push({...c,_score:4});seen.add(c.id);} });
    return results.sort((a,b)=>a._score-b._score);
  },[q]);
  const cats = useMemo(()=>{const m={};filteredConds.forEach(c=>{if(!m[c.cat])m[c.cat]=[];m[c.cat].push(c);});return m;},[filteredConds]);
  const medHints = useMemo(()=>search.length<3?[]:filteredConds.filter(c=>c.meds&&c.meds.toLowerCase().includes(q)&&!c.label.toLowerCase().includes(q)),[search,filteredConds]);

  function buildResult(carr, a, male, face) {
    if(!carr.enabled) return {...carr,prem:null,face:null,productName:null,reason:'Carrier disabled'};
    const maxFace=AGE_FACE_BANDS[carr.id]?getFaceCap(carr.id,a):FACE_CAPS[carr.id],stateRule=STATE_RULES[carr.id];
    // State check: new data-driven check (fexStateOK) + legacy STATE_RULES fallback
    if(usState) {
      if(carr.stateCheck && !carr.stateCheck(usState)) return{...carr,prem:null,face:null,productName:null,reason:`Not available in ${STATE_NAMES[usState]||usState}`};
      else if(!carr.stateCheck && stateRule?.excludeStates?.includes(usState)) return{...carr,prem:null,face:null,productName:null,reason:`Not licensed in ${STATE_NAMES[usState]||usState}`};
    }
    // GI fallback: Modified is strictly better than GI for the client.
    // When the agent picks tier E, prefer this carrier's Modified product if
    // it exists and the client's age fits — only show GI when it's the only
    // option this carrier offers.
    let effTier = uwTier;
    if (uwTier === 'E' && carr.product.D && a <= getAgeMax(carr.id, 'D')) {
      effTier = 'D';
    }
    const maxAge = getAgeMax(carr.id, effTier);
    if(a>maxAge) return{...carr,prem:null,face:null,productName:null,reason:`Maximum age is ${maxAge}`};
    const pName=carr.product[effTier];
    if(!pName){const reason=effTier==='E'?'No GI product offered':effTier==='D'?'Level plans only':'Not available for this tier';return{...carr,face:null,prem:null,productName:null,reason};}
    // Check age-dependent face cap
    if(maxFace && face > maxFace) return{...carr,face:null,prem:null,productName:pName,activeTier:effTier,reason:`Max coverage ${fmtF(maxFace)}`};
    let prem=null, effFace=face, pNameEff=pName, subOverride=null, roundedTo=null;
    try{
      const res=carr.fn(a,male,smoker,effTier,face);
      if(res && typeof res==='object') {
        // factorCalc maxFace exceeded: {prem:null, face:null, maxFace:N}
        if(res.maxFace && res.prem==null) return{...carr,face:null,prem:null,productName:pNameEff,activeTier:effTier,reason:`Max coverage ${fmtF(res.maxFace)}`};
        prem=res.prem; effFace=res.face; // fexLookup/factorCalc returns {prem,face}
        if(res.sub) subOverride=res.sub; // age-dependent product variant (e.g. MOO Children's WL)
        if(res.productName) pNameEff=res.productName;
        if(res.roundedTo) roundedTo=res.roundedTo; // carrier's face increment (factorCalc only)
      } else {
        prem=res; // legacy csvLookup carriers return a plain number
      }
    }catch(e){console.error(`[QuoteMark] ${carr.id} fn() threw:`,e.message,{age:a,tier:effTier,face});prem=null;}
    // If Modified fallback didn't pan out, retry as GI (the user picked tier E
    // originally — show GI when Modified isn't actually quotable for this profile)
    if(prem==null && effTier==='D' && uwTier==='E' && carr.product.E){
      effTier='E';
      pNameEff=carr.product.E;
      const eMaxAge=getAgeMax(carr.id,'E');
      if(a>eMaxAge) return{...carr,prem:null,face:null,productName:null,reason:`Maximum age is ${eMaxAge}`};
      effFace=face;
      try{
        const res=carr.fn(a,male,smoker,'E',face);
        if(res && typeof res==='object'){
          if(res.maxFace && res.prem==null) return{...carr,face:null,prem:null,productName:pNameEff,activeTier:'E',reason:`Max coverage ${fmtF(res.maxFace)}`};
          prem=res.prem; effFace=res.face;
        } else { prem=res; }
      }catch(e){console.error(`[QuoteMark] ${carr.id} GI retry threw:`,e.message);prem=null;}
    }
    let reason;
    if(prem==null){if(effTier==='D'&&a>75)reason='Modified not available after 75';else if(effTier==='E'&&a>80)reason='GI not available after 80';else reason='Not available for this profile';}
    const isCapped = prem!=null && effFace !== face;
    // Banded carriers: show with nearest band if carrier CAN write the requested face
    // Only hide if the carrier's max face for this age is less than requested
    if(isCapped){
      // maxFace already checked above — if we got here, carrier accepts this face
      // Show with the nearest available band premium
      return{...carr,...(subOverride?{sub:subOverride}:{}),face:effFace,prem,productName:pNameEff,activeTier:effTier,capped:true,roundedTo,reason:null};
    }
    return{...carr,...(subOverride?{sub:subOverride}:{}),face:prem!=null?effFace:null,prem,productName:pNameEff,activeTier:effTier,roundedTo,reason};
  }



  const activeCarriers = useMemo(()=>{
    return carriers.filter(c => {
      // FEX mode: exclude term-only carriers
      if (quoteMode === 'fe' && c.termOnly) return false;
      // Term mode: only term-only carriers
      if (quoteMode === 'term' && !c.termOnly) return false;
      // For term carriers with a linked FEX carrier, use the FEX carrier's enabled state
      if (c.termOnly && c.linkedId) {
        const linked = carriers.find(x => x.id === c.linkedId);
        return linked ? linked.enabled : c.enabled;
      }
      return c.enabled;
    });
  },[carriers, quoteMode]);

  // Defer the slider-driven inputs so dragging a slider doesn't fire the full
  // carrier scan (in budget mode, ~85 carriers × an 80-step binary search) on
  // every pixel tick. The input stays visually live; the expensive recompute
  // runs against the settled value. React 18 useDeferredValue.
  const dFaceAmt   = useDeferredValue(faceAmt);
  const dBudget    = useDeferredValue(budget);
  const dGsbFace   = useDeferredValue(gsbFace);
  const dTermFace  = useDeferredValue(termFace);
  const dTermBudget= useDeferredValue(termBudget);
  const dIulFace   = useDeferredValue(iulFace);
  const dIulPremium= useDeferredValue(iulPremium);

  const results = useMemo(()=>{
    if(!hasQuoted||!ageOK) return null;
    const a=ageNum,male=gender==='male';
    if(gsbOn){
      return activeCarriers.map(carr=>{
        const tiers={};let anyPrem=false;
        GSB.forEach(g=>{const r=buildResult(carr,a,male,dGsbFace[g.key]);tiers[g.key]=r;if(r.prem!=null)anyPrem=true;});
        return{...carr,tiers,anyPrem};
      }).sort((a,b)=>{const ap=a.tiers?.gold?.prem??Infinity,bp=b.tiers?.gold?.prem??Infinity;if(ap!==Infinity&&bp!==Infinity)return ap-bp;if(ap!==Infinity)return-1;if(bp!==Infinity)return 1;return 0;});
    }
    let list;
    if(mode==='face'){
      list=activeCarriers.map(carr=>buildResult(carr,a,male,dFaceAmt));
    } else {
      list=activeCarriers.map(carr=>{
        const solved=solveForFace(dBudget,a,male,smoker,uwTier,carr.fn);
        if(!solved)return{...carr,face:null,prem:null,productName:carr.product[uwTier],reason:'Budget too low'};
        // Clamp to the carrier's age-aware face cap so a big budget doesn't
        // solve past what the carrier writes and get bounced by buildResult
        const cap=AGE_FACE_BANDS[carr.id]?getFaceCap(carr.id,a):FACE_CAPS[carr.id];
        return buildResult(carr,a,male,cap?Math.min(solved,cap):solved);
      });
    }
    return list.sort((a,b)=>{
      if(a.prem==null&&b.prem==null)return 0;
      if(a.prem==null)return 1;
      if(b.prem==null)return-1;
      if(a.capped!==b.capped)return a.capped?1:-1;
      // Budget mode: sort by face DESC (most coverage for the budget) — matches ITK.
      // Face mode: sort by premium ASC (cheapest first).
      if (mode==='budget') {
        if (a.face !== b.face) return (b.face||0) - (a.face||0);
        return a.prem - b.prem; // tiebreaker
      }
      return a.prem - b.prem;
    });
  },[hasQuoted,age,gender,smoker,uwTier,mode,dFaceAmt,dBudget,gsbOn,dGsbFace,activeCarriers,usState]);

  // ── TERM LIFE RESULTS ──
  // Term mode reuses the shared age/gender/smoker/usState fields. Term length
  // and face come from termLength / termFace state.
  const termResults = useMemo(()=>{
    if(quoteMode!=='term'||!ageOK) return null;
    const male = gender==='male';
    const a = ageNum;

    // Budget-mode: per-carrier binary search for max face fitting the monthly budget.
    // Step is $1k for finer matching — term face anchors are usually $25k+ but
    // the per-$1000 rate scaling means a tighter step gets the premium much
    // closer to the agent's budget.
    const solveTermFace = (carrFn) => {
      let lo=25000, hi=1000000, best=0;
      for(let i=0;i<60;i++){
        const mid=Math.round((lo+hi)/2/1000)*1000;
        if(lo>hi)break;
        let p=null;
        try { p = carrFn(a, male, smoker, termHealth, mid, termLength); } catch(e) {}
        if (p!=null && p<=dTermBudget+0.001) { best=mid; lo=mid+1000; } else { hi=mid-1000; }
      }
      return best>0 ? best : null;
    };

    // Build one result per term carrier (each carrier=one product in TERM_RATES)
    const results = activeCarriers.filter(c => c.termOnly).map(carr => {
      let prem = null;
      let effFace = dTermFace;
      let tierUsed = null;
      try {
        const productData = TERM_RATES?.[carr.product]?.[String(termLength)];
        tierUsed = resolveTier(productData, termHealth);
        if (termMode === 'budget') {
          effFace = solveTermFace(carr.fn);
          if (effFace) prem = carr.fn(a, male, smoker, termHealth, effFace, termLength);
        } else {
          prem = carr.fn(a, male, smoker, termHealth, dTermFace, termLength);
        }
      } catch (e) {
        console.error(`[QuoteMark] term ${carr.id} threw:`, e);
      }

      if (prem == null) {
        return null;
      }
      return {
        ...carr,
        available: true,
        face: effFace,
        prem,
        termLen: termLength,
        tierUsed: tierUsed || 'Standard',
        uwType: getUwType(carr.product, tierUsed || 'Standard'),
      };
    }).filter(r => r != null);

    // Sort: budget-mode → largest face first; face-mode → cheapest premium first
    return results.sort((a, b) => termMode === 'budget' ? b.face - a.face : a.prem - b.prem);
  }, [quoteMode, ageOK, ageNum, termLength, dTermFace, termMode, dTermBudget, gender, smoker, activeCarriers, termHealth]);

  // Compatibility map — which term carriers can/can't write at the entered age.
  // Helps explain why a 65yo client only sees 19 of 28 carriers without making
  // the agent guess. Uses the issue-age range we precomputed on TERM_CARRIERS.
  const termCompat = useMemo(()=>{
    if (quoteMode !== 'term' || !ageOK) return null;
    const a = ageNum;
    const writers = [], excluded = [];
    for (const c of TERM_CARRIERS) {
      const min = c.minAge ?? 0;
      const max = c.maxAge ?? 100;
      if (a >= min && a <= max) writers.push(c);
      else excluded.push({name:c.name, sub:c.sub, minAge:c.minAge, maxAge:c.maxAge, reason: a > max ? `Max age ${c.maxAge}` : `Min age ${c.minAge}`});
    }
    return { writers: writers.length, total: TERM_CARRIERS.length, excluded };
  }, [quoteMode, ageOK, ageNum]);

  // ── IUL QUOTE RESULTS ──
  // Two modes:
  //   'face'    = input is iulPremium, output is face per carrier
  //   'premium' = input is iulFace, output is required premium per carrier
  const iulQuoteResults = useMemo(()=>{
    if (quoteMode !== 'iul' || !ageOK || !hasQuoted) return null;
    const male = gender === 'male';
    const a = ageNum;
    const results = IUL_QUOTE_CARRIERS.map(c => {
      // State-gate: the agent picked a state this carrier isn't sold in (e.g.
      // AmAm Intelligent Choice IUL is not available in MI). Keep it in the list
      // but flag it unavailable so it renders grayed-out (not silently dropped).
      if (c.availableStates && usState && !c.availableStates.has(usState))
        return { ...c, unavailable: true, reason: `Not available in ${STATE_NAMES[usState] || usState}` };
      // Issue-age gate: outside the carrier's real min/max age → not offered.
      if (!iulAgeOK(c.product, a)) return null;
      // Carrier's real max face for this age (age-banded), independent of data.
      const bandMax = iulMaxFace(c.product, a);
      if (iulMode === 'face') {
        let face = c.fn(a, male, smoker, dIulPremium);
        if (face == null) return null;
        // Clamp to what the carrier will actually issue at this age.
        let capped = false;
        if (bandMax !== Infinity && face > bandMax) { face = bandMax; capped = true; }
        return { ...c, face, premium: dIulPremium, capped, maxFace: capped ? bandMax : undefined };
      } else {
        // Don't solve above the carrier's band max — cap the target first.
        const bandCapped = bandMax !== Infinity && dIulFace > bandMax;
        const target = bandCapped ? bandMax : dIulFace;
        const res = c.fnPremium(a, male, smoker, target);
        if (res == null) return null;
        // res is {premium, capped?, maxFace?, floored?, minFace?}:
        //  - capped: requested face exceeds what the DATA reaches at this age.
        //  - floored: requested face is below the carrier's minimum.
        // bandCapped (carrier issue limit) takes precedence for the shown face.
        const effCapped = bandCapped || res.capped;
        const effMax    = bandCapped ? bandMax : res.maxFace;
        const effFace   = bandCapped ? bandMax : res.capped ? res.maxFace : res.floored ? res.minFace : dIulFace;
        return { ...c, face: effFace, premium: res.premium, capped: effCapped, floored: res.floored, maxFace: effMax, targetFace: dIulFace };
      }
    }).filter(r => r != null);
    // Rank quotable carriers first, then can't-issue-this-face (red: below min OR
    // above the carrier's max — premium mode only), then state-blocked (gray) at
    // the bottom. Within a rank: face-mode by face desc, else premium asc.
    const rank = r => r.unavailable ? 2 : (iulMode === 'premium' && (r.floored || r.capped)) ? 1 : 0;
    return results.sort((a, b) => {
      if (rank(a) !== rank(b)) return rank(a) - rank(b);
      return iulMode === 'face' ? (b.face || 0) - (a.face || 0) : (a.premium ?? Infinity) - (b.premium ?? Infinity);
    });
  }, [quoteMode, ageOK, ageNum, gender, smoker, dIulPremium, dIulFace, iulMode, hasQuoted, usState]);

  // ── INPUT STYLES (redesign: 36px fields, 7px radius, joined segmented toggles) ──
  const inp = {background:'#fff',border:'1px solid #dedcd7',color:'#191817',borderRadius:7,padding:'9px 10px',fontSize:13,width:'100%',boxSizing:'border-box',outline:'none',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif"};
  // Joined segmented control: wrap segments in segWrap; selected = solid ink.
  const segWrap = {display:'flex',gap:0,border:'1px solid #dedcd7',borderRadius:7,overflow:'hidden'};
  const togBtn = (active) => ({flex:1,padding:0,height:34,minHeight:34,border:'none',cursor:'pointer',fontSize:12.5,display:'flex',alignItems:'center',justifyContent:'center',fontWeight:active?600:400,background:active?'#191817':'#fff',color:active?'#fff':'#78746e',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif"});
  const sec = {background:'#fff',border:'1px solid #eae9e6',borderRadius:10,padding:16,boxShadow:'0 1px 2px rgba(25,24,23,0.04)'};
  const lbl = {fontSize:11,fontWeight:600,letterSpacing:'0.08em',color:'#a09c94',textTransform:'uppercase',marginBottom:12};
  // Mobile-tier styles (also used by ClientInfoBlock in desktop calls when sized for variant)
  // fontSize 16 minimum — below 16px iOS Safari auto-zooms on input focus (zoom is enabled now)
  const mInp = {background:'#fff',border:'1px solid #dedcd7',color:'#191817',borderRadius:8,padding:'12px 14px',fontSize:16,width:'100%',boxSizing:'border-box',outline:'none',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif",WebkitAppearance:'none'};
  const mTogBtn = (active) => ({flex:1,padding:0,minHeight:44,border:'none',cursor:'pointer',fontSize:14,display:'flex',alignItems:'center',justifyContent:'center',fontWeight:active?600:400,background:active?'#191817':'#fff',color:active?'#fff':'#78746e',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif"});

  // ── DOB → age auto-sync (when all 3 fields valid, compute age and set it) ──
  // ── REUSABLE CLIENT INFO BLOCK ──
  // Used by FE / Term / IUL on both mobile and desktop.
  // Reads/writes shared state: dob, age, gender, smoker, usState.
  // Defined as a render *function* (not a component) so React doesn't
  // remount it on each App render — that would steal focus from DOB inputs.
  // ── CLIENT SHEET FORM ── one plain column; shared by mobile + desktop.
  const renderClientSheet = (isM=true) => {
    const inpS = isM ? mInp : {...inp, fontSize:13, padding:'8px 10px'};
    const SENSITIVE = new Set(['ssn','account','sp_ssn']);
    const F = ({k, label, ph, im, span}) => (
      <div style={span===2?{gridColumn:'1 / -1'}:undefined}>
        <div style={{fontSize:12,color:'#78746e',marginBottom:5}}>{label}</div>
        {SENSITIVE.has(k) ? (
          <div style={{position:'relative'}}>
            <input value={sheet[k]} inputMode={im||'text'} placeholder={ph||''}
              type={showSensitive[k]?'text':'password'} autoComplete="off"
              onChange={e=>setSheet(p=>({...p,[k]:e.target.value}))}
              style={{...inpS,paddingRight:52}}/>
            <button onClick={()=>setShowSensitive(p=>({...p,[k]:!p[k]}))} style={{
              position:'absolute',right:6,top:'50%',transform:'translateY(-50%)',
              background:'transparent',border:'none',cursor:'pointer',padding:'4px 6px',
              fontSize:11.5,fontWeight:600,color:'#4a45d1',fontFamily:'inherit'
            }}>{showSensitive[k]?'Hide':'Show'}</button>
          </div>
        ) : (
          <input value={sheet[k]} inputMode={im||'text'} placeholder={ph||''}
            onChange={e=>setSheet(p=>({...p,[k]:e.target.value}))}
            style={inpS}/>
        )}
      </div>
    );
    const Eyebrow = ({children,first}) => (
      <div style={{gridColumn:'1 / -1',fontSize:11,fontWeight:600,letterSpacing:'0.08em',color:'#4a45d1',textTransform:'uppercase',paddingBottom:8,borderBottom:'1px solid #f0efed',marginTop:first?0:10}}>{children}</div>
    );
    const grid = cols => ({display:'grid',gridTemplateColumns:isM?'1fr':cols,gap:12});
    return (
      <div style={{display:'flex',flexDirection:'column',gap:14}}>
        <div style={{display:'flex',justifyContent:'flex-end'}}>
          <button onClick={()=>{ if(window.confirm('Clear the client sheet? This wipes every field.')) { setSheet({...SHEET_EMPTY}); setSheetMsg(null); } }}
            style={{background:'transparent',border:'none',color:'#4a45d1',fontSize:12.5,fontWeight:600,cursor:'pointer',padding:0,fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif"}}>Clear sheet</button>
        </div>
        <div style={{background:'#fff',border:'1px solid #eae9e6',borderRadius:12,padding:isM?16:22,display:'flex',flexDirection:'column',gap:14,boxShadow:'0 1px 2px rgba(25,24,23,0.04)'}}>
          {/* CLIENT */}
          <div style={grid('1fr 1fr')}>
            <Eyebrow first>Client</Eyebrow>
            <F k="fullName" label="Full name"/>
            <F k="dob" label="Date of birth" ph="mm/dd/yyyy"/>
            <F k="address" label="Address" span={2}/>
            <F k="phone" label="Phone" im="tel"/>
            <F k="email" label="Email" im="email"/>
            <F k="heightWeight" label="Height / weight" ph={'5\'10" / 190 lb'}/>
            <F k="dl" label="Driver's license #"/>
            <F k="ssn" label="Social Security #"/>
          </div>
          {/* Husband & wife checkbox */}
          <button onClick={()=>setSheet(p=>({...p,spouse:!p.spouse}))} style={{
            padding:'10px 12px',borderRadius:8,border:`1px solid ${sheet.spouse?'#c9c6f2':'#e8e7e4'}`,
            background:sheet.spouse?'#eef0fe':'#fbfbfa',color:'#191817',textAlign:'left',cursor:'pointer',
            fontSize:12.5,fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif",display:'flex',alignItems:'center',gap:8
          }}>
            <span style={{width:16,height:16,borderRadius:4,border:`1px solid ${sheet.spouse?'#4a45d1':'#dedcd7'}`,background:sheet.spouse?'#4a45d1':'#fff',display:'flex',alignItems:'center',justifyContent:'center',fontSize:11,color:'#fff',fontWeight:700,flexShrink:0}}>{sheet.spouse?'✓':''}</span>
            <span>Husband &amp; wife — add a second client</span>
          </button>
          {sheet.spouse && (
            <div style={grid('1fr 1fr')}>
              <Eyebrow>Client 2 (Spouse)</Eyebrow>
              <F k="sp_fullName" label="Full name"/>
              <F k="sp_dob" label="Date of birth" ph="mm/dd/yyyy"/>
              <F k="sp_heightWeight" label="Height / weight"/>
              <F k="sp_dl" label="Driver's license #"/>
              <F k="sp_ssn" label="Social Security #"/>
            </div>
          )}
          {/* BENEFICIARIES */}
          <div style={grid('2fr 1fr')}>
            <Eyebrow>Beneficiaries</Eyebrow>
            <F k="bene1" label="Primary beneficiary"/>
            <F k="bene1Dob" label="DOB" ph="mm/dd/yyyy"/>
            <F k="bene2" label="Contingent beneficiary"/>
            <F k="bene2Dob" label="DOB" ph="mm/dd/yyyy"/>
          </div>
          {/* BANKING */}
          <div style={grid('1fr 1fr 1fr')}>
            <Eyebrow>Banking</Eyebrow>
            <F k="bankName" label="Bank name"/>
            <F k="routing" label="Routing number" im="numeric"/>
            <F k="account" label="Account number" im="numeric"/>
          </div>
          {/* POLICY STATUS */}
          <div style={grid('1fr 1fr 1fr')}>
            <Eyebrow>Policy status</Eyebrow>
            <F k="carrier" label="Carrier"/>
            <F k="ap" label="Annual premium" im="decimal"/>
            <F k="effectiveDate" label="Effective date" ph="mm/dd/yyyy"/>
            <F k="declinedThrough" label="Declined through"/>
            <F k="inUnderwriting" label="In underwriting"/>
            <F k="approved" label="Approved"/>
          </div>
          {/* NOTES */}
          <div>
            <div style={{fontSize:12,color:'#78746e',marginBottom:5}}>Notes</div>
            <textarea value={sheet.notes} rows={4} placeholder="Anything worth remembering about this client or call…"
              onChange={e=>setSheet(p=>({...p,notes:e.target.value}))}
              style={{...inpS,resize:'vertical',minHeight:88,fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif"}}/>
          </div>
        </div>
        <button className="qm-cta" onClick={saveClientSheet} disabled={sheetSaving}
          style={{width:'100%',minHeight:46,borderRadius:9,border:'none',cursor:'pointer',background:'#4a45d1',color:'#fff',fontSize:14.5,fontWeight:600,boxShadow:'0 1px 2px rgba(74,69,209,.3)',opacity:sheetSaving?0.6:1,fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif"}}>
          {sheetSaving ? 'Building PDF…' : 'Save client sheet (PDF)'}
        </button>
        {sheetMsg && (
          <div style={{fontSize:12.5,textAlign:'center',padding:'9px 12px',borderRadius:8,lineHeight:1.5,
            color:sheetMsg.t==='ok'?'#177452':'#b42318',
            background:sheetMsg.t==='ok'?'#e8f6ef':'#fdecec',
            border:`1px solid ${sheetMsg.t==='ok'?'#c4e8d6':'#f5c6c2'}`}}>{sheetMsg.m}</div>
        )}
        <div style={{fontSize:11.5,color:'#a09c94',textAlign:'center',lineHeight:1.6}}>
          Saves as <span style={{fontFamily:"ui-monospace,'SF Mono',Menlo,monospace",fontSize:10.5,color:'#78746e'}}>{sheetFileName(sheet)}</span>
        </div>
      </div>
    );
  };

  const renderClientInfo = ({variant='mobile', wrap=true, showState=true, hideTobacco=false, hideGender=false}={}) => {
    const isM = variant==='mobile';
    const I  = isM ? mInp : inp;
    const T  = isM ? mTogBtn : togBtn;
    const rDd = isM ? dobDdRef  : dobDdRefD;
    const rYy = isM ? dobYyyyRef: dobYyyyRefD;
    const titleStyle = {fontSize:11,fontWeight:600,letterSpacing:'0.08em',color:'#a09c94',textTransform:'uppercase',marginBottom:12};
    const labelStyle = {fontSize:isM?12:11,color:C.t3,marginBottom:isM?8:6,fontWeight:600};
    const inner = (
      <>
        <div style={titleStyle}>Client Info</div>
        {/* DOB / Age row */}
        <div style={{marginBottom:isM?14:12}}>
          <div style={{fontSize:isM?12:11,color:C.t3,marginBottom:isM?8:6}}>Date of birth <span style={{color:C.t4}}>· or enter age →</span></div>
          <div style={{display:'flex',gap:isM?6:5,alignItems:'center',flexWrap:'wrap'}}>
            <input type="text" inputMode="numeric" maxLength="2" placeholder="mm" value={dob.mm}
              onChange={e=>{const v=e.target.value.replace(/\D/g,'');setDob(p=>({...p,mm:v}));if(v.length===2)rDd.current?.focus();}}
              style={{...I,width:isM?52:44,textAlign:'center',padding:isM?'11px 4px':'8px 4px'}}/>
            <span style={{color:C.t4,fontSize:isM?14:12}}>/</span>
            <input ref={rDd} type="text" inputMode="numeric" maxLength="2" placeholder="dd" value={dob.dd}
              onChange={e=>{const v=e.target.value.replace(/\D/g,'');setDob(p=>({...p,dd:v}));if(v.length===2)rYy.current?.focus();}}
              style={{...I,width:isM?52:44,textAlign:'center',padding:isM?'11px 4px':'8px 4px'}}/>
            <span style={{color:C.t4,fontSize:isM?14:12}}>/</span>
            <input ref={rYy} type="text" inputMode="numeric" maxLength="4" placeholder="yyyy" value={dob.yyyy}
              onChange={e=>setDob(p=>({...p,yyyy:e.target.value.replace(/\D/g,'')}))}
              style={{...I,width:isM?72:62,textAlign:'center',padding:isM?'11px 6px':'8px 4px'}}/>
            <span style={{color:C.t4,fontSize:isM?12:11,flexShrink:0}}>or</span>
            <input type="text" inputMode="numeric" maxLength="2" placeholder="age" value={age}
              onChange={e=>{setAge(e.target.value.replace(/\D/g,''));setDob({mm:'',dd:'',yyyy:''});}}
              style={{...I,width:isM?56:48,padding:isM?'11px 6px':'8px 4px',borderColor:age&&!ageOK?'#b42318':C.bd,textAlign:'center'}}/>
            {/* +/- steppers */}
            <div style={{display:'flex',flexDirection:'column',gap:2,flexShrink:0}}>
              <button onClick={()=>{const n=Math.min(89,(parseInt(age)||0)+1);setAge(String(n));setDob({mm:'',dd:'',yyyy:''});}}
                aria-label="Age up" style={{background:C.bg3,border:`1px solid ${C.bd}`,borderRadius:5,color:C.t2,width:isM?28:24,height:isM?22:18,padding:0,cursor:'pointer',fontSize:isM?12:10,lineHeight:1,fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif"}}>▲</button>
              <button onClick={()=>{const n=Math.max(1,(parseInt(age)||1)-1);setAge(String(n));setDob({mm:'',dd:'',yyyy:''});}}
                aria-label="Age down" style={{background:C.bg3,border:`1px solid ${C.bd}`,borderRadius:5,color:C.t2,width:isM?28:24,height:isM?22:18,padding:0,cursor:'pointer',fontSize:isM?12:10,lineHeight:1,fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif"}}>▼</button>
            </div>
          </div>
          {age&&ageOK&&<div style={{fontSize:isM?11:10,color:C.green,marginTop:isM?6:4}}>✓ Age {age}</div>}
          {age&&!ageOK&&parseInt(age)>0&&<div style={{fontSize:isM?11:10,color:'#b42318',marginTop:isM?6:4}}>Age must be 1–89</div>}
        </div>

        {/* Tobacco + Gender — full-width joined segmented toggles, stacked (design 2a) */}
        {!hideTobacco && (
          <div style={{...segWrap,marginBottom:isM?14:12}}>
            <button className={smoker?'qm-seg':undefined} style={T(!smoker)} onClick={()=>setSmoker(false)}>Non-smoker</button>
            <button className={!smoker?'qm-seg':undefined} style={T(smoker)} onClick={()=>setSmoker(true)}>Smoker</button>
          </div>
        )}
        {!hideGender && (
          <div style={{...segWrap,marginBottom:showState?(isM?14:12):0}}>
            <button className={gender!=='male'?'qm-seg':undefined} style={T(gender==='male')} onClick={()=>setGender('male')}>Male</button>
            <button className={gender!=='female'?'qm-seg':undefined} style={T(gender==='female')} onClick={()=>setGender('female')}>Female</button>
          </div>
        )}

        {/* State */}
        {showState && (
          <div>
            <div style={{...labelStyle,marginBottom:isM?8:4}}>State {!isM&&<span style={{color:C.t4,fontWeight:400}}>· filters carrier availability</span>}</div>
            <select value={usState} onChange={e=>setUsState(e.target.value)} style={{...I,cursor:'pointer'}}>
              <option value="">— Select state —</option>
              {US_STATES.map(s=><option key={s} value={s}>{s} — {STATE_NAMES[s]}</option>)}
            </select>
          </div>
        )}
      </>
    );
    if (!wrap) return inner;
    return isM
      ? <div style={{background:C.bg2,border:`1px solid ${C.bd}`,borderRadius:12,padding:16}}>{inner}</div>
      : <div style={sec}>{inner}</div>;
  };

  // ─────────────────────────────────────────────────
  // ── MOBILE RENDER (<768px)
  // ─────────────────────────────────────────────────
  if (isMobile) {
    return (
      <div style={{fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif",background:'#f8f8f7',minHeight:'100vh',color:C.t1,position:'relative',paddingBottom:100}}>

        {/* ── MOBILE HEADER — 52px bar (design 3d) ── */}
        <div style={{background:'#fff',borderBottom:'1px solid #eae9e6',height:52,padding:'0 16px',display:'flex',alignItems:'center',justifyContent:'space-between',position:'sticky',top:0,zIndex:50}}>
          <div style={{fontSize:17,fontWeight:700,color:'#191817',letterSpacing:'-0.01em'}}>
            Quotemarko<span style={{color:'#4a45d1'}}>.</span>
          </div>
          <div style={{display:'flex',alignItems:'center',gap:8}}>
            <button className="qm-secondary" onClick={()=>setShowContact(true)} style={{padding:'7px 12px',minHeight:36,borderRadius:8,border:'1px solid #dedcd7',background:'#fff',color:'#191817',fontSize:12,fontWeight:600,cursor:'pointer',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif"}}>
              Contact
            </button>
            <button onClick={()=>setMobileTab2('profile')} aria-label="Profile" style={{width:36,height:36,borderRadius:'50%',border:'1px solid #e8e7e4',background:'#f0efed',color:'#78746e',fontSize:13,fontWeight:600,cursor:'pointer',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif"}}>
              {(profileName||session?.user?.email||'?').slice(0,1).toUpperCase()}
            </button>
          </div>
        </div>

        {/* ── PRODUCT PILL TABS — horizontal scroll, active solid ink (design 3d) ── */}
        <div style={{background:'#fff',borderBottom:'1px solid #eae9e6',padding:'8px 16px',position:'sticky',top:52,zIndex:49,overflowX:'auto',WebkitOverflowScrolling:'touch'}}>
          <div style={{display:'flex',gap:6,width:'max-content'}}>
            {[['fe','FEX / WL'],['term','Term'],['iul','IUL'],['cv','CV'],['sheet','Sheet']].map(([id,label])=>(
              <button key={id} onClick={()=>{track('Tab Switch',{to:id});setQuoteMode(id);setMobileTab('quote');window.scrollTo({top:0,behavior:'instant'});}} style={{
                padding:'9px 16px',minHeight:38,borderRadius:99,border:`1px solid ${quoteMode===id?'#191817':'#dedcd7'}`,
                background:quoteMode===id?'#191817':'#fff',
                color:quoteMode===id?'#fff':'#78746e',
                fontSize:13,fontWeight:quoteMode===id?600:400,cursor:'pointer',whiteSpace:'nowrap',
                fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif"
              }}>{label}</button>
            ))}
          </div>
        </div>

        {/* ── MOBILE BODY ── */}
        <div style={{padding:'16px 16px 0'}}>

          {/* ── QUOTE TAB ── */}
          {mobileTab === 'quote' && (
            <div style={{display:'flex',flexDirection:'column',gap:12}}>

              {quoteMode==='fe' ? (
              <>
              {/* Unified Client Info (juvenile hides tobacco) */}
              {renderClientInfo({variant:'mobile', hideTobacco:isJuvenile})}

              {/* Age-bracket banner */}
              {isJuvenile && (
                <div style={{background:'rgba(59,130,246,0.10)',border:'1px solid rgba(59,130,246,0.35)',borderRadius:10,padding:'12px 14px',fontSize:12,color:C.t2,lineHeight:1.5}}>
                  <div style={{fontWeight:700,color:'#60A5FA',marginBottom:4,fontSize:11,letterSpacing:0.5}}>👶 JUVENILE WHOLE LIFE</div>
                  No medical questions. Coverage typically $15k–$45k. Most carriers issue guaranteed for ages 0–17.
                </div>
              )}
              {isYoungAdult && (
                <div style={{background:'rgba(23,116,82,0.08)',border:'1px solid rgba(23,116,82,0.28)',borderRadius:10,padding:'12px 14px',fontSize:12,color:C.t2,lineHeight:1.5}}>
                  <div style={{fontWeight:700,color:'#177452',marginBottom:4,fontSize:11,letterSpacing:0.5}}>👤 ADULT WHOLE LIFE</div>
                  Standard adult underwriting. Tip: term life is usually cheaper for ages 18–49 if pure death benefit is the goal.
                </div>
              )}

              {/* Quote Target */}
              <div style={{background:C.bg2,border:`1px solid ${C.bd}`,borderRadius:12,padding:16,overflow:'hidden'}}>
                <div style={{display:'flex',alignItems:'center',justifyContent:'space-between',marginBottom:12}}>
                  <div style={{fontSize:11,fontWeight:700,letterSpacing:1.8,color:C.t4,textTransform:'uppercase'}}>Quote Target</div>
                  <button className="qm-gsb-toggle" onClick={()=>{setGsbOn(p=>{const n=!p;track('GSB Toggle',{on:n});return n;});setMode('face');}} style={{
                    display:'flex',alignItems:'center',gap:6,
                    padding:'6px 12px 6px 8px',borderRadius:20,
                    border:`2px solid ${gsbOn ? C.goldBd : isDark?'#374151':'#D0CDBE'}`,
                    background:gsbOn ? C.goldBg : (isDark?'#0F172A':'#FAF9F6'),
                    color:gsbOn?C.gold:C.t3,
                    fontSize:11,fontWeight:700,cursor:'pointer',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif",
                    boxShadow:gsbOn?(isDark?'0 2px 8px rgba(0,0,0,0.3)':'0 1px 4px rgba(74,69,209,0.15)'):'none'
                  }}>
                    <span style={{position:'relative',display:'inline-block',width:28,height:16,borderRadius:8,
                      background:gsbOn ? C.gold : (isDark?'#1E293B':'#E2E8F0'),
                      border:`1px solid ${gsbOn?C.goldBd:isDark?'#374151':'#D0CDBE'}`,
                      transition:'all 0.2s',flexShrink:0}}>
                      <span style={{position:'absolute',top:2,left:gsbOn?13:2,width:10,height:10,borderRadius:'50%',background:'white',transition:'left 0.2s',boxShadow:'0 1px 3px rgba(0,0,0,0.3)'}}/>
                    </span>
                    {gsbOn?'🥇🥈🥉':'🏆'} GSB Mode
                  </button>
                </div>

                {!gsbOn ? (
                  <>
                    <div style={{display:'flex',gap:6,marginBottom:14}}>
                      <button className='qm-btn' style={{...mTogBtn(mode==='face')}} onClick={()=>setMode('face')}>Face Amount</button>
                      <button className='qm-btn' style={{...mTogBtn(mode==='budget')}} onClick={()=>setMode('budget')}>Premium Amount</button>
                    </div>
                    {mode==='face' ? (
                      <>
                        <div style={{display:'flex',justifyContent:'space-between',fontSize:12,color:C.t3,marginBottom:8}}>
                          <span>Coverage amount</span>
                          <span style={{fontSize:10,color:C.t4}}>$2,000 – $100,000</span>
                        </div>
                        <div style={{position:'relative'}}>
                      <span style={{position:'absolute',left:14,top:'50%',transform:'translateY(-50%)',color:C.t3,fontSize:16,pointerEvents:'none'}}>$</span>
                      <input type="text" inputMode="numeric" placeholder="10,000" value={faceAmt?faceAmt.toLocaleString():''}
                        onChange={e=>{const n=Number(e.target.value.replace(/[^0-9]/g,''));setFaceAmt(isNaN(n)?0:n);}}
                        style={{...mInp,paddingLeft:30,fontFamily:"'Instrument Sans',sans-serif",fontSize:17}}/>
                    </div>
                      </>
                    ) : (
                      <>
                        <div style={{fontSize:12,color:C.t3,marginBottom:8}}>Monthly premium</div>
                        <div style={{position:'relative'}}>
                          <span style={{position:'absolute',left:14,top:'50%',transform:'translateY(-50%)',color:C.t3,fontSize:16,pointerEvents:'none'}}>$</span>
                          <input type="text" inputMode="decimal" placeholder="100" value={budget||''}
                            onChange={e=>{const v=e.target.value.replace(/[^0-9.]/g,'').replace(/^0+(?=\d)/,'');const n=Number(v);setBudget(v===''||isNaN(n)?0:n);}}
                            style={{...mInp,paddingLeft:30,fontFamily:"'Instrument Sans',sans-serif",fontSize:17}}/>
                        </div>
                        <div style={{fontSize:11,color:C.t4,marginTop:6}}>Finds max coverage at this premium</div>
                      </>
                    )}
                  </>
                ) : (
                  <div>
                    <div style={{fontSize:12,color:C.t3,marginBottom:10}}>Coverage per tier</div>
                    {GSB.map(g=>(
                      <div key={g.key} style={{display:'flex',alignItems:'center',gap:10,marginBottom:10}}>
                        <span style={{fontSize:18,flexShrink:0}}>{g.medal}</span>
                        <span style={{fontSize:13,fontWeight:700,color:g.color,width:50,flexShrink:0}}>{g.label}</span>
                        <div style={{position:'relative',flex:1}}>
                          <span style={{position:'absolute',left:12,top:'50%',transform:'translateY(-50%)',color:C.t4,fontSize:13}}>$</span>
                          <input type="text" inputMode="numeric" value={gsbRaw[g.key]}
                            onChange={e=>handleGsbChange(g.key,e.target.value)}
                            onBlur={()=>handleGsbBlur(g.key)}
                            onFocus={e=>e.target.select()}
                            style={{...mInp,paddingLeft:24,fontFamily:"'Instrument Sans',sans-serif"}}/>
                        </div>
                      </div>
                    ))}
                  </div>
                )}
              </div>

              {/* Health conditions — search only (hidden for juveniles) */}
              {!isJuvenile && (
              <div style={{background:C.bg2,border:`1px solid ${C.bd}`,borderRadius:12,padding:16,overflow:'hidden',boxSizing:'border-box',width:'100%'}}>
                <div style={{fontSize:11,fontWeight:700,letterSpacing:1.8,color:C.t4,textTransform:'uppercase',marginBottom:6}}>Health Conditions & Meds</div>
                <div style={{fontSize:11,color:C.t4,marginBottom:10,lineHeight:1.4}}>Type a medication name (lisinopril, metformin, eliquis…) and we'll suggest the condition. Or type the condition directly.</div>
                <div style={{position:'relative',marginBottom:10,display:'flex',alignItems:'center',width:'100%',boxSizing:'border-box'}}>
                  <span style={{position:'absolute',left:12,top:'50%',transform:'translateY(-50%)',fontSize:14,color:C.t4,pointerEvents:'none',zIndex:1}}>🔍</span>
                  <input placeholder="Type meds or conditions…"
                    value={search} onChange={e=>setSearch(e.target.value)}
                    style={{...mInp,paddingLeft:36,flex:1,minWidth:0,width:0,fontSize:13}}/>
                </div>
                {medHints.length>0&&search.length>=3&&(
                  <div style={{background:C.goldBg,border:`1px solid ${C.goldBd}`,borderRadius:10,padding:'10px 12px',marginBottom:10}}>
                    <div style={{fontSize:12,color:C.gold,fontWeight:600,marginBottom:6}}>💊 "{search}" may indicate:</div>
                    <div style={{display:'flex',flexWrap:'wrap',gap:5}}>
                      {medHints.map(c=>(<span key={c.id} onClick={()=>toggleCond(c.id)} style={{background:TIER_INFO[c.tier].pill,border:`1px solid ${TIER_INFO[c.tier].bd}`,color:TIER_INFO[c.tier].dot,borderRadius:6,padding:'4px 10px',cursor:'pointer',fontWeight:600,fontSize:12}}>+ {c.label}</span>))}
                    </div>
                  </div>
                )}
                {/* Smart suggestions — max 5, no accordion */}
                {search.length>=2 && (
                  <div style={{marginBottom:8}}>
                    {filteredConds.slice(0,5).map(c=>{
                      const active=selected.includes(c.id),tc=TIER_INFO[c.tier].dot;
                      return(
                        <div key={c.id} onClick={()=>{toggleCond(c.id);setSearch('');}} style={{
                          display:'flex',alignItems:'center',gap:10,padding:'11px 12px',borderRadius:9,
                          cursor:'pointer',fontSize:14,marginBottom:5,
                          background:active?TIER_INFO[c.tier].pill:C.bg3,
                          border:`1px solid ${active?TIER_INFO[c.tier].bd:C.bd2}`,
                          color:active?C.t0:C.t1
                        }}>
                          <span style={{width:8,height:8,borderRadius:'50%',background:active?tc:TIER_INFO[c.tier].dot,flexShrink:0}}/>
                          <span style={{flex:1,lineHeight:1.35}}>{c.label}</span>
                          <span style={{fontSize:11,color:C.t4}}>{c.cat}</span>
                          {active&&<span style={{color:tc,fontSize:13}}>✓</span>}
                        </div>
                      );
                    })}
                    {filteredConds.length===0&&<div style={{fontSize:13,color:C.t4,padding:'8px 4px'}}>No matches — try a different term</div>}
                  </div>
                )}
                {/* Active condition chips */}
                {selected.filter(c=>c!=='none').length>0&&(
                  <div style={{marginTop:10}}>
                    <div style={{fontSize:10,color:C.t4,marginBottom:6,fontWeight:600,letterSpacing:0.5}}>ACTIVE CONDITIONS</div>
                    <div style={{display:'flex',flexWrap:'wrap',gap:5}}>
                      {selected.filter(c=>c!=='none').map(id=>{
                        const cond=CONDITIONS.find(c=>c.id===id);
                        return(
                          <span key={id} onClick={()=>toggleCond(id)} style={{
                            background:TIER_INFO[cond?.tier||'B'].pill,
                            border:`1px solid ${TIER_INFO[cond?.tier||'B'].bd}`,
                            borderRadius:20,padding:'4px 12px 4px 10px',fontSize:12,
                            color:TIER_INFO[cond?.tier||'B'].dot,
                            cursor:'pointer',display:'inline-flex',alignItems:'center',gap:5,
                            fontWeight:600
                          }}>
                            <span style={{width:5,height:5,borderRadius:'50%',background:TIER_INFO[cond?.tier||'B'].dot,flexShrink:0}}/>
                            {cond?.label.replace('⚠ ','')}
                            <span style={{opacity:0.7,fontWeight:400,fontSize:14,lineHeight:1}}>×</span>
                          </span>
                        );
                      })}
                    </div>
                  </div>
                )}
                {/* UW Tier selector — tappable on mobile */}
                <div style={{marginTop:12}}>
                  <div style={{display:'flex',justifyContent:'space-between',alignItems:'center',marginBottom:7}}>
                    <span style={{fontSize:11,color:C.t4,fontWeight:600,letterSpacing:0.5}}>UW TIER</span>
                    <div style={{display:'flex',alignItems:'center',gap:6}}>
                      {tierOvr?(
                        <button onClick={()=>setTierOvr(null)} style={{background:'transparent',border:'none',color:C.gold,fontSize:11,fontWeight:700,cursor:'pointer',padding:0,fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif"}}>
                          ↺ Auto
                        </button>
                      ):(
                        <span style={{fontSize:10,color:C.t4,fontStyle:'italic'}}>auto-detected</span>
                      )}
                    </div>
                  </div>
                  <div style={{display:'grid',gridTemplateColumns:'1fr 1fr 1fr 1fr',gap:6}}>
                    {['B','C','D','E'].map(t=>{
                      const ti=TIER_INFO[t],isOvr=tierOvr===t,isAuto=!tierOvr&&autoTier===t;
                      const active=isOvr||isAuto;
                      return(
                        <button key={t} onClick={()=>toggleOvr(t)} style={{
                          padding:'10px 4px',borderRadius:9,
                          border:`1px solid ${active?ti.bd:C.bd}`,
                          background:isOvr?ti.pill:'transparent',
                          color:active?ti.txt:C.t4,
                          cursor:'pointer',textAlign:'center',
                          display:'flex',flexDirection:'column',alignItems:'center',gap:4,
                          fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif",
                          boxShadow:isOvr?`0 0 0 2px ${ti.dot}44`:'none'
                        }}>
                          <span style={{width:7,height:7,borderRadius:'50%',background:active?ti.dot:'#334155'}}/>
                          <span style={{fontSize:11,fontWeight:700,color:'inherit'}}>{ti.short.split(' ')[0]}</span>
                          {isAuto&&!tierOvr&&<span style={{fontSize:8,color:ti.txt,opacity:0.65,fontWeight:500}}>auto</span>}
                          {isOvr&&<span style={{fontSize:9}}>✓</span>}
                        </button>
                      );
                    })}
                  </div>
                  <div style={{marginTop:8,padding:'7px 10px',background:C.bg3,borderRadius:8,border:`1px solid ${TIER_INFO[uwTier].bd}44`,fontSize:11,color:C.t3,lineHeight:1.5}}>
                    {uwTier==='B'?'Preferred — clean or minor conditions. Full benefit day 1.':uwTier==='C'?'Standard — moderate history. Full benefit day 1.':uwTier==='D'?'Modified — graded benefit, 2–3 yr waiting period.':'GI Only — knockout conditions. No health questions.'}
                  </div>
                </div>
              </div>
              )}

              {/* GET QUOTES button */}
              <button className="qm-cta" onClick={e=>fireCta(e,()=>{if(ageOK){track('Quote Requested',{tier:uwTier,mode,gsb:gsbOn,face:faceBand(faceAmt)});setHasQuoted(true);if(isMobile){setMobileTab('results');setTimeout(()=>window.scrollTo({top:0,behavior:'instant'}),0);}}})} style={{
                width:'100%',minHeight:48,borderRadius:9,border:'none',
                cursor:ageOK?'pointer':'not-allowed',
                background:ageOK?'#4a45d1':'#dedcd7',
                color:'#fff',
                fontSize:15,fontWeight:600,
                boxShadow:ageOK?'0 1px 2px rgba(74,69,209,.3)':'none',
                fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif"
              }}>
                Get quotes
              </button>
              {!ageOK && <div style={{fontSize:11,color:C.t4,textAlign:'center',marginTop:8}}>Enter client age above to quote</div>}
              </>
              ) : quoteMode==='term' ? (
              <>
                {/* ── MOBILE TERM INPUTS ── */}
                <div style={{display:'flex',flexDirection:'column',gap:12}}>
                  {/* Unified Client Info */}
                  {renderClientInfo({variant:'mobile'})}

                  {/* Term settings — carded like the FEX Quote Target section (tabs read as one product) */}
                  <div style={{background:C.bg2,border:`1px solid ${C.bd}`,borderRadius:12,padding:16,display:'flex',flexDirection:'column',gap:12}}>
                  <div style={{fontSize:10,fontWeight:700,letterSpacing:1.8,color:C.t4,textTransform:'uppercase'}}>Term Settings</div>
                  {/* Term Length pills */}
                  <div>
                    <div style={{fontSize:11,color:C.t3,marginBottom:6,fontWeight:600}}>Term Length (years)</div>
                    <div style={{display:'grid',gridTemplateColumns:'repeat(7,1fr)',gap:4}}>
                      {['10','15','20','25','30','35','40'].map(t=>(
                        <button key={t} onClick={()=>setTermLength(t)} style={{
                          padding:'9px 0',borderRadius:7,border:`1px solid ${termLength===t?'#191817':'#dedcd7'}`,
                          background:termLength===t?'#191817':'#fff',color:termLength===t?'#fff':'#78746e',
                          fontSize:12,fontWeight:700,cursor:'pointer',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif"
                        }}>{t}</button>
                      ))}
                    </div>
                  </div>
                  {/* Face amount — Term quoter pulls face only (no budget mode) */}
                  <div>
                    <div style={{fontSize:11,color:C.t3,marginBottom:6,display:'flex',justifyContent:'space-between'}}>
                      <span>Coverage amount</span>
                      <span style={{fontSize:10,color:C.t4}}>$25,000 – $1,000,000</span>
                    </div>
                    <div style={{position:'relative'}}>
                      <span style={{position:'absolute',left:14,top:'50%',transform:'translateY(-50%)',color:C.t3,fontSize:16,pointerEvents:'none'}}>$</span>
                      <input type="text" inputMode="numeric" placeholder="100,000" value={termFace?termFace.toLocaleString():''}
                        onChange={e=>{const n=Number(e.target.value.replace(/[^0-9]/g,''));setTermFace(isNaN(n)?0:n);}}
                        style={{...mInp,paddingLeft:30,fontFamily:"'Instrument Sans',sans-serif",fontSize:17}}/>
                    </div>
                  </div>
                  {/* Height / Weight → BMI */}
                  <div>
                    <div style={{fontSize:11,color:C.t3,marginBottom:6,fontWeight:600,display:'flex',justifyContent:'space-between'}}>
                      <span>Height & Weight</span>
                      {termBMI!=null && <span style={{color:C.t4,fontWeight:500,fontSize:10,fontFamily:"'Instrument Sans',sans-serif"}}>BMI {termBMI.toFixed(1)}</span>}
                    </div>
                    <div style={{display:'grid',gridTemplateColumns:'1fr 1fr 1.2fr',gap:6}}>
                      <input inputMode="numeric" placeholder="ft" value={termHtFt}
                        onChange={e=>setTermHtFt(e.target.value.replace(/\D/g,'').slice(0,1))}
                        style={{...mInp,textAlign:'center',fontFamily:"'Instrument Sans',sans-serif"}}/>
                      <input inputMode="numeric" placeholder="in" value={termHtIn}
                        onChange={e=>setTermHtIn(e.target.value.replace(/\D/g,'').slice(0,2))}
                        style={{...mInp,textAlign:'center',fontFamily:"'Instrument Sans',sans-serif"}}/>
                      <input inputMode="numeric" placeholder="weight (lb)" value={termWtLb}
                        onChange={e=>setTermWtLb(e.target.value.replace(/\D/g,'').slice(0,3))}
                        style={{...mInp,textAlign:'center',fontFamily:"'Instrument Sans',sans-serif"}}/>
                    </div>
                  </div>
                  {/* Family history toggle */}
                  <button onClick={()=>setTermFamHx(v=>!v)} style={{
                    padding:'10px 12px',borderRadius:8,border:`1px solid ${termFamHx?'#4a45d1':C.bd}`,
                    background:termFamHx?'rgba(74,69,209,0.12)':C.bg2,color:C.t2,textAlign:'left',cursor:'pointer',
                    fontSize:12,fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif",display:'flex',alignItems:'center',gap:8
                  }}>
                    <span style={{width:16,height:16,borderRadius:4,border:`2px solid ${termFamHx?'#4a45d1':C.bd2}`,background:termFamHx?'#4a45d1':'transparent',display:'flex',alignItems:'center',justifyContent:'center',fontSize:11,color:C.bg0,fontWeight:900,flexShrink:0}}>{termFamHx?'✓':''}</span>
                    <span>Family history of cancer or heart disease before age 60</span>
                  </button>
                  {/* Conditions picker (shares `selected` state with FE tab) */}
                  <div>
                    <div style={{fontSize:11,color:C.t3,marginBottom:6,fontWeight:600,display:'flex',justifyContent:'space-between'}}>
                      <span>Health Conditions</span>
                      {selected.filter(c=>c!=='none').length>0 && <span style={{color:C.t4,fontWeight:500,fontSize:10}}>{selected.filter(c=>c!=='none').length} active</span>}
                    </div>
                    <input placeholder="Search conditions or medications…" value={search} onChange={e=>setSearch(e.target.value)} style={{...mInp,fontSize:13,padding:'10px 12px'}}/>
                    {search.length>=2 && filteredConds.length>0 && (
                      <div style={{marginTop:6,maxHeight:200,overflowY:'auto',background:C.bg3,border:`1px solid ${C.bd}`,borderRadius:8,padding:6,display:'flex',flexDirection:'column',gap:3}}>
                        {filteredConds.slice(0,8).map(c=>{
                          const active=selected.includes(c.id);
                          return (
                            <div key={c.id} onClick={()=>{toggleCond(c.id);setSearch('');}} style={{
                              display:'flex',alignItems:'center',justifyContent:'space-between',gap:8,
                              padding:'7px 10px',borderRadius:6,cursor:'pointer',
                              background:active?'rgba(74,69,209,0.10)':'transparent',
                              fontSize:12,color:C.t1
                            }}>
                              <span>{c.label.replace('⚠ ','')}</span>
                              <span style={{fontSize:9,color:C.t4}}>{c.cat}</span>
                            </div>
                          );
                        })}
                      </div>
                    )}
                    {search.length>=2 && filteredConds.length===0 && (
                      <div style={{marginTop:6,fontSize:12,color:C.t4,padding:'8px 4px'}}>No matches — try a different term</div>
                    )}
                    {selected.filter(c=>c!=='none').length>0 && (
                      <div style={{marginTop:8,display:'flex',flexWrap:'wrap',gap:5}}>
                        {selected.filter(c=>c!=='none').map(id=>{
                          const cond=CONDITIONS.find(c=>c.id===id);
                          return(
                            <span key={id} onClick={()=>toggleCond(id)} style={{
                              background:TIER_INFO[cond?.tier||'B'].pill,
                              border:`1px solid ${TIER_INFO[cond?.tier||'B'].bd}`,
                              borderRadius:20,padding:'4px 10px 4px 8px',fontSize:11,
                              color:TIER_INFO[cond?.tier||'B'].dot,
                              cursor:'pointer',display:'inline-flex',alignItems:'center',gap:4,fontWeight:600
                            }}>
                              {cond?.label.replace('⚠ ','')}
                              <span style={{opacity:0.7,fontWeight:400,fontSize:13,lineHeight:1}}>×</span>
                            </span>
                          );
                        })}
                      </div>
                    )}
                  </div>
                  {/* Health class pills */}
                  <div>
                    <div style={{fontSize:11,color:C.t3,marginBottom:6,fontWeight:600,display:'flex',justifyContent:'space-between',alignItems:'center'}}>
                      <span title="Pref+ is the toughest underwriting class — ideal BMI, no tobacco, no chronic conditions, no family history of cardiac/cancer before 60. ~10-15% of applicants qualify. Hover each pill for full criteria.">Health Class <span style={{fontSize:9,color:C.t4,cursor:'help'}}>ⓘ</span></span>
                      {termRec.recommended==='decline' ? (
                        <span style={{color:'#b42318',fontWeight:700,fontSize:10}}>⚠ Likely Decline</span>
                      ) : !termHealthManual && termRec.reasons.length ? (
                        <span style={{color:C.gold,fontWeight:600,fontSize:10}}>✨ Auto · {HEALTH_CLASS_SHORT[termRec.recommended]}</span>
                      ) : termHealthManual ? (
                        <button onClick={()=>setTermHealthManual(false)} style={{background:'transparent',border:'none',color:C.t4,fontSize:10,cursor:'pointer',padding:0,textDecoration:'underline'}}>reset to auto</button>
                      ) : (
                        <span style={{color:C.t4,fontWeight:500,fontSize:10}}>{HEALTH_CLASS_LABEL[termHealth]}</span>
                      )}
                    </div>
                    <div style={{display:'grid',gridTemplateColumns:'repeat(4,1fr)',gap:4}}>
                      {[
                        {k:'pp', label:'Pref+', tip:'PREFERRED PLUS — The toughest underwriting class. Best rates available. Requires: ideal BMI (~21-27), no tobacco use ever, no chronic conditions, no family history of cardiac/cancer before 60, perfect cholesterol & blood pressure, no DUIs, ideal driving record. Maybe 10-15% of applicants qualify.'},
                        {k:'p',  label:'Pref',  tip:'PREFERRED — Excellent health but not perfect. Requires: BMI ~28 or below, no tobacco use ever, minor controlled conditions OK (mild HTN with 1 med, mild HCL), generally good labs. About 25% of applicants qualify.'},
                        {k:'sp', label:'Std+',  tip:'STANDARD PLUS — Slightly better than average health. Often controlled conditions like diabetes oral, mild family history, or BMI 30-32. Common starting point for older applicants. About 25-30% qualify.'},
                        {k:'s',  label:'Std',   tip:'STANDARD — Average health risk. Multiple controlled conditions, BMI 32-35, slight family history, older smokers often land here. The most common class. About 30-40% qualify.'},
                      ].map(({k,label,tip})=>(
                        <button key={k} title={tip} onClick={()=>{setTermHealth(k);setTermHealthManual(true);}} style={{
                          padding:'10px 0',borderRadius:7,border:`1px solid ${termHealth===k?'#191817':'#dedcd7'}`,
                          background:termHealth===k?'#191817':'#fff',color:termHealth===k?'#fff':'#78746e',
                          fontSize:12,fontWeight:700,cursor:'pointer',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif"
                        }}>{label}</button>
                      ))}
                    </div>
                    {termRec.reasons.length > 0 && (
                      <div style={{marginTop:6,fontSize:10,color:C.t4,lineHeight:1.5}}>
                        {termRec.recommended==='decline'?'Decline factors':'Class driven by'}: {termRec.reasons.join(' · ')}
                      </div>
                    )}
                  </div>
                  </div>
                  <button className="qm-cta" onClick={e=>fireCta(e,()=>{if(ageOK){track('Quote Requested',{tier:'term',mode:'term',gsb:false,face:faceBand(termFace)});setHasQuoted(true);setMobileTab('results');setTimeout(()=>window.scrollTo({top:0,behavior:'instant'}),0);}})}
                    style={{width:'100%',minHeight:52,borderRadius:9,border:'none',cursor:ageOK?'pointer':'not-allowed',background:ageOK?'#4a45d1':'#dedcd7',color:'#fff',fontSize:16,fontWeight:600,boxShadow:ageOK?'0 1px 2px rgba(74,69,209,.3)':'none',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif",marginTop:8}}>
                    Get term quotes
                  </button>
                  {!ageOK && <div style={{fontSize:11,color:C.t4,textAlign:'center',marginTop:8}}>Enter client age above to quote</div>}
                </div>
              </>
              ) : null}

              {/* ── IUL MOBILE — Quote inputs + carrier face cards ── */}
              {quoteMode==='iul' && (
                <div style={{display:'flex',flexDirection:'column',gap:14}}>
                  {/* Unified Client Info */}
                  {renderClientInfo({variant:'mobile'})}
                  {/* Quote Target — carded like the FEX section (tabs read as one product) */}
                  <div style={{background:C.bg2,border:`1px solid ${C.bd}`,borderRadius:12,padding:16,display:'flex',flexDirection:'column',gap:12}}>
                  <div>
                    <div style={lbl}>Quote Target</div>
                    <div style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:6}}>
                      <button className='qm-btn' onClick={()=>setIulMode('premium')} title="Input target face amount, see required premium per carrier" style={{...mTogBtn(iulMode==='premium')}}>Face Amount</button>
                      <button className='qm-btn' onClick={()=>setIulMode('face')} title="Input monthly premium, see face amount each carrier will issue" style={{...mTogBtn(iulMode==='face')}}>Premium Amount</button>
                    </div>
                  </div>

                  {/* Premium input box (face mode) or face slider (premium mode) */}
                  {iulMode === 'face' ? (
                    <div>
                      <div style={{fontSize:12,color:C.t3,marginBottom:8}}>Monthly Premium</div>
                      <div style={{position:'relative'}}>
                        <span style={{position:'absolute',left:14,top:'50%',transform:'translateY(-50%)',color:C.t3,fontSize:16,pointerEvents:'none'}}>$</span>
                        <input type="text" inputMode="decimal" placeholder="200" value={iulPremium||''}
                          onChange={e=>{const v=e.target.value.replace(/[^0-9.]/g,'').replace(/^0+(?=\d)/,'');const n=Number(v);setIulPremium(v===''||isNaN(n)?0:n);}}
                          style={{...mInp,paddingLeft:30,fontFamily:"'Instrument Sans',sans-serif",fontSize:17}}/>
                      </div>
                      <div style={{fontSize:11,color:C.t4,marginTop:6}}>Each carrier returns the face they'd issue for this premium</div>
                    </div>
                  ) : (
                    <div>
                      <div style={{fontSize:11,color:C.t3,marginBottom:6,display:'flex',justifyContent:'space-between'}}>
                        <span>Target Face Amount</span>
                        <span style={{fontSize:10,color:C.t4}}>$25k – $500k</span>
                      </div>
                      <div style={{position:'relative'}}>
                      <span style={{position:'absolute',left:14,top:'50%',transform:'translateY(-50%)',color:C.t3,fontSize:16,pointerEvents:'none'}}>$</span>
                      <input type="text" inputMode="numeric" placeholder="150,000" value={iulFace?iulFace.toLocaleString():''}
                        onChange={e=>{const n=Number(e.target.value.replace(/[^0-9]/g,''));setIulFace(isNaN(n)?0:n);}}
                        style={{...mInp,paddingLeft:30,fontFamily:"'Instrument Sans',sans-serif",fontSize:17}}/>
                    </div>
                    </div>
                  )}

                  <div style={{padding:'10px 12px',background:C.bg3,border:`1px solid ${C.bd}`,borderRadius:8,fontSize:11,color:C.t4,lineHeight:1.6,marginTop:6}}>
                    💡 {iulMode==='face'
                      ? 'Face amounts shown are what each carrier will issue for your client at this premium.'
                      : 'Premiums shown are what each carrier requires for this face amount.'} Pull the carrier's full illustration before presenting to the client.
                  </div>
                  </div>

                  {/* Get IUL Quotes — match FE/Term pattern */}
                  <button className="qm-cta" onClick={e=>fireCta(e,()=>{if(ageOK){track('Quote Requested',{tier:'iul',mode:iulMode,gsb:false});setHasQuoted(true);setMobileTab('results');setTimeout(()=>window.scrollTo({top:0,behavior:'instant'}),0);}})}
                    style={{width:'100%',minHeight:52,borderRadius:9,border:'none',cursor:ageOK?'pointer':'not-allowed',background:ageOK?'#4a45d1':'#dedcd7',color:'#fff',fontSize:16,fontWeight:600,boxShadow:ageOK?'0 1px 2px rgba(74,69,209,.3)':'none',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif",marginTop:4}}>
                    Get IUL quotes
                  </button>
                  {!ageOK && <div style={{fontSize:11,color:C.t4,textAlign:'center',marginTop:8}}>Enter client age above to quote</div>}

                  {/* Original spec snapshot kept below */}
                  <div style={{fontSize:10,fontWeight:700,letterSpacing:1.8,color:C.t4,textTransform:'uppercase',marginTop:14,paddingTop:14,borderTop:`1px solid ${C.bd}`}}>Carrier Specs</div>
                  <div style={{display:'flex',alignItems:'center',gap:10,paddingBottom:8}}>
                    <span style={{fontSize:24}}>📈</span>
                    <div>
                      <div style={{fontSize:16,fontWeight:800,color:C.t0}}>IUL Carrier Specs</div>
                      <div style={{fontSize:11,color:C.t4,marginTop:1}}>Spec snapshot · Agent reference only</div>
                    </div>
                  </div>
                  {IUL_CARRIERS.map(c => (
                    <div key={c.id} style={{background:C.bg3,border:`1px solid ${C.bd}`,borderRadius:12,overflow:'hidden'}}>
                      <div style={{height:4,background:c.brand}}/>
                      <div style={{padding:'14px 14px 12px'}}>
                        <div style={{display:'flex',justifyContent:'space-between',alignItems:'flex-start',gap:8,marginBottom:10}}>
                          <div style={{flex:1,minWidth:0}}>
                            <div style={{display:'flex',alignItems:'center',gap:6}}>
                              <div style={{fontSize:11,color:C.t4,fontWeight:600}}>{c.name}</div>
                            </div>
                            <div style={{fontSize:15,fontWeight:800,color:C.t0,marginTop:1,lineHeight:1.2}}>{c.product}</div>
                          </div>
                          {c.needsVerify && <span style={{fontSize:9,color:'#F59E0B',background:'rgba(245,158,11,0.12)',border:'1px solid rgba(245,158,11,0.35)',borderRadius:4,padding:'2px 6px',fontWeight:700,flexShrink:0}}>VERIFY</span>}
                        </div>
                        {/* Quick specs */}
                        <div style={{display:'flex',flexDirection:'column',gap:5,fontSize:11,color:C.t2}}>
                          <div style={{display:'flex',justifyContent:'space-between'}}><span style={{color:C.t4}}>Issue ages</span><span style={{fontFamily:"'Instrument Sans',sans-serif"}}>{c.issueAges.min}–{c.issueAges.max}</span></div>
                          <div style={{display:'flex',justifyContent:'space-between'}}><span style={{color:C.t4}}>Face range</span><span style={{fontFamily:"'Instrument Sans',sans-serif"}}>{fmtFace(c.faceRange.min)}–{fmtFace(c.faceRange.max)}</span></div>
                          <div style={{display:'flex',justifyContent:'space-between'}}><span style={{color:C.t4}}>Index</span><span style={{fontSize:10}}>{c.indexStrategy}</span></div>
                          <div style={{display:'flex',justifyContent:'space-between'}}><span style={{color:C.t4}}>UW</span><span style={{fontSize:10}}>{c.underwriting}</span></div>
                          <div style={{display:'flex',justifyContent:'space-between'}}><span style={{color:C.t4}}>DB options</span><span style={{fontSize:10}}>{c.dbOptions.join(', ')}</span></div>
                        </div>
                        {/* Age-banded face if present */}
                        {c.faceRange.ageBands && (
                          <div style={{marginTop:10,background:C.bg2,borderRadius:6,padding:'8px 10px',fontSize:10,color:C.t3}}>
                            <div style={{fontWeight:700,marginBottom:4,color:C.t4,letterSpacing:0.5}}>FACE BY AGE BAND</div>
                            {c.faceRange.ageBands.map(b => (
                              <div key={b.ages} style={{display:'flex',justifyContent:'space-between'}}>
                                <span>{b.ages}</span><span style={{fontFamily:"'Instrument Sans',sans-serif"}}>{fmtFace(b.min)}–{fmtFace(b.max)}</span>
                              </div>
                            ))}
                          </div>
                        )}
                        {/* Riders */}
                        <div style={{marginTop:12}}>
                          <div style={{fontSize:9,color:C.t4,fontWeight:700,letterSpacing:1,marginBottom:5}}>RIDERS AVAILABLE</div>
                          <div style={{display:'flex',flexWrap:'wrap',gap:4}}>
                            {c.riders.map(r => (
                              <span key={r} style={{fontSize:10,background:C.bg2,border:`1px solid ${C.bd}`,borderRadius:4,padding:'3px 7px',color:C.t2}}>{r}</span>
                            ))}
                          </div>
                        </div>
                        {/* Knockouts */}
                        {c.knockouts && c.knockouts.length > 0 && (
                          <div style={{marginTop:10}}>
                            <div style={{fontSize:9,color:'#b42318',fontWeight:700,letterSpacing:1,marginBottom:5}}>AUTO-DECLINE</div>
                            <div style={{fontSize:10,color:C.t3,lineHeight:1.5}}>{c.knockouts.join(' · ')}</div>
                          </div>
                        )}
                        {/* Best for */}
                        <div style={{marginTop:12,padding:'9px 11px',background:`${c.brand}11`,border:`1px solid ${c.brand}33`,borderRadius:7,fontSize:11,color:C.t2,lineHeight:1.5,fontStyle:'italic'}}>
                          {c.bestFor}
                        </div>
                      </div>
                    </div>
                  ))}
                </div>
              )}

              {/* ── CV MOBILE INPUTS ── */}
              {quoteMode==='cv' && (
                <div style={{display:'flex',flexDirection:'column',gap:12}}>
                  {/* Unified Client Info — same as FE/Term/IUL (no state needed for CV) */}
                  {renderClientInfo({variant:'mobile', showState:false, hideTobacco:true})}

                  <div style={{fontSize:10,fontWeight:700,letterSpacing:1.8,color:C.t4,textTransform:'uppercase',marginTop:4}}>Existing Policy</div>
                  <div style={{background:C.bg3,border:`1px solid ${C.bd}`,borderRadius:12,padding:14,display:'flex',flexDirection:'column',gap:14}}>
                    <div>
                      <div style={{fontSize:12,color:C.t3,marginBottom:6,fontWeight:600}}>Monthly Premium ($)</div>
                      <input inputMode="decimal" placeholder="e.g. 52.00" value={cvMonthly}
                        onChange={e=>setCvMonthly(e.target.value)}
                        style={{...mInp,fontFamily:"'Instrument Sans',sans-serif",fontSize:18}}/>
                    </div>
                    <div>
                      <div style={{fontSize:12,color:C.t3,marginBottom:6,fontWeight:600}}>Years In-Force</div>
                      <input inputMode="numeric" placeholder="e.g. 7" value={cvPolicyYrs}
                        onChange={e=>setCvPolicyYrs(e.target.value.replace(/[^0-9]/g,''))}
                        style={{...mInp,fontFamily:"'Instrument Sans',sans-serif",fontSize:18}}/>
                      <div style={{fontSize:10,color:C.t4,marginTop:4}}>How long has the client had this policy?</div>
                    </div>
                  </div>

                  {/* Results — uses shared age (current) and computes issue age from policy years */}
                  {(()=>{
                    if(!ageOK||!cvMonthly||!cvPolicyYrs||!Number(cvMonthly)||!Number(cvPolicyYrs)) return null;
                    const currentAge = ageNum;
                    const issueAge = Math.max(0, currentAge - Number(cvPolicyYrs));
                    const d=calculateCVCorridor(Number(cvMonthly),Number(cvPolicyYrs),issueAge);
                    const cov = estimateCoverage({ currentAge, policyYears: Number(cvPolicyYrs), monthlyPremium: Number(cvMonthly), gender });
                    const amber = '#4a45d1';
                    return (
                      <>
                      <div ref={cvResultRef} style={{background:'rgba(74,69,209,0.06)',border:'1px solid rgba(74,69,209,0.22)',borderRadius:10,padding:'14px 16px'}}>
                        <div style={{fontSize:9,color:'#4a45d1',fontWeight:700,marginBottom:10,letterSpacing:1.2,textTransform:'uppercase'}}>Estimated Cash Value · Age {currentAge} · Issued at {issueAge}</div>
                        <div style={{display:'flex',alignItems:'baseline',gap:8,marginBottom:10,flexWrap:'wrap'}}>
                          <span style={{fontSize:22,fontWeight:800,color:C.t1,fontFamily:"'Instrument Sans',sans-serif"}}>${Math.round(d.low).toLocaleString()}</span>
                          <span style={{fontSize:16,color:C.t4}}>–</span>
                          <span style={{fontSize:22,fontWeight:800,color:'#4a45d1',fontFamily:"'Instrument Sans',sans-serif"}}>${Math.round(d.high).toLocaleString()}</span>
                        </div>
                        <div style={{display:'flex',justifyContent:'space-between',fontSize:12,paddingTop:8,borderTop:`1px solid ${C.bd}`}}>
                          <span style={{color:C.t3}}>Total Premiums Paid</span>
                          <strong style={{color:C.t2,fontFamily:"'Instrument Sans',sans-serif"}}>${Math.round(d.totalPaid).toLocaleString()}</strong>
                        </div>
                        {Number(cvPolicyYrs)<=2&&<div style={{fontSize:11,color:'#b42318',marginTop:8,lineHeight:1.5}}>⏳ Most policies accumulate little or no CV in years 1–2. Significant growth typically starts year 3+.</div>}
                      </div>

                      {/* Coverage estimate card */}
                      <div style={{marginTop:14,background:isDark?'rgba(74,69,209,0.05)':'rgba(74,69,209,0.04)',border:`1px solid ${amber}4D`,borderRadius:10,padding:'14px 16px'}}>
                        <div style={{display:'flex',alignItems:'center',gap:8,marginBottom:10}}>
                          <span style={{fontSize:18}}>🛡️</span>
                          <div style={{fontSize:13,fontWeight:700,color:C.t0,letterSpacing:0.3}}>Coverage Estimate</div>
                        </div>
                        {cov.error ? (
                          <div style={{fontSize:11,color:C.t3,padding:'8px 0'}}>
                            {cov.error === 'insufficient products matched'
                              ? 'No reference data at this issue age. Estimator covers issue ages 50–85.'
                              : cov.error === 'policy years exceed current age'
                              ? 'Policy years exceed current age.'
                              : 'Awaiting inputs.'}
                          </div>
                        ) : (
                          <>
                            <div style={{fontSize:9,color:C.t4,fontWeight:700,letterSpacing:1.2,textTransform:'uppercase',marginBottom:6}}>Estimated Coverage · Issued at {cov.issueAge}</div>
                            <div style={{display:'flex',alignItems:'baseline',gap:8,marginBottom:8,flexWrap:'wrap'}}>
                              <span style={{fontSize:22,fontWeight:800,color:C.t2,fontFamily:"'Instrument Sans',sans-serif"}}>${cov.low.toLocaleString()}</span>
                              <span style={{fontSize:14,color:C.t4}}>–</span>
                              <span style={{fontSize:22,fontWeight:800,color:amber,fontFamily:"'Instrument Sans',sans-serif"}}>${cov.high.toLocaleString()}</span>
                            </div>
                          </>
                        )}
                        <div style={{fontSize:10,color:C.t4,marginTop:10,fontStyle:'italic',lineHeight:1.5}}>
                          (If this is a ROP or Guaranteed Issue policy, actual coverage is likely much less.)
                        </div>
                        <div style={{fontSize:9,color:C.t4,marginTop:8,lineHeight:1.6,borderTop:`1px solid ${C.bd}`,paddingTop:8}}>
                          ⚠️ Educational estimate. For conversational guidance only — verify actual coverage with policy documents.
                        </div>
                      </div>
                      </>
                    );
                  })()}
                  <div style={{height:32}}/>
                </div>
              )}

              {/* ── CLIENT SHEET MOBILE ── */}
              {quoteMode==='sheet' && renderClientSheet(true)}

              {/* Bottom spacer — keeps button clear of fixed tab bar */}
              <div style={{height:32}}/>
            </div>
          )}

          {/* ── RESULTS TAB ── */}
          {mobileTab === 'results' && (
            <div ref={mobileResultsRef}>
              {/* Collapsed client summary bar + Edit (design 3d) */}
              {quoteMode!=='sheet' && ageOK && (
                <div style={{background:'#fbfbfa',border:'1px solid #eae9e6',borderRadius:10,padding:'10px 14px',marginBottom:12,display:'flex',alignItems:'center',gap:8,flexWrap:'wrap'}}>
                  <span style={{fontSize:13,fontWeight:600,color:'#191817'}}>
                    {quoteMode==='fe' ? (mode==='face'?fmtF(faceAmt):`$${budget}/mo`) :
                     quoteMode==='term' ? (termMode==='budget'?`$${termBudget}/mo`:fmtF(termFace)) :
                     quoteMode==='iul' ? (iulMode==='face'?`$${iulPremium}/mo`:`$${(iulFace/1000).toFixed(0)}k`) :
                     `$${cvMonthly||0}/mo`}
                  </span>
                  <span style={{fontSize:12.5,color:'#78746e'}}>{quoteMode==='term'?`· ${termLength}y `:''}· Age {age}{usState?` · ${usState}`:''} · {gender==='male'?'M':'F'} · {smoker?'Smoker':'NS'}</span>
                  {quoteMode==='fe' && <span style={{fontSize:11.5,fontWeight:600,color:'#4740c8'}}>UW: {TIER_INFO[uwTier].short}</span>}
                  {quoteMode==='term' && <span style={{fontSize:11.5,fontWeight:600,color:'#4740c8'}}>{HEALTH_CLASS_SHORT[termHealth]}</span>}
                  <button onClick={()=>setMobileTab('quote')} style={{marginLeft:'auto',padding:'6px 14px',minHeight:32,borderRadius:8,border:'1px solid #dedcd7',background:'#fff',color:'#191817',fontSize:12,fontWeight:600,cursor:'pointer',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif"}}>Edit</button>
                </div>
              )}
              {quoteMode==='sheet' ? (
                <div style={{display:'flex',flexDirection:'column',alignItems:'center',justifyContent:'center',paddingTop:80,gap:14,textAlign:'center'}}>
                  <div style={{fontFamily:"'Instrument Sans',sans-serif",fontSize:20,fontWeight:700,color:C.t4}}>The Client Sheet lives on the Quote tab</div>
                  <button onClick={()=>setMobileTab('quote')} style={{padding:'12px 26px',borderRadius:10,border:`1px solid ${C.bd2}`,background:C.bg2,color:C.t2,fontSize:13,fontWeight:600,cursor:'pointer',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif"}}>← Back to Quote</button>
                </div>
              ) : quoteMode==='iul' ? (
                /* ── MOBILE IUL RESULTS ── */
                !ageOK ? (
                  <div style={{display:'flex',flexDirection:'column',alignItems:'center',justifyContent:'center',paddingTop:80,gap:16,textAlign:'center'}}>
                    <div style={{fontSize:52,opacity:0.4}}>📋</div>
                    <div style={{fontFamily:"'Instrument Sans',sans-serif",fontSize:22,fontWeight:700,color:C.t4}}>Enter client info first</div>
                    <button onClick={()=>setMobileTab('quote')} style={{marginTop:8,padding:'13px 28px',borderRadius:10,border:`1px solid ${C.bd2}`,background:C.bg2,color:C.t2,fontSize:14,fontWeight:600,cursor:'pointer',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif"}}>← Back to Quote</button>
                  </div>
                ) : !iulQuoteResults || iulQuoteResults.length === 0 ? (
                  <div style={{display:'flex',flexDirection:'column',alignItems:'center',justifyContent:'center',paddingTop:80,gap:16,textAlign:'center'}}>
                    <div style={{fontSize:52,opacity:0.4}}>🔍</div>
                    <div style={{fontFamily:"'Instrument Sans',sans-serif",fontSize:20,fontWeight:700,color:C.t4}}>No IUL quotes for this profile</div>
                    <div style={{fontSize:12,color:C.t4,maxWidth:260,lineHeight:1.6}}>Try a different age, premium, or face amount.</div>
                  </div>
                ) : (
                  <>
                    <div style={{display:'flex',justifyContent:'space-between',alignItems:'center',padding:'8px 4px 12px',gap:8,fontSize:11,color:C.t3,borderBottom:`1px solid ${C.bd}`,marginBottom:14}}>
                      <div style={{display:'flex',gap:6,alignItems:'center',flexWrap:'wrap'}}>
                        <span style={{fontFamily:"'Instrument Sans',sans-serif",fontWeight:600,color:C.t0}}>
                          {iulMode==='face' ? `$${iulPremium}/mo` : `$${(iulFace/1000).toFixed(0)}k face`}
                        </span>
                        <span style={{color:C.t4}}>·</span>
                        <span>Age {age}</span>
                        <span style={{color:C.t4}}>·</span>
                        <span>{gender==='male'?'M':'F'} · {smoker?'Smoker':'NS'}</span>
                      </div>
                    </div>
                    <div style={{fontSize:10,fontWeight:700,letterSpacing:1.8,color:'#4a45d1',textTransform:'uppercase',marginBottom:8}}>
                      {iulMode==='face' ? 'Face Amounts' : 'Required Premiums'}
                    </div>
                    <div style={{display:'flex',flexDirection:'column',gap:10}}>
                      {iulQuoteResults.map(r => {
                        const RED='#b42318';
                        const limitBlocked = iulMode==='premium' && (r.floored || r.capped);
                        const limitKind = r.floored ? 'min' : 'max';
                        const problem = r.unavailable ? 'state' : limitBlocked ? 'limit' : null;
                        return (
                        <div key={r.id} className="qm-rise" style={{background:isDark?'#1E293B':'#FFFFFF',border:`1px solid ${problem==='limit'?RED+'55':C.bd2}`,borderLeft:`4px solid ${problem==='state'?C.bd2:problem==='limit'?RED:r.brand}`,borderRadius:10,padding:'12px 14px',display:'flex',alignItems:'center',gap:12,boxShadow:C.cardShadow,opacity:problem==='state'?0.5:1,filter:problem==='state'?'grayscale(0.6)':'none'}}>
                          <div style={{flexShrink:0}}><CarrierLogo carrierId={r.id} name={r.name} small={true}/></div>
                          <div style={{flex:1,minWidth:0}}>
                            <div style={{fontSize:14,fontWeight:700,color:C.t0,whiteSpace:'nowrap',overflow:'hidden',textOverflow:'ellipsis'}}>{r.name}</div>
                            <div style={{fontSize:10,color:C.t4,marginTop:2,whiteSpace:'nowrap',overflow:'hidden',textOverflow:'ellipsis'}}>{r.sub}</div>
                            {problem==='state' && <div style={{fontSize:9,color:C.t3,marginTop:3,fontWeight:600}}>⊘ {r.reason}</div>}
                            {problem==='limit' && <div style={{fontSize:9,color:RED,marginTop:3,fontWeight:700}}>{limitKind==='min'?'Min':'Max'} ${(r.face/1000).toFixed(0)}k · won't issue ${(r.targetFace/1000).toFixed(0)}k</div>}
                          </div>
                          <div style={{textAlign:'right',flexShrink:0}}>
                            {problem==='state' ? (
                              <div style={{fontSize:11,color:C.t3,fontWeight:700,letterSpacing:0.5,textTransform:'uppercase'}}>Unavailable</div>
                            ) : problem==='limit' ? (
                              <>
                                <div style={{fontFamily:"'Instrument Sans',sans-serif",fontSize:18,fontWeight:800,color:RED,lineHeight:1}}>N/A</div>
                                <div style={{fontSize:9,color:RED,marginTop:2,fontWeight:700}}>{limitKind==='min'?'MIN':'MAX'} ${(r.face/1000).toFixed(0)}K</div>
                              </>
                            ) : iulMode==='face' ? (
                              <>
                                <div style={{fontFamily:"'Instrument Sans',sans-serif",fontSize:20,fontWeight:800,color:C.t0,lineHeight:1}}>${(r.face/1000).toFixed(0)}k</div>
                                <div style={{fontSize:9,color:C.t4,marginTop:2}}>face · ${iulPremium}/mo</div>
                              </>
                            ) : (
                              <>
                                <div style={{fontFamily:"'Instrument Sans',sans-serif",fontSize:20,fontWeight:800,color:C.t0,lineHeight:1}}>${r.premium.toFixed(0)}<span style={{fontSize:11,color:C.t3,fontWeight:600}}>/mo</span></div>
                                <div style={{fontSize:9,color:C.t4,marginTop:2}}>{`for $${(r.face/1000).toFixed(0)}k face`}</div>
                              </>
                            )}
                          </div>
                        </div>
                        );
                      })}
                    </div>
                    <div style={{height:80}}/>
                  </>
                )
              ) : quoteMode==='term' ? (
                /* ── MOBILE TERM RESULTS ── */
                !ageOK ? (
                  <div style={{display:'flex',flexDirection:'column',alignItems:'center',justifyContent:'center',paddingTop:80,gap:16,textAlign:'center'}}>
                    <div style={{fontSize:52,opacity:0.4}}>📋</div>
                    <div style={{fontFamily:"'Instrument Sans',sans-serif",fontSize:22,fontWeight:700,color:C.t4}}>Enter client info first</div>
                    <button onClick={()=>setMobileTab('quote')} style={{marginTop:8,padding:'13px 28px',borderRadius:10,border:`1px solid ${C.bd2}`,background:C.bg2,color:C.t2,fontSize:14,fontWeight:600,cursor:'pointer',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif"}}>← Back to Quote</button>
                  </div>
                ) : !termResults || termResults.length === 0 ? (
                  <div style={{display:'flex',flexDirection:'column',alignItems:'center',justifyContent:'center',paddingTop:80,gap:16,textAlign:'center'}}>
                    <div style={{fontSize:52,opacity:0.4}}>🔍</div>
                    <div style={{fontFamily:"'Instrument Sans',sans-serif",fontSize:20,fontWeight:700,color:C.t4}}>No quotes for this profile</div>
                    <div style={{fontSize:12,color:C.t4,maxWidth:260,lineHeight:1.6}}>Try a different age, face amount, or term length. Some carriers don't offer 25/35/40-year term or above age 65.</div>
                  </div>
                ) : (
                  <>
                    {termRec.recommended === 'decline' && (
                      <div style={{background:'linear-gradient(135deg, rgba(180,35,24,0.18), rgba(180,35,24,0.08))',border:'1.5px solid rgba(180,35,24,0.5)',borderRadius:12,padding:'14px 16px',marginBottom:12,display:'flex',gap:12,alignItems:'flex-start',boxShadow:'0 2px 12px rgba(180,35,24,0.15)'}}>
                        <div style={{fontSize:26,lineHeight:1,flexShrink:0}}>⚠</div>
                        <div style={{flex:1,minWidth:0}}>
                          <div style={{fontSize:15,fontWeight:800,color:'#FCA5A5',letterSpacing:0.2,marginBottom:4}}>Profile may not qualify for term</div>
                          <div style={{fontSize:11,color:C.t3,lineHeight:1.55}}>{termRec.reasons.join(' · ')}. Quotes below are <strong>best-case estimates only</strong> — verify with carrier underwriting before promising rates.</div>
                        </div>
                      </div>
                    )}
                    {/* Compatible carriers banner — mobile */}
                    {termCompat && termCompat.excluded.length > 0 && (
                      <div style={{background:isDark?'rgba(59,130,246,0.06)':'rgba(59,130,246,0.04)',border:`1px solid ${isDark?'rgba(59,130,246,0.25)':'rgba(59,130,246,0.2)'}`,borderRadius:10,marginBottom:12,fontSize:12,overflow:'hidden'}}>
                        <button onClick={()=>setTermCompatOpen(v=>!v)} style={{
                          width:'100%',background:'transparent',border:'none',cursor:'pointer',
                          padding:'10px 12px',display:'flex',alignItems:'center',justifyContent:'space-between',gap:8,
                          fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif",textAlign:'left'
                        }}>
                          <div style={{display:'flex',alignItems:'center',gap:8,minWidth:0}}>
                            <span style={{fontSize:12,color:'#60A5FA',transition:'transform 0.18s',transform:termCompatOpen?'rotate(90deg)':'rotate(0deg)',flexShrink:0}}>▸</span>
                            <span style={{color:C.t2,fontSize:11,lineHeight:1.4}}>
                              <strong style={{color:C.t0}}>{termCompat.writers}/{termCompat.total}</strong> writing at age {age} · <span style={{color:C.t4}}>{termCompat.excluded.length} excluded</span>
                            </span>
                          </div>
                          <span style={{fontSize:10,color:C.t4,flexShrink:0}}>{termCompatOpen?'Hide':'See'}</span>
                        </button>
                        {termCompatOpen && (
                          <div style={{padding:'4px 12px 10px',display:'flex',flexDirection:'column',gap:4}}>
                            {termCompat.excluded.map(e => (
                              <div key={e.name+'·'+e.sub} style={{display:'flex',justifyContent:'space-between',gap:8,fontSize:10,padding:'5px 8px',background:C.bg2,borderRadius:5,border:`1px solid ${C.bd}`}}>
                                <span style={{color:C.t2,minWidth:0,overflow:'hidden',textOverflow:'ellipsis',whiteSpace:'nowrap'}}><strong style={{color:C.t1}}>{e.name}</strong> · <span style={{color:C.t4}}>{e.sub}</span></span>
                                <span style={{color:C.t4,fontFamily:"'Instrument Sans',sans-serif",flexShrink:0}}>{e.reason}</span>
                              </div>
                            ))}
                          </div>
                        )}
                      </div>
                    )}
                    <div style={{display:'flex',flexDirection:'column',gap:8, opacity: termRec.recommended === 'decline' ? 0.55 : 1, filter: termRec.recommended === 'decline' ? 'grayscale(40%)' : 'none', transition:'opacity 0.2s, filter 0.2s'}}>
                      {termResults.map(r => {
                        const brandColor = CARRIER_META[r.id]?.brand || r.brand || '#4a45d1';
                        return (
                          <div key={r.id} className="qm-rise" style={{background:'#fff',border:'1px solid #eae9e6',borderRadius:10,padding:'12px 14px',display:'flex',alignItems:'center',gap:12}}>
                            <CarrierLogo carrierId={r.id} name={r.name} small={true}/>
                            <div style={{flex:1,minWidth:0}}>
                              <div style={{fontSize:14.5,fontWeight:600,color:'#191817',whiteSpace:'nowrap',overflow:'hidden',textOverflow:'ellipsis'}}>{r.name}</div>
                              <div style={{fontSize:12,color:'#78746e',marginTop:1,whiteSpace:'nowrap',overflow:'hidden',textOverflow:'ellipsis'}}>{r.sub}{r.tierUsed && r.tierUsed !== r.sub && r.tierUsed !== 'Approved' ? ` · ${r.tierUsed}` : ''}</div>
                              <div style={{marginTop:4}}><UWBadge uwType={r.uwType} productName={r.product} tier={r.tierUsed} small={true}/></div>
                            </div>
                            <div style={{textAlign:'right',flexShrink:0}}>
                              {termMode==='budget' ? (
                                <>
                                  <div style={{fontSize:17,fontWeight:650,color:'#4740c8',letterSpacing:'-0.01em',lineHeight:1.25}}>{fmtF(r.face)}</div>
                                  <div style={{fontSize:11,color:'#b5b1a8',marginTop:1}}>${r.prem.toFixed(0)}/mo · {r.termLen}y</div>
                                </>
                              ) : (
                                <>
                                  <div style={{fontSize:17,fontWeight:650,color:'#191817',letterSpacing:'-0.01em',lineHeight:1.25}}>${r.prem.toFixed(2)}<span style={{fontSize:11,fontWeight:400,color:'#a09c94'}}> /mo</span></div>
                                  <div style={{fontSize:11,color:'#b5b1a8',marginTop:1}}>${(r.prem*12).toFixed(0)} /yr · {r.termLen}y</div>
                                </>
                              )}
                            </div>
                          </div>
                        );
                      })}
                    </div>
                  </>
                )
              ) : quoteMode==='cv' ? (
                /* ── MOBILE CV RESULTS — the estimator, never FEX quotes ── */
                (ageOK && cvMonthly && cvPolicyYrs && Number(cvMonthly)>0 && Number(cvPolicyYrs)>0) ? (
                  <div style={{display:'flex',flexDirection:'column'}}>
                    <CashValueProjection monthlyPremium={Number(cvMonthly)} policyYears={Number(cvPolicyYrs)} issueAge={Math.max(0, ageNum - Number(cvPolicyYrs))} C={C} isDark={isDark}/>
                    <CoverageEstimate monthlyPremium={Number(cvMonthly)} policyYears={Number(cvPolicyYrs)} currentAge={ageNum} gender={gender} C={C} isDark={isDark}/>
                  </div>
                ) : (
                  <div style={{display:'flex',flexDirection:'column',alignItems:'center',justifyContent:'center',paddingTop:80,gap:16,textAlign:'center'}}>
                    <div style={{fontFamily:"'Instrument Sans',sans-serif",fontSize:20,fontWeight:700,color:C.t4}}>Enter policy info first</div>
                    <div style={{fontSize:13,color:C.t4,maxWidth:280,lineHeight:1.6}}>Age, monthly premium, and years in-force drive the cash value estimate.</div>
                    <button onClick={()=>setMobileTab('quote')} style={{marginTop:8,padding:'13px 28px',borderRadius:9,border:'1px solid #dedcd7',background:'#fff',color:'#191817',fontSize:14,fontWeight:600,cursor:'pointer',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif"}}>← Back to Quote</button>
                  </div>
                )
              ) : !hasQuoted ? (
                <div style={{display:'flex',flexDirection:'column',alignItems:'center',justifyContent:'center',paddingTop:80,gap:16,textAlign:'center'}}>
                  <div style={{fontSize:52,opacity:0.4}}>📋</div>
                  <div style={{fontFamily:"'Instrument Sans',sans-serif",fontSize:22,fontWeight:700,color:C.t4}}>Fill in client info first</div>
                  <button onClick={()=>setMobileTab('quote')} style={{marginTop:8,padding:'13px 28px',borderRadius:10,border:`1px solid ${C.bd2}`,background:C.bg2,color:C.t2,fontSize:14,fontWeight:600,cursor:'pointer',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif"}}>
                    ← Back to Quote
                  </button>
                </div>
              ) : (
                <>
                  {/* Cards — single column full width */}
                  <div style={{display:'flex',flexDirection:'column',gap:8}}>
                    {results && results.map((r,idx)=>{
                      const isBest = false;
                      const isGhost = !r.prem;
                      const brandColor = CARRIER_META[r.id]?.brand || C.bd2;
                      const premColor = brandColor;
                      if(gsbOn){
                        const gsbBrand = brandColor;
                        return(
                          <div key={r.id} style={{
                            background:r.anyPrem?C.bg3:C.bg2,
                            border:`1px solid ${r.anyPrem?C.bd2:C.bd}`,
                            borderTop:`3px solid ${r.anyPrem?gsbBrand:C.bd}`,
                            borderRadius:12,padding:'14px 14px 12px',
                            opacity:r.anyPrem?1:0.35,
                          }}>
                            <div style={{marginBottom:12}}>
                              <div style={{fontSize:17,fontWeight:700,color:C.t0}}>{r.name}</div>
                              <div style={{fontSize:11,color:C.t4,marginTop:2}}>{r.sub}</div>
                            </div>
                            <div style={{display:'flex',gap:8}}>
                              {GSB.map(g=>{
                                const tier=r.tiers?.[g.key];
                                const hasPrem=tier?.prem!=null;
                                const darkMetal=g.key==='gold'?'#4a45d1':g.key==='silver'?'#94A3B8':'#CD7F32';
                                const lightLabel=g.key==='gold'?'#855D10':g.key==='silver'?'#475569':'#92400E';
                                const isGoldKey=g.key==='gold';
                                return(
                                  <div key={g.key} style={{
                                    flex:1,
                                    background: g.key==='silver'
                                      ? (isDark?'rgba(74,69,209,0.04)':'rgba(74,69,209,0.05)')
                                      : (isDark?(hasPrem?C.bg1:C.bg0):'#FFFFFF'),
                                    border:`1px solid ${isDark?(hasPrem?darkMetal+'55':C.bd):'#E2E8F0'}`,
                                    borderTop: isDark
                                      ? `2px solid ${hasPrem?darkMetal:C.bd}`
                                      : (isGoldKey?`4px solid #4a45d1`:`2px solid ${hasPrem?(g.key==='silver'?'#94A3B8':'#C2700A'):C.bd}`),
                                    borderRadius:9,padding:'10px 8px',textAlign:'center',
                                    opacity:hasPrem?1:0.35,
                                    boxShadow:!isDark&&hasPrem&&isGoldKey?'0 2px 8px rgba(74,69,209,0.18)':'none'
                                  }}>
                                    <div style={{
                                      fontSize:9,fontWeight:700,marginBottom:3,letterSpacing:1,textTransform:'uppercase',
                                      color:isDark?darkMetal:lightLabel
                                    }}>{g.label}</div>
                                    <div style={{fontSize:11,color:C.t3,marginBottom:4}}>{fmtF(tier?.face||0)}</div>
                                    {hasPrem?(
                                      <div style={{
                                        fontSize:16,fontWeight:800,
                                        color:isDark?darkMetal:'#0F172A',
                                        fontFamily:"'Instrument Sans',sans-serif"
                                      }}>{fmt$(tier.prem)}<span style={{fontSize:10,fontWeight:400,color:C.t4}}>/mo</span></div>
                                    ):(
                                      <div style={{fontSize:16,color:C.t4}}>—</div>
                                    )}
                                  </div>
                                );
                              })}
                            </div>
                            {r.anyPrem && CARRIER_META[r.id]?.eapp && (
                              <a href={CARRIER_META[r.id].eapp} target="_blank" rel="noopener noreferrer" style={{
                                display:'block',marginTop:10,padding:'14px 0',borderRadius:8,textAlign:'center',
                                background:'transparent',border:`1.5px solid ${isDark?'#374151':'#D0CDBE'}`,
                                color:isDark?'#94A3B8':'#3E4A59',fontSize:13,lineHeight:'20px',fontWeight:600,textDecoration:'none',
                                boxShadow:'none'
                              }}>e-App →</a>
                            )}
                          </div>
                        );
                      }
                      // Compact quote row (design 3d): 38px logo tile, name +
                      // product + small tier pill, right-aligned price.
                      return(
                        <div key={r.id} className="qm-rise" style={{
                          background:'#fff',border:'1px solid #eae9e6',borderRadius:10,
                          padding:'12px 14px',display:'flex',alignItems:'center',gap:12,
                          opacity:isGhost?0.55:r.capped?0.7:1,
                          filter:isGhost?'grayscale(1)':'none'
                        }}>
                          <CarrierLogo carrierId={r.id} name={r.name} small={true}/>
                          <div style={{flex:1,minWidth:0}}>
                            <div style={{fontSize:14.5,fontWeight:600,color:'#191817',whiteSpace:'nowrap',overflow:'hidden',textOverflow:'ellipsis'}}>{r.name}</div>
                            <div style={{fontSize:12,color:'#78746e',marginTop:1,whiteSpace:'nowrap',overflow:'hidden',textOverflow:'ellipsis'}}>{r.sub}{isGhost?'':` · ${fmtF(r.face||0)}`}{r.capped?' · capped':''}</div>
                            {!isGhost && <div style={{marginTop:4,display:'flex',gap:5,alignItems:'center'}}><TierBadge tier={r.activeTier} productName={r.productName}/></div>}
                            {isGhost && <div style={{fontSize:11.5,color:'#a09c94',fontStyle:'italic',marginTop:2}}>{r.reason}</div>}
                          </div>
                          {!isGhost && (
                            <div style={{textAlign:'right',flexShrink:0}}>
                              <div style={{fontSize:17,fontWeight:650,color:mode==='budget'?'#4740c8':'#191817',letterSpacing:'-0.01em',lineHeight:1.25}}>
                                {mode==='budget'?fmtF(r.face||0):fmt$(r.prem)}{mode!=='budget'&&<span style={{fontSize:11,fontWeight:400,color:'#a09c94'}}> /mo</span>}
                              </div>
                              <div style={{fontSize:11,color:'#b5b1a8',marginTop:1}}>{mode==='budget'?`${fmt$(r.prem)} /mo`:`$${((r.prem??0)*12).toFixed(0)} /yr`}</div>
                              {CARRIER_META[r.id]?.eapp && (
                                <a href={CARRIER_META[r.id].eapp} target="_blank" rel="noopener noreferrer"
                                  onClick={()=>track('e-App Click',{carrier:r.id,variant:'mobile-row'})}
                                  style={{display:'inline-block',marginTop:5,fontSize:12,fontWeight:600,color:'#4a45d1',textDecoration:'none',padding:'4px 2px'}}>e-App →</a>
                              )}
                            </div>
                          )}
                        </div>
                      );
                    })}
                  </div>
                </>
              )}
            </div>
          )}
        </div>

        {/* ── BOTTOM TAB BAR ── */}
        <div style={{
          position:'fixed',bottom:0,left:0,right:0,
          background:'#fff',borderTop:'1px solid #eae9e6',
          display:'flex',zIndex:100,
          paddingBottom:'env(safe-area-inset-bottom,0px)'
        }}>
          <button onClick={()=>setMobileTab('quote')} style={{
            flex:1,padding:'13px 0 11px',minHeight:48,border:'none',cursor:'pointer',
            background:'transparent',
            color:mobileTab==='quote'?'#191817':'#a09c94',
            fontSize:12,fontWeight:mobileTab==='quote'?600:400,
            fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif",
            borderTop:mobileTab==='quote'?'2px solid #191817':'2px solid transparent'
          }}>
            Quote
          </button>
          <button onClick={()=>{setMobileTab('results');setTimeout(()=>window.scrollTo({top:0,behavior:'instant'}),0);}} style={{
            flex:1,padding:'13px 0 11px',minHeight:48,border:'none',cursor:'pointer',
            background:'transparent',
            color:mobileTab==='results'?'#191817':'#a09c94',
            fontSize:12,fontWeight:mobileTab==='results'?600:400,
            fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif",
            borderTop:mobileTab==='results'?'2px solid #191817':'2px solid transparent',
            position:'relative'
          }}>
            Results
            {hasQuoted&&results&&results.filter(r=>r.prem!=null).length>0&&(
              <span style={{position:'absolute',top:8,right:'26%',background:'#4a45d1',color:'#fff',fontSize:10,fontWeight:600,borderRadius:10,padding:'1px 6px',minWidth:16,textAlign:'center'}}>
                {results.filter(r=>r.prem!=null).length}
              </span>
            )}
          </button>

        </div>

        {/* ── CARRIER PANEL (mobile) ── */}
        {/* ── CONTACT MODAL ── */}
      {showContact&&(
        <div onClick={()=>{if(!contactSending)setShowContact(false);}} style={{position:'fixed',inset:0,zIndex:400,background:'rgba(0,0,0,0.65)',display:'flex',alignItems:'center',justifyContent:'center',padding:'16px'}}>
          <div onClick={e=>e.stopPropagation()} style={{background:C.bg1,borderRadius:16,width:480,maxWidth:'100%',maxHeight:'90vh',overflowY:'auto',boxShadow:'0 24px 80px rgba(0,0,0,0.5)'}}>
            <div style={{display:'flex',alignItems:'center',justifyContent:'space-between',padding:'18px 20px 0'}}>
              <div style={{fontSize:17,fontWeight:700,color:C.t1}}>Contact Quotemarko</div>
              <button onClick={()=>setShowContact(false)} style={{background:'transparent',border:'none',color:C.t4,fontSize:22,cursor:'pointer',lineHeight:1}}>✕</button>
            </div>
            {contactSent?(
              <div style={{padding:'32px 24px',textAlign:'center'}}>
                <div style={{fontSize:40,marginBottom:12}}>✅</div>
                <div style={{fontSize:16,fontWeight:700,color:C.t1,marginBottom:8}}>Message Sent!</div>
                <div style={{fontSize:13,color:C.t3,marginBottom:24}}>We'll get back to you shortly.</div>
                <button onClick={()=>{setShowContact(false);setContactSent(false);setContactForm({name:'',email:'',company:'',message:''}); }} style={{padding:'11px 28px',borderRadius:8,border:'none',background:C.gold,color:C.bg0,fontWeight:700,fontSize:14,cursor:'pointer',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif"}}>Close</button>
              </div>
            ):(
              <div style={{padding:'16px 20px 24px',display:'flex',flexDirection:'column',gap:14}}>
                {/* Type selector */}
                <div>
                  <div style={{fontSize:11,fontWeight:700,letterSpacing:1.2,color:C.t4,textTransform:'uppercase',marginBottom:10}}>Inquiry Type</div>
                  {[
                    {id:'carrier',    emoji:'📋', label:'Submit a New Carrier', desc:'Request a carrier be added to Quotemarko'},
                    {id:'suggestion', emoji:'💡', label:'General Suggestion',    desc:'Feature ideas, feedback, or improvements'},
                    {id:'partnership',emoji:'🤝', label:'Partnership / Whitelabel', desc:'Agency inquiries, 5+ seats, or white-label licensing'},
                  ].map(opt=>(
                    <div key={opt.id} onClick={()=>setContactType(opt.id)}
                      style={{display:'flex',alignItems:'center',gap:12,padding:'11px 14px',borderRadius:9,border:`1px solid ${contactType===opt.id?C.gold+'88':C.bd}`,background:contactType===opt.id?C.gold+'0D':'transparent',cursor:'pointer',marginBottom:7}}>
                      <span style={{fontSize:18,flexShrink:0}}>{opt.emoji}</span>
                      <div>
                        <div style={{fontSize:13,fontWeight:600,color:contactType===opt.id?C.gold:C.t1}}>{opt.label}</div>
                        <div style={{fontSize:11,color:C.t4}}>{opt.desc}</div>
                      </div>
                      {contactType===opt.id&&<span style={{marginLeft:'auto',color:C.gold,fontSize:14}}>✓</span>}
                    </div>
                  ))}
                </div>
                {/* Fields */}
                {[
                  {key:'name',    label:'Your Name',    ph:'Full name',          req:true},
                  {key:'email',   label:'Email',        ph:'your@email.com',     req:true},
                  {key:'company', label:'Company / Agency', ph:'Optional',      req:false},
                ].map(f=>(
                  <div key={f.key}>
                    <div style={{fontSize:12,color:C.t4,marginBottom:5}}>{f.label}{f.req&&<span style={{color:'#b42318'}}> *</span>}</div>
                    <input value={contactForm[f.key]} onChange={e=>setContactForm(p=>({...p,[f.key]:e.target.value}))}
                      placeholder={f.ph}
                      style={{width:'100%',boxSizing:'border-box',background:C.bg3,border:`1px solid ${C.bd2}`,borderRadius:8,color:C.t1,fontSize:14,padding:'11px 12px',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif",outline:'none'}}/>
                  </div>
                ))}
                <div>
                  <div style={{fontSize:12,color:C.t4,marginBottom:5}}>Message <span style={{color:'#b42318'}}>*</span></div>
                  <textarea value={contactForm.message} onChange={e=>setContactForm(p=>({...p,message:e.target.value}))}
                    placeholder={contactType==='carrier'?'Carrier name, website, and any relevant product details…':(contactType==='suggestion'?'Describe your idea or feedback…':'Tell us about your agency, team size, and what you are looking for…')}
                    rows={4}
                    style={{width:'100%',boxSizing:'border-box',background:C.bg3,border:`1px solid ${C.bd2}`,borderRadius:8,color:C.t1,fontSize:14,padding:'11px 12px',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif",outline:'none',resize:'vertical'}}/>
                </div>
                {contactErr&&<div style={{fontSize:12,color:'#b42318',padding:'8px 10px',background:'rgba(180,35,24,0.08)',borderRadius:7}}>{contactErr}</div>}
                <button onClick={sendContact} disabled={contactSending||!contactForm.name||!contactForm.email||!contactForm.message}
                  style={{padding:'13px',borderRadius:9,border:'none',background:C.gold,color:C.bg0,fontWeight:700,fontSize:14,cursor:'pointer',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif",opacity:(contactSending||!contactForm.name||!contactForm.email||!contactForm.message)?0.5:1}}>
                  {contactSending?'Sending…':'Send Message'}
                </button>
              </div>
            )}
          </div>
        </div>
      )}

      {/* ── MOBILE PROFILE SLIDE-UP SHEET ── */}
      {mobileTab2==='profile'&&(
        <div style={{position:'fixed',inset:0,zIndex:200,display:'flex',flexDirection:'column'}} onClick={()=>setMobileTab2(null)}>
          <div style={{flex:1,background:'rgba(0,0,0,0.5)'}}/>
          <div onClick={e=>e.stopPropagation()} style={{background:C.bg1,borderRadius:'18px 18px 0 0',maxHeight:'88vh',overflowY:'auto',paddingBottom:'env(safe-area-inset-bottom,16px)'}}>
            <div style={{display:'flex',alignItems:'center',justifyContent:'space-between',padding:'16px 18px 0'}}>
              <div style={{fontSize:16,fontWeight:700,color:C.t1}}>My Profile</div>
              <button onClick={()=>setMobileTab2(null)} style={{background:'transparent',border:'none',color:C.t4,fontSize:22,cursor:'pointer',lineHeight:1}}>✕</button>
            </div>
            {/* ══ PROFILE PANEL CONTENT ══ */}
        {(()=>{
          const inp={width:'100%',boxSizing:'border-box',background:C.bg3,border:`1px solid ${C.bd2}`,borderRadius:8,color:C.t1,fontSize:14,padding:'11px 12px',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif",outline:'none'};
          return(
          <div style={{padding:'16px 16px 32px',display:'flex',flexDirection:'column',gap:20}}>

            {/* Account Info */}
            <div style={{background:C.bg2,borderRadius:12,padding:16,border:`1px solid ${C.bd}`}}>
              <div style={{fontSize:11,fontWeight:700,letterSpacing:1.5,color:C.t4,textTransform:'uppercase',marginBottom:14}}>Account</div>
              <div style={{fontSize:12,color:C.t4,marginBottom:4}}>Email</div>
              <div style={{fontSize:14,color:C.t2,marginBottom:14,padding:'10px 12px',background:C.bg3,borderRadius:8,border:`1px solid ${C.bd}`}}>{session?.user?.email||'—'}</div>
              <div style={{fontSize:12,color:C.t4,marginBottom:6}}>Display Name</div>
              <input value={profileName} onChange={e=>setProfileName(e.target.value)} placeholder="Your name (optional)" style={inp}/>
              <button onClick={saveProfile} disabled={profileSaving} style={{marginTop:10,width:'100%',padding:'11px',borderRadius:8,border:'none',background:profileSaved?'#16A34A':C.gold,color:profileSaved?'#fff':C.bg0,fontWeight:700,fontSize:13,cursor:'pointer',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif",transition:'background 0.2s'}}>
                {profileSaving?'Saving…':profileSaved?'✓ Saved!':'Save Name'}
              </button>
            </div>

            {/* Change Password */}
            <div style={{background:C.bg2,borderRadius:12,padding:16,border:`1px solid ${C.bd}`}}>
              <div style={{fontSize:11,fontWeight:700,letterSpacing:1.5,color:C.t4,textTransform:'uppercase',marginBottom:14}}>Change Password</div>
              {['New Password','Confirm Password'].map((lbl,i)=>{
                const key=i===0?'next':'confirm';
                return(<div key={lbl} style={{marginBottom:10}}>
                  <div style={{fontSize:12,color:C.t4,marginBottom:5}}>{lbl}</div>
                  <input type="password" value={pwForm[key]} onChange={e=>setPwForm(p=>({...p,[key]:e.target.value}))} placeholder={lbl} style={inp}/>
                </div>);
              })}
              {pwMsg&&<div style={{fontSize:12,color:pwMsg.err?'#b42318':'#177452',marginBottom:8,padding:'7px 10px',background:pwMsg.err?'rgba(180,35,24,0.08)':'rgba(23,116,82,0.08)',borderRadius:7}}>{pwMsg.msg}</div>}
              <button onClick={changePassword} disabled={pwChanging||!pwForm.next} style={{width:'100%',padding:'11px',borderRadius:8,border:'none',background:C.gold,color:C.bg0,fontWeight:700,fontSize:13,cursor:'pointer',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif",opacity:pwForm.next?1:0.5}}>
                {pwChanging?'Updating…':'Update Password'}
              </button>
            </div>

            {/* Carrier Preferences */}
            <div style={{background:C.bg2,borderRadius:12,padding:16,border:`1px solid ${C.bd}`}}>
              <div style={{fontSize:11,fontWeight:700,letterSpacing:1.5,color:C.t4,textTransform:'uppercase',marginBottom:4}}>My Carriers</div>
              <div style={{fontSize:12,color:C.t4,marginBottom:14}}>Toggle which carriers appear in your quote results. Saved to your profile.</div>
              <div style={{display:'flex',flexDirection:'column',gap:8}}>
                {carriers.filter(c => !c.linkedId && !FORCE_DISABLED.has(c.id)).map(c=>{
                  const brand=CARRIER_META[c.id]?.brand||C.bd2;
                  return(
                  <div key={c.id} onClick={()=>setCarriers(p=>{const t=p.find(x=>x.id===c.id);if(!t)return p;const ne=!t.enabled;return p.map(x=>x.id===c.id||x.linkedId===c.id?{...x,enabled:ne}:x);})}
                    style={{display:'flex',alignItems:'center',gap:12,padding:'10px 12px',borderRadius:9,border:`1px solid ${c.enabled?brand+'66':C.bd}`,background:c.enabled?brand+'0D':'transparent',cursor:'pointer',transition:'all 0.15s'}}>
                    <div style={{width:18,height:18,borderRadius:5,border:`2px solid ${c.enabled?brand:C.bd2}`,background:c.enabled?brand:'transparent',display:'flex',alignItems:'center',justifyContent:'center',flexShrink:0,transition:'all 0.15s'}}>
                      {c.enabled&&<span style={{color:'#fff',fontSize:11,fontWeight:700,lineHeight:1}}>✓</span>}
                    </div>
                    <div style={{flex:1,minWidth:0}}>
                      <div style={{fontSize:13,fontWeight:600,color:c.enabled?C.t1:C.t3}}>{c.name}</div>
                      <div style={{fontSize:11,color:C.t4}}>{c.sub}</div>
                    </div>
                    <div style={{width:8,height:8,borderRadius:'50%',background:c.enabled?brand:'#334155',flexShrink:0}}/>
                  </div>);
                })}
              </div>
              <button onClick={saveCarrierPrefs} style={{marginTop:14,width:'100%',padding:'12px',borderRadius:8,border:'none',background:carriersSaved?'#16A34A':C.gold,color:carriersSaved?'#fff':C.bg0,fontWeight:700,fontSize:13,cursor:'pointer',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif",transition:'background 0.2s'}}>
                {carriersSaved?'✓ Carriers Saved!':'Save Carrier Preferences'}
              </button>
            </div>

            {/* Sign Out */}
            <button onClick={openCustomerPortal} disabled={portalLoading} style={{padding:'12px',borderRadius:10,border:`1px solid ${C.bd2}`,background:'transparent',color:C.t1,fontWeight:600,fontSize:13,cursor:portalLoading?'not-allowed':'pointer',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif",opacity:portalLoading?0.6:1,marginBottom:8}}>
              {portalLoading ? 'Opening…' : 'Manage Subscription'}
            </button>
            <button onClick={signOut} style={{padding:'13px',borderRadius:10,border:`1px solid ${C.bd2}`,background:'transparent',color:'#b42318',fontWeight:600,fontSize:14,cursor:'pointer',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif"}}>
              Sign Out
            </button>

          </div>
          );
        })()}
          </div>
        </div>
      )}

      {carrierPanel&&(
          <div style={{position:'fixed',inset:0,background:'rgba(0,0,0,0.7)',zIndex:200,display:'flex',alignItems:'flex-end'}} onClick={()=>setCarrierPanel(false)}>
            <div style={{background:C.bg1,borderRadius:'16px 16px 0 0',width:'100%',maxHeight:'80vh',overflow:'hidden',display:'flex',flexDirection:'column'}} onClick={e=>e.stopPropagation()}>
              <div style={{padding:'16px 18px 12px',borderBottom:`1px solid ${C.bd}`,display:'flex',justifyContent:'space-between',alignItems:'center'}}>
                <div style={{fontSize:15,fontWeight:700,color:C.t0}}>Active Carriers</div>
                <button onClick={()=>setCarrierPanel(false)} style={{background:'transparent',border:'none',color:C.t3,fontSize:22,cursor:'pointer',lineHeight:1}}>×</button>
              </div>
              {/* Tab bar */}
              <div style={{display:'flex',borderBottom:`1px solid ${C.bd}`,background:C.bg2}}>
                {[['fex','🏛️ FEX'],['term','⏱️ Term'],['iul','📈 IUL']].map(([tab,label])=>(
                  <button key={tab} onClick={()=>setCarrierPanelTab(tab)} style={{
                    flex:1,padding:'10px 0',border:'none',background:'transparent',
                    color:carrierPanelTab===tab?C.t0:C.t4,fontSize:12,fontWeight:carrierPanelTab===tab?700:500,
                    cursor:'pointer',borderBottom:carrierPanelTab===tab?`2px solid ${C.blue}`:'2px solid transparent',
                    fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif",transition:'all 0.15s'
                  }}>{label}</button>
                ))}
              </div>
              <div style={{overflowY:'auto',padding:'12px 16px',flex:1}}>
                {carriers
                  .filter(c => !c.linkedId && (
                    carrierPanelTab==='fex'  ? !c.termOnly && !c.iulOnly :
                    carrierPanelTab==='term' ? c.termOnly :
                    carrierPanelTab==='iul'  ? c.iulOnly : false
                  ))
                  .sort((a,b)=>a.name.localeCompare(b.name))
                  .map(c=>{
                  const active=c.enabled;
                  return(
                    <div key={c.id} style={{display:'flex',alignItems:'center',gap:12,padding:'12px 0',borderBottom:`1px solid ${C.bd}`}} onClick={()=>toggleCarrier(c.id)}>
                      <div style={{width:36,height:36,borderRadius:8,background:C.bg3,display:'flex',alignItems:'center',justifyContent:'center',fontSize:10,fontWeight:700,color:active?CARRIER_META[c.id]?.brand||C.t2:C.t4,border:`1px solid ${active?CARRIER_META[c.id]?.brand+'44'||C.bd:C.bd}`}}>{c.abbr}</div>
                      <div style={{flex:1}}>
                        <div style={{fontSize:14,fontWeight:600,color:active?C.t0:C.t4}}>{c.name}</div>
                        <div style={{fontSize:11,color:C.t4,marginTop:1}}>{c.sub}</div>
                      </div>
                      <div style={{width:44,height:26,borderRadius:13,background:active?C.blue:'#1E3A5A',position:'relative',flexShrink:0}}>
                        <div style={{position:'absolute',top:4,left:active?22:4,width:16,height:16,borderRadius:'50%',background:'white',transition:'left 0.2s'}}/>
                      </div>
                    </div>
                  );
                })}

                {/* ── Request a Carrier (mobile) ── */}
                <div style={{marginTop:20,paddingTop:16,borderTop:`1px solid ${C.bd}`}}>
                  <div style={{fontSize:11,fontWeight:700,color:C.t4,letterSpacing:1.5,textTransform:'uppercase',marginBottom:4}}>Request a Carrier</div>
                  <div style={{fontSize:12,color:C.t3,marginBottom:14,lineHeight:1.6}}>Don't see your carrier? Submit a request and we'll add them.</div>
                  {reqSent?(
                    <div style={{background:'rgba(23,116,82,0.1)',border:'1px solid rgba(23,116,82,0.3)',borderRadius:10,padding:'14px 16px',textAlign:'center'}}>
                      <div style={{fontSize:22,marginBottom:6}}>✓</div>
                      <div style={{fontSize:14,color:C.green,fontWeight:600}}>Request submitted</div>
                      <div style={{fontSize:12,color:C.t3,marginTop:3}}>We'll review and add within 48 hrs</div>
                      <button onClick={()=>{setReqSent(false);setReqForm({name:'',state:'',notes:''}); }} style={{marginTop:10,padding:'8px 16px',borderRadius:8,border:`1px solid ${C.bd}`,background:C.bg2,color:C.t3,fontSize:12,cursor:'pointer',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif"}}>Submit another</button>
                    </div>
                  ):(
                    <div style={{display:'flex',flexDirection:'column',gap:10}}>
                      <div>
                        <div style={{fontSize:12,color:C.t3,marginBottom:5}}>Carrier name *</div>
                        <input placeholder="e.g. Mutual of Omaha" value={reqForm.name}
                          onChange={e=>setReqForm(p=>({...p,name:e.target.value}))}
                          style={{background:C.bg3,border:`1px solid ${C.bd}`,color:C.t1,borderRadius:9,padding:'11px 13px',fontSize:14,width:'100%',outline:'none',boxSizing:'border-box',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif"}}/>
                      </div>
                      <div>
                        <div style={{fontSize:12,color:C.t3,marginBottom:5}}>State(s) needed</div>
                        <input placeholder="e.g. FL, TX, GA" value={reqForm.state}
                          onChange={e=>setReqForm(p=>({...p,state:e.target.value}))}
                          style={{background:C.bg3,border:`1px solid ${C.bd}`,color:C.t1,borderRadius:9,padding:'11px 13px',fontSize:14,width:'100%',outline:'none',boxSizing:'border-box',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif"}}/>
                      </div>
                      <div>
                        <div style={{fontSize:12,color:C.t3,marginBottom:5}}>Notes</div>
                        <textarea placeholder="Product type, tier, special requirements…" value={reqForm.notes}
                          onChange={e=>setReqForm(p=>({...p,notes:e.target.value}))}
                          rows={3} style={{background:C.bg3,border:`1px solid ${C.bd}`,color:C.t1,borderRadius:9,padding:'11px 13px',fontSize:14,width:'100%',outline:'none',resize:'none',boxSizing:'border-box',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif"}}/>
                      </div>
                      <button
                        onClick={()=>{if(reqForm.name.trim())setReqSent(true);}}
                        style={{padding:'13px 0',borderRadius:10,border:'none',background:reqForm.name.trim()?C.blue:'#1E3A5A',color:reqForm.name.trim()?C.t0:C.t4,fontSize:14,fontWeight:600,cursor:reqForm.name.trim()?'pointer':'not-allowed',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif"}}>
                        Submit Request
                      </button>
                    </div>
                  )}
                </div>
              </div>
            </div>
          </div>
        )}
      </div>
    );
  }
  // ─────────────────────────────────────────────────
  // ── END MOBILE RENDER — DESKTOP BELOW
  // ─────────────────────────────────────────────────

  return (
    <div style={{fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif",background:'#f8f8f7',minHeight:'100vh',color:C.t1,position:'relative'}}>
      {/* ── HEADER — 60px white bar, wordmark + text-pill tabs + Contact/Profile ── */}
      <div style={{display:'flex',alignItems:'center',justifyContent:'space-between',height:60,padding:'0 28px',background:'#fff',borderBottom:'1px solid #eae9e6',position:'sticky',top:0,zIndex:50}}>
        <div style={{display:'flex',alignItems:'center',gap:36}}>
          <span style={{fontSize:17,fontWeight:700,color:'#191817',letterSpacing:'-0.01em'}}>Quotemarko<span style={{color:'#4a45d1'}}>.</span></span>
          <div style={{display:'flex',gap:4,fontSize:13}}>
            {[['fe','FEX / WL'],['term','Term Life'],['iul','IUL'],['cv','CV / Coverage'],['sheet','Client Sheet']].map(([id,label])=>(
              <button key={id} className={quoteMode===id?undefined:'qm-navtab'} onClick={()=>{track('Tab Switch',{to:id});setQuoteMode(id);}} style={{
                padding:'7px 14px',borderRadius:7,border:'none',cursor:'pointer',fontSize:13,fontFamily:'inherit',
                background:quoteMode===id?'#f0efed':'transparent',
                color:quoteMode===id?'#191817':'#78746e',
                fontWeight:quoteMode===id?600:500,whiteSpace:'nowrap'
              }}>{label}</button>
            ))}
          </div>
        </div>
        <div style={{display:'flex',alignItems:'center',gap:10}}>
          <button className="qm-secondary" onClick={()=>setShowContact(true)} style={{padding:'7px 14px',borderRadius:8,border:'1px solid #dedcd7',background:'#fff',fontSize:12.5,fontWeight:600,color:'#191817',cursor:'pointer',fontFamily:'inherit'}}>Contact</button>
          <button className="qm-pill" onClick={()=>setShowProfileModal(true)} title={session?.user?.email} style={{display:'inline-flex',alignItems:'center',gap:8,padding:'4px 12px 4px 5px',borderRadius:99,border:'1px solid #e8e7e4',background:'#fff',fontSize:12.5,fontWeight:600,color:'#191817',cursor:'pointer',fontFamily:'inherit'}}>
            <span style={{width:24,height:24,borderRadius:'50%',background:'#e8e7e4',display:'inline-flex',alignItems:'center',justifyContent:'center',fontSize:11,fontWeight:600,color:'#78746e'}}>{(profileName||session?.user?.email||'?').slice(0,1).toUpperCase()}</span>
            Profile
          </button>
        </div>
      </div>

      {/* ── LAYOUT — 264px card-stack sidebar + results, on the warm page bg ── */}
      <div style={{display:'flex',gap:24,padding:'24px 28px',alignItems:'flex-start',minHeight:'calc(100vh - 61px)'}}>

        {/* ── SIDEBAR ── */}
        <div style={{width:264,flex:'none',display:'flex',flexDirection:'column',gap:16}}>

          {quoteMode==='fe' ? (
          <>
          {/* 1 — CLIENT INFO (unified, juvenile hides tobacco) */}
          {renderClientInfo({variant:'desktop', hideTobacco:isJuvenile})}

          {/* Age-bracket banner */}
          {isJuvenile && (
            <div style={{background:'rgba(59,130,246,0.10)',border:'1px solid rgba(59,130,246,0.35)',borderRadius:10,padding:'10px 12px',fontSize:11,color:C.t2,lineHeight:1.5}}>
              <div style={{fontWeight:700,color:'#60A5FA',marginBottom:3,fontSize:10,letterSpacing:0.5}}>👶 JUVENILE WHOLE LIFE</div>
              No medical questions. Coverage typically $15k–$45k.
            </div>
          )}
          {isYoungAdult && (
            <div style={{background:'rgba(23,116,82,0.08)',border:'1px solid rgba(23,116,82,0.28)',borderRadius:10,padding:'10px 12px',fontSize:11,color:C.t2,lineHeight:1.5}}>
              <div style={{fontWeight:700,color:'#177452',marginBottom:3,fontSize:10,letterSpacing:0.5}}>👤 ADULT WHOLE LIFE</div>
              Standard underwriting. Term is usually cheaper for pure DB.
            </div>
          )}

          {/* 2 — QUOTE TARGET */}
          <div style={sec}>
            <div style={{display:'flex',alignItems:'center',justifyContent:'space-between',marginBottom:10}}>
              <div style={lbl}>Quote Target</div>
              <button className="qm-gsb-toggle" onClick={()=>{setGsbOn(p=>{const n=!p;track('GSB Toggle',{on:n});return n;});setMode('face');}} style={{
                display:'flex',alignItems:'center',gap:7,
                padding:'6px 12px 6px 8px',borderRadius:20,
                border:`2px solid ${gsbOn ? C.goldBd : isDark?'#374151':'#D0CDBE'}`,
                background:gsbOn ? C.goldBg : (isDark?'#0F172A':'#FAF9F6'),
                color:gsbOn?C.gold:C.t3,
                fontSize:11,fontWeight:700,cursor:'pointer',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif",
                boxShadow:gsbOn?(isDark?'0 2px 8px rgba(0,0,0,0.3)':'0 1px 4px rgba(74,69,209,0.15)'):'none'
              }}>
                <span style={{
                  position:'relative',display:'inline-block',
                  width:28,height:16,borderRadius:8,
                  background:gsbOn ? C.gold : (isDark?'#1E293B':'#E2E8F0'),
                  border:`1px solid ${gsbOn?C.goldBd:isDark?'#374151':'#D0CDBE'}`,
                  transition:'all 0.2s',flexShrink:0
                }}>
                  <span style={{
                    position:'absolute',top:2,
                    left:gsbOn?13:2,width:10,height:10,
                    borderRadius:'50%',background:'white',
                    transition:'left 0.2s',
                    boxShadow:'0 1px 3px rgba(0,0,0,0.3)'
                  }}/>
                </span>
                {gsbOn?'🥇🥈🥉':'🏆'} Gold / Silver / Bronze
              </button>
            </div>
            {!gsbOn?(
              <>
                <div style={{display:'flex',gap:6,marginBottom:14}}>
                  <button className='qm-btn' style={{...togBtn(mode==='face')}} onClick={()=>{setMode('face');}}>Face amount</button>
                  <button className='qm-btn' style={{...togBtn(mode==='budget')}} onClick={()=>{setMode('budget');}}>Premium amount</button>
                </div>
                {mode==='face'?(
                  <>
                    <div style={{fontSize:11,color:C.t3,marginBottom:6,display:'flex',justifyContent:'space-between'}}><span>Coverage amount</span><span style={{fontSize:10,color:C.t4}}>$2,000 – $100,000</span></div>
                    <div style={{position:'relative'}}>
                      <span style={{position:'absolute',left:12,top:'50%',transform:'translateY(-50%)',color:C.t3,fontSize:14,pointerEvents:'none'}}>$</span>
                      <input type="text" inputMode="numeric" placeholder="10,000" value={faceAmt?faceAmt.toLocaleString():''}
                        onChange={e=>{const n=Number(e.target.value.replace(/[^0-9]/g,''));setFaceAmt(isNaN(n)?0:n);}}
                        style={{...inp,paddingLeft:26,fontFamily:"'Instrument Sans',sans-serif",fontSize:16}}/>
                    </div>
                  </>
                ):(
                  <>
                    <div style={{fontSize:11,color:C.t3,marginBottom:5}}>Monthly premium</div>
                    <div style={{position:'relative'}}>
                      <span style={{position:'absolute',left:12,top:'50%',transform:'translateY(-50%)',color:C.t3,fontSize:14,pointerEvents:'none'}}>$</span>
                      <input type="text" inputMode="decimal" placeholder="100" value={budget||''}
                        onChange={e=>{const v=e.target.value.replace(/[^0-9.]/g,'').replace(/^0+(?=\d)/,'');const n=Number(v);setBudget(v===''||isNaN(n)?0:n);}}
                        style={{...inp,paddingLeft:26,fontFamily:"'Instrument Sans',sans-serif",fontSize:16}}/>
                    </div>
                    <div style={{fontSize:11,color:C.t4,marginTop:6}}>Finds max coverage at this premium</div>
                  </>
                )}
              </>
            ):(
              <div>
                <div style={{fontSize:11,color:C.t3,marginBottom:10}}>Set coverage amounts per tier</div>
                {GSB.map(g=>(
                  <div key={g.key} style={{display:'flex',alignItems:'center',gap:10,marginBottom:9}}>
                    <span style={{fontSize:16,flexShrink:0}}>{g.medal}</span>
                    <span style={{fontSize:12,fontWeight:600,color:g.color,width:44,flexShrink:0}}>{g.label}</span>
                    <div style={{position:'relative',flex:1}}>
                      <span style={{position:'absolute',left:10,top:'50%',transform:'translateY(-50%)',color:C.t4,fontSize:12}}>$</span>
                      <input type="text" inputMode="numeric" value={gsbRaw[g.key]}
                        onChange={e=>handleGsbChange(g.key,e.target.value)}
                        onBlur={()=>handleGsbBlur(g.key)}
                        onFocus={e=>e.target.select()}
                        style={{...inp,paddingLeft:22,fontSize:13,fontFamily:"'Instrument Sans',sans-serif"}}/>
                    </div>
                  </div>
                ))}
              </div>
            )}
          </div>

          {/* 3 — UW ASSESSMENT (hidden for juveniles — GIWL kids' policies have no UW tiers) */}
          {!isJuvenile && (
          <div style={{...sec,border:`1px solid ${TIER_INFO[uwTier].bd}`}}>
            <div onClick={()=>setUwOpen(p=>!p)} style={{display:'flex',alignItems:'center',justifyContent:'space-between',cursor:'pointer',userSelect:'none',marginBottom:uwOpen?12:0}}>
              <div style={lbl}>UW Assessment</div>
              <div style={{display:'flex',alignItems:'center',gap:8}}>
                <TierPill tier={uwTier}/>
                {tierOvr&&<span style={{fontSize:9,color:C.gold,fontWeight:700,background:C.goldBg,border:`1px solid ${C.goldBd}`,borderRadius:4,padding:'2px 6px'}}>Manual</span>}
                <span style={{fontSize:10,color:C.t4,transform:uwOpen?'rotate(180deg)':'none',transition:'transform 0.2s'}}>▼</span>
              </div>
            </div>

            {uwOpen&&(
              <>
                <div style={{fontSize:12,color:C.t2,marginBottom:12,lineHeight:1.6,padding:'8px 10px',background:C.bg2,borderRadius:8,border:`1px solid ${TIER_INFO[uwTier].bd}`}}>
                  {TIER_INFO[uwTier].label} — {uwTier==='B'?'Full benefit from day 1. Minor or well-controlled conditions.':uwTier==='C'?'Full benefit from day 1. Moderate history present.':uwTier==='D'?'Graded/modified benefit — 2–3 year waiting period.':'Knockout conditions present. GI only (RN, ages 50–80).'}
                </div>
                <div style={{fontSize:10,fontWeight:700,letterSpacing:1.5,color:C.t4,textTransform:'uppercase',marginBottom:8}}>Force tier override</div>
                <div style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:6,marginBottom:12}}>
                  {['B','C','D','E'].map(t=>{
                    const ti=TIER_INFO[t],isOvr=tierOvr===t,isAuto=!tierOvr&&autoTier===t;
                    return(
                      <button key={t} onClick={()=>toggleOvr(t)} style={{
                        padding:'8px 10px',borderRadius:8,border:`1px solid ${isOvr||isAuto?ti.bd:C.bd}`,
                        background:isOvr?ti.pill:'transparent',
                        color:isOvr||isAuto?ti.txt:C.t3,
                        cursor:'pointer',fontSize:12,fontWeight:600,textAlign:'left',
                        display:'flex',alignItems:'center',gap:7,fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif"
                      }}>
                        <span style={{width:6,height:6,borderRadius:'50%',background:isOvr||isAuto?ti.dot:'#334155',flexShrink:0}}/>
                        <span style={{flex:1}}>{ti.short}</span>
                        {isAuto&&!tierOvr&&<span style={{fontSize:9,color:ti.txt,opacity:0.7}}>auto</span>}
                        {isOvr&&<span style={{fontSize:10}}>✓</span>}
                      </button>
                    );
                  })}
                </div>
                {selected.filter(c=>c!=='none').length>0&&(
                  <div>
                    <div style={{fontSize:10,color:C.t4,marginBottom:5}}>Active conditions</div>
                    <div style={{display:'flex',flexWrap:'wrap',gap:4}}>
                      {selected.filter(c=>c!=='none').map(id=>{
                        const cond=CONDITIONS.find(c=>c.id===id);
                        return(
                          <span key={id} onClick={()=>toggleCond(id)} style={{background:C.bg1,border:`1px solid ${C.bd}`,borderRadius:5,padding:'2px 8px',fontSize:10,color:C.t2,cursor:'pointer',display:'flex',alignItems:'center',gap:4}}>
                            <span style={{color:TIER_INFO[cond?.tier||'B'].dot,fontSize:7}}>●</span>
                            {cond?.label.replace('⚠ ','')}
                            <span style={{color:C.t4}}>×</span>
                          </span>
                        );
                      })}
                    </div>
                  </div>
                )}
              </>
            )}
          </div>
          )}

          {/* 4 — HEALTH CONDITIONS & MEDS (hidden for juveniles) */}
          {!isJuvenile && (
          <div style={sec}>
            <div style={lbl}>Health Conditions & Meds</div>
            <div style={{fontSize:10,color:C.t4,marginBottom:8,lineHeight:1.4}}>Type a medication (lisinopril, metformin, eliquis…) — we'll suggest the condition.</div>
            <div style={{position:'relative',marginBottom:8,display:'flex',alignItems:'center'}}>
              <span style={{position:'absolute',left:10,top:'50%',transform:'translateY(-50%)',fontSize:12,color:C.t4,pointerEvents:'none',zIndex:1}}>🔍</span>
              <input placeholder="Type meds or conditions…"
                value={search} onChange={e=>setSearch(e.target.value)}
                style={{...inp,paddingLeft:30,fontSize:12,flex:1,minWidth:0,width:0}}/>
            </div>
            {medHints.length>0&&search.length>=3&&(
              <div style={{background:C.goldBg,border:`1px solid ${C.goldBd}`,borderRadius:8,padding:'8px 10px',marginBottom:8}}>
                <div style={{fontSize:11,color:C.gold,fontWeight:600,marginBottom:5}}>💊 "{search}" may indicate:</div>
                <div style={{display:'flex',flexWrap:'wrap',gap:4}}>
                  {medHints.map(c=>(<span key={c.id} onClick={()=>{toggleCond(c.id);setSearch('');}} style={{background:TIER_INFO[c.tier].pill,border:`1px solid ${TIER_INFO[c.tier].bd}`,color:TIER_INFO[c.tier].dot,borderRadius:5,padding:'3px 8px',cursor:'pointer',fontWeight:600,fontSize:11}}>+ {c.label}</span>))}
                </div>
              </div>
            )}
            {search.length>=2&&(
              <div style={{marginBottom:8}}>
                {filteredConds.slice(0,6).map(c=>{
                  const active=selected.includes(c.id),tc=TIER_INFO[c.tier].dot;
                  return(
                    <div key={c.id} onClick={()=>{toggleCond(c.id);setSearch('');}} style={{
                      display:'flex',alignItems:'center',gap:8,padding:'8px 10px',borderRadius:8,
                      cursor:'pointer',fontSize:12.5,marginBottom:4,
                      background:active?TIER_INFO[c.tier].pill:C.bg3,
                      border:`1px solid ${active?TIER_INFO[c.tier].bd:C.bd2}`,
                      color:active?C.t0:C.t1,transition:'all 0.1s'
                    }}>
                      <span style={{width:7,height:7,borderRadius:'50%',background:active?tc:TIER_INFO[c.tier].dot,flexShrink:0}}/>
                      <span style={{flex:1,lineHeight:1.35}}>{c.label}</span>
                      <span style={{fontSize:10,color:C.t4}}>{c.cat}</span>
                      {active&&<span style={{color:tc,fontSize:11}}>✓</span>}
                    </div>
                  );
                })}
                {filteredConds.length===0&&<div style={{fontSize:12,color:C.t4,padding:'6px 4px'}}>No matches — try a different term</div>}
              </div>
            )}
            {selected.filter(c=>c!=='none').length>0&&(
              <div style={{marginTop:6}}>
                <div style={{fontSize:10,color:C.t4,marginBottom:5,fontWeight:600,letterSpacing:0.5}}>ACTIVE CONDITIONS</div>
                <div style={{display:'flex',flexWrap:'wrap',gap:4}}>
                  {selected.filter(c=>c!=='none').map(id=>{
                    const cond=CONDITIONS.find(c=>c.id===id);
                    return(
                      <span key={id} onClick={()=>toggleCond(id)} style={{
                        background:TIER_INFO[cond?.tier||'B'].pill,
                        border:`1px solid ${TIER_INFO[cond?.tier||'B'].bd}`,
                        borderRadius:20,padding:'3px 10px 3px 8px',fontSize:11,
                        color:TIER_INFO[cond?.tier||'B'].dot,
                        cursor:'pointer',display:'inline-flex',alignItems:'center',gap:5,
                        fontWeight:600,transition:'all 0.1s'
                      }}>
                        <span style={{width:5,height:5,borderRadius:'50%',background:TIER_INFO[cond?.tier||'B'].dot}}/>
                        {cond?.label.replace('⚠ ','')}
                        <span style={{opacity:0.7,fontWeight:400,fontSize:13,lineHeight:1}}>×</span>
                      </span>
                    );
                  })}
                </div>
              </div>
            )}
          </div>
          )}

          <button className="qm-cta" onClick={e=>fireCta(e,()=>{if(ageOK){track('Quote Requested',{tier:uwTier,mode,gsb:gsbOn,face:faceBand(faceAmt)});setHasQuoted(true);if(isMobile){setMobileTab('results');setTimeout(()=>window.scrollTo({top:0,behavior:'instant'}),0);}}})} style={{
            width:'100%',height:44,borderRadius:9,border:'none',
            cursor:ageOK?'pointer':'not-allowed',
            background:ageOK?'#4a45d1':'#dedcd7',
            color:'#fff',
            fontSize:14,fontWeight:600,
            boxShadow:ageOK?'0 1px 2px rgba(74,69,209,.3)':'none',
            fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif"
          }}>
            Get quotes
          </button>
          {!ageOK && <div style={{fontSize:11,color:C.t4,textAlign:'center',marginTop:8}}>Enter client age above to quote</div>}
          </>
          ) : quoteMode==='term' ? (
          <>
            {/* ── DESKTOP TERM INPUTS — FEX-style sectioning ── */}
            {/* 1 — CLIENT INFO */}
            {renderClientInfo({variant:'desktop'})}

            {/* 2 — QUOTE TARGET (term length + face/budget) */}
            <div style={sec}>
              <div style={lbl}>Quote Target</div>
              <div style={{marginBottom:12}}>
                <div style={{fontSize:11,color:C.t3,marginBottom:6,fontWeight:600}}>Term Length (years)</div>
                <div style={{display:'grid',gridTemplateColumns:'repeat(7,1fr)',gap:4}}>
                  {['10','15','20','25','30','35','40'].map(t=>(
                    <button key={t} onClick={()=>setTermLength(t)} style={{
                      padding:'9px 0',borderRadius:7,border:`1px solid ${termLength===t?'#191817':'#dedcd7'}`,
                      background:termLength===t?'#191817':'#fff',color:termLength===t?'#fff':'#78746e',
                      fontSize:12,fontWeight:700,cursor:'pointer',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif"
                    }}>{t}</button>
                  ))}
                </div>
              </div>
              {/* Face amount — Term quoter pulls face only (no budget mode) */}
              <div>
                <div style={{fontSize:11,color:C.t3,marginBottom:6,display:'flex',justifyContent:'space-between'}}>
                  <span>Coverage amount</span>
                  <span style={{fontSize:10,color:C.t4}}>$25,000 – $1,000,000</span>
                </div>
                <div style={{position:'relative'}}>
                      <span style={{position:'absolute',left:12,top:'50%',transform:'translateY(-50%)',color:C.t3,fontSize:14,pointerEvents:'none'}}>$</span>
                      <input type="text" inputMode="numeric" placeholder="100,000" value={termFace?termFace.toLocaleString():''}
                        onChange={e=>{const n=Number(e.target.value.replace(/[^0-9]/g,''));setTermFace(isNaN(n)?0:n);}}
                        style={{...inp,paddingLeft:26,fontFamily:"'Instrument Sans',sans-serif",fontSize:16}}/>
                    </div>
              </div>
            </div>

            {/* 3 — UW ASSESSMENT (health class) */}
            <div style={sec}>
              <div style={{...lbl,marginBottom:10,display:'flex',justifyContent:'space-between',alignItems:'center'}}>
                <span>UW Assessment</span>
                {termRec.recommended==='decline' ? (
                  <span style={{color:'#b42318',fontWeight:700,fontSize:10,textTransform:'none',letterSpacing:0}}>⚠ Likely Decline</span>
                ) : !termHealthManual && termRec.reasons.length ? (
                  <span style={{color:C.gold,fontWeight:700,fontSize:10,textTransform:'none',letterSpacing:0}}>✨ Auto · {HEALTH_CLASS_SHORT[termRec.recommended]}</span>
                ) : termHealthManual ? (
                  <button onClick={()=>setTermHealthManual(false)} style={{background:'transparent',border:'none',color:C.t4,fontSize:10,cursor:'pointer',padding:0,textDecoration:'underline',textTransform:'none',letterSpacing:0}}>reset to auto</button>
                ) : (
                  <span style={{color:C.t4,fontWeight:500,fontSize:10,textTransform:'none',letterSpacing:0}}>{HEALTH_CLASS_LABEL[termHealth]}</span>
                )}
              </div>
              <div style={{fontSize:11,color:C.t3,marginBottom:6,fontWeight:600}}>Health Class</div>
              <div style={{display:'grid',gridTemplateColumns:'repeat(4,1fr)',gap:4,marginBottom:10}}>
                {[{k:'pp',label:'Pref+'},{k:'p',label:'Pref'},{k:'sp',label:'Std+'},{k:'s',label:'Std'}].map(({k,label})=>(
                  <button key={k} onClick={()=>{setTermHealth(k);setTermHealthManual(true);}} style={{
                    padding:'9px 0',borderRadius:7,border:`1px solid ${termHealth===k?'#191817':'#dedcd7'}`,
                    background:termHealth===k?'#191817':'#fff',color:termHealth===k?'#fff':'#78746e',
                    fontSize:12,fontWeight:700,cursor:'pointer',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif"
                  }}>{label}</button>
                ))}
              </div>
              {termRec.reasons.length > 0 && (
                <div style={{fontSize:10,color:C.t4,lineHeight:1.5,padding:'7px 10px',background:C.bg2,borderRadius:6,border:`1px solid ${C.bd}`}}>
                  {termRec.recommended==='decline'?'Decline factors':'Class driven by'}: {termRec.reasons.join(' · ')}
                </div>
              )}
            </div>

            {/* 4 — HEALTH PROFILE (height/weight, family history, conditions) */}
            <div style={sec}>
              <div style={lbl}>Health Profile</div>
              <div style={{marginBottom:12}}>
                <div style={{fontSize:11,color:C.t3,marginBottom:6,fontWeight:600,display:'flex',justifyContent:'space-between'}}>
                  <span>Height & Weight</span>
                  {termBMI!=null && <span style={{color:C.t4,fontWeight:500,fontSize:10,fontFamily:"'Instrument Sans',sans-serif"}}>BMI {termBMI.toFixed(1)}</span>}
                </div>
                <div style={{display:'grid',gridTemplateColumns:'1fr 1fr 1.2fr',gap:6}}>
                  <input inputMode="numeric" placeholder="ft" value={termHtFt}
                    onChange={e=>setTermHtFt(e.target.value.replace(/\D/g,'').slice(0,1))}
                    style={{...inp,textAlign:'center',fontFamily:"'Instrument Sans',sans-serif"}}/>
                  <input inputMode="numeric" placeholder="in" value={termHtIn}
                    onChange={e=>setTermHtIn(e.target.value.replace(/\D/g,'').slice(0,2))}
                    style={{...inp,textAlign:'center',fontFamily:"'Instrument Sans',sans-serif"}}/>
                  <input inputMode="numeric" placeholder="weight (lb)" value={termWtLb}
                    onChange={e=>setTermWtLb(e.target.value.replace(/\D/g,'').slice(0,3))}
                    style={{...inp,textAlign:'center',fontFamily:"'Instrument Sans',sans-serif"}}/>
                </div>
              </div>
              <button onClick={()=>setTermFamHx(v=>!v)} style={{
                width:'100%',padding:'9px 11px',borderRadius:8,border:`1px solid ${termFamHx?'#4a45d1':C.bd}`,
                background:termFamHx?'rgba(74,69,209,0.12)':C.bg2,color:C.t2,textAlign:'left',cursor:'pointer',
                fontSize:11,fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif",display:'flex',alignItems:'center',gap:8,marginBottom:12
              }}>
                <span style={{width:14,height:14,borderRadius:3,border:`2px solid ${termFamHx?'#4a45d1':C.bd2}`,background:termFamHx?'#4a45d1':'transparent',display:'flex',alignItems:'center',justifyContent:'center',fontSize:10,color:C.bg0,fontWeight:900,flexShrink:0}}>{termFamHx?'✓':''}</span>
                <span>Family history of cancer / heart disease (pre-60)</span>
              </button>
              <div>
                <div style={{fontSize:11,color:C.t3,marginBottom:6,fontWeight:600,display:'flex',justifyContent:'space-between'}}>
                  <span>Conditions & Meds</span>
                  {selected.filter(c=>c!=='none').length>0 && <span style={{color:C.t4,fontWeight:500,fontSize:10}}>{selected.filter(c=>c!=='none').length} active</span>}
                </div>
                <input placeholder="Type meds or conditions…" value={search} onChange={e=>setSearch(e.target.value)} style={{...inp,fontSize:12}}/>
                {search.length>=2 && filteredConds.length>0 && (
                  <div style={{marginTop:6,maxHeight:180,overflowY:'auto',background:C.bg3,border:`1px solid ${C.bd}`,borderRadius:8,padding:5,display:'flex',flexDirection:'column',gap:2}}>
                    {filteredConds.slice(0,8).map(c=>(
                      <div key={c.id} onClick={()=>{toggleCond(c.id);setSearch('');}} style={{
                        display:'flex',alignItems:'center',justifyContent:'space-between',gap:8,
                        padding:'6px 9px',borderRadius:5,cursor:'pointer',
                        background:selected.includes(c.id)?'rgba(74,69,209,0.10)':'transparent',
                        fontSize:11,color:C.t1
                      }}>
                        <span>{c.label.replace('⚠ ','')}</span>
                        <span style={{fontSize:9,color:C.t4}}>{c.cat}</span>
                      </div>
                    ))}
                  </div>
                )}
                {search.length>=2 && filteredConds.length===0 && (
                  <div style={{marginTop:6,fontSize:11,color:C.t4,padding:'6px 4px'}}>No matches — try a different term</div>
                )}
                {selected.filter(c=>c!=='none').length>0 && (
                  <div style={{marginTop:7,display:'flex',flexWrap:'wrap',gap:4}}>
                    {selected.filter(c=>c!=='none').map(id=>{
                      const cond=CONDITIONS.find(c=>c.id===id);
                      return(
                        <span key={id} onClick={()=>toggleCond(id)} style={{
                          background:TIER_INFO[cond?.tier||'B'].pill,
                          border:`1px solid ${TIER_INFO[cond?.tier||'B'].bd}`,
                          borderRadius:20,padding:'3px 9px 3px 7px',fontSize:10,
                          color:TIER_INFO[cond?.tier||'B'].dot,
                          cursor:'pointer',display:'inline-flex',alignItems:'center',gap:4,fontWeight:600
                        }}>
                          {cond?.label.replace('⚠ ','')}
                          <span style={{opacity:0.7,fontWeight:400,fontSize:12,lineHeight:1}}>×</span>
                        </span>
                      );
                    })}
                  </div>
                )}
              </div>
            </div>

            <button className="qm-cta" onClick={e=>fireCta(e,()=>{if(ageOK){track('Quote Requested',{tier:'term',mode:'term',gsb:false,face:faceBand(termFace)});setHasQuoted(true);}})}
              style={{width:'100%',minHeight:44,borderRadius:9,border:'none',cursor:ageOK?'pointer':'not-allowed',background:ageOK?'#4a45d1':'#dedcd7',color:'#fff',fontSize:14,fontWeight:600,boxShadow:ageOK?'0 1px 2px rgba(74,69,209,.3)':'none',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif"}}>
              Get term quotes
            </button>
            {!ageOK && <div style={{fontSize:11,color:C.t4,textAlign:'center',marginTop:8}}>Enter client age above to quote</div>}
          </>
          ) : null}

          {quoteMode==='iul' && (
            <>
              {/* 1 — CLIENT INFO */}
              {renderClientInfo({variant:'desktop'})}

              {/* 2 — QUOTE TARGET */}
              <div style={sec}>
                <div style={lbl}>Quote Target</div>
                <div style={{marginBottom:12}}>
                  <div style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:6}}>
                    <button onClick={()=>setIulMode('premium')} title="Input target face amount, see required premium per carrier" style={togBtn(iulMode==='premium')}>Face amount</button>
                    <button onClick={()=>setIulMode('face')} title="Input monthly premium, see face amount each carrier will issue" style={togBtn(iulMode==='face')}>Premium amount</button>
                  </div>
                </div>
                {iulMode === 'face' ? (
                  <div>
                    <div style={{fontSize:11,color:C.t3,marginBottom:5}}>Monthly Premium</div>
                    <div style={{position:'relative'}}>
                      <span style={{position:'absolute',left:12,top:'50%',transform:'translateY(-50%)',color:C.t3,fontSize:14,pointerEvents:'none'}}>$</span>
                      <input type="text" inputMode="decimal" placeholder="200" value={iulPremium||''}
                        onChange={e=>{const v=e.target.value.replace(/[^0-9.]/g,'').replace(/^0+(?=\d)/,'');const n=Number(v);setIulPremium(v===''||isNaN(n)?0:n);}}
                        style={{...inp,paddingLeft:26,fontFamily:"'Instrument Sans',sans-serif",fontSize:16}}/>
                    </div>
                    <div style={{fontSize:11,color:C.t4,marginTop:6}}>Each carrier returns the face they'd issue for this premium</div>
                  </div>
                ) : (
                  <div>
                    <div style={{fontSize:11,color:C.t3,marginBottom:6,display:'flex',justifyContent:'space-between'}}>
                      <span>Target Face Amount</span>
                      <span style={{fontSize:10,color:C.t4}}>$25k – $500k</span>
                    </div>
                    <div style={{position:'relative'}}>
                      <span style={{position:'absolute',left:12,top:'50%',transform:'translateY(-50%)',color:C.t3,fontSize:14,pointerEvents:'none'}}>$</span>
                      <input type="text" inputMode="numeric" placeholder="150,000" value={iulFace?iulFace.toLocaleString():''}
                        onChange={e=>{const n=Number(e.target.value.replace(/[^0-9]/g,''));setIulFace(isNaN(n)?0:n);}}
                        style={{...inp,paddingLeft:26,fontFamily:"'Instrument Sans',sans-serif",fontSize:16}}/>
                    </div>
                  </div>
                )}
              </div>

              {/* 3 — CARRIER NOTES */}
              <div style={sec}>
                <div style={lbl}>Carrier Notes</div>
                <div style={{background:'rgba(74,69,209,0.08)',border:'1px solid rgba(74,69,209,0.3)',borderRadius:8,padding:'10px 12px',fontSize:11,color:C.t3,lineHeight:1.5,marginBottom:10}}>
                  ⚠ Cap rates reset periodically. Verify against the carrier's current brochure before quoting.
                </div>
                <div style={{padding:'10px 11px',background:C.bg2,border:`1px solid ${C.bd}`,borderRadius:8,fontSize:11,color:C.t3,lineHeight:1.6}}>
                  <div style={{fontWeight:700,color:C.t2,marginBottom:4}}>💡 Tip</div>
                  {iulMode==='face'
                    ? 'Face shown is what each carrier issues for this premium. Use it to pick the carrier, then pull the carrier\'s official illustration for the client.'
                    : 'Premium shown is what each carrier needs to issue this face. Lower = better. Pull the carrier\'s official illustration before promising rates.'}
                </div>
              </div>

              <button className="qm-cta" onClick={e=>fireCta(e,()=>{if(ageOK){track('Quote Requested',{tier:'iul',mode:iulMode,gsb:false});setHasQuoted(true);}})}
                style={{width:'100%',minHeight:44,borderRadius:9,border:'none',cursor:ageOK?'pointer':'not-allowed',background:ageOK?'#4a45d1':'#dedcd7',color:'#fff',fontSize:14,fontWeight:600,boxShadow:ageOK?'0 1px 2px rgba(74,69,209,.3)':'none',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif"}}>
                Get IUL quotes
              </button>
              {!ageOK && <div style={{fontSize:11,color:C.t4,textAlign:'center',marginTop:8}}>Enter client age above to quote</div>}
            </>
          )}

          {quoteMode==='cv' && (
            <>
              {/* 1 — CLIENT INFO (no state, no tobacco needed for CV) */}
              {renderClientInfo({variant:'desktop', showState:false, hideTobacco:true})}

              {/* 2 — EXISTING POLICY */}
              <div style={sec}>
                <div style={lbl}>Existing Policy</div>
                <div style={{marginBottom:12}}>
                  <div style={{fontSize:11,color:C.t3,marginBottom:6,fontWeight:600}}>Monthly Premium ($)</div>
                  <input inputMode="decimal" placeholder="e.g. 52.00" value={cvMonthly}
                    onChange={e=>setCvMonthly(e.target.value)}
                    style={{...inp,fontFamily:"'Instrument Sans',sans-serif",fontSize:15}}/>
                </div>
                <div>
                  <div style={{fontSize:11,color:C.t3,marginBottom:6,fontWeight:600}}>Years In-Force</div>
                  <input inputMode="numeric" placeholder="e.g. 7" value={cvPolicyYrs}
                    onChange={e=>setCvPolicyYrs(e.target.value.replace(/\D/g,''))}
                    style={{...inp,fontFamily:"'Instrument Sans',sans-serif",fontSize:15}}/>
                  <div style={{fontSize:10,color:C.t4,marginTop:4}}>How long has the client had this policy?</div>
                </div>
              </div>

              {/* 3 — QUICK LOOK preview (only when inputs valid) */}
              {(()=>{
                if(!ageOK||!cvMonthly||!cvPolicyYrs||!Number(cvMonthly)||!Number(cvPolicyYrs)) return null;
                const currentAge = ageNum;
                const issueAge = Math.max(0, currentAge - Number(cvPolicyYrs));
                const d=calculateCVCorridor(Number(cvMonthly),Number(cvPolicyYrs),issueAge);
                return (
                  <div style={sec}>
                    <div style={{...lbl,color:'#4a45d1',marginBottom:10}}>Quick Look · Issued at {issueAge}</div>
                    <div style={{display:'flex',justifyContent:'space-between',fontSize:12.5,marginBottom:6}}>
                      <span style={{color:'#78746e'}}>Conservative</span>
                      <strong style={{color:'#191817',fontWeight:600}}>${Math.round(d.low).toLocaleString()}</strong>
                    </div>
                    <div style={{display:'flex',justifyContent:'space-between',fontSize:12.5,paddingBottom:8,marginBottom:8,borderBottom:'1px solid #f0efed'}}>
                      <span style={{color:'#78746e'}}>Target</span>
                      <strong style={{color:'#4a45d1',fontWeight:600}}>${Math.round(d.high).toLocaleString()}</strong>
                    </div>
                    <div style={{display:'flex',justifyContent:'space-between',fontSize:12.5}}>
                      <span style={{color:'#78746e'}}>Total paid</span>
                      <strong style={{color:'#191817',fontWeight:600}}>${Math.round(d.totalPaid).toLocaleString()}</strong>
                    </div>
                    {Number(cvPolicyYrs)<=2&&<div style={{fontSize:11,color:'#b42318',marginTop:8,lineHeight:1.5}}>Most policies accumulate little or no CV in years 1–2. Significant growth typically starts year 3+.</div>}
                  </div>
                );
              })()}

              {/* CTA */}
              <button className="qm-cta" onClick={e=>fireCta(e,()=>{if(ageOK&&Number(cvMonthly)>0&&Number(cvPolicyYrs)>0){track('CV Estimate Viewed');setHasQuoted(true);}})}
                style={{width:'100%',minHeight:44,borderRadius:9,border:'none',cursor:(ageOK&&Number(cvMonthly)>0&&Number(cvPolicyYrs)>0)?'pointer':'not-allowed',background:(ageOK&&Number(cvMonthly)>0&&Number(cvPolicyYrs)>0)?'#4a45d1':'#dedcd7',color:'#fff',fontSize:14,fontWeight:600,boxShadow:(ageOK&&Number(cvMonthly)>0&&Number(cvPolicyYrs)>0)?'0 1px 2px rgba(74,69,209,.3)':'none',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif"}}>
                View cash value estimate
              </button>
            </>
          )}

          {quoteMode==='sheet' && (
            <>
              <div style={sec}>
                <div style={lbl}>How it works</div>
                <div style={{fontSize:12,color:C.t2,lineHeight:1.7}}>
                  Fill the sheet on the right during the call, hit <strong>Save</strong>, and a PDF lands on your machine — no loose paper.
                </div>
              </div>
              <div style={sec}>
                <div style={lbl}>File name</div>
                <div style={{fontSize:11.5,color:'#78746e',lineHeight:1.7,fontFamily:"ui-monospace,'SF Mono',Menlo,monospace",wordBreak:'break-all',background:'#fbfbfa',border:'1px solid #f0efed',borderRadius:6,padding:'8px 10px'}}>{sheetFileName(sheet)}</div>
                <div style={{fontSize:10,color:C.t4,marginTop:8,lineHeight:1.6}}>first_last_carrier_month_year — carrier pulls from the Carrier field (falls back to APPROVED / In Underwriting).</div>
              </div>
              <div style={{padding:'10px 11px',background:C.bg2,border:`1px solid ${C.bd}`,borderRadius:8,fontSize:11,color:C.t3,lineHeight:1.6}}>
                <div style={{fontWeight:700,color:C.t2,marginBottom:4}}>💡 Tip</div>
                The sheet keeps itself saved in this browser while you work — use <strong>Clear</strong> when you start the next client.
              </div>
            </>
          )}

        </div>

        {/* ── RESULTS PANEL ── */}
        <div style={{flex:1,minWidth:0,display:'flex',flexDirection:'column'}}>
          {quoteMode==='iul'?(
            <div>
              <div style={{display:'flex',alignItems:'center',gap:14,marginBottom:16}}>
                <div style={{flex:1}}>
                  <div style={{fontSize:20,fontWeight:700,color:'#191817',letterSpacing:'-0.01em'}}>IUL quote · {iulMode==='face' ? 'Premium → Face' : 'Face → Premium'}</div>
                  <div style={{fontSize:13,color:'#78746e',marginTop:2}}>{IUL_QUOTE_CARRIERS.length} carriers · live lookup mirrors Insurance Toolkits</div>
                </div>
                <span style={{background:'#f0efed',borderRadius:99,padding:'3px 10px',fontSize:11.5,color:'#78746e',fontWeight:600,whiteSpace:'nowrap'}}>
                  Agent reference only
                </span>
              </div>

              {/* Quote results */}
              {!ageOK ? (
                <div style={{padding:'40px 24px',textAlign:'center',color:C.t4}}>
                  <div style={{fontSize:48,opacity:0.4,marginBottom:8}}>📋</div>
                  <div style={{fontSize:16,fontWeight:700,marginBottom:6,color:C.t3}}>Enter client info</div>
                  <div style={{fontSize:12,maxWidth:340,margin:'0 auto',lineHeight:1.6}}>Set age, sex, smoker, and monthly premium in the sidebar to see face amounts each IUL carrier will issue.</div>
                </div>
              ) : !iulQuoteResults || iulQuoteResults.length === 0 ? (
                <div style={{padding:'40px 24px',textAlign:'center',color:C.t4}}>
                  <div style={{fontSize:48,opacity:0.4,marginBottom:8}}>🔍</div>
                  <div style={{fontSize:16,fontWeight:700,marginBottom:6,color:C.t3}}>No IUL quotes for this profile</div>
                  <div style={{fontSize:12,maxWidth:340,margin:'0 auto',lineHeight:1.6}}>Try a different age, premium, or face amount. Data range: ages 18–75, premium $50–$500/mo, face $25k–$500k.</div>
                </div>
              ) : (
                <div style={{marginBottom:24}}>
                  <div style={{display:'flex',alignItems:'baseline',justifyContent:'space-between',gap:8,marginBottom:14,flexWrap:'wrap'}}>
                    <div style={{display:'flex',alignItems:'baseline',gap:8,fontSize:13,color:'#78746e'}}>
                      <span style={{fontSize:20,fontWeight:700,color:'#191817',letterSpacing:'-0.01em'}}>
                        {iulMode==='face' ? `$${iulPremium}/mo` : `$${(iulFace/1000).toFixed(0)}k face`}
                      </span>
                      <span>· Age {age} · {gender==='male'?'M':'F'} · {smoker?'Smoker':'NS'}</span>
                    </div>
                    <span style={{fontSize:12.5,color:'#a09c94'}}>{iulQuoteResults.filter(r=>!r.unavailable&&!(iulMode==='premium'&&(r.floored||r.capped))).length} carriers · sorted by {iulMode==='face' ? 'face desc' : 'premium asc'}</span>
                  </div>
                  <div style={{display:'flex',flexDirection:'column',gap:10}}>
                    {iulQuoteResults.map(r => {
                      const RED = '#b42318';
                      // In premium mode, a carrier that can't issue the requested face —
                      // below its minimum (floored) OR above its max (capped) — is a red
                      // "unavailable" state, NOT a real quote. Showing the min/max-face
                      // premium as the headline number misleads (it's not the price of
                      // the requested face). Face mode shows the real capped face, so no
                      // problem there.
                      const limitBlocked = iulMode === 'premium' && (r.floored || r.capped);
                      const limitKind = r.floored ? 'min' : 'max';
                      const problem = r.unavailable ? 'state' : limitBlocked ? 'limit' : null;
                      return (
                      <div key={r.id} className={problem?undefined:'qm-row'} style={{
                        background:'#fff',
                        border:`1px solid ${problem==='limit'?'#f5c6c2':'#eae9e6'}`,
                        borderRadius:10,padding:'12px 20px',
                        display:'flex',alignItems:'center',gap:16,
                        opacity:problem==='state'?0.55:1,
                        filter:problem==='state'?'grayscale(0.7)':'none'}}>
                        <CarrierLogo carrierId={r.id} name={r.name} small={false}/>
                        <div style={{flex:1,minWidth:0}}>
                          <div style={{fontSize:14.5,fontWeight:600,color:'#191817'}}>{r.name}</div>
                          <div style={{fontSize:12.5,color:'#78746e',marginTop:2}}>{r.sub}</div>
                          {problem==='state' ? (
                            <div style={{fontSize:12,color:'#78746e',marginTop:4,fontWeight:600}}>{r.reason}</div>
                          ) : problem==='limit' ? (
                            <div style={{fontSize:12,color:RED,marginTop:4,fontWeight:600}}>{limitKind==='min'?'Minimum':'Maximum'} face ${(r.face/1000).toFixed(0)}k at age {age} · won't issue ${(r.targetFace/1000).toFixed(0)}k</div>
                          ) : null}
                        </div>
                        <div style={{textAlign:'right',flexShrink:0}}>
                          {problem==='state' ? (
                            <span style={{display:'inline-block',padding:'3px 10px',borderRadius:99,background:'#f0efed',color:'#78746e',fontSize:11.5,fontWeight:600}}>Unavailable</span>
                          ) : problem==='limit' ? (
                            <>
                              <div style={{fontSize:19,fontWeight:650,color:RED,lineHeight:1.25}}>N/A</div>
                              <div style={{fontSize:11.5,color:'#b5b1a8',marginTop:2}}>${r.premium.toFixed(2)}/mo at ${(r.face/1000).toFixed(0)}k {limitKind}</div>
                            </>
                          ) : iulMode==='face' ? (
                            <>
                              <div style={{fontSize:19,fontWeight:650,color:'#191817',letterSpacing:'-0.01em',lineHeight:1.25}}>${r.face.toLocaleString()}</div>
                              <div style={{fontSize:11.5,color:'#b5b1a8',marginTop:2}}>face at ${iulPremium}/mo</div>
                            </>
                          ) : (
                            <>
                              <div style={{fontSize:19,fontWeight:650,color:'#191817',letterSpacing:'-0.01em',lineHeight:1.25}}>${r.premium.toFixed(2)}<span style={{fontSize:12,fontWeight:400,color:'#a09c94'}}> /mo</span></div>
                              <div style={{fontSize:11.5,color:'#b5b1a8',marginTop:2}}>for ${(iulFace/1000).toFixed(0)}k face</div>
                            </>
                          )}
                        </div>
                      </div>
                      );
                    })}
                  </div>
                </div>
              )}

              {/* Spec cards header — click to expand/collapse */}
              <button onClick={()=>setIulSpecsOpen(v=>!v)} style={{
                width:'100%',display:'flex',alignItems:'center',justifyContent:'space-between',gap:10,
                paddingBottom:10,marginBottom:iulSpecsOpen?14:0,
                borderTop:`1px solid ${C.bd}`,paddingTop:18,
                background:'transparent',border:'none',borderTopColor:C.bd,cursor:'pointer',
                fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif"
              }}>
                <div style={{display:'flex',alignItems:'center',gap:10}}>
                  <span style={{fontSize:14,color:C.t4,transition:'transform 0.18s',transform:iulSpecsOpen?'rotate(90deg)':'rotate(0deg)'}}>▸</span>
                  <span style={{fontSize:11,color:C.t4,fontWeight:700,letterSpacing:1.5,textTransform:'uppercase'}}>Carrier Specs</span>
                </div>
                <span style={{fontSize:11,color:C.t4,fontWeight:500}}>{iulSpecsOpen?'Click to hide':`Show ${IUL_CARRIERS.length} carrier spec sheets`}</span>
              </button>
              {iulSpecsOpen && (
              <div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(360px,1fr))',gap:14}}>
                {IUL_CARRIERS.map(c => (
                  <div key={c.id} style={{background:C.bg3,border:`1px solid ${C.bd}`,borderRadius:12,overflow:'hidden',display:'flex',flexDirection:'column'}}>
                    <div style={{height:5,background:c.brand}}/>
                    <div style={{padding:'16px 18px 14px',display:'flex',flexDirection:'column',gap:12,flex:1}}>
                      {/* Header */}
                      <div style={{display:'flex',justifyContent:'space-between',alignItems:'flex-start',gap:10}}>
                        <div style={{flex:1,minWidth:0}}>
                          <div style={{display:'flex',alignItems:'center',gap:8}}>
                            <div style={{fontSize:11,color:C.t4,fontWeight:600,textTransform:'uppercase',letterSpacing:0.8}}>{c.name}</div>
                          </div>
                          <div style={{fontSize:17,fontWeight:800,color:C.t0,marginTop:2,lineHeight:1.2}}>{c.product}</div>
                        </div>
                        {c.needsVerify && <span style={{fontSize:10,color:'#F59E0B',background:'rgba(245,158,11,0.12)',border:'1px solid rgba(245,158,11,0.35)',borderRadius:5,padding:'3px 8px',fontWeight:700,flexShrink:0,letterSpacing:0.5}}>VERIFY</span>}
                      </div>
                      {/* Specs */}
                      <div style={{display:'flex',flexDirection:'column',gap:5,fontSize:12,color:C.t2}}>
                        <div style={{display:'flex',justifyContent:'space-between'}}><span style={{color:C.t4}}>Issue ages</span><span style={{fontFamily:"'Instrument Sans',sans-serif"}}>{c.issueAges.min}–{c.issueAges.max}</span></div>
                        <div style={{display:'flex',justifyContent:'space-between'}}><span style={{color:C.t4}}>Face range</span><span style={{fontFamily:"'Instrument Sans',sans-serif"}}>{fmtFace(c.faceRange.min)}–{fmtFace(c.faceRange.max)}</span></div>
                        <div style={{display:'flex',justifyContent:'space-between',gap:8}}><span style={{color:C.t4,flexShrink:0}}>Index</span><span style={{fontSize:11,textAlign:'right'}}>{c.indexStrategy}</span></div>
                        <div style={{display:'flex',justifyContent:'space-between',gap:8}}><span style={{color:C.t4,flexShrink:0}}>UW</span><span style={{fontSize:11,textAlign:'right'}}>{c.underwriting}</span></div>
                        <div style={{display:'flex',justifyContent:'space-between'}}><span style={{color:C.t4}}>DB options</span><span style={{fontSize:11}}>{c.dbOptions.join(', ')}</span></div>
                      </div>
                      {/* Age bands */}
                      {c.faceRange.ageBands && (
                        <div style={{background:C.bg2,borderRadius:7,padding:'8px 11px',fontSize:11}}>
                          <div style={{fontWeight:700,marginBottom:5,color:C.t4,letterSpacing:0.5,fontSize:9}}>FACE BY AGE BAND</div>
                          {c.faceRange.ageBands.map(b => (
                            <div key={b.ages} style={{display:'flex',justifyContent:'space-between',color:C.t2,marginBottom:2}}>
                              <span>{b.ages}</span><span style={{fontFamily:"'Instrument Sans',sans-serif"}}>{fmtFace(b.min)}–{fmtFace(b.max)}</span>
                            </div>
                          ))}
                        </div>
                      )}
                      {/* Riders */}
                      <div>
                        <div style={{fontSize:9,color:C.t4,fontWeight:700,letterSpacing:1.2,marginBottom:6}}>RIDERS AVAILABLE</div>
                        <div style={{display:'flex',flexWrap:'wrap',gap:5}}>
                          {c.riders.map(r => (
                            <span key={r} style={{fontSize:10,background:C.bg2,border:`1px solid ${C.bd}`,borderRadius:5,padding:'3px 8px',color:C.t2}}>{r}</span>
                          ))}
                        </div>
                      </div>
                      {/* Knockouts */}
                      {c.knockouts && c.knockouts.length > 0 && (
                        <div>
                          <div style={{fontSize:9,color:'#b42318',fontWeight:700,letterSpacing:1.2,marginBottom:6}}>AUTO-DECLINE</div>
                          <div style={{fontSize:11,color:C.t3,lineHeight:1.55}}>{c.knockouts.join(' · ')}</div>
                        </div>
                      )}
                      {/* Best for */}
                      <div style={{marginTop:'auto',padding:'10px 12px',background:`${c.brand}11`,border:`1px solid ${c.brand}33`,borderRadius:8,fontSize:12,color:C.t2,lineHeight:1.55,fontStyle:'italic'}}>
                        {c.bestFor}
                      </div>
                    </div>
                  </div>
                ))}
              </div>
              )}
              {iulSpecsOpen && (
              <div style={{marginTop:18,padding:'14px 18px',background:C.bg3,border:`1px solid ${C.bd}`,borderRadius:10,fontSize:11,color:C.t4,lineHeight:1.7}}>
                <strong style={{color:C.t3}}>About this comparison.</strong> Cap rates, par rates, face limits, and issue ages are based on the most recent carrier brochures available. IUL products change frequently — always confirm against the carrier's current spec sheet before quoting clients. This view is for agent decision support, not client deliverables. For client-ready illustrations, use each carrier's official illustration software.
              </div>
              )}
            </div>
          ):quoteMode==='sheet'?(
            <div style={{display:'flex',justifyContent:'center',width:'100%',overflowY:'auto',padding:'32px 24px'}}>
              <div style={{width:'100%',maxWidth:620}}>
                <div style={{fontSize:20,fontWeight:700,color:'#191817',letterSpacing:'-0.01em',marginBottom:4}}>Client sheet</div>
                <div style={{fontSize:13,color:'#78746e',marginBottom:14,lineHeight:1.6}}>Everything from the call in one place — Save downloads a PDF so nothing lives on loose paper.</div>
                {renderClientSheet(false)}
              </div>
            </div>
          ):quoteMode==='cv'?(
            <div style={{display:'flex',justifyContent:'center',width:'100%',overflowY:'auto'}}>
              {(()=>{
                const inputsReady = ageOK && cvMonthly && cvPolicyYrs && Number(cvMonthly)>0 && Number(cvPolicyYrs)>0;
                if (!inputsReady || !hasQuoted) {
                  return (
                    <div style={{display:'flex',flexDirection:'column',alignItems:'center',justifyContent:'center',height:'60vh',gap:16}}>
                      <span style={{fontSize:52}}>💰</span>
                      <div style={{fontSize:18,fontWeight:700,color:C.t2}}>Cash Value Estimator</div>
                      <div style={{fontSize:13,textAlign:'center',maxWidth:280,lineHeight:1.6,color:C.t4}}>
                        {!inputsReady
                          ? 'Enter age, monthly premium, and years in-force in the sidebar to estimate surrender value.'
                          : 'Click View cash value estimate to run the calculation.'}
                      </div>
                    </div>
                  );
                }
                const issueAge = Math.max(0, ageNum - Number(cvPolicyYrs));
                return (
                  <div style={{display:'flex',flexDirection:'column',width:'100%',maxWidth:680,margin:'0 auto'}}>
                    <CashValueProjection monthlyPremium={Number(cvMonthly)} policyYears={Number(cvPolicyYrs)} issueAge={issueAge} C={C} isDark={isDark}/>
                    <CoverageEstimate
                      monthlyPremium={Number(cvMonthly)}
                      policyYears={Number(cvPolicyYrs)}
                      currentAge={ageNum}
                      gender={gender}
                      C={C} isDark={isDark}
                    />
                  </div>
                );
              })()}
            </div>
          ):quoteMode==='term'?(
            !ageOK ? (
              <div style={{display:'flex',flexDirection:'column',alignItems:'center',justifyContent:'center',height:'100%',gap:14,padding:40}}>
                <div style={{fontSize:48,opacity:0.5}}>📋</div>
                <div style={{fontFamily:"'Instrument Sans',sans-serif",fontSize:22,fontWeight:700,color:C.t4}}>Enter client info to get term quotes</div>
                <div style={{fontSize:13,color:C.t4,textAlign:'center',lineHeight:1.8}}>
                  {carriers.filter(c=>c.enabled&&c.termOnly).length} term carriers · {Object.keys(TERM_RATES).length} products
                </div>
              </div>
            ) : !termResults || termResults.length === 0 ? (
              <div style={{display:'flex',flexDirection:'column',alignItems:'center',justifyContent:'center',height:'100%',gap:14,padding:40,textAlign:'center'}}>
                <div style={{fontSize:48,opacity:0.5}}>🔍</div>
                <div style={{fontFamily:"'Instrument Sans',sans-serif",fontSize:22,fontWeight:700,color:C.t4}}>No quotes for this profile</div>
                <div style={{fontSize:13,color:C.t4,maxWidth:340,lineHeight:1.7}}>Try a different age, face amount, or term length. Most carriers don't quote 25/35/40-year term, or above age 65 on shorter terms.</div>
              </div>
            ) : (
              <div>
                {termRec.recommended === 'decline' && (
                  <div style={{background:'#fdecec',border:'1px solid #f5c6c2',borderRadius:10,padding:'16px 20px',marginBottom:14,display:'flex',gap:14,alignItems:'flex-start'}}>
                    <div style={{fontSize:22,lineHeight:1,flexShrink:0,color:'#b42318'}}>⚠</div>
                    <div style={{flex:1,minWidth:0}}>
                      <div style={{fontSize:15,fontWeight:600,color:'#b42318',marginBottom:4,lineHeight:1.2}}>Profile may not qualify for term</div>
                      <div style={{fontSize:13,color:'#78746e',lineHeight:1.6}}>{termRec.reasons.join(' · ')}. Quotes below are <strong style={{color:'#191817'}}>best-case estimates only</strong> — verify with carrier underwriting before promising rates.</div>
                    </div>
                  </div>
                )}
                {/* ── RESULTS HEADER + ISSUE-TYPE FILTER (design 3a) ── */}
                {(()=>{
                  const filtered = termResults.filter(r =>
                    issueFilter==='ALL' ? true :
                    issueFilter==='INSTANT' ? r.uwType==='instant' :
                    r.uwType==='medical');
                  return (
                  <>
                  <div style={{display:'flex',alignItems:'center',justifyContent:'space-between',gap:12,flexWrap:'wrap',marginBottom:14}}>
                    <div style={{display:'flex',alignItems:'baseline',gap:8,fontSize:13,color:'#78746e',flexWrap:'wrap'}}>
                      <span style={{fontSize:20,fontWeight:700,color:'#191817',letterSpacing:'-0.01em'}}>{termMode==='budget' ? `$${termBudget}/mo` : fmtF(termFace)}</span>
                      <span>· {termLength}-year · Age {age} · {gender==='male'?'M':'F'} · {smoker?'Smoker':'NS'} · {HEALTH_CLASS_SHORT[termHealth]}{termBMI!=null?` · BMI ${termBMI.toFixed(1)}`:''}</span>
                    </div>
                    <div style={{display:'flex',alignItems:'center',gap:12}}>
                      <span style={{fontSize:12.5,color:'#a09c94'}}>{filtered.length} quotes · sorted by {termMode==='budget' ? 'face' : 'price'}</span>
                      <div style={{display:'flex',gap:2,background:'#fff',border:'1px solid #eae9e6',borderRadius:9,padding:3}}>
                        {[['ALL','All'],['INSTANT','Instant'],['EXAM','Med exam']].map(([id,label])=>(
                          <button key={id} className={issueFilter===id?undefined:'qm-seg'} onClick={()=>setIssueFilter(id)} style={{
                            padding:'5px 12px',borderRadius:7,border:'none',cursor:'pointer',fontSize:12.5,fontFamily:'inherit',
                            background:issueFilter===id?'#191817':'transparent',
                            color:issueFilter===id?'#fff':'#78746e',
                            fontWeight:issueFilter===id?600:400,whiteSpace:'nowrap'
                          }}>{label}</button>
                        ))}
                      </div>
                    </div>
                  </div>
                  {/* Availability banner card */}
                  {termCompat && termCompat.excluded.length > 0 && (
                    <div style={{background:'#fff',border:'1px solid #eae9e6',borderRadius:10,marginBottom:14,fontSize:13,overflow:'hidden',boxShadow:'0 1px 2px rgba(25,24,23,0.04)'}}>
                      <button onClick={()=>setTermCompatOpen(v=>!v)} style={{
                        width:'100%',background:'transparent',border:'none',cursor:'pointer',
                        padding:'12px 18px',display:'flex',alignItems:'center',justifyContent:'space-between',gap:10,
                        fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif",textAlign:'left'
                      }}>
                        <span style={{color:'#78746e',fontSize:13}}>
                          <strong style={{color:'#191817',fontWeight:600}}>{termCompat.writers} of {termCompat.total}</strong> term products will write at age {age} · {termCompat.excluded.length} can't issue at this age
                        </span>
                        <span style={{fontSize:12.5,color:'#4a45d1',fontWeight:600}}>{termCompatOpen?'Hide':'See excluded'}</span>
                      </button>
                      {termCompatOpen && (
                        <div style={{padding:'4px 18px 14px',display:'flex',flexDirection:'column',gap:5}}>
                          {termCompat.excluded.map(e => (
                            <div key={e.name+'·'+e.sub} style={{display:'flex',justifyContent:'space-between',gap:10,fontSize:12,padding:'7px 12px',background:'#fbfbfa',borderRadius:6,border:'1px solid #f0efed'}}>
                              <span style={{color:'#78746e'}}><strong style={{color:'#191817',fontWeight:600}}>{e.name}</strong> · {e.sub}</span>
                              <span style={{color:'#a09c94'}}>{e.reason}</span>
                            </div>
                          ))}
                        </div>
                      )}
                    </div>
                  )}
                  {/* Column headers — term grid (design 3a) */}
                  <div style={{display:'grid',gridTemplateColumns:'42px 1fr 132px 104px 150px 96px',gap:16,padding:'2px 21px 8px',fontSize:10.5,fontWeight:600,color:'#a09c94',letterSpacing:'0.08em'}}>
                    <span>CARRIER</span><span>PRODUCT · COVERAGE</span><span>ISSUE TYPE</span><span>UW TIER</span><span style={{textAlign:'right'}}>MONTHLY PREMIUM</span><span></span>
                  </div>
                  <div style={{display:'flex',flexDirection:'column',gap:8, opacity: termRec.recommended === 'decline' ? 0.55 : 1, filter: termRec.recommended === 'decline' ? 'grayscale(45%)' : 'none', transition:'opacity 0.25s, filter 0.25s'}}>
                    {filtered.map(r => {
                      // Term UW tier pill — Super Pref-style solid for Pref+, tints below.
                      const tu = (r.tierUsed||'').toLowerCase();
                      const pill = tu.includes('preferred plus')||tu.includes('pref+')||tu.includes('super')
                        ? {bg:'#0d6a86',color:'#fff',label:'Pref+'}
                        : tu.includes('preferred') ? {bg:'#eef0fe',color:'#4740c8',label:'Preferred'}
                        : tu.includes('standard plus')||tu.includes('std+') ? {bg:'#e8f6ef',color:'#177452',label:'Std+'}
                        : tu.includes('standard') ? {bg:'#fdf3e0',color:'#96660f',label:'Standard'}
                        : {bg:'#f0efed',color:'#78746e',label:r.tierUsed||'—'};
                      return (
                        <div key={r.id} className="qm-row" style={{
                            display:'grid',gridTemplateColumns:'42px 1fr 132px 104px 150px 96px',
                            alignItems:'center',gap:16,padding:'12px 20px',
                            background:'#fff',border:'1px solid #eae9e6',borderRadius:10,cursor:'pointer'
                          }}>
                          <CarrierLogo carrierId={r.id} name={r.name} small={false}/>
                          <div style={{display:'flex',flexDirection:'column',gap:2,minWidth:0}}>
                            <span style={{fontSize:14.5,fontWeight:600,color:'#191817',whiteSpace:'nowrap',overflow:'hidden',textOverflow:'ellipsis'}}>{r.name}</span>
                            <span style={{fontSize:12.5,color:'#78746e',whiteSpace:'nowrap',overflow:'hidden',textOverflow:'ellipsis'}}>{r.sub} · {fmtF(r.face)}</span>
                          </div>
                          <div><UWBadge uwType={r.uwType} productName={r.product} tier={r.tierUsed} small={true}/></div>
                          <div><span style={{display:'inline-block',padding:'3px 10px',borderRadius:99,background:pill.bg,color:pill.color,fontSize:11.5,fontWeight:600,whiteSpace:'nowrap'}}>{pill.label}</span></div>
                          <div style={{display:'flex',flexDirection:'column',alignItems:'flex-end',gap:1}}>
                            <span style={{fontSize:19,fontWeight:650,color:'#191817',letterSpacing:'-0.01em',lineHeight:1.25}}>{fmt$(r.prem)}<span style={{fontSize:12,fontWeight:400,color:'#a09c94'}}> /mo</span></span>
                            <span style={{fontSize:11.5,color:'#b5b1a8'}}>${(r.prem*12).toFixed(0)} /yr · {r.termLen}y</span>
                          </div>
                          <div style={{display:'flex',justifyContent:'flex-end'}}>
                            <EAppBtn carrierId={r.id} compact={true}/>
                          </div>
                        </div>
                      );
                    })}
                    {filtered.length===0 && (
                      <div style={{padding:'28px 20px',textAlign:'center',background:'#fbfbfa',border:'1px solid #eae9e6',borderRadius:10,fontSize:13,color:'#78746e'}}>
                        No {issueFilter==='INSTANT'?'instant-decision':'med-exam'} products in these results — switch the filter back to All.
                      </div>
                    )}
                  </div>
                  </>
                  );
                })()}
              </div>
            )
          ):!hasQuoted?(
            /* ── EMPTY STATE (design 3c): numbered steps, step 1 solid ink ── */
            <div style={{display:'flex',flexDirection:'column',alignItems:'center',justifyContent:'center',minHeight:'60vh',gap:8,padding:40}}>
              <div style={{fontSize:22,fontWeight:700,color:'#191817',letterSpacing:'-0.01em'}}>Quote in three steps</div>
              <div style={{fontSize:12.5,color:'#a09c94',marginBottom:22}}>{carriers.filter(c=>c.enabled&&!c.termOnly).length} carriers enabled · sorted by price when you quote</div>
              <div style={{display:'flex',flexDirection:'column',gap:18,maxWidth:340,width:'100%'}}>
                {[
                  ['1','Client age','DOB or age — quotes are age-driven'],
                  ['2','Health picture','Type meds or conditions; the UW tier sets itself'],
                  ['3','Get quotes','Every carrier, sorted by price — pivot tabs to compare products'],
                ].map(([n,title,sub],i)=>(
                  <div key={n} style={{display:'flex',alignItems:'flex-start',gap:14}}>
                    <span style={{width:28,height:28,borderRadius:'50%',border:i===0?'none':'1px solid #dedcd7',background:i===0?'#191817':'#fff',color:i===0?'#fff':'#78746e',fontSize:13,fontWeight:600,display:'flex',alignItems:'center',justifyContent:'center',flexShrink:0}}>{n}</span>
                    <div>
                      <div style={{fontSize:14.5,fontWeight:600,color:'#191817'}}>{title}</div>
                      <div style={{fontSize:13,color:'#78746e',marginTop:2,lineHeight:1.5}}>{sub}</div>
                    </div>
                  </div>
                ))}
              </div>
            </div>
          ):(
            <>
              {/* ── RESULTS HEADER (design 2a) ── */}
              <div style={{display:'flex',alignItems:'center',justifyContent:'space-between',gap:12,flexWrap:'wrap',marginBottom:14}}>
                <div style={{display:'flex',alignItems:'baseline',gap:8,fontSize:13,color:'#78746e',flexWrap:'wrap'}}>
                  <span style={{fontSize:20,fontWeight:700,color:'#191817',letterSpacing:'-0.01em'}}>
                    {!gsbOn&&(mode==='face'?fmtF(faceAmt):`$${budget}/mo`)}
                    {gsbOn&&`${fmtF(gsbFace.gold)} / ${fmtF(gsbFace.silver)} / ${fmtF(gsbFace.bronze)}`}
                  </span>
                  <span>· Age {age}{usState?` · ${usState}`:''} · {gender==='male'?'Male':'Female'} · {smoker?'Smoker':'Non-smoker'}</span>
                  {tierOvr&&<span style={{color:'#4740c8',fontWeight:600,fontSize:11.5}}>· Manual override</span>}
                </div>
                <div style={{display:'flex',alignItems:'center',gap:10}}>
                  <TierPill tier={uwTier}/>
                  <span style={{fontSize:12.5,color:'#a09c94'}}>{(results||[]).filter(r=>gsbOn?r.anyPrem:r.prem!=null).length} quotes · sorted by price</span>
                </div>
              </div>

              <div>
                {/* ── COLUMN HEADERS (design: dummy-proof labels above the row grid) ── */}
                {!gsbOn&&(
                  <div style={{display:'grid',gridTemplateColumns:'42px 1fr 104px 150px 96px',gap:16,padding:'2px 21px 8px',fontSize:10.5,fontWeight:600,color:'#a09c94',letterSpacing:'0.08em'}}>
                    <span>CARRIER</span><span>PRODUCT · COVERAGE</span><span>UW TIER</span><span style={{textAlign:'right'}}>MONTHLY PREMIUM</span><span></span>
                  </div>
                )}
                {/* ── QUOTE CARDS — active ── */}
                <div style={gsbOn
                  ? {display:'grid',gridTemplateColumns:'repeat(auto-fill,minmax(420px,1fr))',gap:20,marginBottom:20,alignItems:'start'}
                  : {display:'flex',flexDirection:'column',gap:8,marginBottom:20}
                }>
                  {results&&results.filter(r=>gsbOn?r.anyPrem:r.prem!=null).map((r,idx)=>{
                    const activeResults = results.filter(x=>gsbOn?x.anyPrem:(x.prem!=null && !x.capped));
                    const isBest = false; // removed Best Value badge
                    const isGhost = false;
                    if(gsbOn){
                      const gsbBrand = CARRIER_META[r.id]?.brand || C.bd2;
                      return(
                        <div key={r.id} style={{
                          background: isDark?(r.anyPrem?C.bg3:C.bg2):(r.anyPrem?'#FFFFFF':C.bg3),
                          border:`1px solid ${r.anyPrem?C.bd2:C.bd}`,
                          borderTop:`3px solid ${r.anyPrem?gsbBrand:C.bd}`,
                          borderRadius:12,padding:'16px 16px 14px',
                          opacity:r.anyPrem?1:0.4,
                          overflow:'visible',
                          transition:'transform 0.18s,box-shadow 0.18s',
                          boxShadow:isDark||!r.anyPrem?'none':'0 4px 6px -1px rgba(0,0,0,0.07)',
                          filter:!r.anyPrem&&!isDark?'grayscale(1)':'none'
                        }}>
                          {/* Card header: name only — no logo */}
                          <div style={{marginBottom:14}}>
                            <div style={{fontSize:18,fontWeight:700,color:C.t0,letterSpacing:'-0.3px',lineHeight:1.2}}>{r.name}</div>
                            <div style={{fontSize:11,color:C.t4,marginTop:3}}>{r.sub}</div>
                          </div>
                          {/* Three tier boxes — tighter gap */}
                          <div style={{display:'grid',gridTemplateColumns:'1fr 1fr 1fr',gap:5}}>
                            {GSB.map(g=>{
                              const tr=r.tiers[g.key];
                              const darkColor=g.color; // brand champagne gold / silver / bronze
                              const lightLabel=g.key==='gold'?'#855D10':g.key==='silver'?'#475569':'#92400E';
                              const isGoldKey=g.key==='gold';
                              return(
                                <div key={g.key} style={{
                                  background: g.key==='silver'
                                    ? (isDark?'rgba(74,69,209,0.04)':'rgba(74,69,209,0.05)')
                                    : (isDark?C.bg1:'#FFFFFF'),
                                  borderRadius:8,
                                  padding:'12px 10px',
                                  border:`1px solid ${isDark?(tr.prem?C.bd2:C.bd):'#E2E8F0'}`,
                                  borderTop: isDark
                                    ? `2px solid ${darkColor}`
                                    : (isGoldKey?`4px solid #4a45d1`:`2px solid ${g.key==='silver'?'#94A3B8':'#C2700A'}`),
                                  textAlign:'center',
                                  boxShadow:!isDark&&isGoldKey&&tr.prem?'0 2px 12px rgba(74,69,209,0.16)':'none',
                                  opacity:tr.prem?1:0.4
                                }}>
                                  <div style={{
                                    fontSize:9,fontWeight:700,marginBottom:5,
                                    letterSpacing:1.2,textTransform:'uppercase',
                                    color:isDark?darkColor:lightLabel
                                  }}>{g.medal} {g.label}</div>
                                  <div style={{fontSize:13,color:C.t3,marginBottom:8,fontFamily:"'Instrument Sans',sans-serif",fontWeight:500}}>{fmtF(gsbFace[g.key])}</div>
                                  {tr.prem!=null?(
                                    <>
                                      <div style={{
                                        fontFamily:"'Instrument Sans',sans-serif",fontSize:22,fontWeight:800,
                                        color:isDark?darkColor:'#0F172A',
                                        letterSpacing:'-0.5px',lineHeight:1
                                      }}>{fmt$(tr.prem)}</div>
                                      <div style={{fontSize:9,color:C.t4,marginTop:4,fontWeight:400}}>/mo EFT</div>
                                      <div style={{fontSize:9,color:C.t4,marginTop:2,fontWeight:400,fontFamily:"'Instrument Sans',sans-serif"}}>${((tr.prem??0)*12).toFixed(0)}<span style={{fontSize:8}}>/yr</span></div>
                                    </>
                                  ):(
                                    <div style={{fontSize:18,color:C.t4,marginTop:8,minHeight:52,display:'flex',alignItems:'center',justifyContent:'center'}}>—</div>
                                  )}
                                </div>
                              );
                            })}
                          </div>
                          {/* Footer: coverage badge + eApp full-width */}
                          {r.anyPrem&&(
                            <div style={{marginTop:12}}>
                              <div style={{display:'flex',alignItems:'center',gap:6,marginBottom:10}}>
                                <span style={{width:4,height:4,borderRadius:'50%',background:TIER_INFO[uwTier].dot,flexShrink:0}}/>
                                <TierBadge tier={uwTier} productName={r.tiers.gold.productName||r.tiers.silver.productName||r.tiers.bronze.productName}/>
                              </div>
                              <EAppBtn carrierId={r.id} compact={false} lightMode={!isDark}/>
                            </div>
                          )}
                        </div>
                      );
                    }
                    // Single mode — design 2a row: grid 42px 1fr 104px 150px 96px,
                    // each row its own white card, .qm-row hover (tint+shift).
                    const covNote = r.capped ? ' · capped' : (!r.capped&&r.roundedTo&&mode==='budget') ? ` · $${r.roundedTo>=1000?r.roundedTo/1000+'k':r.roundedTo} increments` : '';
                    return(
                      <div key={r.id} className="qm-row" style={{
                          display:'grid',gridTemplateColumns:'42px 1fr 104px 150px 96px',
                          alignItems:'center',gap:16,padding:'12px 20px',
                          background:'#fff',border:'1px solid #eae9e6',borderRadius:10,
                          opacity:r.capped?0.65:1,cursor:'pointer'
                        }}>
                        <CarrierLogo carrierId={r.id} name={r.name} small={false}/>
                        <div style={{display:'flex',flexDirection:'column',gap:2,minWidth:0}}>
                          <span style={{fontSize:14.5,fontWeight:600,color:'#191817'}}>{r.name}</span>
                          <span style={{fontSize:12.5,color:'#78746e',whiteSpace:'nowrap',overflow:'hidden',textOverflow:'ellipsis'}}>
                            {r.sub}{r.productName&&r.productName!==r.sub?` · ${r.productName}`:''} · <span style={mode==='budget'?{fontWeight:600,color:'#4740c8'}:undefined}>{fmtF(r.face)}</span>{covNote}
                          </span>
                        </div>
                        <div><TierBadge tier={r.activeTier} productName={r.productName}/></div>
                        <div style={{display:'flex',flexDirection:'column',alignItems:'flex-end',gap:1}}>
                          <span style={{fontSize:19,fontWeight:650,color:'#191817',letterSpacing:'-0.01em',lineHeight:1.25}}>{fmt$(r.prem)}<span style={{fontSize:12,fontWeight:400,color:'#a09c94'}}> /mo</span></span>
                          <span style={{fontSize:11.5,color:'#b5b1a8'}}>${((r.prem??0)*12).toFixed(0)} /yr</span>
                        </div>
                        <div style={{display:'flex',justifyContent:'flex-end'}}>
                          <EAppBtn carrierId={r.id} compact={true}/>
                        </div>
                      </div>
                    );
                  })}
                </div>
                {/* ── GHOST / UNAVAILABLE CARDS ── */}
                {results&&(()=>{
                  const ghosts = results.filter(r=>gsbOn?!r.anyPrem:r.prem==null);
                  const avail  = results.filter(r=>gsbOn?r.anyPrem:r.prem!=null);
                  if(!ghosts.length) return null;
                  return(
                    <div style={{marginBottom:20}}>
                      {avail.length===0 && (
                        <div style={{
                          background:C.bg3,border:`1px solid ${C.bd}`,borderRadius:12,
                          padding:'18px 20px',marginBottom:14,
                        }}>
                          <div style={{fontSize:14,fontWeight:600,color:C.t1,marginBottom:4}}>No carriers can write this combination</div>
                          <div style={{fontSize:12,color:C.t3}}>Try lowering the face amount, easing the UW tier, or checking state availability. All {ghosts.length} carriers are listed below with their reason.</div>
                        </div>
                      )}
                      <div style={{
                        display:'flex',alignItems:'center',gap:10,
                        marginBottom:10,paddingBottom:8,borderBottom:`1px solid ${C.bd}`,
                      }}>
                        <span style={{fontSize:10,fontWeight:700,letterSpacing:1.4,textTransform:'uppercase',color:C.t4}}>Unavailable</span>
                        <span style={{fontSize:11,color:C.t4}}>{ghosts.length} {ghosts.length===1?'carrier':'carriers'}</span>
                      </div>
                      <div style={{display:'grid',gridTemplateColumns:'repeat(auto-fill,minmax(240px,1fr))',gap:8}}>
                        {ghosts.map(r=>(
                          <div key={r.id} style={{
                            background:'#fbfbfa',
                            border:'1px solid #eae9e6',
                            borderRadius:8,padding:'10px 14px',
                            opacity:0.6,
                            display:'flex',alignItems:'center',gap:10,
                            filter:'grayscale(1)'
                          }}>
                            <CarrierLogo carrierId={r.id} name={r.name} small={true}/>
                            <div style={{minWidth:0,flex:1}}>
                              <div style={{fontSize:12,fontWeight:600,color:C.t3,whiteSpace:'nowrap',overflow:'hidden',textOverflow:'ellipsis'}}>{r.name}</div>
                              <div style={{fontSize:10,color:C.t4,fontStyle:'italic',whiteSpace:'nowrap',overflow:'hidden',textOverflow:'ellipsis'}}>{r.reason||'Not available'}</div>
                            </div>
                          </div>
                        ))}
                      </div>
                    </div>
                  );
                })()}

                {/* ── FOOTER STRIP (design 2a) ── */}
                {!gsbOn&&results&&(()=>{
                  const avail = results.filter(r=>r.prem!=null);
                  if(avail.length<1) return null;
                  const cheapest=avail.reduce((a,b)=>a.prem<b.prem?a:b);
                  const mostExp=avail.reduce((a,b)=>a.prem>b.prem?a:b);
                  return(
                    <div style={{display:'flex',alignItems:'center',gap:32,padding:'14px 20px',background:'#fbfbfa',border:'1px solid #eae9e6',borderRadius:10,fontSize:12.5,color:'#78746e',flexWrap:'wrap'}}>
                      <span>Price range <span style={{fontWeight:600,color:'#191817'}}>{fmt$(cheapest.prem)} – {fmt$(mostExp.prem)}</span> /mo</span>
                      <span>Available <span style={{fontWeight:600,color:'#191817'}}>{avail.length} of {results.length}</span></span>
                      <span>UW: <span style={{fontWeight:600,color:'#191817'}}>{TIER_INFO[uwTier].short}</span></span>
                    </div>
                  );
                })()}

                <div style={{marginTop:16,fontSize:11,color:C.t4,lineHeight:1.7,borderTop:`1px solid ${C.bd}`,paddingTop:12}}>
                  Rates are estimates for illustration only. Actual premiums subject to underwriting. Confirm state availability and face amount limits with each carrier. Agent use only.
                </div>
              </div>
            </>
          )}
        </div>
      </div>

      {/* ── CARRIER PANEL OVERLAY ── */}
      {/* ── CONTACT MODAL ── */}
      {showContact&&(
        <div onClick={()=>{if(!contactSending)setShowContact(false);}} style={{position:'fixed',inset:0,zIndex:400,background:'rgba(0,0,0,0.65)',display:'flex',alignItems:'center',justifyContent:'center',padding:'16px'}}>
          <div onClick={e=>e.stopPropagation()} style={{background:C.bg1,borderRadius:16,width:480,maxWidth:'100%',maxHeight:'90vh',overflowY:'auto',boxShadow:'0 24px 80px rgba(0,0,0,0.5)'}}>
            <div style={{display:'flex',alignItems:'center',justifyContent:'space-between',padding:'18px 20px 0'}}>
              <div style={{fontSize:17,fontWeight:700,color:C.t1}}>Contact Quotemarko</div>
              <button onClick={()=>setShowContact(false)} style={{background:'transparent',border:'none',color:C.t4,fontSize:22,cursor:'pointer',lineHeight:1}}>✕</button>
            </div>
            {contactSent?(
              <div style={{padding:'32px 24px',textAlign:'center'}}>
                <div style={{fontSize:40,marginBottom:12}}>✅</div>
                <div style={{fontSize:16,fontWeight:700,color:C.t1,marginBottom:8}}>Message Sent!</div>
                <div style={{fontSize:13,color:C.t3,marginBottom:24}}>We'll get back to you shortly.</div>
                <button onClick={()=>{setShowContact(false);setContactSent(false);setContactForm({name:'',email:'',company:'',message:''}); }} style={{padding:'11px 28px',borderRadius:8,border:'none',background:C.gold,color:C.bg0,fontWeight:700,fontSize:14,cursor:'pointer',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif"}}>Close</button>
              </div>
            ):(
              <div style={{padding:'16px 20px 24px',display:'flex',flexDirection:'column',gap:14}}>
                {/* Type selector */}
                <div>
                  <div style={{fontSize:11,fontWeight:700,letterSpacing:1.2,color:C.t4,textTransform:'uppercase',marginBottom:10}}>Inquiry Type</div>
                  {[
                    {id:'carrier',    emoji:'📋', label:'Submit a New Carrier', desc:'Request a carrier be added to Quotemarko'},
                    {id:'suggestion', emoji:'💡', label:'General Suggestion',    desc:'Feature ideas, feedback, or improvements'},
                    {id:'partnership',emoji:'🤝', label:'Partnership / Whitelabel', desc:'Agency inquiries, 5+ seats, or white-label licensing'},
                  ].map(opt=>(
                    <div key={opt.id} onClick={()=>setContactType(opt.id)}
                      style={{display:'flex',alignItems:'center',gap:12,padding:'11px 14px',borderRadius:9,border:`1px solid ${contactType===opt.id?C.gold+'88':C.bd}`,background:contactType===opt.id?C.gold+'0D':'transparent',cursor:'pointer',marginBottom:7}}>
                      <span style={{fontSize:18,flexShrink:0}}>{opt.emoji}</span>
                      <div>
                        <div style={{fontSize:13,fontWeight:600,color:contactType===opt.id?C.gold:C.t1}}>{opt.label}</div>
                        <div style={{fontSize:11,color:C.t4}}>{opt.desc}</div>
                      </div>
                      {contactType===opt.id&&<span style={{marginLeft:'auto',color:C.gold,fontSize:14}}>✓</span>}
                    </div>
                  ))}
                </div>
                {/* Fields */}
                {[
                  {key:'name',    label:'Your Name',    ph:'Full name',          req:true},
                  {key:'email',   label:'Email',        ph:'your@email.com',     req:true},
                  {key:'company', label:'Company / Agency', ph:'Optional',      req:false},
                ].map(f=>(
                  <div key={f.key}>
                    <div style={{fontSize:12,color:C.t4,marginBottom:5}}>{f.label}{f.req&&<span style={{color:'#b42318'}}> *</span>}</div>
                    <input value={contactForm[f.key]} onChange={e=>setContactForm(p=>({...p,[f.key]:e.target.value}))}
                      placeholder={f.ph}
                      style={{width:'100%',boxSizing:'border-box',background:C.bg3,border:`1px solid ${C.bd2}`,borderRadius:8,color:C.t1,fontSize:14,padding:'11px 12px',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif",outline:'none'}}/>
                  </div>
                ))}
                <div>
                  <div style={{fontSize:12,color:C.t4,marginBottom:5}}>Message <span style={{color:'#b42318'}}>*</span></div>
                  <textarea value={contactForm.message} onChange={e=>setContactForm(p=>({...p,message:e.target.value}))}
                    placeholder={contactType==='carrier'?'Carrier name, website, and any relevant product details…':(contactType==='suggestion'?'Describe your idea or feedback…':'Tell us about your agency, team size, and what you are looking for…')}
                    rows={4}
                    style={{width:'100%',boxSizing:'border-box',background:C.bg3,border:`1px solid ${C.bd2}`,borderRadius:8,color:C.t1,fontSize:14,padding:'11px 12px',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif",outline:'none',resize:'vertical'}}/>
                </div>
                {contactErr&&<div style={{fontSize:12,color:'#b42318',padding:'8px 10px',background:'rgba(180,35,24,0.08)',borderRadius:7}}>{contactErr}</div>}
                <button onClick={sendContact} disabled={contactSending||!contactForm.name||!contactForm.email||!contactForm.message}
                  style={{padding:'13px',borderRadius:9,border:'none',background:C.gold,color:C.bg0,fontWeight:700,fontSize:14,cursor:'pointer',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif",opacity:(contactSending||!contactForm.name||!contactForm.email||!contactForm.message)?0.5:1}}>
                  {contactSending?'Sending…':'Send Message'}
                </button>
              </div>
            )}
          </div>
        </div>
      )}


      {/* ── DESKTOP PROFILE MODAL ── */}
      {showProfileModal&&(
        <div onClick={()=>setShowProfileModal(false)} style={{position:'fixed',inset:0,zIndex:300,background:'rgba(0,0,0,0.6)',display:'flex',alignItems:'center',justifyContent:'center'}}>
          <div onClick={e=>e.stopPropagation()} style={{background:C.bg1,borderRadius:16,width:460,maxWidth:'94vw',maxHeight:'88vh',overflowY:'auto',boxShadow:'0 24px 80px rgba(0,0,0,0.5)'}}>
            <div style={{display:'flex',alignItems:'center',justifyContent:'space-between',padding:'18px 20px 0'}}>
              <div style={{fontSize:17,fontWeight:700,color:C.t1}}>My Profile</div>
              <button onClick={()=>setShowProfileModal(false)} style={{background:'transparent',border:'none',color:C.t4,fontSize:22,cursor:'pointer',lineHeight:1}}>✕</button>
            </div>
            <div style={{padding:'16px 16px 32px',display:'flex',flexDirection:'column',gap:20}}>
              <div style={{background:C.bg2,borderRadius:12,padding:16,border:`1px solid ${C.bd}`}}>
                <div style={{fontSize:11,fontWeight:700,letterSpacing:1.5,color:C.t4,textTransform:'uppercase',marginBottom:14}}>Account</div>
                <div style={{fontSize:12,color:C.t4,marginBottom:4}}>Email</div>
                <div style={{fontSize:14,color:C.t2,marginBottom:14,padding:'10px 12px',background:C.bg3,borderRadius:8,border:`1px solid ${C.bd}`}}>{session?.user?.email||'—'}</div>
                <div style={{fontSize:12,color:C.t4,marginBottom:6}}>Display Name</div>
                <input value={profileName} onChange={e=>setProfileName(e.target.value)} placeholder="Your name (optional)" style={{width:'100%',boxSizing:'border-box',background:C.bg3,border:`1px solid ${C.bd2}`,borderRadius:8,color:C.t1,fontSize:14,padding:'11px 12px',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif",outline:'none'}}/>
                <button onClick={saveProfile} disabled={profileSaving} style={{marginTop:10,width:'100%',padding:'11px',borderRadius:8,border:'none',background:profileSaved?'#16A34A':C.gold,color:profileSaved?'#fff':C.bg0,fontWeight:700,fontSize:13,cursor:'pointer',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif"}}>
                  {profileSaving?'Saving…':profileSaved?'✓ Saved!':'Save Name'}
                </button>
              </div>
              <div style={{background:C.bg2,borderRadius:12,padding:16,border:`1px solid ${C.bd}`}}>
                <div style={{fontSize:11,fontWeight:700,letterSpacing:1.5,color:C.t4,textTransform:'uppercase',marginBottom:14}}>Change Password</div>
                {[['New Password','next'],['Confirm Password','confirm']].map(([lbl,key])=>(
                  <div key={key} style={{marginBottom:10}}>
                    <div style={{fontSize:12,color:C.t4,marginBottom:5}}>{lbl}</div>
                    <input type="password" value={pwForm[key]} onChange={e=>setPwForm(p=>({...p,[key]:e.target.value}))} placeholder={lbl} style={{width:'100%',boxSizing:'border-box',background:C.bg3,border:`1px solid ${C.bd2}`,borderRadius:8,color:C.t1,fontSize:14,padding:'11px 12px',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif",outline:'none'}}/>
                  </div>
                ))}
                {pwMsg&&<div style={{fontSize:12,color:pwMsg.err?'#b42318':'#177452',marginBottom:8,padding:'7px 10px',background:pwMsg.err?'rgba(180,35,24,0.08)':'rgba(23,116,82,0.08)',borderRadius:7}}>{pwMsg.msg}</div>}
                <button onClick={changePassword} disabled={pwChanging||!pwForm.next} style={{width:'100%',padding:'11px',borderRadius:8,border:'none',background:C.gold,color:C.bg0,fontWeight:700,fontSize:13,cursor:'pointer',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif",opacity:pwForm.next?1:0.5}}>
                  {pwChanging?'Updating…':'Update Password'}
                </button>
              </div>
              <div style={{background:C.bg2,borderRadius:12,padding:16,border:`1px solid ${C.bd}`}}>
                <div style={{fontSize:11,fontWeight:700,letterSpacing:1.5,color:C.t4,textTransform:'uppercase',marginBottom:4}}>My Carriers</div>
                <div style={{fontSize:12,color:C.t4,marginBottom:14}}>Toggle which carriers appear in your results. Saved to your profile.</div>
                <div style={{display:'flex',flexDirection:'column',gap:8}}>
                  {carriers.filter(c => !c.linkedId && !FORCE_DISABLED.has(c.id)).map(c=>{
                    const brand=CARRIER_META[c.id]?.brand||C.bd2;
                    return(
                      <div key={c.id} onClick={()=>setCarriers(p=>{const t=p.find(x=>x.id===c.id);if(!t)return p;const ne=!t.enabled;return p.map(x=>x.id===c.id||x.linkedId===c.id?{...x,enabled:ne}:x);})}
                        style={{display:'flex',alignItems:'center',gap:12,padding:'10px 12px',borderRadius:9,border:`1px solid ${c.enabled?brand+'66':C.bd}`,background:c.enabled?brand+'0D':'transparent',cursor:'pointer'}}>
                        <div style={{width:18,height:18,borderRadius:5,border:`2px solid ${c.enabled?brand:C.bd2}`,background:c.enabled?brand:'transparent',display:'flex',alignItems:'center',justifyContent:'center',flexShrink:0}}>
                          {c.enabled&&<span style={{color:'#fff',fontSize:11,fontWeight:700,lineHeight:1}}>✓</span>}
                        </div>
                        <div style={{flex:1,minWidth:0}}>
                          <div style={{fontSize:13,fontWeight:600,color:c.enabled?C.t1:C.t3}}>{c.name}</div>
                          <div style={{fontSize:11,color:C.t4}}>{c.sub}</div>
                        </div>
                        <div style={{width:8,height:8,borderRadius:'50%',background:c.enabled?brand:'#334155',flexShrink:0}}/>
                      </div>
                    );
                  })}
                </div>
                <button onClick={saveCarrierPrefs} style={{marginTop:14,width:'100%',padding:'12px',borderRadius:8,border:'none',background:carriersSaved?'#16A34A':C.gold,color:carriersSaved?'#fff':C.bg0,fontWeight:700,fontSize:13,cursor:'pointer',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif"}}>
                  {carriersSaved?'✓ Carriers Saved!':'Save Carrier Preferences'}
                </button>
              </div>
              <button onClick={openCustomerPortal} disabled={portalLoading} style={{padding:'12px',borderRadius:10,border:`1px solid ${C.bd2}`,background:'transparent',color:C.t1,fontWeight:600,fontSize:13,cursor:portalLoading?'not-allowed':'pointer',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif",opacity:portalLoading?0.6:1,marginBottom:8}}>
              {portalLoading ? 'Opening…' : 'Manage Subscription'}
            </button>
            <button onClick={signOut} style={{padding:'13px',borderRadius:10,border:`1px solid ${C.bd2}`,background:'transparent',color:'#b42318',fontWeight:600,fontSize:14,cursor:'pointer',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif"}}>
                Sign Out
              </button>
            </div>
          </div>
        </div>
      )}

      {carrierPanel&&(
        <div style={{position:'fixed',inset:0,zIndex:100,display:'flex'}}>
          <div onClick={()=>setCarrierPanel(false)} style={{flex:1,background:'rgba(0,0,0,0.55)'}}/>
          <div style={{width:380,background:C.bg2,borderLeft:`1px solid ${C.bd}`,padding:24,overflowY:'auto',display:'flex',flexDirection:'column',gap:20}}>
            <div style={{display:'flex',alignItems:'center',justifyContent:'space-between'}}>
              <div style={{fontFamily:"'Instrument Sans',sans-serif",fontSize:22,fontWeight:800,color:C.t0}}>Carrier Settings</div>
              <button onClick={()=>setCarrierPanel(false)} style={{background:'none',border:'none',color:C.t3,fontSize:20,cursor:'pointer',lineHeight:1}}>×</button>
            </div>

            {/* Active carriers */}
            <div>
              {/* Tab bar */}
              <div style={{display:'flex',gap:4,marginBottom:14,background:C.bg3,borderRadius:8,padding:3}}>
                {[['fex','🏛️ FEX'],['term','⏱️ Term'],['iul','📈 IUL']].map(([tab,label])=>(
                  <button key={tab} onClick={()=>setCarrierPanelTab(tab)} style={{
                    flex:1,padding:'6px 0',border:'none',borderRadius:6,fontSize:11,fontWeight:700,cursor:'pointer',
                    background:carrierPanelTab===tab?(isDark?C.blue:'#0F172A'):'transparent',
                    color:carrierPanelTab===tab?'#FFFFFF':C.t4,
                    fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif",transition:'all 0.15s'
                  }}>{label}</button>
                ))}
              </div>
              {CARRIERS
                .filter(c => !c.linkedId && (
                  carrierPanelTab==='fex'  ? !c.termOnly && !c.iulOnly :
                  carrierPanelTab==='term' ? c.termOnly :
                  carrierPanelTab==='iul'  ? c.iulOnly : false
                ))
                .sort((a,b)=>a.name.localeCompare(b.name))
                .map(c=>{
                const active=carriers.find(x=>x.id===c.id)?.enabled;
                return(
                  <div key={c.id} style={{display:'flex',alignItems:'center',gap:12,padding:'12px 14px',borderRadius:10,marginBottom:6,background:active?C.bg3:C.bg1,border:`1px solid ${active?C.bd2:C.bd}`,cursor:'pointer',transition:'all 0.1s'}}
                    onClick={()=>toggleCarrier(c.id)}>
                    <div style={{width:38,height:38,borderRadius:9,background:C.bg0,border:`1px solid ${C.bd2}`,display:'flex',alignItems:'center',justifyContent:'center',fontSize:11,fontWeight:700,color:active?C.t2:C.t4}}>{c.abbr}</div>
                    <div style={{flex:1}}>
                      <div style={{fontSize:13,fontWeight:600,color:active?C.t0:C.t4}}>{c.name}</div>
                      <div style={{fontSize:11,color:C.t4,marginTop:1}}>{c.sub}</div>
                    </div>
                    {/* Toggle switch */}
                    <div style={{width:38,height:22,borderRadius:11,background:active?C.blue:'#1E3A5A',border:`1px solid ${active?C.blue:'#1E3A5A'}`,position:'relative',transition:'all 0.2s',flexShrink:0}}>
                      <div style={{position:'absolute',top:3,left:active?18:3,width:14,height:14,borderRadius:'50%',background:'white',transition:'left 0.2s'}}/>
                    </div>
                  </div>
                );
              })}
            </div>

            <div style={{height:1,background:C.bd}}/>

            {/* Request a carrier */}
            <div>
              <div style={{fontSize:10,fontWeight:700,color:C.t4,letterSpacing:1.8,textTransform:'uppercase',marginBottom:4}}>Request a Carrier</div>
              <div style={{fontSize:12,color:C.t3,marginBottom:14,lineHeight:1.6}}>Don't see a carrier you're contracted with? Submit a request and we'll add them to the system.</div>
              {reqSent?(
                <div style={{background:'rgba(23,116,82,0.1)',border:'1px solid rgba(23,116,82,0.3)',borderRadius:10,padding:'14px 16px',textAlign:'center'}}>
                  <div style={{fontSize:20,marginBottom:6}}>✓</div>
                  <div style={{fontSize:13,color:C.green,fontWeight:600}}>Request submitted</div>
                  <div style={{fontSize:11,color:C.t3,marginTop:3}}>We'll review and add within 48 hrs</div>
                  <button onClick={()=>{setReqSent(false);setReqForm({name:'',state:'',notes:''});}} style={{marginTop:10,padding:'6px 14px',borderRadius:7,border:`1px solid ${C.bd}`,background:C.bg2,color:C.t3,fontSize:11,cursor:'pointer',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif"}}>Submit another</button>
                </div>
              ):(
                <div style={{display:'flex',flexDirection:'column',gap:10}}>
                  <div>
                    <div style={{fontSize:11,color:C.t3,marginBottom:4}}>Carrier name *</div>
                    <input placeholder="e.g. Mutual of Omaha" value={reqForm.name}
                      onChange={e=>setReqForm(p=>({...p,name:e.target.value}))}
                      style={{background:C.bg3,border:`1px solid ${C.bd}`,color:C.t1,borderRadius:8,padding:'9px 12px',fontSize:13,width:'100%',outline:'none',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif"}}/>
                  </div>
                  <div>
                    <div style={{fontSize:11,color:C.t3,marginBottom:4}}>State(s) needed</div>
                    <input placeholder="e.g. FL, TX, GA" value={reqForm.state}
                      onChange={e=>setReqForm(p=>({...p,state:e.target.value}))}
                      style={{background:C.bg3,border:`1px solid ${C.bd}`,color:C.t1,borderRadius:8,padding:'9px 12px',fontSize:13,width:'100%',outline:'none',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif"}}/>
                  </div>
                  <div>
                    <div style={{fontSize:11,color:C.t3,marginBottom:4}}>Notes</div>
                    <textarea placeholder="Product type, tier info, special requirements…" value={reqForm.notes}
                      onChange={e=>setReqForm(p=>({...p,notes:e.target.value}))}
                      rows={3} style={{background:C.bg3,border:`1px solid ${C.bd}`,color:C.t1,borderRadius:8,padding:'9px 12px',fontSize:13,width:'100%',outline:'none',resize:'vertical',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif"}}/>
                  </div>
                  <button
                    onClick={()=>{if(reqForm.name.trim())setReqSent(true);}}
                    style={{padding:'11px 0',borderRadius:9,border:'none',background:reqForm.name.trim()?C.blue:'#1E3A5A',color:reqForm.name.trim()&&C.t0||C.t4,fontSize:13,fontWeight:600,cursor:reqForm.name.trim()?'pointer':'not-allowed',fontFamily:"'Instrument Sans','Helvetica Neue',Arial,sans-serif"}}>
                    Submit request
                  </button>
                </div>
              )}
            </div>
          </div>
        </div>
      )}
    </div>
  );
}

// Rate engine v2 — data-driven fexLookup — Mon Mar 16 01:06:40 UTC 2026
